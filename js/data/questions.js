// js/data/questions.js
// Quiz questions for the Cluster Architecture, Installation and Configuration
// domain (25% exam weight) and the Services and Networking domain (20% exam
// weight). Grounded in kubernetes.io and helm.sh documentation fetched into
// .cache/aws-docs/ (see scripts/fetch-doc.mjs).
// Written in the author's own words, not copied verbatim from the source docs.

export const QUESTIONS = [
  // --- RBAC and access control (cluster-001..007) ---
  {
    id: 'cluster-001',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'Which statement correctly distinguishes a Role from a ClusterRole in Kubernetes RBAC?',
    options: [
      'A ClusterRole can only be used to grant access across every namespace at once; it has no way to scope permissions to a single namespace.',
      'A Role only sets permissions inside the namespace it was created in, while a ClusterRole is not tied to any namespace and can cover cluster-scoped resources such as Nodes.',
      'As long as it is attached with a ClusterRoleBinding, a Role can grant permissions over cluster-scoped resources like Nodes.',
      'Role and ClusterRole are both cluster-scoped objects; the only difference between them is which verbs each one supports.',
    ],
    correctIndexes: [1],
    explanation: "A Role must be created within, and only ever applies to, a specific namespace, whereas a ClusterRole is cluster-scoped and can grant access to resources that live outside any namespace, such as Nodes, as well as non-resource API paths.",
  },
  {
    id: 'cluster-002',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: "A RoleBinding named 'secrets-viewer' lives in the 'billing' namespace and its roleRef points at the ClusterRole 'secret-reader', with the user 'priya' as its only subject. What access does priya end up with?",
    options: [
      'Priya can read Secrets in every namespace in the cluster, because binding any ClusterRole always grants that ClusterRole permissions cluster-wide.',
      'The binding is invalid, because a RoleBinding is not allowed to reference a ClusterRole as its roleRef.',
      "Priya can read Secrets only inside the 'billing' namespace, since the RoleBinding's own namespace determines where the referenced ClusterRole's permissions actually apply.",
      "Priya receives full administrative rights over the 'billing' namespace, since referencing any ClusterRole through a binding is equivalent to granting cluster-admin there.",
    ],
    correctIndexes: [2],
    explanation: "A RoleBinding can reference a ClusterRole to reuse a common set of rules across namespaces, but the permissions it grants still only take effect within the RoleBinding's own namespace, not cluster-wide.",
  },
  {
    id: 'cluster-003',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'A cluster administrator needs to change which role a particular RoleBinding grants to its subjects. What does Kubernetes require them to do?',
    options: [
      'Delete the existing RoleBinding and create a new one that points at the desired role.',
      'Run kubectl edit on the RoleBinding and change its roleRef.name field in place.',
      'Use kubectl patch to update only the roleRef.kind field, leaving roleRef.name untouched.',
      'Add a second roleRef block to the same RoleBinding so both roles apply together.',
    ],
    correctIndexes: [0],
    explanation: "A binding's roleRef is immutable once created; attempting to change it produces a validation error, so swapping in a different role requires deleting the binding and creating a replacement rather than editing the existing object.",
  },
  {
    id: 'cluster-004',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'A brand-new ServiceAccount is created in an application namespace with no RoleBindings pointing at it yet. Under the default RBAC policies, what can this ServiceAccount do?',
    options: [
      "Read and write most resources within its own namespace, similar to the built-in 'edit' role.",
      'Read every Secret across all namespaces, so its Pods can fetch credentials as needed.',
      "Everything the built-in 'admin' ClusterRole permits, since that is the implicit default for new accounts.",
      'Essentially nothing beyond the read-only API discovery information already available to any authenticated identity.',
    ],
    correctIndexes: [3],
    explanation: 'Default RBAC policy grants no permissions to ServiceAccounts outside kube-system beyond what the API discovery ClusterRoles already expose to authenticated users; anything else a workload needs must be granted through an explicit binding.',
  },
  {
    id: 'cluster-005',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: "Why does the built-in 'view' ClusterRole deliberately exclude the ability to read Secrets, even though it otherwise grants broad read-only access?",
    options: [
      'Because Secrets are stored outside etcd and are therefore technically unreadable through the standard API regardless of permission.',
      "Because reading a Secret's contents could expose ServiceAccount credentials, letting the reader act as any ServiceAccount in the namespace.",
      "Because the 'view' role only covers cluster-scoped resources, and Secrets are always namespaced objects.",
      'Because Secrets require a separate authentication mechanism that RBAC rules are not able to express.',
    ],
    correctIndexes: [1],
    explanation: "The view role withholds Secret access specifically because reading Secrets can reveal ServiceAccount credentials, which would let the viewer act with that ServiceAccount's own API access — a form of privilege escalation the role is designed to avoid.",
  },
  {
    id: 'cluster-006',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'A user cannot currently list Secrets cluster-wide. Under what condition can that user still create a new ClusterRole whose rules include permission to list Secrets cluster-wide?',
    options: [
      'Automatically, as soon as the user is granted the create verb on clusterroles, regardless of what the new role contains.',
      'Only if the user belongs to the system:authenticated group, which is exempt from the escalation check.',
      'Only if the user has been explicitly granted authorization to use the escalate verb against the clusterroles resource type.',
      'Never — only a cluster-admin binding itself can create a role that contains a permission the creator lacks.',
    ],
    correctIndexes: [2],
    explanation: 'Creating or updating a Role or ClusterRole requires either already holding every permission it would contain, or carrying explicit authorization to perform the escalate verb against roles or clusterroles resources; merely holding the create verb on the object itself is not enough.',
  },
  {
    id: 'cluster-007',
    domain: 'cluster',
    questionType: 'multiple-response',
    question: 'Which of the following statements about granting API permissions to ServiceAccounts are accurate? (Select all that apply.)',
    options: [
      'Granting a role to an application-specific ServiceAccount, one created just for that workload, is the most secure of the documented approaches.',
      'Granting cluster-admin to every ServiceAccount in the cluster at once is called out as a policy that should be avoided.',
      'It is impossible to grant a role to every ServiceAccount in a namespace at once; each ServiceAccount must be bound individually.',
      "A Pod that does not set serviceAccountName in its spec automatically runs under the 'default' ServiceAccount of its own namespace.",
      "The built-in 'view' ClusterRole is automatically bound to the default ServiceAccount of every namespace as soon as the namespace is created.",
    ],
    correctIndexes: [0, 1, 3],
    explanation: "Application-specific ServiceAccount bindings rank as the most secure documented approach, and granting cluster-admin to every ServiceAccount cluster-wide is flagged as a policy to avoid; a Pod without an explicit serviceAccountName falls back to its namespace's default ServiceAccount. Binding a role to every ServiceAccount in one namespace is in fact possible via a group like system:serviceaccounts:<namespace>, and none of the built-in view, edit, or admin roles carry a default ClusterRoleBinding at all.",
  },

  // --- Preparing infrastructure and creating clusters with kubeadm (cluster-008..014) ---
  {
    id: 'cluster-008',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'An administrator runs kubeadm init on a single machine without setting --control-plane-endpoint, planning to keep it a single-node cluster indefinitely. Months later, growth requires making it highly available. What does the kubeadm documentation say about this path?',
    options: [
      "It isn't possible: kubeadm does not support later adding that shared endpoint and converting a lone control-plane node into an HA setup.",
      'It works automatically the first time a second machine runs kubeadm join with the --control-plane flag.',
      "It's supported, but only if the cluster's etcd was configured as an external topology from the very first init.",
      "It's supported by re-running kubeadm init --upload-certs on the original node without any cluster downtime.",
    ],
    correctIndexes: [0],
    explanation: "kubeadm's own guidance flags this exact scenario as unsupported: once a cluster has been bootstrapped without a shared control-plane endpoint address, there is no supported path to retrofit high availability onto it, which is precisely why setting that flag on day one is recommended for any cluster that might eventually grow.",
  },
  {
    id: 'cluster-009',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: "Per kubeadm's stated prerequisites, what is the minimum number of CPUs required on a machine intended to serve as a control-plane node?",
    options: ['1', '4', '8', '2'],
    correctIndexes: [3],
    explanation: "kubeadm's documented prerequisites call for at least 2 CPUs on any machine used as a control-plane node, alongside a minimum of 2 GiB of RAM on every machine in the cluster.",
  },
  {
    id: 'cluster-010',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'On a cluster that has just finished kubeadm init but has no Pod network add-on applied yet, what is true of CoreDNS?',
    options: [
      'CoreDNS starts immediately after kubeadm init completes, independent of whether any Pod network add-on exists.',
      'CoreDNS will not start running until a CNI-based Pod network add-on has been deployed with kubectl apply.',
      'CoreDNS only fails to start if the cluster has more than one control-plane node.',
      'CoreDNS requires a worker node to have already joined before it can be scheduled anywhere.',
    ],
    correctIndexes: [1],
    explanation: "kubeadm's setup guidance is explicit that cluster DNS will not start up before a Pod network add-on is installed, which is why applying a CNI-based network add-on comes right after control-plane initialization, ahead of joining any other nodes.",
  },
  {
    id: 'cluster-011',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'How many separate Pod network add-ons can be active on a single kubeadm-managed cluster at the same time?',
    options: [
      'Exactly two — one to handle IPv4 traffic and a second for IPv6.',
      'As many as an administrator wants to install, one per namespace.',
      'Exactly one.',
      "None by default; kubeadm ships with a built-in Pod network that can't be replaced.",
    ],
    correctIndexes: [2],
    explanation: "The documentation states that only one Pod network can be installed per cluster; kubeadm itself doesn't bundle a default Pod network implementation, so the single add-on has to be chosen and applied by the administrator.",
  },
  {
    id: 'cluster-012',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'kubeadm init produces both an admin.conf and a super-admin.conf kubeconfig file. What makes the identity inside super-admin.conf different from the one in admin.conf?',
    options: [
      'It belongs to the system:masters group, a break-glass identity that bypasses normal authorization checks.',
      'It is a read-only identity meant for auditors rather than for cluster administrators.',
      'It authenticates with a short-lived ServiceAccount token instead of an X.509 client certificate.',
      'It is deleted automatically once the two-hour window for --upload-certs certificate sharing expires.',
    ],
    correctIndexes: [0],
    explanation: 'admin.conf carries a certificate bound to the cluster-admin ClusterRole through the kubeadm:cluster-admins group, while super-admin.conf carries a certificate in system:masters, a break-glass superuser group that bypasses the authorization layer entirely; both files are sensitive and neither should be shared.',
  },
  {
    id: 'cluster-013',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'What is the correct sequence for cleanly removing a node from a kubeadm-created cluster?',
    options: [
      'Delete the Node object with kubectl delete node first, then run kubeadm reset, then drain the node.',
      'Run kubeadm reset on the node first, then drain it, and finally delete the Node object.',
      'Delete the Node object and run kubeadm reset at the same time; draining is only needed for control-plane nodes.',
      'Drain the node with kubectl drain, run kubeadm reset on that node, then delete its Node object with kubectl delete node.',
    ],
    correctIndexes: [3],
    explanation: 'Cleanly decommissioning a node means draining it first so the scheduler can move its workloads elsewhere, then running kubeadm reset locally to undo the local state kubeadm created, and only afterward removing the Node object from the API.',
  },
  {
    id: 'cluster-014',
    domain: 'cluster',
    questionType: 'multiple-response',
    question: 'Which of the following are described as considerations when moving from a single-machine learning setup to a production-quality Kubernetes cluster? (Select all that apply.)',
    options: [
      'Keeping control-plane services off the worker machines and running each control-plane component on more than one node.',
      'Confining the cluster to a single availability zone, which the documentation recommends for simplifying network routing.',
      "Putting a load balancer in front of the cluster's API server.",
      'Standardizing on one shared ServiceAccount that every workload in the cluster uses, to simplify auditing.',
      'Choosing an authentication method, such as client certificates or bearer tokens, together with an authorization mode like RBAC.',
    ],
    correctIndexes: [0, 2, 4],
    explanation: 'Production planning covers separating and replicating the control plane, load-balancing API server traffic, and layering authentication with an authorization mode such as RBAC. The documentation actually favors spanning multiple zones for resilience rather than confining a cluster to one, and it recommends a dedicated ServiceAccount per application rather than one shared account for everything.',
  },

  // --- Cluster lifecycle and high availability (cluster-015..021) ---
  {
    id: 'cluster-015',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'At a high level, what order does kubeadm follow when upgrading a cluster to a new Kubernetes minor version?',
    options: [
      'Upgrade every worker node first so the new version is validated before any control-plane node changes.',
      'Upgrade the first control-plane node, then the remaining control-plane nodes, then the worker nodes.',
      'Upgrade all control-plane nodes and all worker nodes at the same time to shorten the maintenance window.',
      'Upgrade the etcd hosts first, then the worker nodes, and finish with the control-plane nodes.',
    ],
    correctIndexes: [1],
    explanation: "kubeadm's documented upgrade workflow starts with the primary control-plane node, moves on to any additional control-plane nodes, and only afterward reaches the worker nodes, a few at a time so running capacity is never compromised.",
  },
  {
    id: 'cluster-016',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'Which command does an administrator run on the very first control-plane node during a kubeadm minor-version upgrade, that is not used on the remaining control-plane nodes?',
    options: [
      'kubeadm upgrade node',
      'kubeadm join --control-plane --upgrade',
      'kubeadm upgrade apply',
      'kubeadm init --upgrade-existing',
    ],
    correctIndexes: [2],
    explanation: 'The first control-plane node runs kubeadm upgrade apply against the target version; every remaining control-plane node instead runs kubeadm upgrade node, which is also what worker nodes use for their own kubelet-configuration upgrade.',
  },
  {
    id: 'cluster-017',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'A cluster is running Kubernetes 1.34. Which upgrade target is a single kubeadm upgrade apply operation able to reach?',
    options: ['1.35', '1.36', '1.37', '1.33'],
    correctIndexes: [0],
    explanation: 'kubeadm only supports advancing one Kubernetes minor version per upgrade; skipping a minor version is unsupported, so from 1.34 the only valid forward target for a single upgrade is 1.35.',
  },
  {
    id: 'cluster-018',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: "Why does kubeadm's upgrade guidance call for draining a control-plane node before touching its kubelet, even though it's a control-plane node rather than an ordinary worker?",
    options: [
      "Because draining is a prerequisite step for renewing the node's etcd certificates.",
      'Because kubeadm refuses to touch static Pod manifests on any node that still has Pods scheduled to it.',
      'Because the node must be powered off entirely before kubeadm upgrade node will run.',
      'Because that control-plane node could currently be hosting CoreDNS or another critical workload.',
    ],
    correctIndexes: [3],
    explanation: 'Control-plane nodes can end up running ordinary workloads such as CoreDNS, so draining before a minor-version kubelet upgrade gives the scheduler a chance to move those Pods elsewhere before the upgrade disrupts that node.',
  },
  {
    id: 'cluster-019',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'What structurally distinguishes the stacked etcd topology from the external etcd topology in a kubeadm HA cluster?',
    options: [
      'The stacked topology relies on one shared etcd instance for the whole cluster, while external etcd gives every control-plane node its own private etcd cluster.',
      'In the stacked topology, each control-plane node runs its own etcd member; in the external topology, etcd instead runs as its own separate cluster reached over the network.',
      'External etcd removes the need for a load balancer in front of the API servers, while stacked etcd still requires one.',
      'Stacked etcd is limited to two control-plane nodes, while external etcd requires at least five.',
    ],
    correctIndexes: [1],
    explanation: "Stacked etcd co-locates an etcd member with each control-plane node's other services, needing less infrastructure; external etcd separates etcd onto its own independent machines that every control-plane node reaches over the network, needing more hardware but isolating etcd failures from the rest of the control plane. Both topologies still call for a load balancer in front of the API servers.",
  },
  {
    id: 'cluster-020',
    domain: 'cluster',
    questionType: 'multiple-response',
    question: 'Which of the following are accurate about the minimum setup needed for a highly-available kubeadm cluster? (Select all that apply.)',
    options: [
      'A load balancer in front of the control-plane nodes only becomes necessary once the cluster has more than five control-plane nodes.',
      'At least three machines are needed for the control-plane nodes.',
      'An odd number of control-plane nodes helps with leader election if a machine or zone is lost.',
      'In an external etcd topology, at least three separate machines are needed to run etcd.',
      'SSH access from a single device to every node is unnecessary as long as kubeadm join is run manually on each host.',
    ],
    correctIndexes: [1, 2, 3],
    explanation: 'The documentation calls for at least three control-plane machines, and for external etcd at least three separate etcd machines, with an odd count helping the consensus protocol reach a clean majority. It lists a load balancer in front of every control-plane API server, and SSH access from one device to every node, as prerequisites from the very first control-plane node onward, not optional conveniences added later.',
  },
  {
    id: 'cluster-021',
    domain: 'cluster',
    questionType: 'multiple-response',
    question: "Which of the following statements about etcd backups and kubeadm's own backup behavior are accurate? (Select all that apply.)",
    options: [
      "A kubeadm cluster running a single control-plane node has no built-in resilience against that node failing, since etcd holds the cluster's entire state.",
      'Once an upgrade finishes successfully, kubeadm automatically deletes its backup folders to reclaim disk space.',
      'Backing up etcd is described as unnecessary in a stacked-etcd HA topology, since control-plane nodes replicate each other automatically.',
      "kubeadm's default location for the etcd data directory on a control-plane host is /var/lib/etcd.",
      'kubeadm upgrade automatically writes backup folders for etcd and static Pod manifests before an upgrade proceeds.',
    ],
    correctIndexes: [0, 3, 4],
    explanation: "Because etcd holds every object and Secret in the cluster, a single-control-plane kubeadm cluster has effectively no resilience against that node's loss; kubeadm defaults to /var/lib/etcd for its data directory and writes its own etcd and manifest backups before an upgrade. Those backup folders are left in place for manual cleanup rather than deleted automatically, and nothing in the documentation exempts HA topologies from needing their own backup plan.",
  },

  // --- Extending the cluster: Helm, Kustomize, CNI/CSI/CRI, CRDs (cluster-022..028) ---
  {
    id: 'cluster-022',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: "In Helm's terminology, what gets created every time helm install is run against a chart, even if that same chart is installed several times in one cluster?",
    options: [
      'A brand-new chart, since Helm treats each install as authoring a fresh chart definition.',
      "A new entry in the chart's source repository pointing back to this install.",
      'A new, independently named release.',
      'A dedicated Kubernetes namespace created just for that one installation.',
    ],
    correctIndexes: [2],
    explanation: 'Helm calls each installed instance of a chart a release; installing the same chart twice — two separate database instances, say — produces two independent, separately named releases that can each be tracked, upgraded, or removed on their own.',
  },
  {
    id: 'cluster-023',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'What does helm upgrade do differently from tearing down a release entirely and reinstalling the chart from scratch?',
    options: [
      'It tries to perform the least invasive change, updating only the pieces of the release that actually changed.',
      "It always recreates every resource in the release, even ones that didn't change, to guarantee a clean state.",
      'It only takes effect if the --recreate-pods flag is passed alongside it.',
      'It automatically reverts to the prior revision the moment any resource in the release is modified.',
    ],
    correctIndexes: [0],
    explanation: "Because charts can be large and complex, Helm's upgrade path is deliberately conservative — it updates only what changed since the previous release rather than rebuilding everything, which is also why a separate helm rollback command exists for undoing an upgrade that didn't go well.",
  },
  {
    id: 'cluster-024',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'Which command renders and prints the manifests a kustomization directory describes, without those resources needing to already exist in the cluster?',
    options: [
      'kubectl apply -k <kustomization_directory> --preview-only',
      'kubectl get -k <kustomization_directory>',
      'kubectl edit -k <kustomization_directory>',
      'kubectl kustomize <kustomization_directory>',
    ],
    correctIndexes: [3],
    explanation: 'kubectl kustomize renders and prints the resources a kustomization directory describes without touching the cluster; kubectl get -k, by contrast, queries objects that have already been applied and must already be live in the cluster, so it is not a stand-in for a pure preview.',
  },
  {
    id: 'cluster-025',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: "Starting with which Kubernetes version does the kubelet refuse to register a node whose container runtime doesn't implement the v1 CRI API?",
    options: ['v1.14', 'v1.26', 'v1.20', 'v1.30'],
    correctIndexes: [1],
    explanation: 'From Kubernetes v1.26 onward, the kubelet requires its container runtime to support the v1 CRI API; if the runtime does not implement that version, the kubelet will not register the node at all.',
  },
  {
    id: 'cluster-026',
    domain: 'cluster',
    questionType: 'multiple-choice',
    question: 'Which CNI plugin does the Kubernetes documentation point administrators toward for adding hostPort support to their network configuration?',
    options: [
      'The official bandwidth plugin.',
      'The loopback plugin.',
      'The official portmap plugin.',
      'The CoreDNS plugin.',
    ],
    correctIndexes: [2],
    explanation: "hostPort support comes from the portmap plugin (or an equivalent with portMapping capability); the bandwidth plugin instead handles traffic shaping, and the loopback plugin only supplies the lo interface each sandbox needs — CoreDNS isn't a CNI plugin at all.",
  },
  {
    id: 'cluster-027',
    domain: 'cluster',
    questionType: 'multiple-response',
    question: 'Which of the following are advantages of a CustomResourceDefinition (CRD) over building an aggregated API server, according to the Kubernetes documentation? (Select all that apply.)',
    options: [
      'A CRD can use a custom storage backend with a different performance profile than the default.',
      "A CRD doesn't require writing and running a separate API server.",
      'Creating a CRD requires no programming at all.',
      'A CRD gives client applications built around Protocol Buffers native support out of the box.',
      "There's no additional service to run, since the existing API server handles CRDs directly.",
    ],
    correctIndexes: [1, 2, 4],
    explanation: 'CRDs skip the need for a separate API server, need no programming to define, and add no extra service for the cluster to run since the main API server handles them directly. Custom storage backends and out-of-the-box Protocol Buffers support are advantages the documentation reserves for aggregated API servers, not CRDs.',
  },
  {
    id: 'cluster-028',
    domain: 'cluster',
    questionType: 'multiple-response',
    question: 'Which of the following statements about the Container Storage Interface (CSI) are accurate? (Select all that apply.)',
    options: [
      'Migrating to a CSI driver via CSIMigration requires rewriting every existing StorageClass, PersistentVolume, and PersistentVolumeClaim to name the new driver.',
      'A single CSI driver deployment can only ever serve one Kubernetes cluster at a time.',
      "CSI lets a storage vendor ship a driver for their system without that vendor's code being compiled into Kubernetes itself.",
      "Before CSI existed, storage integrations were built directly into Kubernetes' own source tree as 'in-tree' plugins.",
      'A csi-type volume can be consumed in a Pod through a reference to a PersistentVolumeClaim.',
    ],
    correctIndexes: [2, 3, 4],
    explanation: "CSI is an out-of-tree interface that replaced building storage support directly into Kubernetes' source tree, and Pods can consume CSI-backed storage the ordinary way, through a PersistentVolumeClaim. CSIMigration is specifically designed to redirect existing configuration to the equivalent CSI driver without requiring changes to existing StorageClasses, PersistentVolumes, or PersistentVolumeClaims, and nothing in the documentation limits a CSI driver to a single cluster.",
  },

  // --- Pod connectivity (services-001..003) ---
  {
    id: 'services-001',
    domain: 'services',
    questionType: 'multiple-choice',
    question: 'A cluster uses separate components to hand out addresses to Pods, Services, and Nodes. Which component takes on the job of handing Pods their IP addresses?',
    options: [
      'The kube-apiserver.',
      'kube-proxy.',
      'The network plugin.',
      'Either the kubelet or, where one exists, the cloud-controller-manager.',
    ],
    correctIndexes: [2],
    explanation: "Kubernetes documentation splits address allocation three ways: Pod addressing falls to the network plugin, Service addressing falls to the kube-apiserver, and Node addressing falls to whichever of the kubelet and cloud-controller-manager is present; kube-proxy plays no role in this allocation.",
  },
  {
    id: 'services-002',
    domain: 'services',
    questionType: 'multiple-choice',
    question: "The Kubernetes documentation identifies highly-coupled container-to-container communication as one of four distinct networking problems a cluster must address. How does it say this particular problem is solved?",
    options: [
      "By giving each container its own routable address through the cluster's CNI plugin, the same mechanism used for Pod-to-Pod traffic.",
      'By grouping the containers together inside a shared Pod, where they can reach each other over localhost.',
      'By placing a Service in front of the containers so callers get a stable virtual IP no matter which node they land on.',
      'By having kube-proxy forward requests between the containers whenever they share a node.',
    ],
    correctIndexes: [1],
    explanation: "Kubernetes lists four networking problems, and it solves the container-to-container case through Pods themselves: containers in the same Pod share a network namespace and can talk over localhost, without needing a CNI-assigned address, a Service, or kube-proxy involvement.",
  },
  {
    id: 'services-003',
    domain: 'services',
    questionType: 'multiple-choice',
    question: 'What actually implements the Kubernetes Pod network model on each node, according to the documentation?',
    options: [
      'The kube-scheduler, when it chooses which node has free IP addresses available.',
      'Whichever Ingress controller is deployed in the cluster.',
      'kube-proxy, by writing the iptables or IPVS rules for every Pod interface.',
      'The container runtime on that node, most commonly through a CNI plugin.',
    ],
    correctIndexes: [3],
    explanation: "Per the documentation, each node's container runtime is what carries out the Pod network model in practice, and most of those runtimes lean on CNI plugins to handle the networking and security work.",
  },

  // --- Network Policies (services-004..008) ---
  {
    id: 'services-004',
    domain: 'services',
    questionType: 'multiple-choice',
    question: "Before any NetworkPolicy in a namespace selects a given Pod, what is that Pod's isolation state?",
    options: [
      'Non-isolated for both ingress and egress, so every inbound and outbound connection is allowed.',
      'Isolated for ingress only, since inbound traffic is denied by default until a policy allows it.',
      'Isolated for egress only, since outbound traffic is denied by default until a policy allows it.',
      'Isolated for both ingress and egress, matching a default-deny security posture.',
    ],
    correctIndexes: [0],
    explanation: 'Kubernetes leaves a Pod free of restriction in both directions right up until some NetworkPolicy actually selects it and names that direction under policyTypes; only from that point on does isolation, and therefore filtering, begin for the Pod in that direction.',
  },
  {
    id: 'services-005',
    domain: 'services',
    questionType: 'multiple-choice',
    question: "Pod A is isolated for egress by a policy whose egress list allows connections to Pod B. Pod B is also isolated for ingress, but no policy's ingress list grants Pod B permission to receive traffic from Pod A. What happens when Pod A tries to connect to Pod B?",
    options: [
      "The connection succeeds, since only the connecting Pod's own egress rules matter for the attempt.",
      'The connection is blocked only if Pod A and Pod B run on different nodes.',
      "The connection is blocked, because both the source's egress policy and the destination's ingress policy must allow it.",
      'The connection succeeds, because NetworkPolicies never restrict traffic between two Pods in the same namespace.',
    ],
    correctIndexes: [2],
    explanation: 'For a connection to succeed, the source Pod\'s applicable egress policy and the destination Pod\'s applicable ingress policy both have to allow it; if either side withholds permission, the connection does not happen, regardless of same-namespace placement or node location.',
  },
  {
    id: 'services-006',
    domain: 'services',
    questionType: 'multiple-choice',
    question: "A NetworkPolicy's ingress rule has a single `from` list entry that nests both a `namespaceSelector` and a `podSelector` under it, rather than listing them as two separate entries. What traffic does that one entry allow?",
    options: [
      'Traffic from any Pod that matches either the podSelector or the namespaceSelector alone.',
      'Only traffic from Pods matching podSelector that also run in a namespace matching namespaceSelector.',
      'Traffic from every Pod in any namespace matching the namespaceSelector, ignoring Pod-level labels entirely.',
      'The API server rejects the policy, since one entry cannot combine two different selector types.',
    ],
    correctIndexes: [1],
    explanation: 'Nesting namespaceSelector and podSelector inside the same from entry narrows the match to Pods carrying the given labels within namespaces carrying the given labels (an AND). Splitting them into two separate list entries instead produces a broader OR: Pods matching either selector.',
  },
  {
    id: 'services-007',
    domain: 'services',
    questionType: 'multiple-choice',
    question: 'A namespace gets a NetworkPolicy with an empty podSelector, policyTypes set to Egress, and no egress rules, intended as a default-deny-egress baseline. What commonly-needed traffic does this policy silently break on its own?',
    options: [
      "Traffic to the Pod's own node, since default-deny-egress also blocks node-local connections.",
      'All ingress traffic into those same Pods, since Kubernetes treats deny-egress and deny-ingress rules as permanently linked together.',
      'Nothing; DNS lookups are automatically exempted from NetworkPolicy enforcement.',
      "DNS resolution, since Pods can no longer reach the cluster's own DNS service without an added policy.",
    ],
    correctIndexes: [3],
    explanation: "The documentation carries an explicit caution that this kind of default-deny-egress rule has the side effect of cutting off DNS lookups too, so any workload that still needs name resolution needs its own extra NetworkPolicy explicitly permitting egress to the cluster's DNS service.",
  },
  {
    id: 'services-008',
    domain: 'services',
    questionType: 'multiple-response',
    question: 'According to the Kubernetes documentation, which of the following are things you cannot currently do using the NetworkPolicy API alone? (Select all that apply.)',
    options: [
      "Restrict which destination TCP port a Pod's egress traffic may use.",
      'Target a specific Service by name inside an ingress rule or an egress rule.',
      'Select traffic using a namespaceSelector combined with a podSelector.',
      'Write an explicit "deny" rule; the model only supports adding allow rules on top of an implicit deny.',
      'Log which connections a given policy blocked or permitted.',
    ],
    correctIndexes: [1, 3, 4],
    explanation: "The documentation's list of NetworkPolicy limitations explicitly includes targeting Services by name, explicitly denying traffic (the model is additive-allow only), and logging blocked or accepted connections. Restricting a destination port and combining namespaceSelector with podSelector are both ordinary, supported NetworkPolicy capabilities.",
  },

  // --- Service types and endpoints (services-009..012) ---
  {
    id: 'services-009',
    domain: 'services',
    questionType: 'multiple-choice',
    question: "A Service manifest omits the `type` field entirely and leaves `.spec.clusterIP` unset. What kind of Service results?",
    options: [
      'A ClusterIP Service, reachable only inside the cluster, with a cluster-assigned virtual IP.',
      'A headless Service, since leaving both fields unset is how a headless Service is requested.',
      'A NodePort Service, since Kubernetes defaults new Services to be reachable from outside the cluster.',
      'An ExternalName Service that resolves through a DNS CNAME record.',
    ],
    correctIndexes: [0],
    explanation: "ClusterIP is the default Service type used whenever type isn't explicitly set, and it assigns a cluster-internal virtual IP from the cluster's reserved address pool. A headless Service instead requires explicitly setting clusterIP to the string \"None\".",
  },
  {
    id: 'services-010',
    domain: 'services',
    questionType: 'multiple-choice',
    question: 'With the default NodePort range of 30000-32767, which statement correctly describes how the static and dynamic port bands work?',
    options: [
      'Both bands are exactly the same size, and neither one gets preference during automatic assignment.',
      'The bands only matter for LoadBalancer Services; plain NodePort Services ignore the split entirely.',
      'Auto-assignment reaches for the upper band (30086-32767) first, so a manually chosen port from the lower band (30000-30085) risks fewer collisions.',
      'Manually requested NodePort values must come from the dynamic band; the static band is reserved for system Services.',
    ],
    correctIndexes: [2],
    explanation: 'Splitting the default range into a lower band (30000-30085) and an upper band (30086-32767) matters because Kubernetes reaches for the upper one first whenever it auto-picks a port, so an administrator manually requesting a port from the lower band faces less chance of a collision with something auto-assigned.',
  },
  {
    id: 'services-011',
    domain: 'services',
    questionType: 'multiple-choice',
    question: 'On a cloud provider that supports it, what does Kubernetes typically do first when a Service of type LoadBalancer is created, before the external load balancer itself is configured?',
    options: [
      "It writes a public IP address directly into the Service's spec.clusterIP field right away.",
      'It makes the same changes as a type: NodePort Service, then the cloud-controller-manager points the external load balancer at that node port.',
      "It sends traffic straight to Pod IPs in every configuration, skipping any node-level port entirely.",
      'It converts the Service to type: ExternalName so clients resolve it straight to the cloud load balancer hostname.',
    ],
    correctIndexes: [1],
    explanation: "Kubernetes documentation states that implementing a LoadBalancer Service typically starts by making the same changes as a NodePort Service; from there, the cloud-controller-manager sets up the external load balancer so it directs traffic at that already-assigned node port, with the balancer's own provisioning happening asynchronously.",
  },
  {
    id: 'services-012',
    domain: 'services',
    questionType: 'multiple-response',
    question: 'Which of the following are reasons the Kubernetes documentation gives for preferring the EndpointSlice API over the older, deprecated Endpoints API? (Select all that apply.)',
    options: [
      'EndpointSlice supports dual-stack clusters, which the Endpoints API does not.',
      'EndpointSlice is required before a Service can use a label selector at all; Endpoints never supported selectors.',
      "EndpointSlice avoids truncating the list of endpoints when a Service has a very large number of backing Pods.",
      'Only EndpointSlice objects can be created for headless Services; Endpoints objects are rejected for them.',
      'EndpointSlice carries information needed for newer features, such as trafficDistribution, that Endpoints lacks.',
    ],
    correctIndexes: [0, 2, 4],
    explanation: 'The documentation lists three specific problems with the deprecated Endpoints API relative to EndpointSlice: no dual-stack support, missing data for newer features like trafficDistribution, and truncation once the endpoint list grows too large for one object. Selector-based Services and headless Services both predate and work with either API.',
  },

  // --- Gateway API (services-013..017) ---
  {
    id: 'services-013',
    domain: 'services',
    questionType: 'multiple-choice',
    question: "Gateway API's resource model is described as \"role-oriented.\" Which option correctly matches a concern with the organizational role the documentation associates it with?",
    options: [
      'Infrastructure Provider: concerned with per-app configuration settings and how one Service is put together.',
      'Application Developer: concerned with managing infrastructure that lets isolated clusters serve multiple tenants.',
      'GatewayClass: a human role played by whoever writes HTTPRoute objects, not an API kind.',
      'Cluster Operator: concerned with policies, network access, and application permissions across the cluster.',
    ],
    correctIndexes: [3],
    explanation: "The documentation assigns policies, network access, and application permissions to the Cluster Operator role; per-app settings and how a Service gets put together fall to the Application Developer instead, and standing up multi-tenant infrastructure is the Infrastructure Provider's job — the two distractors describing those roles have simply swapped which role owns which concern, and GatewayClass is an API kind, not a role at all.",
  },
  {
    id: 'services-014',
    domain: 'services',
    questionType: 'multiple-choice',
    question: 'What is the relationship between a Gateway object and a GatewayClass?',
    options: [
      'A Gateway must reference one GatewayClass, which names the controller implementing Gateways of that class.',
      'A GatewayClass is optional; a Gateway can function correctly without ever referencing one.',
      'A single GatewayClass can only ever be referenced by one Gateway object at a time.',
      "GatewayClass defines the HTTP routing rules directly, while Gateway only stores the controller's name.",
    ],
    correctIndexes: [0],
    explanation: 'Each Gateway points at a single GatewayClass, and that GatewayClass is what identifies the controller tasked with managing every Gateway built from it; many Gateways can share one GatewayClass, and routing rules live in Route objects, not in GatewayClass.',
  },
  {
    id: 'services-015',
    domain: 'services',
    questionType: 'multiple-choice',
    question: 'By default, from which namespaces will a Gateway accept attached Routes, such as HTTPRoutes?',
    options: [
      'Routes from any namespace in the cluster, with no restriction unless one is explicitly configured.',
      'Only Routes that happen to share the same name as the Gateway, regardless of namespace.',
      "Only Routes that were created in that Gateway's own namespace, unless allowedRoutes says otherwise.",
      "Only Routes created in whichever namespace the Gateway's GatewayClass itself lives in.",
    ],
    correctIndexes: [2],
    explanation: "Out of the box, a Gateway restricts itself to Routes living alongside it in that one namespace; letting a Route from elsewhere attach means setting the Gateway's allowedRoutes field to permit it.",
  },
  {
    id: 'services-016',
    domain: 'services',
    questionType: 'multiple-choice',
    question: 'What does adopting Gateway API mean for a cluster that already has existing Ingress resources?',
    options: [
      'Ingress and Gateway API share the exact same underlying resource, so no conversion of existing objects is needed.',
      "Ingress resources need a one-time conversion to Gateway API resources, since Gateway API doesn't include an Ingress kind.",
      'Gateway API can only be installed in clusters that have never created an Ingress object.',
      "Existing Ingress objects are automatically translated into HTTPRoutes as soon as Gateway API's CRDs are installed.",
    ],
    correctIndexes: [1],
    explanation: 'Gateway API is documented as the successor to Ingress, but it does not include an Ingress kind of its own, so migrating means a one-time, manual-or-tooled conversion of existing Ingress resources into Gateway API resources; nothing happens automatically.',
  },
  {
    id: 'services-017',
    domain: 'services',
    questionType: 'multiple-response',
    question: "Which of the following statements about Gateway API's HTTPRoute and GRPCRoute kinds are correct, according to the documentation? (Select all that apply.)",
    options: [
      "HTTPRoute takes HTTP traffic arriving at a Gateway listener and maps it onward to backend endpoints, typically a Service.",
      'A Gateway handling GRPCRoute traffic must speak HTTP/2 from the very first request, skipping any upgrade from a plain HTTP/1 connection.',
      'Only one HTTPRoute can ever attach to a given Gateway listener at a time.',
      'A GRPCRoute can restrict its match to one specific gRPC service and method, instead of routing all traffic for a hostname.',
      'HTTPRoute and GRPCRoute are both deprecated in favor of the older Ingress path-matching rules.',
    ],
    correctIndexes: [0, 1, 3],
    explanation: 'HTTPRoute and GRPCRoute are current, actively-used Gateway API kinds, not deprecated ones. HTTPRoute maps HTTP traffic to backends such as Services; GRPCRoute needs HTTP/2 without an upgrade dance so gRPC works, and it can narrow matching down to a specific service-and-method pair, as shown in the documentation\'s Login-method example. Multiple HTTPRoutes may attach to one listener.',
  },

  // --- Ingress and Ingress controllers (services-018..021) ---
  {
    id: 'services-018',
    domain: 'services',
    questionType: 'multiple-choice',
    question: 'A cluster administrator creates an Ingress resource but has not deployed any Ingress controller. What is the practical effect?',
    options: [
      'The kube-apiserver rejects the Ingress object at creation time with a validation error.',
      'Kubernetes automatically installs a default Ingress controller the first time an Ingress object is created.',
      "The cluster's default Service proxy routes traffic for it until a dedicated controller is installed.",
      "Nothing observable happens; the Ingress object alone has no effect without a controller.",
    ],
    correctIndexes: [3],
    explanation: "The documentation is explicit that a running Ingress controller is what actually satisfies an Ingress; the Ingress object on its own, with nothing implementing it, changes nothing about how traffic flows.",
  },
  {
    id: 'services-019',
    domain: 'services',
    questionType: 'multiple-choice',
    question: 'Which statement correctly describes how TLS works for a standard Ingress resource?',
    options: [
      'Ingress supports only port 443 for TLS, terminating it right at the ingress, so the onward hop to the Service and Pods stays unencrypted.',
      'Ingress lets administrators terminate TLS on any port they choose, and the connection to backend Pods stays encrypted end-to-end.',
      'TLS on an Ingress is configured per-Service rather than per-hostname, so every Service behind one Ingress must share a certificate.',
      'Ingress resources cannot terminate TLS at all; that responsibility always falls to the backend Pods themselves.',
    ],
    correctIndexes: [0],
    explanation: "Only port 443 can carry TLS on an Ingress, and decryption happens right there at the ingress point, so what continues on toward the Service and its Pods travels as plain, unencrypted traffic rather than staying encrypted end-to-end.",
  },
  {
    id: 'services-020',
    domain: 'services',
    questionType: 'multiple-choice',
    question: 'An Ingress rule sets `pathType: Prefix` with `path: /foo`. Based on the documented path-matching examples, which incoming request path is NOT matched by this rule?',
    options: [
      '/foo',
      '/foo/bar',
      '/foobar',
      '/foo/',
    ],
    correctIndexes: [2],
    explanation: "Prefix matching works element by element, splitting both the rule's path and the request path on slashes and comparing piece by piece; a trailing substring like /foobar doesn't line up element-for-element and so fails to match, mirroring the documented /aaa/bbb-versus-/aaa/bbbxyz case. /foo, /foo/, and /foo/bar are each documented as matches (an exact hit, a match ignoring the trailing slash, and a matched subpath).",
  },
  {
    id: 'services-021',
    domain: 'services',
    questionType: 'multiple-response',
    question: 'Which of the following statements about IngressClass are accurate, according to the Kubernetes documentation? (Select all that apply.)',
    options: [
      'The ingressClassName field on an Ingress is a direct, one-to-one equivalent of the older kubernetes.io/ingress.class annotation.',
      "Marking more than one IngressClass as default causes the admission controller to block creation of Ingress objects lacking an ingressClassName.",
      "An IngressClass's .spec.controller field names the controller that should implement Ingresses of that class.",
      'The IngressClass API object itself can be created as a namespaced resource whenever its parameters are namespace-scoped.',
      'IngressClass parameters can be scoped to a single namespace instead of being cluster-wide.',
    ],
    correctIndexes: [1, 2, 4],
    explanation: "The documentation warns that having more than one default IngressClass makes the admission controller block Ingresses without an explicit ingressClassName, states that .spec.controller names the implementing controller, and allows .spec.parameters to be namespace-scoped. It also explicitly says ingressClassName is a replacement for the old annotation but \"not a direct equivalent,\" and that no matter how its parameters are scoped, the IngressClass object stays a cluster-scoped API on its own.",
  },

  // --- CoreDNS (services-022..025) ---
  {
    id: 'services-022',
    domain: 'services',
    questionType: 'multiple-choice',
    question: "A workload running inside the `test` namespace looks up the plain name `data`, with no dot and no namespace qualifier. A Service named `data` exists only in the `prod` namespace. What happens?",
    options: [
      "The query resolves successfully to the prod Service, because CoreDNS searches every namespace by default.",
      "The query returns no results, because an unqualified DNS query is scoped to the Pod's own namespace.",
      'The query fails outright, because unqualified single-label names are always invalid Kubernetes DNS queries.',
      "The query resolves only if the Pod's own ServiceAccount happens to hold RBAC permission to read Services over in prod.",
    ],
    correctIndexes: [1],
    explanation: "Leaving a namespace out of a DNS query confines the lookup to whatever namespace the requesting Pod itself lives in, so a bare lookup for \"data\" from a Pod in test returns nothing; the same client would need to query \"data.prod\" to reach the Service that actually lives in the prod namespace.",
  },
  {
    id: 'services-023',
    domain: 'services',
    questionType: 'multiple-choice',
    question: 'A headless Service (clusterIP: None) defines a selector. How does its DNS resolution differ from a normal ClusterIP Service?',
    options: [
      'The DNS name does not resolve at all, since headless Services are not assigned any DNS records.',
      'The DNS name resolves to the same cluster IP a normal Service would get, just without proxy-layer load balancing.',
      'The DNS name resolves only through SRV records; A/AAAA lookups always fail for headless Services.',
      "The DNS name resolves directly to the set of individual Pod IP addresses backing the Service, instead of one cluster IP.",
    ],
    correctIndexes: [3],
    explanation: "For a headless Service with a selector, the cluster's DNS layer hands back address records that point straight at the IPs of the individual Pods behind the Service, rather than one virtual cluster IP, leaving clients to pick from or round-robin across that set themselves.",
  },
  {
    id: 'services-024',
    domain: 'services',
    questionType: 'multiple-choice',
    question: "A Pod spec does not set the `dnsPolicy` field at all. Which DNS policy actually applies to it?",
    options: [
      '"ClusterFirst", even though a policy literally named "Default" also exists.',
      '"Default", since that is the literal name of the fallback policy Kubernetes documents.',
      '"None", which forces the Pod to supply its own dnsConfig or receive no DNS resolution.',
      '"ClusterFirstWithHostNet", used as the safe fallback for Pods lacking an explicit policy.',
    ],
    correctIndexes: [0],
    explanation: 'The documentation flags this as a naming trap: even though a policy literally called "Default" exists, it is not the one Kubernetes falls back to; leaving dnsPolicy unset instead yields "ClusterFirst."',
  },
  {
    id: 'services-025',
    domain: 'services',
    questionType: 'multiple-response',
    question: "Which of the following statements about a Pod's dnsConfig field and the \"None\" DNS policy are correct, according to the documentation? (Select all that apply.)",
    options: [
      'Choosing "None" as the dnsPolicy value means dnsConfig.nameservers has to list at least one entry.',
      'dnsConfig only has an effect when dnsPolicy is set to "None"; it does nothing under any other policy.',
      "A Pod's dnsConfig can specify at most 3 nameserver IP addresses.",
      "A Pod's merged DNS search domain list can hold as many as 32 entries.",
      'Setting dnsPolicy to "None" makes the Pod inherit DNS settings from its node, the same as the "Default" policy.',
    ],
    correctIndexes: [0, 2, 3],
    explanation: 'The documentation requires dnsConfig to list at least one nameserver address once dnsPolicy is "None", caps that same list at 3 addresses, and permits as many as 32 search domains overall. It also describes dnsConfig as compatible with every dnsPolicy value, not only "None," contradicting the claim that it does nothing elsewhere; inheriting the node\'s DNS settings is what the "Default" policy does, not "None", which instead relies entirely on dnsConfig.',
  },
];
