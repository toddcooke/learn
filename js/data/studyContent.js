// js/data/studyContent.js
// Study notes for Domain 1: Design Secure Architectures (30% exam weight).
// Grounded in AWS documentation fetched into .cache/aws-docs/ (see
// scripts/fetch-doc.mjs). Written in the author's own words, not copied
// verbatim from AWS's docs.

export const STUDY_CONTENT = [
  {
    domain: 'secure',
    taskStatement: 'Task 1.1: Design secure access to AWS resources',
    topics: [
      {
        title: 'IAM foundations: users, groups, roles, and policies',
        body: "AWS Identity and Access Management (IAM) is the service that controls who can authenticate into your account and what they're authorized to do once they're in. A request only succeeds if a principal — an IAM user, a role, or a federated identity — is matched against policies that explicitly grant the permission being requested; everything is denied by default. A flexible authorization model puts people into groups so permissions can be managed collectively instead of per-user, uses roles for workloads and cross-account access instead of long-lived credentials, and attaches JSON policy documents that list allowed or denied actions on specific resources. Policies come in two flavors that matter for exam scenarios: identity-based policies attached to a user, group, or role, and resource-based policies (such as an S3 bucket policy or a KMS key policy) attached directly to the resource, which is the mechanism to use when you need to grant access from a different account without creating a role there. Designing around the principle of least privilege means starting every policy with the minimum actions and resources needed and expanding only when a real business need is demonstrated, rather than granting broad access and trying to restrict it later.",
      },
      {
        title: 'Securing the root user and requiring MFA',
        body: 'Every AWS account starts with a root user that has unrestricted access to every resource and billing setting in that account, which is exactly why AWS recommends never using it for day-to-day work. Best practice is to create individual IAM identities (or federated roles) for administrators and everyday users immediately after account creation, reserve the root credentials for the handful of tasks that genuinely require them, and lock those credentials away. Multi-factor authentication (MFA) should be enabled on the root user without exception and applied broadly to IAM users as well, since it forces a second proof of identity — a hardware key, virtual authenticator app, or similar — beyond a password before sensitive or privileged actions can succeed. Because a compromised root or admin credential without MFA gives an attacker the same access level the account owner has, MFA enforcement is one of the highest-leverage controls a solutions architect can specify in a security design.',
      },
      {
        title: 'The shared responsibility model and AWS global infrastructure',
        body: "AWS's shared responsibility model splits accountability into security 'of' the cloud and security 'in' the cloud. AWS is responsible for protecting the underlying hardware, software, networking, and facilities that run its services — the Regions, Availability Zones, and edge locations that make up its global infrastructure — and that responsibility is independently audited under AWS's compliance programs. The customer is responsible for everything they configure on top of that: identity and access management, operating-system and network configuration for anything they manage, and how they encrypt and protect their own data. Understanding this split is what lets an architect reason correctly about which controls (network ACLs, IAM policies, encryption settings) are the customer's job to design versus which physical and infrastructure protections AWS already provides, and it also explains why choosing which Regions and Availability Zones to deploy into is itself a customer-owned design decision that affects both resilience and compliance.",
      },
      {
        title: 'Temporary credentials, role switching, and cross-account access with AWS STS',
        body: "AWS Security Token Service (STS) issues temporary security credentials that expire after a configurable period ranging from minutes to hours, in contrast to long-term IAM user access keys that live until someone rotates or revokes them. Because they expire automatically and are generated on demand rather than stored, temporary credentials are the mechanism behind IAM roles: an EC2 instance profile, a Lambda execution role, or a human user who 'assumes a role' all work by having STS hand out short-lived credentials scoped to that role's policy. This is also the standard way to design cross-account access — rather than creating a duplicate IAM user in every account, you create a role in the target account that trusts the source account (or a specific principal in it), and users or applications call AssumeRole to switch into it for exactly as long as they need it. This delegation pattern scales far better than distributing long-term credentials and gives you a natural point (the role's trust policy and permissions policy) to control and audit exactly who can act as whom across account boundaries.",
      },
      {
        title: 'Multi-account security strategy with AWS Organizations, SCPs, and Control Tower',
        body: "As organizations grow, AWS recommends spreading workloads across multiple accounts rather than one, since accounts are natural boundaries for security, cost, and blast-radius containment. AWS Organizations lets you centrally create and group those accounts into organizational units and apply governance across all of them at once. Service control policies (SCPs) are the key access-control tool here: they set the maximum permissions available within an account or OU, acting as a guardrail that no identity-based policy inside that account can override, which is how a security team enforces rules like 'no member account may disable CloudTrail' organization-wide. AWS Control Tower builds on Organizations by automating the setup of a multi-account landing zone with pre-packaged guardrails for security, auditing, and compliance, so architects don't have to hand-roll SCPs and account structure from scratch. Together these services are the standard answer for 'how do you enforce a consistent security posture and access boundary across many AWS accounts.'",
      },
      {
        title: 'Federated access with IAM Identity Center and external directories',
        body: 'IAM Identity Center is AWS\'s purpose-built service for connecting a workforce — the people in your company — to both AWS accounts and AWS-managed applications from a single place, without creating an individual IAM user per person per account. It can synchronize users and groups from an existing identity provider or manage them natively, and its multi-account permissions feature lets you assign job-function-based access to many AWS accounts from one console, which is the modern replacement for hand-managing cross-account IAM roles at scale. Determining when to federate a directory service with IAM roles comes down to where your identities already live: if your organization already runs Active Directory or another SAML 2.0 / OIDC-compatible identity provider, federation lets those existing credentials map to an IAM role and receive temporary AWS credentials through STS, rather than duplicating every employee as a separate AWS identity. This federation approach (via IAM Identity Center or direct SAML/OIDC federation with STS) is the recommended pattern any time you need to onboard an existing workforce or partner directory to AWS without creating a second, parallel set of credentials to manage.',
      },
    ],
  },
  {
    domain: 'secure',
    taskStatement: 'Task 1.2: Design secure workloads and applications',
    topics: [
      {
        title: 'VPC security components: security groups, network ACLs, and route tables',
        body: "A security group acts as a stateful virtual firewall attached to individual resources such as EC2 instances: you define inbound and outbound rules by source/destination, port range, and protocol, and because it's stateful, a response to allowed traffic is automatically permitted back out regardless of the outbound rules. A network ACL (NACL) instead operates at the subnet boundary and is stateless, meaning inbound and outbound rules are evaluated independently and return traffic must be explicitly allowed in both directions — its numbered rules are evaluated in order, and the first match wins. Using both together gives you defense in depth: NACLs provide a coarse, subnet-wide layer of allow/deny rules, while security groups provide fine-grained, per-resource control. Route tables are the third piece of VPC network security: they determine where traffic from a subnet or gateway is sent next, and controlling which subnets' route tables point at an internet gateway versus a NAT gateway versus stay entirely internal is what actually enforces the public/private split described in network segmentation.",
      },
      {
        title: 'Network segmentation with public and private subnets, NAT gateways, and VPC endpoints',
        body: "Segmenting a VPC into public and private subnets is the standard pattern for controlling ports, protocols, and traffic flow: public subnets have a route to an internet gateway and hold resources that need direct inbound internet access (like load balancers), while private subnets have no such route and hold resources like databases and internal application servers that should never be reachable directly from the internet. A NAT gateway placed in a public subnet lets instances in a private subnet initiate outbound connections (for software updates or calls to external APIs) while blocking any unsolicited inbound connection from outside — traffic flows out through the NAT gateway's Elastic IP but nothing can reach the private instances directly. For workloads that need to reach other AWS services without traversing the public internet at all, VPC endpoints (and AWS's regional service endpoints generally) let resources in a private subnet call services like S3 or DynamoDB over private AWS network paths, removing the need for a NAT gateway or internet gateway for that traffic and shrinking the attack surface accordingly.",
      },
      {
        title: 'Defending against external threats with AWS Shield and AWS WAF',
        body: "AWS Shield defends against Distributed Denial of Service (DDoS) attacks: every AWS customer gets AWS Shield Standard automatically at no extra cost, which protects against common network- and transport-layer volumetric attacks like SYN floods and UDP reflection, while Shield Advanced adds more advanced detection, mitigation, and access to a dedicated response team for internet-facing applications that need a guaranteed higher level of protection. AWS WAF operates at a different layer, inspecting the actual HTTP/HTTPS requests reaching resources like a CloudFront distribution, an Application Load Balancer, or an API Gateway API, and it lets you write rules that allow, block, count, or challenge requests based on conditions such as source IP, country, header values, request length, or signatures associated with SQL injection and cross-site scripting attempts. Used together, Shield stops an application from being knocked offline by volumetric traffic and WAF stops malicious application-layer requests (like SQL injection payloads) from being served in the first place, which is why exam scenarios about external threat vectors typically expect both services named as part of the answer.",
      },
      {
        title: 'Securing application-user identity with Cognito and IAM Identity Center',
        body: "Amazon Cognito is built for authenticating and authorizing the end users of a web or mobile application — as opposed to IAM Identity Center, which is built for your internal workforce accessing AWS accounts and AWS-managed applications. Cognito user pools act as a user directory and OIDC identity provider: they handle sign-up, sign-in, MFA, and can federate with third-party identity providers like Google or an enterprise SAML/OIDC provider, issuing standard JWTs your application can trust. Cognito identity pools complement user pools by exchanging a proven identity (from a user pool or another IdP) for temporary AWS credentials via STS, so an authenticated app user can be granted scoped, temporary access to AWS resources like an S3 bucket or DynamoDB table without your application ever holding a long-term AWS credential. Choosing between the two identity services in a design is mostly a question of audience: IAM Identity Center for employees needing console/account access, Cognito for the customers or end users of the application you're building.",
      },
      {
        title: 'Protecting application credentials and detecting threats with Secrets Manager, GuardDuty, and Macie',
        body: "Hard-coding database passwords, API keys, and OAuth tokens into application source code is a classic credential-security failure, and AWS Secrets Manager exists specifically to eliminate it: applications retrieve secrets at runtime through an API call instead, and Secrets Manager can automatically rotate many secret types on a schedule so that a leaked credential has a short useful life without requiring an application redeploy. Amazon GuardDuty keeps a continuous watch over your account, ingesting CloudTrail logs, VPC flow logs, and DNS logs right out of the box, then leans on threat-intelligence feeds and machine learning models (instead of hand-written rules) to catch things like compromised credentials, cryptomining on EC2 or containers, and abnormal database login patterns. Amazon Macie takes a complementary, data-focused angle: it uses machine learning and pattern matching to discover sensitive data (PII, financial data, credentials) sitting in S3 buckets and flags buckets that are misconfigured to be publicly accessible, which is the tool of choice when the security question is 'where is our sensitive data and is it exposed' rather than 'is something actively attacking us.'",
      },
      {
        title: 'Securing external network connections with VPN and AWS Direct Connect',
        body: 'When a workload needs a secure path between an on-premises network and a VPC, AWS Site-to-Site VPN encrypts that traffic over the public internet using IPsec tunnels between a customer gateway on-premises and a virtual private gateway (or transit gateway) in AWS, making it a fast, relatively low-cost option to stand up. AWS Direct Connect instead establishes a dedicated, private physical network connection from on-premises equipment into AWS, bypassing the public internet entirely for more consistent bandwidth and lower, more predictable latency — a common design combines the two, using Direct Connect as the primary private link and a Site-to-Site VPN as an encrypted backup path or as encryption layered on top of Direct Connect. Choosing between them in a design comes down to the trade-off between setup time and cost (VPN can be provisioned quickly and cheaply) versus consistent throughput and latency guarantees (Direct Connect, which typically takes longer to provision through a physical cross-connect).',
      },
    ],
  },
  {
    domain: 'secure',
    taskStatement: 'Task 1.3: Determine appropriate data security controls',
    topics: [
      {
        title: 'Encrypting data at rest with AWS Key Management Service (KMS)',
        body: "AWS KMS is the managed service for creating and controlling the cryptographic keys used to encrypt data across AWS services like S3, EBS, and RDS. KMS keys are protected by FIPS 140-3 validated hardware security modules and never leave the service unencrypted — your applications and integrated AWS services call KMS to encrypt and decrypt rather than handling raw key material themselves. This matters architecturally because it means encryption at rest can usually be turned on as a configuration setting on a resource (pointing it at a KMS key) rather than something an application has to implement itself, and it establishes a key hierarchy where a root/master key you control in KMS protects the data keys that actually encrypt your bytes, so a single compromised data key doesn't expose your master key material.",
      },
      {
        title: 'Key policies: implementing access control for encryption keys',
        body: "Each KMS key is tied to a single key policy — never more than one — and that policy acts as a resource-based control scoped to that particular key, in contrast to IAM policies, which apply broadly across the whole account. By default, nobody — not even the account root user or the person who created the key — has any permission to use a KMS key unless a key policy, IAM policy, or grant explicitly allows it, which makes KMS a rare case of true default-deny even for privileged accounts. A key policy can also delegate authority by including a statement that enables IAM policies to grant access to the key, letting you manage day-to-day permissions through familiar IAM users, groups, and roles while the key policy remains the ultimate gate. Designing least-privilege key access means scoping key policies (and any IAM policies layered on top) so that only the specific roles or services that legitimately need to encrypt or decrypt with a given key can do so, which is especially important for keys protecting regulated or highly sensitive data.",
      },
      {
        title: 'Rotating KMS keys and renewing ACM certificates',
        body: "AWS KMS supports automatic key rotation for customer-managed keys, generating new cryptographic material on a schedule (a year by default, or a custom period you set) while keeping the same key ID and permissions, so rotation is transparent to any application already using that key — KMS automatically tracks which key material was used to encrypt older data and decrypts correctly without code changes. On-demand rotation is available for immediate rotation outside the schedule, and manual rotation (creating a brand-new key and cutting over to it) is the fallback for key types, like asymmetric keys, that don't support automatic rotation. AWS Certificate Manager provides an equivalent lifecycle service for TLS certificates that are actively associated with an integrated AWS service (such as an ALB or CloudFront distribution), but what makes a certificate eligible differs by type: for public certificates, DNS validation lets ACM renew fully unattended, while email validation requires a human to approve a renewal email before expiration; for private certificates there's no domain-validation step at all, so eligibility instead hinges on how the certificate was issued — one requested through ACM's own RequestCertificate API or console (then exported or associated with a service) is eligible for managed renewal, while one issued directly through the AWS Private CA IssueCertificate API is not, regardless of validation method. Certificates imported from a third party, or already expired, are never eligible and must be renewed manually — a detail worth remembering because it directly determines whether 'set it and forget it' certificate renewal actually applies to a given design.",
      },
      {
        title: 'Encrypting data in transit with AWS Certificate Manager and TLS',
        body: "AWS Certificate Manager (ACM) issues and manages the public and private SSL/TLS X.509 certificates that encrypt data in transit for AWS-integrated services, so you can enable HTTPS on a load balancer, API Gateway, or CloudFront distribution without buying, uploading, or manually tracking a certificate. ACM certificates can cover a single domain, several specific domains, or a wildcard covering unlimited subdomains, and certificates are Regional resources, so a certificate used in one AWS Region must be requested or imported again in every other Region it's needed in — notably, any certificate attached to CloudFront must be requested in the US East (N. Virginia) Region regardless of where the distribution's origins live. Because ACM handles both issuance and the automated renewal described above, using it (versus a self-managed certificate) removes one of the most common causes of production outages: an expired TLS certificate nobody remembered to renew.",
      },
      {
        title: 'Data backups, replication, and recovery strategies',
        body: "Data recovery design in AWS generally combines two complementary techniques: backups, which are point-in-time copies you can restore from if data is deleted, corrupted, or encrypted by ransomware, and replication, which keeps a live, continuously updated copy of data (across Availability Zones or Regions) to protect against infrastructure failure and reduce recovery time. AWS Backup lets organizations define centralized backup policies and enforce them across accounts through AWS Organizations, so a security or compliance team can guarantee a consistent backup cadence for every account rather than relying on each team to configure it individually. When designing for a specific workload, the right mix of backup frequency, cross-Region replication, and retention depends on the recovery point objective (how much data loss is tolerable) and recovery time objective (how fast recovery must happen) that the business has set for that data.",
      },
      {
        title: 'Data governance: access control, classification, retention, and compliance alignment',
        body: "Data governance ties together who can access data, how it's classified, how long it's kept, and what regulatory obligations apply to it. Aligning a design to compliance requirements typically means choosing AWS services and configurations that have been validated against the relevant standard (for example, checking a service's PCI DSS, HIPAA, or SOC compliance scope before storing regulated data in it) and layering AWS's own controls, like encryption, access logging, and IAM policies, on top to meet the parts of the shared responsibility model that are the customer's job. Amazon Macie supports the data-classification half of this by automatically discovering and reporting where sensitive data like PII lives across your S3 estate, which feeds directly into deciding what access policies, retention periods, and lifecycle rules (such as moving old data to cheaper, more restrictive storage or deleting it entirely after a defined period) should apply to each class of data. Getting this right is what lets an organization prove, during an audit, that sensitive data is both discoverable and governed by policy rather than scattered across buckets with inconsistent protection.",
      },
    ],
  },
  {
    domain: 'resilient',
    taskStatement: 'Task 2.1: Design scalable and loosely coupled architectures',
    topics: [
      {
        title: 'Achieving loose coupling with SQS, SNS pub/sub, and API Gateway',
        body: "Tight coupling is what happens when one component calls another directly and synchronously, so a slowdown or outage in the callee immediately becomes a slowdown or outage in the caller; loose coupling breaks that direct dependency by putting a buffer between components so each side can fail, scale, or deploy on its own schedule. Amazon SQS is the standard buffer for point-to-point work: a producer drops a message on a queue and moves on, and one or more consumers pull messages off whenever they're ready, so a burst of incoming work piles up in the queue instead of overwhelming or crashing whatever processes it, and a consumer that's temporarily down just means messages wait a little longer rather than getting lost. Amazon SNS extends the same idea into a publish-subscribe pattern: one published notification fans out to many subscribers at once (queues, functions, HTTP endpoints, email, and more), so a producer never needs to know how many downstream listeners exist, and pairing SNS with SQS is a common way to make sure a fanned-out event still lands durably even if a particular subscriber is briefly unavailable. Amazon API Gateway plays a related but different role as the entry point for an API: it terminates client connections, enforces authentication, throttling, and request validation, and routes accepted calls on to backend targets such as Lambda functions or private VPC resources, keeping all of that client-facing traffic management separate from whatever compute is actually doing the work.",
      },
      {
        title: 'Event-driven architectures and workflow orchestration with Step Functions',
        body: "An event-driven design reacts to things happening — a file landing in a bucket, an order being placed, a message hitting a queue — instead of running on a fixed schedule or waiting for a client to ask for work, and that reaction model tends to produce systems that are both more loosely coupled and more elastic, since each event producer and consumer only has to agree on what the event looks like, not on when or how the other side gets around to handling it. AWS Lambda functions are frequently the compute behind that reaction, since Lambda ships with built-in triggers for S3, SQS, DynamoDB Streams, and hundreds of other AWS services, letting you wire a response to an event without standing up a server that sits there polling. Once a single event needs to kick off more than one step — a sequence with branching logic, retries, or a blend of quick calls and long-running jobs — AWS Step Functions is the purpose-built way to coordinate it: you model the process as a state machine, and Step Functions manages the handoffs and error handling between Lambda invocations, ECS or Fargate tasks, and a long list of other integrated services, rather than that orchestration logic being scattered through application code. Step Functions gives you two workflow flavors to pick from depending on the job: one type keeps a complete execution history and guarantees each step runs once, well suited to long-running, auditable business processes, while the other favors very high throughput and short-lived runs, which fits bursty, high-volume event processing where a step might occasionally run more than once.",
      },
      {
        title: 'Scaling strategies: horizontal vs. vertical scaling, caching, and CDNs',
        body: "Vertical scaling means moving a workload onto a bigger instance — more vCPU, memory, or IOPS on the same box — which is straightforward but runs into a hard ceiling (the largest instance type that exists) and usually forces a restart, making it a poor fit for a highly available design that needs to absorb unpredictable spikes. Horizontal scaling instead adds more instances running the same workload behind a load balancer; there's no practical ceiling, it can happen automatically as load changes through Auto Scaling, and because the work is now spread across many hosts, fault tolerance improves as a side effect, since losing any single host only removes a small slice of total capacity rather than the whole service. Caching cuts down how much there is to scale for in the first place: an in-memory cache in front of a database, or a cache built into API Gateway itself, serves repeated requests for the same data without re-hitting the origin every time, lowering both latency and the read load the backend has to absorb. Amazon CloudFront pushes that same caching idea out to the network edge, keeping copies of static and some dynamic content at locations physically close to end users worldwide, so most requests never have to travel back to the origin Region at all — speeding up delivery for users while shielding the origin from having to scale for every individual request directly.",
      },
      {
        title: 'Multi-tier architectures, microservices, and load balancing with ALB',
        body: "A multi-tier architecture separates an application into layers — typically presentation, application logic, and data — each of which can be scaled, secured, and released on its own, and that separation predates microservices as the standard way to structure an application. Microservices carry the idea further by splitting the application layer itself into many small, independently deployable services, usually each with its own data store, so teams can ship and scale one service without coordinating a release of the whole system. A decision that comes up inside any of these layers is whether a given piece is stateless or stateful: a stateless component, like a typical web or API server, keeps no session data of its own, so any running instance can serve any incoming request and an Application Load Balancer can freely add, remove, or swap instances behind it and spread traffic across all of them; a stateful component, such as a database or a server holding session data in memory, needs more careful handling — either moving that state out to a shared store like a managed cache or a database so the layer above it can stay stateless, or falling back to sticky sessions and more deliberate failover when statefulness genuinely can't be avoided. Good scalability design generally pushes as much of a system as it can toward the stateless side of that line, because stateless tiers are exactly the ones a load balancer and an Auto Scaling group can treat as interchangeable.",
      },
      {
        title: 'Containers vs. serverless compute: ECS, EKS, Fargate, and Lambda',
        body: "Amazon ECS and Amazon EKS are both container orchestrators — they place containers on compute, restart the ones that fail, and handle service discovery and scaling — but they speak different languages: ECS uses AWS's own task, service, and cluster model, which is quicker to pick up if a team has no existing Kubernetes tooling or expertise, while EKS runs an upstream-compatible, fully managed Kubernetes control plane, the natural choice when an organization already has Kubernetes manifests, operators, or skills it wants to bring along. Either orchestrator can place its containers on a fleet of EC2 instances you manage yourself, or on AWS Fargate, which takes server management out of the picture entirely — you declare the CPU and memory a container needs and Fargate runs it on infrastructure you never patch, size, or see. Deciding between containers (ECS or EKS, with or without Fargate) and Lambda functions mostly comes down to the shape of the work: Lambda is built for short-lived units of work triggered by an event that need to scale from zero with no standing infrastructure, while containers suit long-running processes, steady traffic, or applications that need a full OS environment, a particular runtime version, or bulky local dependencies that don't map cleanly onto Lambda's execution model. Migrating an existing application into containers is also a common first modernization step for legacy or on-premises workloads, since packaging the app and its dependencies into a container image makes it portable across ECS, EKS, or Fargate without first breaking it apart into individual functions — usually a smaller, lower-risk move than jumping straight to a serverless rewrite.",
      },
      {
        title: 'Storage types, read replicas, and purpose-built managed services (Transfer Family, Secrets Manager)',
        body: "Picking the right storage type is a foundational scalability decision: object storage (Amazon S3) holds independent files reached over an HTTP API and scales to effectively unlimited capacity and request volume, which fits static assets, backups, and data lakes but not something a server expects to mount as a filesystem; file storage (Amazon EFS or FSx) gives you a shared, POSIX- or SMB-style filesystem that many instances or containers can mount and use at the same time, the choice when an application expects ordinary file-and-directory access shared across a fleet; and block storage (Amazon EBS) provides low-latency volumes attached to one instance at a time, the natural fit for a database's own data files or a boot volume. On the database side, read replicas scale read throughput horizontally: a relational database asynchronously streams changes from its primary instance out to one or more replicas, and an application can send read-only work — reporting queries, dashboards, browse traffic — to those replicas instead of piling it all onto the primary, which is a far cheaper way to add read capacity than resizing the primary vertically. Beyond compute and storage, AWS also offers purpose-built managed services that take undifferentiated heavy lifting out of a scalable design rather than leaving it for you to build: AWS Transfer Family stands up a fully managed SFTP, FTPS, FTP, or AS2 endpoint in front of S3 or EFS so you're not running and scaling your own file-transfer server fleet for partner or customer data exchange, and AWS Secrets Manager centralizes (and can automatically rotate) database credentials and API keys, so scaling an application out to many instances doesn't also mean distributing and babysitting long-lived secrets across every one of them.",
      },
    ],
  },
  {
    domain: 'resilient',
    taskStatement: 'Task 2.2: Design highly available and/or fault-tolerant architectures',
    topics: [
      {
        title: 'AWS global infrastructure: Regions, Availability Zones, and Route 53 failover',
        body: "AWS organizes its footprint into Regions — separate geographic areas — each made up of multiple Availability Zones (AZs): distinct facilities with their own power, cooling, and networking, linked to each other by low-latency connections. A resilient design spreads a workload's compute and data across at least two AZs in a Region so the loss of one facility doesn't take the whole application with it, and for workloads that need to survive the loss of an entire Region, that same principle extends outward to operating across two or more Regions at once. Amazon Route 53 sits in front of this topology as the DNS layer: beyond turning domain names into IP addresses, it can run health checks against your endpoints and stop sending traffic to any that fail, and its routing policies — failover, weighted, latency-based, geoproximity — let an architect decide exactly how traffic should redistribute the moment an AZ or Region goes unhealthy. Choosing which AWS services to build on for high availability and fault tolerance largely comes down to favoring ones that are natively Multi-AZ or multi-Region aware — a load balancer spanning AZs, a database with a Multi-AZ standby, a globally replicated data store — over a design that treats resilience as something bolted on after the fact to a single-location resource.",
      },
      {
        title: 'Eliminating single points of failure: route tables, Application Load Balancer, and RDS Proxy',
        body: "A route table decides where traffic leaving a subnet goes next, and getting it wrong is a surprisingly easy way to introduce a hidden single point of failure — pointing every private subnet at one NAT gateway that lives in a single AZ means that AZ's outage cuts off outbound connectivity for every subnet depending on it, so a resilient design gives each AZ its own NAT gateway and a route table that matches. An Application Load Balancer removes a different single point of failure: rather than clients connecting straight to one server, the ALB spreads incoming requests across healthy targets registered in multiple AZs and stops sending traffic to any target that fails its health check, so one bad instance, or one bad AZ, doesn't take the whole service down with it. Amazon RDS Proxy addresses yet another failure mode, one common with serverless or auto-scaled fleets: a burst of application instances can open far more database connections than the database can comfortably handle, so the proxy pools and reuses a fixed set of connections on the application's behalf, and because it holds its own connection to the database, it can switch over to a standby instance in seconds while the client-side connections stay intact — a particularly valuable safety net for older applications that were never written with connection pooling or graceful reconnects in mind.",
      },
      {
        title: 'Choosing a disaster recovery strategy: backup/restore, pilot light, warm standby, and active-active',
        body: "Every disaster recovery strategy trades cost against how fast and how completely you can recover, and AWS groups the common options into four tiers of increasing readiness (and expense). Backup and restore is the cheapest tier: data is snapshotted periodically, infrastructure is captured as code so it can be rebuilt on demand, and nothing actually runs in the recovery Region until a disaster is declared — which also makes it the slowest to recover. Pilot light keeps only the data tier continuously replicated and running live in the recovery Region, while the rest of the stack sits deployed but powered down, ready to be switched on and scaled up when needed. Warm standby raises the bar again: the entire stack already runs in the recovery Region around the clock, just at reduced size, so recovering is purely a matter of scaling capacity up rather than starting anything cold. Multi-site active-active runs full capacity in more than one Region at the same time and actually serves live traffic from each of them, getting closest to zero downtime but at the highest cost and operational complexity of the four. Two numbers decide which tier a given workload actually needs: recovery point objective (RPO), the maximum amount of data loss the business can tolerate, expressed as a span of time, and recovery time objective (RTO), how long the business can be down before the outage is unacceptable — a tight RPO pushes a design toward continuous replication, and a tight RTO pushes it toward a warm or active-active posture instead of cold backups.",
      },
      {
        title: 'Data durability and replication across storage services',
        body: "Durability and availability are easy to conflate but answer different questions: durability is the likelihood that a piece of data survives over time without being lost or silently corrupted, while availability is whether you can reach and use that data right now. Amazon S3 reaches extremely high durability by automatically storing multiple copies of every object across multiple devices and multiple Availability Zones within a Region, and S3 Cross-Region Replication extends that same protection outward to a second Region for workloads that need to survive losing an entire Region, not just one facility. Amazon EBS volumes, by contrast, live inside a single Availability Zone by default, which is exactly why it's standard practice to snapshot them to S3 (itself Regional and multi-AZ) so a volume can still be recovered after an AZ-level failure. Managed databases build replication in as a core feature too: an RDS Multi-AZ deployment keeps a synchronously updated standby in a second AZ purely to fail over to, read replicas provide an asynchronously updated copy that can also absorb read traffic, and promoting a read replica running in another Region is itself a legitimate way to recover from a Regional outage. Knowing which storage and database options already replicate data on your behalf — and which ones leave replication to you — is central to designing for data durability and availability.",
      },
      {
        title: 'Immutable infrastructure and automation for reliable, self-healing deployments',
        body: "Immutable infrastructure means you never patch or reconfigure a running server in place; instead you bake a new machine or container image with the change, deploy fresh instances from it, and retire the old ones, rather than logging into a live fleet and editing configuration by hand. This matters for reliability because a server that's never been touched after launch behaves identically to every sibling built from the same image, which removes an entire category of outages caused by configuration drift — a fix applied to one box but never carried over to the rest. Pairing this with automation (Auto Scaling groups, infrastructure-as-code templates, a pipeline that redeploys on failure) produces infrastructure that repairs itself: when an instance fails a health check, the group terminates it and launches a replacement from the same known-good image without anyone needing to step in. The same pattern is how architects gradually raise the reliability of an older, non-cloud-native application without rewriting it from scratch — placing the existing app inside an Auto Scaling group behind a load balancer, moving its state out to a managed database or shared filesystem so any single instance can be replaced without losing data, and letting automated recovery take over from the manual server-babysitting the application used to depend on. Distributed-systems thinking adds a further layer of protection: designing as though any one component can fail at any time — retrying transient errors, isolating a failing part of the system with patterns like bulkheads or circuit breakers so trouble doesn't cascade, and decoupling components through queues — tends to degrade gracefully under stress instead of failing outright.",
      },
      {
        title: 'Service quotas, throttling, workload visibility with X-Ray, and managed AI services as a resilience pattern',
        body: "Every AWS account runs under service quotas — caps on how many of a given resource you can create, or how fast you can call a particular API — and hitting one unexpectedly, whether it's a concurrency ceiling on Lambda, an instance limit on EC2, or a throttled API call, looks exactly like an outage to your users even though nothing in the infrastructure actually broke. Designing for resilience means estimating which quotas a workload's peak traffic could realistically bump into, requesting increases well ahead of time through Service Quotas rather than mid-incident, and writing application code that handles a throttling response with retry-and-backoff logic instead of simply failing the request outright. AWS X-Ray provides the visibility to catch these problems, along with latency spikes and error patterns, before customers notice them: it follows a request as it hops between your services and their downstream calls, and its service-map view makes it easy to spot which link in a distributed chain is slow, erroring, or bumping against a limit. Reaching for a managed service instead of a self-hosted equivalent is itself a resilience strategy — Amazon Comprehend (natural-language processing such as sentiment scoring and entity extraction) and Amazon Polly (text-to-speech) are good examples of capabilities you'd otherwise have to run and scale yourself on EC2, most likely as a single point of failure, but which AWS instead operates as highly available, auto-scaling APIs you simply call, moving the burden of keeping that capability running onto AWS.",
      },
    ],
  },
  {
    domain: 'performant',
    taskStatement: 'Task 3.1: Determine high-performing and/or scalable storage solutions',
    topics: [
      {
        title: 'Object storage with Amazon S3: buckets, keys, and storage classes',
        body: "Amazon S3 stores data as objects — a file plus its metadata — inside containers called buckets, and every object is reached through the combination of a bucket name and a unique key rather than a filesystem path, which is why applications talk to it over an HTTP API instead of mounting it like a drive. Because there's no fixed capacity to provision, a bucket can absorb effectively unlimited objects and request volume, making S3 the default choice for data lakes, backups, static web assets, and archives — anything that doesn't need to look like a shared directory to a running server. Picking the right storage class is the main performance-and-cost lever inside S3 itself: S3 Standard suits data accessed often, S3 Express One Zone trades multi-AZ redundancy for single-digit-millisecond latency by keeping data in one Availability Zone next to compute, Intelligent-Tiering automatically shifts objects between tiers as access patterns change, and the Glacier tiers push rarely touched data to the cheapest possible storage at the cost of retrieval speed. S3 also guarantees strongly consistent reads immediately after a write or overwrite completes, so an application never has to guess whether a just-uploaded object is visible yet.",
      },
      {
        title: 'File storage with Amazon EFS: elastic, shared, POSIX-style file systems',
        body: "Amazon EFS gives you a network file system that many compute resources — EC2 instances, ECS tasks, EKS pods, Lambda functions, and Fargate — can mount and use at the same time over NFS, which is the option to reach for whenever an application expects ordinary shared file-and-directory access rather than an API call or a single attached disk. Capacity isn't something you provision ahead of time: EFS grows and shrinks on its own as files are added or removed, scaling toward petabytes without any resizing step, and the newer Elastic throughput mode adjusts bandwidth automatically to match whatever a workload is doing at that moment instead of requiring you to set a baseline rate in advance. For availability planning, a Regional file system spreads its data across several Availability Zones in a Region so it keeps serving even if one AZ has a problem, while a One Zone file system keeps everything in a single AZ, accepting a lower resilience bar in exchange for a lower price — a tradeoff worth naming explicitly for any workload where a shared filesystem is the plan.",
      },
      {
        title: 'Block storage with Amazon EBS: volume types sized for performance',
        body: "Amazon EBS hands out durable block volumes that attach to a single EC2 instance and behave like a local disk the instance can format and use directly, which makes it the natural fit for a database's own data files or a boot volume that needs consistently low latency rather than shared, filesystem-style access. Each volume lives inside one Availability Zone, and EBS offers two broad families of volume to match different access patterns: SSD-backed types tuned for transactional, IOPS-heavy work such as a busy database, and HDD-backed types tuned for large, mostly sequential reads and writes where sustained throughput matters more than per-operation latency. Elastic Volumes let a team raise capacity or switch performance tier on a live volume with no downtime, so a design doesn't need to guess the exact final size on day one — it can start modest and grow as real usage data comes in. EBS snapshots copy a volume's data into S3 for durability and give you a way to restore a fresh volume, or move one across an Availability Zone, Region, or account, independent of the original volume's lifecycle.",
      },
      {
        title: 'Matching workload shape to object, file, and block storage',
        body: "A recurring exam skill is recognizing which storage category a described workload actually needs: object storage fits independently addressed files reached over HTTP at effectively unlimited scale, such as a data lake, media library, or backup target; file storage fits any scenario where several compute nodes need to see and modify the exact same directory tree concurrently, like a shared content-management or rendering workload; and block storage fits a single instance that needs a private, low-latency volume it can treat as its own disk, most obviously a database's storage layer. Looking past the initial choice toward future scale changes the calculus slightly — S3 and EFS both absorb growth automatically with no capacity step to plan for, while an EBS volume needs someone (or an automated process) to explicitly grow it or move to a different volume type as demand increases, so a design that expects heavy, unpredictable growth on a block-storage-backed component should build that resizing step into its operational plan rather than assuming it away.",
      },
      {
        title: 'Bridging on-premises and AWS storage: hybrid patterns with Storage Gateway and DataSync',
        body: "AWS Storage Gateway puts an appliance on-premises that speaks the file and block protocols local applications already use — an S3 File Gateway exposes an NFS or SMB share that's actually backed by S3 objects, and Volume Gateway exposes iSCSI block volumes in cached or stored modes — so an existing workload keeps its familiar local access pattern while its data lands durably in AWS storage behind the scenes, which is the standard answer whenever a scenario calls for ongoing hybrid access rather than a one-time move. AWS DataSync instead specializes in moving large datasets: it connects to on-premises NFS, SMB, HDFS, or generic object storage, and AWS storage such as S3, EFS, or FSx (or even between two AWS storage services), using a purpose-built, multi-threaded transfer engine that includes end-to-end encryption and automatic integrity checks, and it can reach AWS storage through a VPC endpoint so the transfer never has to cross the public internet. The practical distinction for a design is durability of the access pattern: reach for Storage Gateway when an application needs to keep working against a local-looking file or volume share indefinitely, and reach for DataSync when the job is a scheduled or one-time bulk migration or sync into AWS.",
      },
    ],
  },
  {
    domain: 'performant',
    taskStatement: 'Task 3.2: Design high-performing and elastic compute solutions',
    topics: [
      {
        title: 'Choosing EC2 instance families, sizes, and processor options for the workload',
        body: "EC2 groups its hardware offerings into instance families by the resource profile they're built for — general purpose, compute optimized, memory optimized, storage optimized, and accelerated computing for GPU or ML-chip workloads — and every size within a family scales vCPU count, memory, and network or storage bandwidth together, so moving up or down a size changes all of those dimensions at once rather than letting you tune just one. Because EC2 bills by the second, AWS's own recommendation is to launch a candidate type, benchmark it against the actual application rather than a synthetic guess, and change type later if it turns out mis-sized, which is a far cheaper way to land on the right instance than trying to calculate it perfectly up front. Beyond the traditional Intel and AMD processor lineup, Graviton (Arm-based) instances aim at the best price-performance ratio for workloads that support the architecture, and the burstable T-family trades a capped baseline CPU allocation for a lower price on spiky, non-sustained traffic. Getting this initial hardware match right — before any scaling policy is layered on top — is the foundation of a high-performing compute design.",
      },
      {
        title: 'Scaling compute automatically: EC2 Auto Scaling, AWS Auto Scaling, and the right metric',
        body: "An EC2 Auto Scaling group keeps a fleet between a minimum, maximum, and desired instance count, replacing any instance that fails its health check and balancing the fleet evenly across whichever Availability Zones you've configured so no single AZ ends up carrying a disproportionate share of capacity. A scaling policy decides when the group actually changes size — target tracking holds a chosen metric near a set point, step scaling reacts to CloudWatch alarm thresholds in defined increments, and scheduled scaling handles predictable time-of-day patterns — and picking the right metric for a given tier matters: a web tier scales cleanly on its load balancer's request count per target or on the fleet's own average CPU utilization, a queue-backed worker fleet scales better against the queue's backlog depth than against CPU, and DynamoDB scales its own provisioned throughput against consumed capacity. AWS Auto Scaling sits a level above any single group: instead of tuning one EC2 Auto Scaling group in isolation, it lets you define one scaling plan that coordinates capacity across several resource types together — EC2 Auto Scaling groups, ECS services, DynamoDB tables, and Aurora replicas among them — which is the tool to reach for when multiple tiers of an application need to grow and shrink in a coordinated way rather than being tuned one at a time.",
      },
      {
        title: 'Serverless compute: right-sizing Lambda memory and running containers on Fargate',
        body: "AWS Lambda removes server provisioning entirely and bills per invocation and per unit of execution time, with the memory setting as the one capacity dial available — allocating more memory to a function also increases the CPU and network bandwidth it gets, proportionally, so an under-provisioned CPU-bound function can quietly run slower (and sometimes end up costing more once total run time is counted) while an over-provisioned one just wastes budget on capacity it never touches. Testing a function's actual duration and cost across a small range of memory settings, rather than picking one number and moving on, is the standard way to land on a genuinely efficient configuration. AWS Fargate extends the same server-free idea to containers: rather than sizing, patching, and scaling a fleet of EC2 hosts underneath an ECS or EKS cluster, you declare the CPU and memory a task needs and Fargate places it on infrastructure it fully owns and operates, which fits any team that wants container packaging without also taking on host-level capacity planning.",
      },
      {
        title: 'Container orchestration with ECS and EKS, on EC2 or Fargate',
        body: "Amazon ECS and Amazon EKS both place containers onto compute, restart ones that fail, and plug into load balancers and service discovery, but they lead with different control-plane models: ECS relies on AWS's own task, service, and cluster constructs, which tends to be the faster on-ramp for a team without existing Kubernetes tooling, while EKS runs a Kubernetes-conformant control plane so an organization already invested in Kubernetes manifests, operators, and community tooling can bring all of that along unchanged. That orchestrator choice is separate from the compute-placement choice underneath it — both ECS and EKS can schedule work onto self-managed EC2 capacity when a team wants direct control over instance type, OS patching, or specialized hardware, or onto Fargate when the goal is to skip host management altogether. Deciding between containers and Lambda for a given piece of compute usually comes down to run duration and how much state or custom runtime a workload carries: short, event-triggered bursts that benefit from scaling to zero point toward Lambda, while long-running processes, heavyweight dependencies, or a need for a full OS environment point toward ECS or EKS instead.",
      },
      {
        title: 'Distributed and batch processing at scale: AWS Batch and Amazon EMR',
        body: "AWS Batch takes a queue of jobs and provisions a right-sized compute environment (backed by EC2 or Fargate) to run them, which removes the need to build a custom job scheduler or babysit cluster capacity for workloads that arrive in bursts rather than a steady stream — nightly ETL runs, scientific simulations, or media-rendering jobs are typical fits. Amazon EMR approaches distributed computing from a different angle, running managed clusters of open-source big-data frameworks — chiefly Apache Spark and Hadoop — so a dataset too large for any single machine gets split across many nodes and processed in parallel; an EMR cluster can pull input straight from S3 or a Kinesis stream, and its size can flex to match the data volume of a given run instead of sitting provisioned at peak capacity around the clock. Both services exist to spread large or unpredictable compute jobs across many machines automatically, sparing a solutions architect from hand-building and coordinating a fixed cluster for work that doesn't arrive on a constant schedule.",
      },
      {
        title: 'Decoupling compute tiers with pub/sub so each layer scales independently',
        body: "Inserting a buffer between two compute tiers — Amazon SQS for one-to-one work handoff, Amazon SNS for one-to-many fan-out to several subscribers at once — means a spike hitting the producer side doesn't force the consumer side to scale in lockstep, and the reverse holds too: the consumer fleet can size itself against its own backlog metric on its own timetable, and a consumer that's briefly unavailable just means work waits in the buffer instead of the producer stalling out. That separation is what lets a design apply the scaling approach and even the compute type that best fits each tier on its own terms — for instance, Lambda behind an SQS queue to absorb bursty, unpredictable spikes, paired with a steadily sized EC2 Auto Scaling group for a tier with more constant throughput — rather than sizing an entire pipeline around whichever stage happens to be hardest to scale.",
      },
    ],
  },
  {
    domain: 'performant',
    taskStatement: 'Task 3.3: Determine high-performing database solutions',
    topics: [
      {
        title: 'Relational, non-relational, and in-memory databases: matching engine type to the access pattern',
        body: "A relational database (Amazon RDS or Aurora) organizes data into tables with a fixed schema and supports joins and multi-table transactions, which suits workloads with complex, interrelated queries and a need for strong consistency across those relationships. A non-relational database like DynamoDB instead stores flexible key-value or document items without a rigid schema or join support, and in exchange it delivers consistent single-digit-millisecond performance whether a table holds a handful of items or spans hundreds of terabytes, which is why it fits workloads with simple, well-known access patterns — a shopping cart, a session store, a leaderboard — that need to scale smoothly without capacity planning around joins. In-memory data stores such as those built on ElastiCache sit apart from both: they trade persistence and query richness for microsecond-to-millisecond latency, so they're the layer you put in front of a relational or non-relational database to absorb read-heavy or latency-sensitive traffic rather than a system of record on their own. Recognizing which of these three shapes a described workload actually needs — relational, non-relational, or purely in-memory — is the starting point before comparing specific services.",
      },
      {
        title: 'Scaling reads and preserving availability with Aurora and RDS read replicas',
        body: "Amazon Aurora is a relational engine, compatible with MySQL and PostgreSQL, that separates compute from a fast, auto-growing distributed storage layer shared across an entire cluster, letting it reach several times the throughput of stock MySQL or PostgreSQL on comparable hardware while handling failover, backup, and storage growth largely on its own. Read replicas — supported by both Aurora and standard RDS engines — give a read-heavy workload somewhere to send query traffic besides the primary: RDS copies changes from the primary out to one or more replicas asynchronously, an application routes reporting or browse-only traffic to those replicas instead of piling it onto the primary, and because a replica can also be promoted to a standalone instance, it doubles as a disaster-recovery option if the primary is lost. A read replica differs from a Multi-AZ standby, which replicates synchronously purely to fail over to and never serves read traffic on its own; a design that needs both extra read capacity and a dedicated failover target typically runs a Multi-AZ standby alongside one or more separate read replicas. For standard RDS engines, adding or removing read replicas as demand shifts is a step you (or an external automation script) take deliberately rather than something RDS does on its own; Aurora is the exception, since its Aurora Auto Scaling feature can add and remove Aurora Replicas for a cluster automatically against a target metric such as average CPU or connection count.",
      },
      {
        title: "Database capacity planning: DynamoDB capacity units and provisioned IOPS storage",
        body: "DynamoDB offers two ways to pay for and provision read and write throughput: on-demand capacity mode bills per request and adjusts instantly to traffic with no capacity to plan ahead of time, which suits workloads with spiky or hard-to-predict demand, while provisioned capacity mode has you set a target amount of read and write capacity units up front (optionally paired with auto scaling of that provisioned amount) at a lower cost per unit for traffic that's steadier and easier to forecast. Because DynamoDB was purpose-built to skip the operational tuning a relational database needs at scale — no joins to optimize, no index maintenance beyond the secondary indexes you explicitly create — capacity planning here is mostly about picking the billing mode that matches how predictable a table's traffic actually is, then layering global tables on top for a workload that needs multi-Region, multi-active reads and writes with no single primary to fail over. On the relational side, the equivalent capacity-planning lever is storage performance rather than a request-based unit: RDS and Aurora let you provision a target IOPS rate for the underlying storage volume so a write- or read-heavy database gets a guaranteed, consistent number of operations per second instead of the variable performance a general-purpose volume delivers, which matters most for transactional workloads with a steady, predictable I/O rate. Getting either capacity model right avoids the two failure modes at either extreme: under-provisioned capacity causing throttled requests or storage-bound latency, or over-provisioned capacity paying for throughput that never gets used.",
      },
      {
        title: 'Offloading read-intensive traffic with ElastiCache and DynamoDB Accelerator (DAX)',
        body: "Amazon ElastiCache runs a managed in-memory data store or cache — built on the Valkey, Memcached, or Redis OSS engines — that sits in front of a primary database and answers repeated reads straight from memory instead of hitting the database on every request, which is the standard way to cut both latency and read-side load once a workload's traffic skews heavily toward the same hot data. It's available as a serverless cache that scales its own memory, compute, and bandwidth without any node sizing, or as a node-based cluster when a team wants direct control over node type, count, and Availability Zone placement, including choosing cluster mode for horizontal scaling versus a simpler single-node setup for vertical scaling. Amazon DynamoDB Accelerator (DAX) is a purpose-built variant of the same idea specifically for DynamoDB: it's a managed, highly available in-memory cache that can cut read response times from milliseconds down to microseconds even under millions of requests per second, handling cache population and invalidation for you so an application doesn't need custom caching logic in front of its DynamoDB calls. Deciding whether to add a caching layer at all generally comes down to how skewed and read-heavy the access pattern is — a cache adds the most value when a small slice of data accounts for a large share of total reads.",
      },
      {
        title: 'Managing database connections at scale with Amazon RDS Proxy',
        body: "A burst of application instances — common with Lambda functions or an auto-scaled fleet — can open far more connections to a relational database than it can comfortably sustain, since each connection consumes memory and CPU on the database itself regardless of how much work it's actually doing. Amazon RDS Proxy sits between the application and the database, pooling and reusing a bounded set of real database connections while serving many more client-side connections against that pool, queuing or shedding load gracefully when incoming requests exceed the limits you've set rather than letting the database itself become overwhelmed. Because RDS Proxy keeps its own connection to the database, it can switch over to a standby in seconds during a failover while every application-side connection stays intact, which is a meaningful reliability win for older applications that were never written with connection pooling or reconnect logic in mind. It also centralizes authentication, letting clients connect using IAM credentials while the proxy itself authenticates to the database with IAM or with credentials stored in Secrets Manager, which keeps long-lived database passwords out of application code.",
      },
      {
        title: 'Choosing and migrating database engines: Aurora vs. DynamoDB, and homogeneous vs. heterogeneous migration',
        body: "Choosing between Aurora and DynamoDB for a new workload comes down to the same relational-versus-non-relational question from a migration angle: pick Aurora when the application already thinks in SQL, needs multi-table transactions or ad-hoc joins, or is being lifted from an existing MySQL or PostgreSQL database with minimal rework, and pick DynamoDB when the access patterns are simple, well-defined key lookups that need to scale to a very large size with predictable, low latency and little schema maintenance. When a database migration is homogeneous — the source and target run the same engine, such as on-premises MySQL moving to RDS or Aurora MySQL — the schema and application code typically carry over untouched, so the migration mostly involves moving data. A heterogeneous migration — moving between different engines, such as a commercial database to Aurora PostgreSQL — usually requires converting the schema and any embedded procedural code first, commonly with the AWS Schema Conversion Tool, before AWS Database Migration Service handles the actual data movement and, where needed, ongoing replication during the cutover window. Recognizing which category a described migration falls into determines how much schema-conversion work has to happen before data migration can even start.",
      },
    ],
  },
  {
    domain: 'performant',
    taskStatement: 'Task 3.4: Determine high-performing and/or scalable network architectures',
    topics: [
      {
        title: 'Accelerating content and global traffic with CloudFront and Global Accelerator',
        body: "Amazon CloudFront caches HTTP and HTTPS content — web pages, images, video, API responses — at edge locations spread around the world, so a viewer's request is answered from whichever nearby edge already holds a copy instead of always traveling back to the origin (an S3 bucket or a custom HTTP server); when content isn't cached yet, CloudFront fetches it from the origin over the AWS backbone and caches it for the next request, cutting both the number of networks a request has to cross and the latency users experience. AWS Global Accelerator solves a related but distinct problem for traffic that isn't necessarily HTTP: it hands out static anycast IP addresses and steers connections onto the AWS global network toward the closest healthy regional endpoint — an Application Load Balancer, Network Load Balancer, EC2 instance, or Elastic IP — re-routing within seconds if health checks detect a problem with an endpoint or an entire Region. The practical distinction for a design is what's being optimized: CloudFront caches and speeds up content delivery for web-style traffic, while Global Accelerator improves the routing path and failover behavior for TCP or UDP traffic terminating on load balancers or instances across one or more Regions.",
      },
      {
        title: 'Designing subnet tiers, routing, and IP addressing for a scalable VPC',
        body: "A subnet is a slice of a VPC's IP address range confined to a single Availability Zone, and a scalable network design typically lays out one subnet per tier per AZ — a public tier with a route to an internet gateway for load balancers, and one or more private tiers with no such route for application servers and databases — so each tier can be sized, secured, and scaled on its own terms. Route tables attached to each subnet decide where outbound traffic actually goes, and getting the routing right (a NAT gateway per AZ rather than one shared NAT gateway, for instance) avoids turning a single routing choice into a hidden bottleneck or single point of failure as traffic grows. Planning IP addressing ahead of time matters more than it first appears: choosing CIDR ranges with enough headroom for future subnets and instances avoids a disruptive re-addressing exercise later, and adopting IPv6 alongside or instead of IPv4 sidesteps the practical limits and cost of public IPv4 address space as a fleet scales into the thousands of instances. VPC endpoints round out the picture by letting resources in a private subnet reach AWS services like S3 or DynamoDB over the AWS network directly, without adding a NAT gateway or internet gateway hop to that traffic path.",
      },
      {
        title: 'Distributing traffic with load balancers: choosing among ALB, NLB, and GWLB',
        body: "Elastic Load Balancing offers several load balancer types tuned for different jobs, and picking the right one is a recurring high-performing-network decision. An Application Load Balancer operates at the application layer, evaluating listener rules that send HTTP and HTTPS traffic toward whichever target group matches the request's path, host header, or other content — the right choice whenever routing decisions need to look inside the request, such as sending traffic for different microservices to different backends from one load balancer. A Network Load Balancer instead operates at the transport layer, built for extreme throughput, very low latency, and static IP addresses, which fits protocols other than HTTP or workloads where every microsecond of added latency matters. A Gateway Load Balancer works at the network layer to transparently insert third-party virtual appliances — firewalls, intrusion-detection systems — into a traffic path without the rest of the architecture needing to know they're there. Because a load balancer automatically spreads registered targets across multiple Availability Zones and stops sending traffic to any target that fails its health check, choosing the right type is as much about eliminating a single point of failure as it is about raw throughput.",
      },
      {
        title: 'Private connectivity options: VPN, Direct Connect, and VPC endpoints',
        body: "AWS Site-to-Site VPN encrypts traffic between an on-premises network and a VPC over the public internet using IPsec tunnels, and because it needs no special physical infrastructure, it can typically be stood up in minutes to hours, making it the fast, low-commitment option for hybrid connectivity or as a backup path. AWS Direct Connect instead runs a dedicated fiber connection from an on-premises router into an AWS Direct Connect location, and from there you provision virtual interfaces — a private virtual interface to reach a VPC, a public virtual interface to reach public AWS services, or a transit virtual interface to reach multiple VPCs through a transit gateway — bypassing the public internet entirely for more consistent bandwidth and lower, more predictable latency, at the cost of a longer physical setup process; a common pattern pairs Direct Connect as the primary path with a Site-to-Site VPN as an encrypted backup. For traffic that never needs to leave AWS's own network in the first place, a VPC endpoint lets resources in a subnet reach an AWS service like S3 or DynamoDB, or a service exposed through AWS PrivateLink, using private IP addresses instead of routing out through an internet gateway or NAT device — removing an unnecessary hop from the path and the NAT gateway's throughput ceiling from the picture entirely.",
      },
      {
        title: 'Placing resources and shaping network topology for performance and future scale',
        body: "Where resources sit relative to each other and to end users is itself a performance lever: keeping tightly coupled tiers within the same Availability Zone reduces the cross-AZ latency and data-transfer cost that would otherwise be paid on every internal call, while still spreading redundant copies of each tier across multiple AZs so no single facility's failure removes the whole service — the two goals are reconciled by replicating the same multi-AZ pattern per tier rather than scattering unrelated tiers across zones at random. EC2 cluster placement groups take this further for tightly coupled, latency-sensitive workloads such as high-performance computing by packing instances physically close together within one Availability Zone for the lowest possible network latency between them. As a topology grows past a handful of VPCs connected by point-to-point peering, a transit gateway acting as a central routing hub keeps the design manageable, connecting many VPCs, VPN connections, and Direct Connect links through one place instead of an ever-growing mesh of individual connections. Planning subnet and address space with room to add capacity, and choosing a topology that scales by replicating a pattern rather than by adding one-off exceptions, is what keeps a network design able to absorb future growth instead of needing a redesign at the next order of magnitude.",
      },
    ],
  },
  {
    domain: 'performant',
    taskStatement: 'Task 3.5: Determine high-performing data ingestion and transformation solutions',
    topics: [
      {
        title: 'Real-time streaming ingestion with Kinesis Data Streams',
        body: "Amazon Kinesis Data Streams takes in continuous, high-volume data — application logs, clickstreams, IoT telemetry, market feeds — as a steady flow of records rather than periodic batches, and a consuming application typically sees a new record available for processing in well under a second after it's written, which is what makes it the right fit whenever a design calls for reacting to data as it arrives instead of waiting for the next scheduled load. Because a stream is elastic, you can add shards to raise its throughput ceiling as ingestion volume grows, and because the data itself persists in the stream for a configurable window, more than one consumer application can read the same stream independently at the same time — one computing rolling aggregates into a database while a separate one archives raw records to S3, for example — without those consumers competing with each other or needing to coordinate. That combination of low end-to-end delay and support for multiple independent readers is the main signal that a workload calling for continuous ingestion and immediate downstream processing should be built on Kinesis rather than a periodic batch job.",
      },
      {
        title: 'Moving data into AWS: DataSync and Storage Gateway for ingestion pipelines',
        body: "When source data lives on premises and needs to land in a data lake, AWS DataSync provides a secure, managed transfer path from on-premises NFS, SMB, HDFS, or object storage straight into S3 (or EFS or FSx), using a parallel, multi-threaded protocol that's substantially faster than generic copy tools and that validates data integrity automatically along the way; scheduling DataSync tasks to run periodically is a common pattern for feeding a lake with recurring batches rather than a single one-time load. Reaching AWS storage through a VPC endpoint rather than the public internet keeps that transfer path private, which addresses the access-security half of an ingestion design alongside IAM roles scoped narrowly to the specific buckets or file systems a transfer job needs to touch. AWS Storage Gateway serves a complementary role when an existing application needs to keep writing through a local-looking file or volume share even as that same data becomes available for analytics in S3 behind the scenes — useful for feeding a lake continuously from a legacy system without redesigning how that system writes its data in the first place.",
      },
      {
        title: 'Cataloging and securing a data lake with AWS Glue and Lake Formation',
        body: "A data lake built on S3 only becomes queryable once something describes what schema lives in which files, and the AWS Glue Data Catalog fills that role: Glue crawlers scan data sources, infer table and column structure automatically, and register the result in a central catalog that Athena, Amazon Redshift Spectrum, and EMR can all query against directly, which avoids hand-maintaining schema definitions in every tool that touches the lake separately. AWS Lake Formation builds on top of that shared catalog as a dedicated authorization layer, letting a team define fine-grained access — down to specific tables, columns, or rows — once, centrally, rather than replicating a patchwork of individual bucket policies across every service and team that needs to read the lake. Together, cataloging and centralized authorization are what let a data lake scale to many datasets and many consuming teams without turning access control into an unmanageable, per-bucket exercise, which is the core of designing secure access into an ingestion pipeline from the start rather than bolting it on afterward.",
      },
      {
        title: 'Transforming data formats and pipelines with AWS Glue ETL',
        body: "AWS Glue runs serverless extract-transform-load jobs on a managed Apache Spark engine, so cleaning, reshaping, or joining datasets at scale doesn't require standing up or sizing a Spark cluster yourself — Glue Studio's visual job canvas generates the underlying code for common transformations, while more complex logic can still be hand-written and run the same way. An especially common transformation in an analytics pipeline is converting row-oriented formats such as CSV or JSON into a columnar format like Parquet: columnar storage lets query engines such as Athena read only the columns a query actually needs and compress similar values more effectively, which cuts both the data scanned per query and its cost. Glue jobs can run on a fixed schedule, on demand, or in response to an event such as a new file landing in a bucket, and multiple jobs and crawlers can be chained into a workflow so that, for example, a crawler refreshes the catalog automatically right after a transformation job finishes writing new output.",
      },
      {
        title: 'Querying and visualizing a data lake with Athena',
        body: "Amazon Athena runs standard SQL directly against data sitting in S3 without requiring any database to be loaded or any infrastructure provisioned ahead of time — point it at a location cataloged by Glue, and it starts returning results in seconds, executing queries in parallel across the underlying files so that performance holds up even against large datasets and complex joins. Billing follows the amount of data a query actually scans, which is precisely why upstream choices like partitioning data by date and converting it to a columnar format such as Parquet pay off twice over: they make individual queries faster and measurably cheaper at the same time. Athena is typically the query layer underneath a business-intelligence tool such as Amazon QuickSight, which connects to Athena to turn ad-hoc SQL results into interactive dashboards and visualizations for end users who never write a query themselves — the combination of a serverless query engine feeding a visualization layer is the standard answer whenever a design needs to expose data-lake content to analysts without operating a traditional data warehouse.",
      },
      {
        title: 'Choosing EMR versus serverless analytics, and matching ingestion frequency to the job',
        body: "Amazon EMR is the right tool when a transformation genuinely needs a distributed compute cluster — custom Spark or Hadoop code, machine-learning training over large datasets, or processing that depends on a specific open-source big-data tool that Athena or Glue don't expose directly — and an EMR cluster's size can be matched to a particular job's data volume rather than left running at a fixed capacity. For lighter or more intermittent transformation and query needs, the serverless combination of Glue for transformation and Athena for querying avoids managing a cluster at all, which is usually the simpler and cheaper choice unless a workload's scale or tooling requirements specifically call for EMR. Ingestion frequency should drive the same kind of choice on the intake side of the pipeline: data that must be reflected in the lake within seconds calls for a streaming path built on Kinesis feeding Lambda or a Glue streaming job, while data that only needs to be current within hours or a day fits comfortably on a scheduled batch pattern using DataSync or a periodic Glue job — building a continuous streaming pipeline for data that only needs a daily refresh adds operational cost without a matching benefit.",
      },
    ],
  },
];
