// Flashcard deck covering core Kubernetes objects and concepts referenced
// across the study content and quiz questions. The field is still named
// `service` for schema/validator compatibility with the copied
// validate-content.mjs, even though these are Kubernetes objects and
// concepts rather than AWS services.
export const FLASHCARDS = [
  {
    id: 'pod',
    service: 'Pod',
    domain: 'Workloads & Scheduling',
    front: 'What is a Pod?',
    back: "Kubernetes' smallest deployable unit: one or more containers that always land on the same node and share a network namespace (one IP, one port space) and any volumes attached to the Pod. Containers in the same Pod can reach each other over localhost.",
  },
  {
    id: 'replicaset',
    service: 'ReplicaSet',
    domain: 'Workloads & Scheduling',
    front: 'What does a ReplicaSet do?',
    back: "Watches a label selector and continuously creates or deletes Pods so the number of matching, running Pods stays at the replica count it was given. In normal use you don't create ReplicaSets directly — a Deployment creates and owns them for you.",
  },
  {
    id: 'deployment',
    service: 'Deployment',
    domain: 'Workloads & Scheduling',
    front: 'What does a Deployment manage, and how?',
    back: "A stateless workload, by owning a chain of ReplicaSets rather than Pods directly. Changing the Pod template creates a fresh ReplicaSet and shifts replicas from the old one to the new one at a controlled pace, which is what makes rollout, pause/resume, and rollback to a prior revision possible.",
  },
  {
    id: 'deployment-rolling-update',
    service: 'Deployment',
    domain: 'Workloads & Scheduling',
    front: 'What do maxUnavailable and maxSurge each control during a rolling update?',
    back: 'maxUnavailable is the largest number (or percentage) of desired replicas allowed to be missing at once while old Pods are torn down; maxSurge is the largest number of Pods allowed above the desired count while new ones are being added. Together they set how fast the old ReplicaSet drains as the new one fills in.',
  },
  {
    id: 'daemonset',
    service: 'DaemonSet',
    domain: 'Workloads & Scheduling',
    front: 'What is a DaemonSet used for?',
    back: 'Runs exactly one copy of a Pod on every node that matches its selector, automatically adding the Pod as new nodes join and cleaning it up as nodes leave. Typical uses: a per-node log shipper, metrics agent, or networking/storage plugin daemon.',
  },
  {
    id: 'statefulset',
    service: 'StatefulSet',
    domain: 'Workloads & Scheduling',
    front: 'How is a StatefulSet different from a Deployment?',
    back: "Its Pods are not interchangeable: each gets a stable ordinal name, network identity, and (usually) its own PersistentVolumeClaim that follow it across rescheduling, and StatefulSets can enforce ordered, one-at-a-time startup and scaling. Use it when an app needs durable per-instance identity or storage rather than uniform, disposable replicas.",
  },
  {
    id: 'job',
    service: 'Job',
    domain: 'Workloads & Scheduling',
    front: 'How does a Job differ from a Deployment or ReplicaSet?',
    back: 'A Job runs Pods to completion rather than indefinitely: it keeps retrying a Pod that fails or gets deleted until enough of them finish successfully, then stops on its own. Deployments and ReplicaSets instead keep a fixed number of Pods running forever.',
  },
  {
    id: 'job-completion-modes',
    service: 'Job',
    domain: 'Workloads & Scheduling',
    front: 'Name the three ways a Job can run its Pods.',
    back: 'Non-parallel (a single Pod, retried until it succeeds), fixed completion count (spec.completions Pods must each succeed for the Job to finish, optionally each getting a distinct index), and work queue (no spec.completions set; Pods pull tasks from a shared queue and coordinate among themselves to detect when the whole Job is done).',
  },
  {
    id: 'cronjob',
    service: 'CronJob',
    domain: 'Workloads & Scheduling',
    front: 'What does a CronJob add on top of a Job?',
    back: "Creates a new Job on a repeating cron-formatted schedule, the way one line of a crontab does. Its concurrencyPolicy decides what happens if the previous run is still going when the next one is due: Allow (default) runs them side by side, Forbid skips the new one, Replace kills the old run and starts the new one.",
  },
  {
    id: 'namespace',
    service: 'Namespace',
    domain: 'Cluster Architecture',
    front: 'What does a Namespace scope?',
    back: "Provides a scope for object names — two Deployments named 'web' can coexist in different namespaces — and a boundary that ResourceQuotas and RBAC bindings can apply to.",
  },
  {
    id: 'namespace-scope-exceptions',
    service: 'Namespace',
    domain: 'Cluster Architecture',
    front: 'What kinds of Kubernetes objects live outside any Namespace?',
    back: "Namespace scoping only applies to namespaced objects; cluster-wide objects like Nodes, PersistentVolumes, and StorageClasses live outside any namespace regardless of which namespace you're working in.",
  },
  {
    id: 'static-pod',
    service: 'Static Pod',
    domain: 'Cluster Architecture',
    front: 'What makes a static Pod different from an ordinary Pod?',
    back: "A kubelet on one specific node manages it directly from a manifest file on that node's disk, without the API server scheduling it. The kubelet reports a read-only mirror Pod to the API server so it's visible via kubectl, but deleting that mirror through kubectl does not stop the real static Pod — only editing or removing the manifest file does.",
  },
  {
    id: 'taints-tolerations',
    service: 'Taints and Tolerations',
    domain: 'Workloads & Scheduling',
    front: 'How do taints and tolerations work together?',
    back: "A taint on a node repels Pods that don't tolerate it; a toleration on a Pod lets it be scheduled onto (or stay on) a tainted node. NoSchedule blocks new scheduling, PreferNoSchedule is a soft version the scheduler tries to honor, and NoExecute also evicts already-running Pods that lack a matching toleration.",
  },
  {
    id: 'node-affinity',
    service: 'Node Affinity',
    domain: 'Workloads & Scheduling',
    front: 'How does node affinity compare to a plain nodeSelector?',
    back: "nodeSelector is an exact-match label filter on the Pod spec. Node affinity is more expressive: it supports operators like In, NotIn, and Exists, plus two flavors — requiredDuringSchedulingIgnoredDuringExecution (a hard requirement) and preferredDuringSchedulingIgnoredDuringExecution (a weighted preference the scheduler tries to satisfy but won't block on).",
  },
  {
    id: 'service-clusterip',
    service: 'Service (ClusterIP)',
    domain: 'Services & Networking',
    front: 'What does a ClusterIP Service provide, and what is a headless Service?',
    back: "The default Service type: a single stable virtual IP, internal to the cluster, that load-balances traffic across the Pods matching its selector even as those Pods are replaced. Setting clusterIP: None makes it headless — no virtual IP is allocated, and DNS lookups return the backing Pods' addresses directly instead.",
  },
  {
    id: 'service-nodeport',
    service: 'Service (NodePort)',
    domain: 'Services & Networking',
    front: 'How does a NodePort Service expose an application?',
    back: 'It opens the same port on every node in the cluster (by default from the 30000-32767 range) and forwards traffic arriving on that port, on any node, to the Service — so a client can reach the app by hitting any node\'s IP on that port, without going through a cloud load balancer.',
  },
  {
    id: 'service-loadbalancer',
    service: 'Service (LoadBalancer)',
    domain: 'Services & Networking',
    front: 'What does a LoadBalancer Service depend on that ClusterIP and NodePort do not?',
    back: "It asks the cloud provider it's running on to provision an external load balancer pointed at the Service, giving it a stable external IP or hostname. Without a cloud provider integration configured, a LoadBalancer Service behaves like a NodePort and never gets that external address assigned.",
  },
  {
    id: 'ingress',
    service: 'Ingress',
    domain: 'Services & Networking',
    front: 'What does an Ingress resource define?',
    back: "HTTP/HTTPS routing rules — which incoming host and URL path should be sent to which backend Service — plus optional TLS termination, all in one object instead of one Service per hostname. An Ingress does nothing by itself; it only takes effect once an Ingress controller is running to implement its rules.",
  },
  {
    id: 'ingress-controller',
    service: 'Ingress Controller',
    domain: 'Services & Networking',
    front: 'Why does an Ingress need a controller?',
    back: "Ingress is just a spec — Kubernetes ships no built-in implementation of it. An Ingress controller (a separate, chosen-by-you piece of software, often bundled with a reverse proxy) watches Ingress objects and actually programs a load balancer or proxy to match their rules; IngressClass names which controller a given Ingress belongs to.",
  },
  {
    id: 'gateway-api',
    service: 'Gateway API',
    domain: 'Services & Networking',
    front: 'How does the Gateway API differ from Ingress?',
    back: 'It is a newer, role-oriented set of resources split across the people who configure infrastructure and the people who configure routing: a GatewayClass names the controller implementation, a Gateway represents a deployed listener bound to that class, and route resources like HTTPRoute attach traffic rules to a Gateway — a cleaner separation than the single, all-in-one Ingress object.',
  },
  {
    id: 'network-policy',
    service: 'NetworkPolicy',
    domain: 'Services & Networking',
    front: 'What does a NetworkPolicy control, and what is true before any policy is applied?',
    back: "Which ingress and/or egress traffic is allowed to/from a set of Pods, matched by podSelector, namespaceSelector, or ipBlock rules. Before any NetworkPolicy selects a Pod, all traffic to and from it is unrestricted — enforcement only begins once at least one policy applies, and it additionally requires a CNI plugin that implements NetworkPolicy.",
  },
  {
    id: 'coredns',
    service: 'CoreDNS',
    domain: 'Services & Networking',
    front: 'How does CoreDNS end up running in a cluster, and what DNS name format does a Pod typically use to reach a Service through it?',
    back: "It's installed by default as the cluster DNS add-on — kubeadm deploys it during init as a Deployment in kube-system — and it answers name lookups so a Pod can reach a Service by name (typically <service>.<namespace>.svc.cluster.local) instead of tracking its virtual IP directly.",
  },
  {
    id: 'pod-dns-policy',
    service: 'Pod DNS Policy',
    domain: 'Services & Networking',
    front: "What does a Pod's dnsPolicy field control?",
    back: "Where a Pod's containers get their DNS configuration from. ClusterFirst (the default) sends cluster-domain lookups to the cluster's DNS service and forwards everything else upstream; Default inherits the node's own resolv.conf; None ignores both and requires dnsConfig to fully specify resolution by hand.",
  },
  {
    id: 'kube-proxy',
    service: 'kube-proxy',
    domain: 'Services & Networking',
    front: 'What does kube-proxy do, and in what modes?',
    back: "Runs on every node and programs that node's packet-forwarding rules so traffic sent to a Service's virtual IP actually reaches one of the matching backend Pods. Its two common modes are iptables (rules chained as KUBE-SERVICES to KUBE-SVC to KUBE-SEP) and IPVS, a kernel load-balancing table built for larger Service counts.",
  },
  {
    id: 'configmap',
    service: 'ConfigMap',
    domain: 'Workloads & Scheduling',
    front: 'What is a ConfigMap for?',
    back: "Holds non-sensitive configuration as key-value pairs, decoupling config from the container image so the same image can run with different settings. A container can pick it up as an injected env var, a CLI flag value, a mounted file inside a volume, or a direct read through the API.",
  },
  {
    id: 'configmap-size-limit',
    service: 'ConfigMap',
    domain: 'Workloads & Scheduling',
    front: 'What is the maximum size of a single ConfigMap?',
    back: 'A single ConfigMap is capped at 1 MiB total.',
  },
  {
    id: 'secret',
    service: 'Secret',
    domain: 'Workloads & Scheduling',
    front: 'How does a Secret differ from a ConfigMap in practice?',
    back: 'Structurally almost identical, but meant for sensitive values — Kubernetes only base64-encodes Secret data, which is obfuscation rather than encryption unless encryption at rest is separately configured. Built-in types standardize common cases: kubernetes.io/tls, kubernetes.io/dockerconfigjson, kubernetes.io/basic-auth, and kubernetes.io/service-account-token among them.',
  },
  {
    id: 'volume',
    service: 'Volume',
    domain: 'Storage',
    front: "What problem do Kubernetes volumes solve that a container's own filesystem does not?",
    back: "A container's own filesystem is wiped on every restart, and files can't easily be shared between containers in a Pod. A Volume is storage attached to the Pod rather than to any one container, so its contents survive container restarts and, depending on the volume type, can be shared across every container in the Pod.",
  },
  {
    id: 'persistent-volume',
    service: 'PersistentVolume',
    domain: 'Storage',
    front: 'What is a PersistentVolume, and how is it provisioned?',
    back: "A cluster-scoped storage resource representing an actual piece of underlying storage, independent of any Pod's lifecycle. It's provisioned either statically (an administrator creates the PV ahead of time) or dynamically (a StorageClass tells the cluster how to create one automatically when a claim needs it).",
  },
  {
    id: 'persistent-volume-claim',
    service: 'PersistentVolumeClaim',
    domain: 'Storage',
    front: 'What is a PersistentVolumeClaim, and how does it relate to a PersistentVolume?',
    back: "A namespaced request for storage — a user asks for a size and access mode without needing to know the storage details — that the control plane binds to a matching PersistentVolume in an exclusive one-to-one pairing. A Pod then references the claim, not the volume, in its spec.",
  },
  {
    id: 'pv-access-modes',
    service: 'PersistentVolume Access Modes',
    domain: 'Storage',
    front: 'Name the PersistentVolume access modes.',
    back: 'ReadWriteOnce (one node at a time gets read and write access), ReadOnlyMany (many nodes can mount it, but none can write), ReadWriteMany (many nodes can mount it with full read and write access), and ReadWriteOncePod, a stricter variant that narrows write access down to a single Pod rather than just a single node.',
  },
  {
    id: 'pv-reclaim-policy',
    service: 'PersistentVolume Reclaim Policy',
    domain: 'Storage',
    front: "What happens to a PersistentVolume's storage after its claim is deleted?",
    back: 'It depends on the reclaim policy: Retain keeps both the PV object and its underlying storage around for manual cleanup or reattachment; Delete removes the PV and its backing storage automatically (the default for most dynamically provisioned volumes); Recycle, which wiped and reused the volume, is deprecated in favor of dynamic provisioning.',
  },
  {
    id: 'storage-class',
    service: 'StorageClass',
    domain: 'Storage',
    front: 'What does a StorageClass represent?',
    back: "A named storage profile an administrator publishes — pointing at a specific provisioner and its parameters — that PersistentVolumeClaims can request by name to get dynamically provisioned storage instead of waiting for a pre-created PV. Its volumeBindingMode can delay binding until a Pod using the claim is actually scheduled (WaitForFirstConsumer), which matters for storage tied to a particular zone or node.",
  },
  {
    id: 'limitrange',
    service: 'LimitRange',
    domain: 'Workloads & Scheduling',
    front: 'What does a LimitRange enforce that ResourceQuota does not?',
    back: "Per-object constraints within a namespace — minimum and maximum CPU/memory for a single Pod or container, a default request/limit auto-injected when a container omits one, and a bound on the ratio between limit and request. ResourceQuota instead caps the namespace's aggregate consumption across all objects combined.",
  },
  {
    id: 'resourcequota',
    service: 'ResourceQuota',
    domain: 'Workloads & Scheduling',
    front: 'What can a ResourceQuota restrict?',
    back: "Total resource consumption across an entire namespace: aggregate CPU/memory requests and limits, storage requested (overall or per StorageClass), and raw object counts like count/pods or count/secrets. A request that would push the namespace over any hard limit is rejected outright, and scopeSelector can narrow a quota to a subset of Pods, such as only BestEffort ones.",
  },
  {
    id: 'pod-disruption-budget',
    service: 'PodDisruptionBudget',
    domain: 'Workloads & Scheduling',
    front: 'What does a PodDisruptionBudget protect against?',
    back: "Voluntary disruptions only — things like a node drain or cluster-autoscaler defragmentation, not a hardware failure. It sets a floor (minAvailable) or ceiling (maxUnavailable) on how many of an app's Pods can be evicted at once through the Eviction API, which tools like kubectl drain are expected to call rather than deleting Pods directly.",
  },
  {
    id: 'image-pull-secrets',
    service: 'ImagePullSecrets',
    domain: 'Workloads & Scheduling',
    front: "What is imagePullSecrets for, and how can it be applied once to cover many Pods instead of being repeated on each one?",
    back: "It tells the kubelet which registry-credential Secret to use when pulling a private container image for a Pod. Rather than repeating the reference on every Pod, the same imagePullSecrets entry can be attached once to a ServiceAccount so every Pod created under that ServiceAccount inherits it automatically.",
  },
  {
    id: 'role',
    service: 'Role',
    domain: 'Cluster Architecture',
    front: 'What can a Role grant, and where?',
    back: "A namespaced set of RBAC rules (API group, resource, and verbs like get/list/create/delete) that only grants permissions on resources inside the one namespace it was created in. Nodes and other objects that live outside every namespace are simply out of its reach, no matter how the rule is written.",
  },
  {
    id: 'rolebinding',
    service: 'RoleBinding',
    domain: 'Cluster Architecture',
    front: 'What does a RoleBinding do, and can it reference a ClusterRole?',
    back: "Grants the permissions of a Role or ClusterRole to one or more subjects — a user, a group, a ServiceAccount, any mix of the three. Yes — a RoleBinding can point at a ClusterRole, but the grant still only applies within the RoleBinding's own namespace, which is the standard way to reuse one shared set of rules across many namespaces without redefining it.",
  },
  {
    id: 'clusterrole',
    service: 'ClusterRole',
    domain: 'Cluster Architecture',
    front: 'When is a ClusterRole required instead of a Role?',
    back: 'When the permission needs to cover resources that exist outside any namespace (Nodes, PersistentVolumes), non-resource API paths like /healthz, or a namespace-scoped resource across every namespace at once — none of which a plain Role can express.',
  },
  {
    id: 'clusterrolebinding',
    service: 'ClusterRoleBinding',
    domain: 'Cluster Architecture',
    front: 'How does a ClusterRoleBinding differ from a RoleBinding?',
    back: 'It grants its referenced ClusterRole to its subjects everywhere in the cluster at once, with no namespace boundary — unlike a RoleBinding, which always confines the grant to the one namespace it lives in even when it points at a ClusterRole.',
  },
  {
    id: 'serviceaccount',
    service: 'Pod Identity',
    domain: 'Cluster Architecture',
    front: 'What identity does a Pod use to call the Kubernetes API?',
    back: "A ServiceAccount, not a human user account — every Pod runs under one, falling back to its namespace's 'default' ServiceAccount if none is specified. By default a ServiceAccount outside kube-system has no permissions beyond basic discovery, so anything a workload needs must be granted through an explicit RoleBinding or ClusterRoleBinding, ideally one dedicated ServiceAccount per application rather than a shared default."
  },
  {
    id: 'admission-controllers',
    service: 'Admission Controller',
    domain: 'Cluster Architecture',
    front: 'When does an admission controller run, and what can it do?',
    back: "It intercepts a create, update, or delete request after authentication and authorization succeed but before the object is persisted to etcd. Mutating admission controllers can modify the request (like injecting a default value); validating ones can only accept or reject it — they can never affect plain read requests like get, list, or watch.",
  },
  {
    id: 'kube-apiserver',
    service: 'kube-apiserver',
    domain: 'Cluster Architecture',
    front: 'What role does the kube-apiserver play in the control plane?',
    back: "The front door of the cluster: it exposes the Kubernetes HTTP API that every other component and every kubectl command goes through, handling authentication, authorization, and admission before reading or writing state, all of which it persists to etcd.",
  },
  {
    id: 'etcd',
    service: 'etcd',
    domain: 'Cluster Architecture',
    front: 'What is etcd, and why does it matter for cluster availability?',
    back: "The consistent, highly-available key-value store that holds all of the API server's data — every object in the cluster ultimately lives there. Because it's the cluster's single source of truth, an HA control plane needs etcd to survive a control-plane node failure, whether that's a stacked etcd member on each control-plane node or an external etcd cluster.",
  },
  {
    id: 'kube-scheduler',
    service: 'kube-scheduler',
    domain: 'Cluster Architecture',
    front: 'What does kube-scheduler decide?',
    back: 'Which node an unbound Pod should run on. It watches for newly created Pods that have no node assigned yet and picks a suitable node for each one, taking constraints like resource requests, taints and tolerations, and affinity rules into account.',
  },
  {
    id: 'kube-controller-manager',
    service: 'kube-controller-manager',
    domain: 'Cluster Architecture',
    front: 'What runs inside kube-controller-manager?',
    back: "A bundle of separate control loops — including the ones behind ReplicaSet, Deployment, Job, and Node lifecycle behavior — each watching the cluster's actual state and driving it toward whatever is declared, compiled into a single control-plane process for operational simplicity.",
  },
  {
    id: 'cloud-controller-manager',
    service: 'cloud-controller-manager',
    domain: 'Cluster Architecture',
    front: 'What does the (optional) cloud-controller-manager do?',
    back: "Integrates the cluster with an underlying cloud provider, handling things a self-managed cluster can't do on its own — provisioning a LoadBalancer Service's external load balancer, or labeling and assigning addresses to Nodes based on cloud metadata, for example.",
  },
  {
    id: 'kubelet',
    service: 'kubelet',
    domain: 'Cluster Architecture',
    front: "What is the kubelet's job on a node?",
    back: "Makes sure the containers described in every Pod assigned to its node are actually running and healthy, talking to the container runtime through the CRI to start and stop them. It's one of the few components that runs directly on the node rather than inside a container, and it's also what runs static Pods from local manifest files.",
  },
  {
    id: 'kubeadm',
    service: 'kubeadm',
    domain: 'Cluster Architecture',
    front: 'What does kubeadm do, and what does it not do?',
    back: "Bootstraps a minimum-viable, best-practice cluster: kubeadm init brings up a brand-new control-plane node and kubeadm join enrolls workers or extra control-plane nodes into that cluster afterward. Setting up Pod networking is left to the operator, so CoreDNS stays Pending and nodes stay NotReady until a CNI plugin is deployed separately.",
  },
  {
    id: 'ephemeral-containers',
    service: 'Ephemeral Container',
    domain: 'Troubleshooting',
    front: 'When would you reach for an ephemeral container instead of kubectl exec?',
    back: "When kubectl exec isn't an option — the target container has already crashed, or its image is distroless and has no shell to exec into. kubectl debug adds a temporary ephemeral container to the already-running Pod that shares its process namespace, giving you a shell without altering the original container image."
  },
  {
    id: 'cri',
    service: 'CRI (Container Runtime Interface)',
    domain: 'Cluster Architecture',
    front: 'What problem does the CRI solve?',
    back: "It's the gRPC-based plugin interface between the kubelet and a container runtime, letting Kubernetes support multiple runtimes (containerd, CRI-O, and others) without runtime-specific code baked into the kubelet itself. Every node needs a CRI-compatible runtime installed before its kubelet can start any Pods.",
  },
  {
    id: 'cni',
    service: 'CNI (Container Network Interface)',
    domain: 'Services & Networking',
    front: "What role does a CNI plugin play, and does Kubernetes ship one?",
    back: "CNI plugins implement Kubernetes' Pod networking model — giving every Pod a routable IP and handling the interface setup that makes cross-node Pod communication work. Kubernetes bundles no default implementation at all; until a compatible CNI plugin is installed, Pods can't get addresses and cluster add-ons like CoreDNS stay stuck Pending.",
  },
  {
    id: 'csi',
    service: 'CSI (Container Storage Interface)',
    domain: 'Storage',
    front: 'What does CSI let a storage vendor do?',
    back: "Ship a storage driver — handling provisioning, attaching, and mounting for their system — as an out-of-tree plugin that Pods consume through ordinary PersistentVolumeClaims, without that vendor's code needing to be compiled into Kubernetes itself. It replaced the older approach where storage integrations lived directly in the Kubernetes source tree.",
  },
  {
    id: 'helm',
    service: 'Helm',
    domain: 'Cluster Architecture',
    front: 'What is a Helm chart, and what is a release?',
    back: "A chart bundles the resource templates and default configuration values needed to run an application, and is published to a repository a Helm client can search or add. Running helm install against a chart produces a release — a distinct, named, trackable installation that can be upgraded or rolled back on its own, and the same chart can be installed multiple times as separate releases.",
  },
  {
    id: 'kustomize',
    service: 'Kustomize',
    domain: 'Cluster Architecture',
    front: 'How does Kustomize customize manifests differently from Helm?',
    back: "It works from plain, unmodified Kubernetes YAML plus a kustomization.yaml that layers transformations on top — generating ConfigMaps/Secrets, adding common labels, or patching fields — rather than filling in a templating language. kubectl has had built-in support since v1.14 via kubectl apply -k, so no separate templating step or tool is required.",
  },
  {
    id: 'hpa',
    service: 'HorizontalPodAutoscaler',
    domain: 'Workloads & Scheduling',
    front: 'What does a HorizontalPodAutoscaler do?',
    back: "Watches a metric (commonly CPU or memory utilization) and adjusts spec.replicas on a scalable workload resource — a Deployment or StatefulSet, for example — up or down to keep the metric near its target.",
  },
  {
    id: 'hpa-daemonset-exception',
    service: 'HorizontalPodAutoscaler',
    domain: 'Workloads & Scheduling',
    front: "Why can't a HorizontalPodAutoscaler target a DaemonSet?",
    back: "Because a DaemonSet's Pod count is fixed by the number of matching nodes, not a replica field, so there's nothing for the HPA to adjust — it can only target workload resources like a Deployment or StatefulSet that expose spec.replicas.",
  },
  {
    id: 'resource-requests',
    service: 'Resource Requests',
    domain: 'Workloads & Scheduling',
    front: 'What does a container resource request actually do?',
    back: "Tells the scheduler how much CPU and memory a container needs, and the scheduler only places a Pod on a node that has that much unallocated capacity. Requests don't cap how much a container can actually use at runtime — that's the job of a limit.",
  },
  {
    id: 'resource-limits',
    service: 'Resource Limits',
    domain: 'Workloads & Scheduling',
    front: 'How are CPU limits enforced differently from memory limits?',
    back: 'A CPU limit is enforced proactively by throttling — a hard ceiling the container cannot exceed, though it keeps running when it hits it. A memory limit is enforced reactively: a container can transiently exceed it, and the kernel only steps in with an OOM kill (OOMKilled) once it actually detects memory pressure, since memory usage can\'t be throttled back the way CPU time can.',
  },
  {
    id: 'qos-classes',
    service: 'Pod QoS Class',
    domain: 'Workloads & Scheduling',
    front: "What determines whether a Pod is Guaranteed, Burstable, or BestEffort?",
    back: "Guaranteed means every container sets a request and a limit for both CPU and memory, with the request equal to the limit in each case. Burstable means at least one container sets some request or limit, but the Pod doesn't meet the stricter Guaranteed bar. BestEffort means no container sets any request or limit at all — these are first in line to be killed for memory during resource pressure.",
  },
  {
    id: 'liveness-probe',
    service: 'Liveness Probe',
    domain: 'Troubleshooting',
    front: 'What action does a failing liveness probe trigger?',
    back: "A container restart. Liveness probes exist to catch an app that's still running as a process but has locked up or otherwise stopped making progress — the kind of broken state a restart, not more traffic-shaping, is the actual fix for.",
  },
  {
    id: 'readiness-probe',
    service: 'Readiness Probe',
    domain: 'Troubleshooting',
    front: 'What does a failing readiness probe do — and what does it not do?',
    back: "It removes the Pod from the Endpoints/EndpointSlices backing its Services, so it stops receiving traffic through them, without restarting the container or otherwise touching the Pod. It's the right tool for a Pod that's temporarily unable to serve requests, such as one still loading a large dataset at startup.",
  },
  {
    id: 'startup-probe',
    service: 'Startup Probe',
    domain: 'Troubleshooting',
    front: 'Why add a startup probe alongside a liveness probe?',
    back: "It gives a slow-starting container a longer grace period — the kubelet holds off on running the liveness (and readiness) probe until the startup probe first succeeds — so a container isn't killed for still being mid-boot under a liveness probe timing tuned for steady-state failures instead.",
  },
  {
    id: 'metrics-server',
    service: 'Metrics Server',
    domain: 'Troubleshooting',
    front: 'What is metrics-server, and what depends on it?',
    back: "A cluster add-on, not something built into Kubernetes core, that polls each kubelet's resource-usage endpoint and aggregates the results into the cluster's Metrics API. kubectl top, the HorizontalPodAutoscaler, and the VerticalPodAutoscaler all read from that API and simply don't work until metrics-server (or an equivalent) has been deployed.",
  },
  {
    id: 'crd',
    service: 'CustomResourceDefinition',
    domain: 'Cluster Architecture',
    front: 'What does a CustomResourceDefinition let you do?',
    back: "Register a brand-new kind of API object — with its own schema, API group, and normal kubectl support — without writing and running a separate aggregated API server. On its own a custom resource is just structured data the API can store and retrieve; it needs a controller acting on it to do anything.",
  },
  {
    id: 'operator-pattern',
    service: 'Operator Pattern',
    domain: 'Cluster Architecture',
    front: 'What is the Operator pattern?',
    back: 'Pairing a CustomResourceDefinition with a custom controller that watches instances of it and continuously drives the cluster toward whatever state they declare — turning that pair into a genuine declarative API. Operators typically encode operational knowledge for one specific piece of software (backups, failover, upgrades) as automation native to the cluster.',
  },
];
