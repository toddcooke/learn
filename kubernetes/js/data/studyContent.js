// js/data/studyContent.js
// Study notes for the CKA exam domains, starting with Cluster Architecture,
// Installation and Configuration (25% exam weight), Services and
// Networking (20% exam weight), Workloads and Scheduling (15% exam
// weight), Storage (10% exam weight), and Troubleshooting (30% exam
// weight, the largest domain). Grounded in kubernetes.io and helm.sh
// documentation fetched into .cache/aws-docs/ (see scripts/fetch-doc.mjs).
// Written in the author's own words, not copied verbatim from the source docs.

export const STUDY_CONTENT = [
  {
    domain: 'cluster',
    taskStatement: 'RBAC and access control',
    topics: [
      {
        title: 'Roles and ClusterRoles: scoping permissions to a namespace or the whole cluster',
        body: "Kubernetes' RBAC authorization model works through four API objects in the rbac.authorization.k8s.io group: Role, ClusterRole, RoleBinding, and ClusterRoleBinding. A Role is namespaced — it must be created inside a specific namespace and only ever grants permissions on resources within that namespace — while a ClusterRole is cluster-scoped and can grant permissions on namespaced resources across every namespace, on resources that don't belong to any namespace such as Nodes, or on non-resource API paths like /healthz. Every rule inside a Role or ClusterRole is purely additive; RBAC has no concept of a rule that denies access, so a subject's effective permissions are simply the union of everything granted to it. A single rule combines an API group, one or more resource types (optionally narrowed to specific resource names or subresources such as pods/log), and a list of verbs like get, list, watch, create, update, or delete, and it's safer to spell out exact resources and verbs than to reach for wildcards, since a wildcard silently grants access to any resource type added later.",
      },
      {
        title: 'RoleBindings and ClusterRoleBindings: attaching a role to a subject',
        body: "A binding is what actually hands a role's permissions to someone; it lists one or more subjects — users, groups, or ServiceAccounts — together with a reference to the Role or ClusterRole being granted. A RoleBinding only takes effect inside its own namespace even when it points at a ClusterRole, which is the standard way to reuse one shared set of rules (say, a common developer ClusterRole) across many namespaces without redefining it each time. A ClusterRoleBinding, by comparison, applies its role everywhere in the cluster at once. Once a binding is created, the role it points to can't be changed in place — swapping in a different role means deleting the binding and creating a new one — while the list of subjects on an existing binding can still be edited freely, which keeps someone from quietly escalating a binding's power without a fresh, auditable object.",
      },
      {
        title: 'ServiceAccounts as the identity workloads use to call the API',
        body: 'A ServiceAccount is the identity a Pod authenticates with when it talks to the Kubernetes API, separate from the human or external identities administrators use; every Pod runs under some ServiceAccount, falling back to the "default" account of its own namespace when none is specified. Out of the box, RBAC hands ServiceAccounts outside kube-system no permissions beyond what the built-in discovery roles expose, so anything a workload needs has to be granted through an explicit binding. The more secure pattern is a dedicated ServiceAccount per application, bound only to the roles that application actually requires, rather than granting access through a namespace-wide default account or, worse, to every ServiceAccount in the cluster — that broadest option effectively lets any running container act with whatever privilege was handed out.',
      },
      {
        title: 'Built-in ClusterRoles: cluster-admin, admin, edit, and view',
        body: "Kubernetes ships several default, user-facing ClusterRoles covering the access levels administrators most often need to grant. cluster-admin is a super-user role that, bound cluster-wide, permits any action on any resource anywhere; admin and edit both give broad read/write access inside a namespace when attached through a RoleBinding, but neither can touch resource quotas, and edit additionally can't view or modify roles and role bindings even though it can still run Pods as any ServiceAccount in the namespace. view is read-only and, notably, withholds access to Secrets, because reading a Secret can itself be a path to acting as another ServiceAccount. These defaults rely on ClusterRole aggregation, so an administrator can extend admin, edit, or view to cover a new custom resource just by creating another ClusterRole carrying the matching rbac.authorization.k8s.io/aggregate-to-* label, instead of editing the built-in role directly.",
      },
      {
        title: 'Preventing privilege escalation through role and binding creation',
        body: "RBAC enforces a check at the API layer — active even when the RBAC authorizer itself isn't the one deciding a request — that stops a subject from granting more than it already has, and it does so through two separate gates. Creating or updating a Role or ClusterRole requires either already holding every permission it would contain at the same scope, or carrying explicit authorization to perform the escalate verb against roles or clusterroles resources. Creating or updating a binding to that role is a distinct check: it requires either already holding the referenced role's own permissions yourself, or carrying explicit authorization to perform the bind verb against that specific role. This closes the obvious loophole where a narrowly-scoped user could define a broader role and then bind it to themselves. The deliberate exception is bootstrapping: the very first roles and bindings on a brand-new cluster are created using a credential from the system:masters group, which the default bindings tie to the cluster-admin role, giving the initial administrator a starting point from which everything else can be granted.",
      },
    ],
  },
  {
    domain: 'cluster',
    taskStatement: 'Preparing infrastructure and creating clusters with kubeadm',
    topics: [
      {
        title: 'Planning a production-ready cluster: availability, scale, and access',
        body: "Moving from a single-machine learning setup to a production deployment means deliberately addressing availability, scale, and access management. Availability means pulling the control plane off the worker nodes, running more than one replica of each control-plane component, and putting a load balancer in front of the API server, rather than accepting the single point of failure a one-machine cluster has. Scale means having enough worker capacity for today's load and a plan for adding more as demand grows, whether that plan is manual or automated. And because a production cluster is typically shared far more widely than a personal one, access shifts from a single administrative account to layered authentication — verifying who's making a request, via client certificates, bearer tokens, or an external identity provider — combined with authorization, almost always RBAC, that decides what an already-verified identity is allowed to do.",
      },
      {
        title: 'Node roles and host prerequisites for kubeadm',
        body: "Every node destined for a kubeadm cluster needs a working container runtime already installed, since the kubelet has no way to start a Pod without one, plus full network connectivity to every other machine in the cluster. kubeadm's stated minimums call for at least 2 CPUs on any host that will run as a control-plane node, along with a memory floor of 2 GiB on every machine — dropping below that leaves an application little breathing room. Nodes take on one of two roles: a control-plane node hosts the cluster's control-plane services, including etcd — the cluster's backing datastore — and the API server that kubectl and every other client talks to, while a worker node is where ordinary application Pods actually get scheduled. kubeadm taints control-plane nodes by default so regular workloads land only on workers, reserving control-plane capacity for cluster management; that taint can be manually removed on small or single-machine setups where the separation isn't needed.",
      },
      {
        title: 'kubeadm init: bootstrapping the first control-plane node',
        body: "Running kubeadm init on a chosen machine stands up the very first control-plane node: it runs a batch of readiness checks, then pulls down and starts the control-plane components. If there's any chance this cluster will grow into a highly-available one later, the guidance is to pass --control-plane-endpoint at this very first init so every future control-plane node shares one stable DNS name or load-balancer address from day one, because a single control-plane cluster built without that flag can't later be converted into an HA one. Once the control plane is up, kubeadm prints a join command carrying a bootstrap token and a discovery hash that other machines will use to authenticate into the cluster, along with instructions for copying the admin kubeconfig so kubectl can be used without root access.",
      },
      {
        title: 'Deploying a Pod network add-on and joining nodes',
        body: "A newly initialized kubeadm cluster isn't functional yet: CoreDNS and ordinary Pod-to-Pod traffic won't work until a CNI-based Pod network add-on has been applied with kubectl apply. Only one Pod network can be installed per cluster, and since kubeadm enables and enforces RBAC by default, whatever add-on gets chosen needs manifests written to work under that enforcement. Once the network add-on is running and CoreDNS shows as Running, more machines can be added by executing the join command saved from the init output on each one, supplying the shared token and certificate hash so the new node can authenticate; the same command with an added --control-plane flag and a certificate key is how additional control-plane nodes — rather than plain workers — are added.",
      },
      {
        title: 'Draining and removing a node with kubeadm',
        body: "Decommissioning a node cleanly starts with kubectl drain against it, which evicts its running workloads and marks it unschedulable, before running kubeadm reset on the node itself to undo the local state kubeadm created there. kubeadm reset deliberately leaves iptables and IPVS rules untouched, so those have to be cleared by hand if a full cleanup is wanted. Only after the reset should the corresponding Node object be removed from the API with kubectl delete node. Getting this order right matters operationally: draining first gives the scheduler a chance to move workloads elsewhere before capacity disappears, whereas resetting or deleting out of turn can leave the cluster believing a node is still schedulable after its kubelet has already stopped.",
      },
    ],
  },
  {
    domain: 'cluster',
    taskStatement: 'Cluster lifecycle and high availability',
    topics: [
      {
        title: 'The kubeadm upgrade order: control plane first, then workers',
        body: "kubeadm upgrades a cluster in a strict sequence: the first control-plane node runs kubeadm upgrade apply against the target version, every remaining control-plane node then runs kubeadm upgrade node, and only once all control-plane nodes are upgraded do worker nodes get upgraded one at a time (or a few at a time) so the workload never drops below the capacity it needs. kubeadm only supports advancing one minor version per upgrade — skipping straight from, say, 1.34 to 1.36 isn't supported — while a node's kubelet is allowed to lag up to three minor versions behind the control plane, which is what makes staged rollouts across a large fleet practical. Crucially, kubeadm upgrade only manages components internal to Kubernetes itself; it never touches application workloads, so backing up anything at the workload level, like database state, stays a separate responsibility.",
      },
      {
        title: 'Draining, upgrading, and uncordoning individual nodes',
        body: "Before a node's kubelet is touched during a minor-version upgrade, that node needs to be drained with kubectl drain so its Pods get evicted and rescheduled elsewhere — important even for control-plane nodes, since one of them might be hosting CoreDNS or another critical workload at that moment. After its kubeadm, kubelet, and kubectl packages are upgraded and the kubelet service restarted, the node is returned to service with kubectl uncordon, clearing the unschedulable mark that drain had set. Every container on a node restarts once its kubelet's configuration changes during an upgrade, so a brief disruption to that one node's workloads is expected, and checking that its status flips back to Ready with kubectl get nodes is the normal way to confirm the upgrade succeeded.",
      },
      {
        title: 'HA control-plane topologies: stacked etcd vs external etcd',
        body: 'kubeadm supports two topologies for a highly-available control plane. In the stacked topology, each control-plane node runs its own etcd member side by side with its API server and other control-plane services, which needs less infrastructure overall; in the external topology, etcd instead runs as its own independent cluster of machines that every control-plane node connects to over the network, which needs more hardware but keeps an etcd failure from taking down a control-plane node\'s other services, and vice versa. Both approaches call for at least three control-plane nodes — and, for external etcd, at least three separate etcd hosts — because an odd number gives etcd\'s underlying consensus protocol a clean majority for leader election if a node or zone is lost. Whichever topology is chosen, a load balancer sitting in front of every control-plane node\'s API server, matching whatever address was set as --control-plane-endpoint, is what lets clients and joining nodes reach a healthy API server without depending on any single node\'s address.',
      },
      {
        title: 'Sharing certificates and adding control-plane nodes to an HA cluster',
        body: "Initializing the first control-plane node with kubeadm init --upload-certs encrypts its certificates and stores them as a Secret in the cluster, so additional control-plane nodes can join with kubeadm join ... --control-plane --certificate-key and have those shared certificates downloaded and decrypted automatically, instead of an administrator having to copy PKI files between hosts by hand. That uploaded Secret and its decryption key expire two hours after creation by default, though they can be regenerated later if more control-plane nodes still need to join. After a second or third control-plane node comes online, restarting the CoreDNS deployment is worth doing so its replicas get spread across nodes, since sequential initialization otherwise tends to leave every CoreDNS replica sitting on the first control-plane node alone.",
      },
      {
        title: 'etcd backups and single-control-plane resilience limits',
        body: "Because etcd holds every object definition, every Secret, and the entire current state of the cluster, a kubeadm cluster running just one control-plane node has effectively no resilience against that node failing — losing it can mean losing cluster data entirely and rebuilding from nothing. Regularly backing up the etcd data directory, which kubeadm places at /var/lib/etcd by default, is the documented safeguard for anyone running a single-node setup, and kubeadm upgrade itself automatically takes its own etcd and static-manifest backups before an upgrade proceeds, specifically so a failed upgrade has something to roll back to. For workloads that genuinely need continuous availability rather than backup-and-restore, running multiple control-plane nodes under either HA topology is what removes etcd, and the rest of the control plane, as a single point of failure in the first place.",
      },
    ],
  },
  {
    domain: 'cluster',
    taskStatement: 'Extending the cluster: Helm, Kustomize, CNI/CSI/CRI, CRDs and operators',
    topics: [
      {
        title: 'Helm: charts, repositories, and releases',
        body: "Helm packages a Kubernetes application as a chart — a bundle of resource templates and default configuration values needed to run it — and charts get published to repositories that a Helm client can search remotely or add locally. Running helm install against a chart doesn't just produce the underlying Kubernetes objects; it produces a release, a distinct, named, trackable installation of that chart, and installing the same chart repeatedly (two separate database instances, say) simply creates two independent releases that can each be upgraded or removed on their own. This release-oriented model is what sets Helm apart from applying raw manifests directly: Helm keeps a history of each release's configuration, which is what makes rolling a release back to an earlier revision or tearing it down as a single unit possible.",
      },
      {
        title: 'Kustomize: layering customization over plain manifests',
        body: "Kustomize takes a different approach than Helm's fill-in-the-template model: it works entirely from a kustomization.yaml file that lists ordinary, unmodified Kubernetes manifests as its resources, then layers transformations on top — generating ConfigMaps or Secrets from literal values or from files, applying common labels, or patching fields across a whole set of objects. Because kubectl has built-in Kustomize support since v1.14, a kustomization directory can be previewed with kubectl kustomize <dir> or applied straight to the cluster with kubectl apply -k <dir>. This generator-driven style suits keeping several environment-specific variants of the same application — dev, staging, production — in sync, since each variant's overlay only has to describe what differs from a shared base rather than duplicating whole manifests, and Kustomize automatically updates any reference to a generated ConfigMap or Secret so nothing breaks when the generated object's name changes.",
      },
      {
        title: 'CRI: how the kubelet talks to a container runtime',
        body: 'The Container Runtime Interface defines the gRPC-based protocol the kubelet uses to instruct a container runtime, and it is what allows Kubernetes to support multiple runtimes — containerd, CRI-O, and others — without runtime-specific logic being built into the kubelet; the kubelet always plays the client role, while the runtime exposes the CRI server side. Every node needs a CRI-compatible runtime installed and running before its kubelet can launch any Pods, and as of Kubernetes v1.26 the kubelet will flatly refuse to register a node whose runtime doesn\'t implement the current v1 CRI API. Because the interface cleanly separates "orchestration" from "running containers," swapping which runtime a node uses is possible without changing anything about Kubernetes itself, as long as the replacement still speaks the same protocol.',
      },
      {
        title: 'CNI: the plugin interface behind Pod networking',
        body: "The Container Network Interface is the plugin mechanism a cluster relies on to implement Kubernetes' networking model, and a cluster is not usable without one — on a kubeadm cluster specifically, CoreDNS won't even start before a CNI-based network add-on has been deployed. Kubernetes doesn't bundle a default network implementation of its own; administrators pick from a broad ecosystem of open- and closed-source CNI plugins that must, at minimum, support the v0.4.0 revision of the CNI specification, and only one such add-on can be active per cluster. Beyond basic connectivity between Pods, individual CNI plugins can layer on optional capabilities such as exposing a container's hostPort or enforcing NetworkPolicy objects, so the plugin an environment settles on can determine whether certain traffic-isolation features are even available.",
      },
      {
        title: 'CSI: pluggable storage drivers outside the core codebase',
        body: "The Container Storage Interface plays roughly the same role for storage that CNI plays for networking: a standard, out-of-tree interface that lets a storage vendor ship a driver handling provisioning, attaching, and mounting for their storage system without that vendor's code ever being compiled into Kubernetes itself. This model replaced the earlier approach where storage integrations were built directly into Kubernetes' own source tree, which meant adding support for a new storage backend was tied to Kubernetes' own release cycle. Once a CSI driver is deployed, Pods consume the storage it exposes the same way they'd consume any other volume, typically through a PersistentVolumeClaim bound to a PersistentVolume that names the driver, and a CSIMigration mechanism can transparently redirect requests aimed at an older built-in storage plugin to the equivalent CSI driver so existing configuration keeps working through a migration.",
      },
      {
        title: 'CustomResourceDefinitions and the operator pattern',
        body: "A CustomResourceDefinition lets a cluster admin register a brand-new kind of API object — complete with its own schema, its own API group, and normal kubectl support — without writing and running a separate API server; that's the simpler of Kubernetes' two extension paths, the other being a fully independent aggregated API server that takes more effort to build but allows deeper control over validation and storage. On its own, a custom resource is just structured data an administrator can create, read, and delete through the API. Pairing that custom resource with a custom controller that watches it and continuously drives the cluster toward whatever state it declares is what turns the pair into a genuine declarative API, and a CRD combined with a controller acting on it is exactly what the operator pattern describes. Operators typically encode the operational know-how for running a specific piece of software — backups, failover, version upgrades — as automation native to the cluster, turning what would otherwise be a manual runbook into editing a custom resource.",
      },
    ],
  },
  {
    domain: 'services',
    taskStatement: 'Pod connectivity and Network Policies',
    topics: [
      {
        title: 'The Kubernetes networking model: every Pod gets a routable IP',
        body: "Kubernetes assumes a flat network in which every Pod is reachable at its own IP address from every other Pod in the cluster, on every node, without any address translation getting in the way — the network plugin running on each node is what actually hands out and routes those addresses. This sidesteps the classic problem of coordinating which host port each process gets to avoid collisions when many applications share a handful of machines, since dynamic port negotiation between applications, load balancers, and the API server doesn't scale cleanly once dozens of teams are involved. Containers within one Pod already share a single network namespace and can reach each other over localhost, so the model really has three remaining connectivity concerns to solve: getting traffic from one Pod to another, routing Pod traffic through a Service, and letting traffic from outside the cluster reach a Service.",
      },
      {
        title: 'How the network plugin allocates addresses for Pods, Services, and Nodes',
        body: "Three different components are each responsible for handing out a different category of address in a cluster: the network plugin hands out Pod addresses, while the kube-apiserver allocates each Service's address out of the configured service IP range, and either the kubelet or, when the cluster integrates with a cloud provider, the cloud-controller-manager assigns addresses to Nodes. All of these have to draw from non-overlapping ranges, and every component has to agree on which IP families — IPv4 only, IPv6 only, or dual-stack — the cluster is actually using. Most container runtimes implement this addressing and routing behavior through a Container Network Interface plugin, and because so many CNI plugins exist, cluster operators can pick one that only handles basic interface setup or one that layers on more advanced features like running multiple plugins together or richer address-management schemes.",
      },
      {
        title: 'Pod isolation: ingress and egress are independent, and off by default',
        body: "A Pod's exposure to network traffic is governed by two separate notions of isolation, one for inbound (ingress) connections and one for outbound (egress) connections, and a Pod starts out non-isolated in both directions — meaning nothing restricts its traffic until some NetworkPolicy says otherwise. A Pod becomes isolated for a given direction as soon as any NetworkPolicy both selects that Pod and lists that direction among its policy types; once that happens, only the connections explicitly permitted by that policy's rules, plus reply traffic for connections already allowed, get through in that direction. Because these effects are purely additive across every applicable policy, a connection needs the union of all ingress rules touching the destination Pod, and separately the union of all egress rules touching the source Pod, to both agree the traffic should proceed.",
      },
      {
        title: 'Targeting traffic with podSelector, namespaceSelector, and ipBlock',
        body: "A NetworkPolicy's ingress sources or egress destinations are built out of up to three selector kinds: a podSelector matching Pods in the policy's own namespace, a namespaceSelector matching every Pod inside namespaces carrying certain labels, and an ipBlock naming external CIDR ranges, since Pod addresses themselves are too short-lived to be useful there. Combining a namespaceSelector and a podSelector inside one list entry narrows the match to Pods with both sets of labels at once, while listing them as two separate entries instead broadens the match to either condition — a YAML nesting detail worth checking with kubectl describe whenever a policy's actual effect is in doubt. Two exceptions apply no matter what a policy selects: a Pod can never be blocked from reaching itself, and traffic between a Pod and the node it happens to be running on is always permitted.",
      },
      {
        title: 'Building default-deny and default-allow policies for a namespace',
        body: "Because nothing is restricted anywhere a NetworkPolicy hasn't been applied, a common pattern is a namespace-wide baseline: a NetworkPolicy with an empty podSelector, matching every Pod in the namespace, paired with no ingress or egress entries creates a default-deny for that direction, while the same empty podSelector paired with a single all-matching rule creates an explicit default-allow that no narrower policy can override. A default-deny-egress policy is easy to get wrong in practice because it also blocks DNS lookups by default, so any namespace adopting one needs a companion rule that explicitly permits egress to the cluster's DNS service. Applying both an ingress and an egress default-deny policy together locks a namespace down in both directions at once, giving administrators a known baseline before layering on narrower allow rules for specific applications.",
      },
      {
        title: 'What NetworkPolicy enforcement requires and does not cover',
        body: "A NetworkPolicy object is inert on its own — it only takes effect once the cluster's network plugin actually implements NetworkPolicy support, so creating one against a plugin that lacks that support changes nothing. Where enforcement does apply, it is scoped to layer 3 and 4 traffic such as TCP, UDP, and optionally SCTP; behavior toward other protocols like ICMP is left undefined and varies by plugin. The API also has real gaps worth knowing when troubleshooting: it cannot target a Service by name, only Pods or namespaces by label, it has no explicit deny rule since the model is deny-by-default with only additive allows, it cannot apply automatically to every namespace cluster-wide, and it offers no built-in way to record which connections a policy blocked or accepted — those needs typically fall to a service mesh, a Layer 7 proxy, or OS-level tooling instead.",
      },
    ],
  },
  {
    domain: 'services',
    taskStatement: 'Service types and endpoints',
    topics: [
      {
        title: 'Why Services exist: stable access to a shifting set of Pod IPs',
        body: "Pods are deliberately disposable: a Deployment can replace them at any time, each replacement gets a brand-new address, and the set of currently healthy Pods backing an application can look completely different only moments later. That creates a real problem for anything depending on those Pods — a frontend has no reliable way to know which backend addresses are currently good without constantly re-discovering them. A Service closes that gap by giving a logical group of Pods, normally the ones matching a label selector, one stable point of contact that keeps working across every Pod replacement, so callers never need to track individual Pod addresses themselves.",
      },
      {
        title: 'ClusterIP: the default type, plus the headless variant',
        body: "ClusterIP is what a Service becomes if no type is specified at all: Kubernetes hands it a virtual address drawn from the cluster's reserved Service range, reachable only from inside the cluster, and every other Service type effectively layers on top of this one. Setting the clusterIP field to the literal value None instead turns a ClusterIP Service headless — no virtual address gets allocated, kube-proxy leaves it alone entirely, and DNS returns the real addresses of the individual backing Pods rather than one shared address, which suits workloads whose clients need to reach a specific Pod directly instead of being load-balanced across the group. A Service can also be created with a chosen address up front, provided it falls inside the configured Service range and isn't already claimed by another Service.",
      },
      {
        title: "NodePort: reaching a Service through every Node's own address",
        body: "Setting a Service's type to NodePort layers a cluster-wide static port on top of the ClusterIP behavior it already gets for free: the control plane picks a port from a configurable range, 30000 through 32767 by default, unless a specific one is requested, and every node in the cluster then listens on that exact port and forwards whatever arrives to a healthy endpoint for the Service. That means any node's address paired with the allocated port reaches the Service from outside the cluster, even a node that isn't currently running any of the backing Pods. To reduce the odds of two people colliding when manually picking a NodePort, the overall range is split into a smaller band reserved for manual assignment and a larger band that automatic allocation draws from first.",
      },
      {
        title: 'LoadBalancer: handing external exposure to a cloud provider',
        body: "A Service of type LoadBalancer asks whatever load-balancing integration the cluster has configured — typically a cloud provider's controller — to provision an external load balancer in front of the Service; because that provisioning happens asynchronously, the resulting external address only shows up in the Service's status once the balancer is actually ready. Under the hood, Kubernetes usually sets this up the same way it would a NodePort Service first, then configures the external balancer to forward onto that allocated node port, though a dedicated field can turn that node-port allocation off for balancer implementations that route straight to Pods instead. A Service can also name a non-default load-balancer class to hand its provisioning to an alternate implementation, useful when a cluster wants something other than whatever the cloud provider offers out of the box.",
      },
      {
        title: 'EndpointSlices: how a Service knows which Pods are ready',
        body: "Behind every selector-based Service, a controller continuously watches for matching Pods and keeps a set of EndpointSlice objects current to reflect them; each EndpointSlice covers a subset of a Service's backing addresses, and Kubernetes starts a new one automatically once the existing slices fill up, by default once each holds around a hundred endpoints. EndpointSlices superseded the older, singular Endpoints API, which had no way to represent dual-stack addressing and would silently truncate its list once a Service passed a thousand backing endpoints. A Service defined without a selector skips this automatic tracking entirely, which is exactly what lets it front an external database or a workload running outside the cluster — in that case, someone has to create matching EndpointSlice objects by hand, tagged with the Service's name, to tell Kubernetes where traffic should actually go.",
      },
    ],
  },
  {
    domain: 'services',
    taskStatement: 'Ingress, the Gateway API, and CoreDNS',
    topics: [
      {
        title: 'The Ingress resource: host- and path-based routing rules',
        body: "An Ingress is an API object that maps incoming HTTP and HTTPS requests to backend Services based on rules an administrator defines, matching on the request's host header and URL path; a rule with no host at all simply applies to any inbound request that doesn't match a more specific rule. Each path in a rule declares a path type — Exact for a case-sensitive full match, Prefix for an element-by-element prefix match, or ImplementationSpecific, whose matching behavior is left to the controller handling it — and when several paths in an Ingress could match the same request, the longest matching path wins, with an Exact match beating a Prefix match at equal length. Because Ingress only understands HTTP(S) semantics, any other protocol a workload needs exposed externally still has to go through a NodePort or LoadBalancer Service instead.",
      },
      {
        title: 'Ingress controllers and IngressClass tie a rule set to an implementation',
        body: "An Ingress resource by itself does nothing — some ingress controller has to be running in the cluster to actually watch for Ingress objects and program a load balancer or proxy to match, and a cluster can run more than one controller side by side as long as each Ingress states which one it wants. That linkage runs through IngressClass: each Ingress can reference an IngressClass object by name, which in turn names the controller responsible for it and can carry additional controller-specific settings; this replaced an older, informally defined annotation that served the same purpose before IngressClass existed. Marking one IngressClass as the cluster's default, through an annotation on that IngressClass, means any Ingress created without an explicit class reference automatically picks it up, though a cluster with more than one IngressClass marked default will instead have new, class-less Ingress creation blocked until the ambiguity is resolved.",
      },
      {
        title: 'TLS termination at the Ingress and the limits of its load balancing',
        body: "Securing an Ingress means pointing its TLS configuration at a Secret holding a certificate and private key pair; the Ingress terminates TLS right there, so everything beyond that point — the hop from Ingress to Service, and onward to the backing Pods — travels as plaintext, and because the resource only supports one TLS-secured port — 443 — multiple hostnames on one Ingress get multiplexed onto that single port using server name indication. Ingress controllers ship with their own baseline load-balancing behavior — algorithm choice, backend weighting, and so on — applied uniformly across every Ingress they manage, and Ingress itself has no field for more advanced needs like sticky sessions or dynamically adjusted weights; those have to come from the underlying Service's own load balancer or from controller-specific configuration instead. Because implementations vary quite a bit in how thoroughly they support TLS and these extra behaviors, checking a chosen controller's own documentation matters before relying on any of it.",
      },
      {
        title: 'The Gateway API: role-oriented successor to Ingress',
        body: "Gateway API is a separate, CRD-based family of resources that the Kubernetes project now recommends over Ingress for exposing network services, built around four stable kinds: GatewayClass names the controller implementation, a Gateway is a concrete instance of traffic-handling infrastructure — a cloud-provisioned load balancer or a proxy server living inside the cluster — that references a GatewayClass and defines listeners, and HTTPRoute or GRPCRoute objects then attach to a Gateway's listeners to describe how matching requests get forwarded to backend Services. Its design deliberately splits configuration along organizational lines — an infrastructure provider manages the GatewayClass, a cluster operator manages Gateways, and an application developer manages the routes attached to them — and a Gateway admits routes only from its own namespace unless its allowedRoutes configuration is explicitly opened up to others. Because Gateway API has no Ingress kind of its own, moving away from Ingress means a one-time conversion of existing rules into GatewayClass, Gateway, and route objects rather than a drop-in swap, though the payoff is native support for things like header-based matching and traffic-weight splitting that Ingress could only reach through vendor-specific annotations.",
      },
      {
        title: "CoreDNS and the cluster's Service and Pod naming scheme",
        body: "CoreDNS, or whatever cluster-aware DNS add-on is deployed, watches the API server for Services and Pods and keeps a matching set of DNS records current, so a Service named my-service in namespace my-ns becomes resolvable as my-service.my-ns from anywhere in the cluster, or as the bare name my-service from within that same namespace thanks to each Pod's DNS search list. An ordinary Service resolves to its cluster address, while a headless Service, or one backed by a named port, can also be queried for an SRV record to look up a specific port by name rather than a hard-coded number. Because an unqualified query gets expanded against that per-Pod search list before it's sent, a lookup for just a bare service name inside one namespace only succeeds if a matching Service exists in that same namespace — reaching a same-named Service in a different namespace requires spelling out the namespace explicitly in the query.",
      },
      {
        title: 'Pod DNS policy: choosing where name resolution comes from',
        body: "Every Pod carries a dnsPolicy field that decides where its resolver configuration comes from, and there's a naming gotcha built in: leaving the field unset does not select the policy literally called Default — that one instead inherits whatever resolver configuration the Pod's own node happens to use — but rather ClusterFirst, the policy that actually routes ordinary lookups through the cluster's own DNS. Under ClusterFirst, any query that doesn't match the cluster's own domain suffix gets forwarded upstream by the cluster DNS server, while ClusterFirstWithHostNet exists specifically for Pods that set hostNetwork to true, since without it such a Pod would silently fall back to Default-style resolution instead of using the cluster's DNS. Setting dnsPolicy to None hands full control to the Pod's own dnsConfig block, where custom nameservers, search domains, and resolver options can be listed explicitly — a Pod configured this way must supply at least one nameserver itself, since nothing is inherited automatically.",
      },
    ],
  },
  {
    domain: 'workloads',
    taskStatement: 'Deployments, rolling updates, and rollbacks',
    topics: [
      {
        title: 'How a Deployment reconciles Pods through the ReplicaSets it owns',
        body: "A Deployment doesn't manage Pods directly — it describes a desired Pod template and replica count, and the Deployment controller drives the cluster toward that state by creating and owning ReplicaSets that in turn create the actual Pods. Applying a Deployment for the first time makes the controller create one ReplicaSet, named by appending a hash of the Pod template to the Deployment's name, and scale it up to the requested replica count. That hash is stamped onto every child ReplicaSet and Pod through a dedicated pod-template-hash label, which is how the controller distinguishes a Deployment's own ReplicaSets from one another and keeps them from fighting over the same Pods; hand-editing that value is explicitly discouraged. Because a Deployment only ever talks to the API server about ReplicaSets and leaves individual Pods to the ReplicaSet controller, day-to-day Deployment work — scaling, updating an image, checking rollout status — never requires touching a ReplicaSet object directly, and doing so anyway is generally discouraged.",
      },
      {
        title: 'What actually triggers a new rollout',
        body: "A Deployment's revision history only grows, and a rollout only starts, when its Pod template (.spec.template) changes — updating a container's image or the Pod template's own labels are the two most common triggers. Purely operational changes such as scaling the replica count up or down, by contrast, create no new revision and start no rollout; the existing ReplicaSet is simply resized in place. This split matters for two practical reasons: it lets you scale a Deployment manually, or hand scaling off to a HorizontalPodAutoscaler, without accidentally kicking off an unrelated image rollout, and it's why rolling a Deployment back to some prior point in its history reverts only the Pod template portion of its spec — a manual scaling change made in between is left untouched by that rollback.",
      },
      {
        title: 'Controlling the pace of a rolling update with maxUnavailable and maxSurge',
        body: "The default RollingUpdate strategy replaces Pods gradually instead of all at once, and two fields under .spec.strategy.rollingUpdate govern how aggressively it does so. maxUnavailable caps how many of the desired Pods can be missing at any point during the rollout, and maxSurge caps how many extra Pods beyond the desired count can exist temporarily; both default to 25%, both accept an absolute number or a percentage, and maxUnavailable rounds its percentage down while maxSurge rounds its up. Neither field can be set to zero while the other is also zero, since that would leave no room to replace anything at all. If a Deployment is scaled — manually or by an autoscaler — while a rollout is already underway, the controller doesn't dump every new replica into the newest ReplicaSet; proportional scaling spreads the additional replicas across all currently active ReplicaSets, weighted toward whichever already holds more Pods, so the rollout keeps converging on the new template rather than stalling.",
      },
      {
        title: 'Recreate vs RollingUpdate, and catching a stalled rollout',
        body: "Setting .spec.strategy.type to Recreate abandons gradual replacement entirely: every existing Pod is terminated first, and only once all of them are gone does the Deployment begin creating Pods from the new template, which guarantees old and new versions never run side by side but does mean a window with no Pods running at all. RollingUpdate, the default, avoids that gap but can still get stuck — a rollout that can't make progress because of a bad image, a failing readiness probe, or exhausted quota is caught by .spec.progressDeadlineSeconds, 600 seconds by default. Once that deadline elapses with no progress, the controller sets a Progressing condition to status False with reason ProgressDeadlineExceeded, and checking the rollout with kubectl rollout status reports failure through its exit status; deliberately pausing a Deployment suspends that deadline check entirely, so a paused rollout is never flagged as stalled no matter how long it sits.",
      },
      {
        title: 'Rolling back to an earlier revision',
        body: "Every retained Deployment revision's configuration lives inside the ReplicaSet that revision created, which is why .spec.revisionHistoryLimit — 10 by default — matters in practice: once an old ReplicaSet is garbage-collected past that limit, rolling back to the revision it represented is no longer possible, and setting the limit to 0 disables rollback for that Deployment altogether. kubectl rollout history deployment/<name> lists the revisions still on hand, each optionally tagged with a change-cause message set through the kubernetes.io/change-cause annotation, and kubectl rollout undo reverts to the immediately preceding revision or, with --to-revision, to a specific one further back. A rollback is implemented the same way any forward rollout is — the controller scales the target revision's ReplicaSet back up while scaling the current one down — and a Deployment that is currently paused cannot be rolled back until it has first been resumed.",
      },
      {
        title: 'Pausing rollouts, and why the label selector is locked after creation',
        body: "kubectl rollout pause lets several edits accumulate on a Deployment's Pod template — a new image, revised resource requests, and so on — without each one separately triggering a rollout; nothing new rolls out until kubectl rollout resume is run, at which point one new ReplicaSet picks up every accumulated change at once. Separately, .spec.selector becomes immutable the moment a Deployment is created under apps/v1, and none of the usual update paths — kubectl's patch, edit, or apply subcommands, nor a Helm upgrade — are able to change it; selecting a different set of Pods means tearing the Deployment down and standing up a fresh one in its place. The rationale traces back to the pre-apps/v1 API versions that still permitted selector updates: removing a label key was a safe, overlapping change that simply let the existing ReplicaSet keep matching what it already matched, but adding a key or changing an existing key's value was a non-overlapping change that orphaned the old ReplicaSet and its Pods and forced a brand-new ReplicaSet into existence — exactly the kind of silent orphaning apps/v1's immutability rule now rules out entirely.",
      },
    ],
  },
  {
    domain: 'workloads',
    taskStatement: 'Configuring applications with ConfigMaps and Secrets',
    topics: [
      {
        title: "ConfigMap's role and data model",
        body: "A ConfigMap holds non-confidential configuration as key-value pairs, letting an environment-specific setting — say, a database hostname that's localhost in development and a Service name once deployed — live outside the container image, so the exact same image runs unmodified everywhere. Unlike most Kubernetes objects, a ConfigMap has no spec section at all; its payload sits directly under a data field for UTF-8 string values and an optional binaryData field for base64-encoded binary values, and one ConfigMap can freely mix short property-style entries with longer, file-like blocks. The API caps a ConfigMap at 1 MiB total specifically to discourage using it as a stand-in for a real file store or database, and a ConfigMap can only configure Pods sitting in its own namespace, with the one exception being application code that reads it straight through the Kubernetes API rather than through the kubelet's normal Pod-launch machinery.",
      },
      {
        title: 'Four ways to feed ConfigMap data into a container, and how updates propagate',
        body: "A Pod can pull ConfigMap values into a container's command-line arguments, into individual environment variables through configMapKeyRef, into every key at once through envFrom's configMapRef, or as files inside a mounted read-only volume where each key becomes its own filename — or a chosen subset of them, if the volume's items list narrows things down. A less common fifth path is application code calling the Kubernetes API directly, which is also the only route that lets a workload watch for live changes or read a ConfigMap sitting in a different namespace. These paths differ sharply in how current they stay: the kubelet keeps a periodically-refreshed local cache of mounted ConfigMaps, so a volume's files do eventually catch up to an edit, with the exact lag depending on the sync period plus whichever cache strategy — watch, TTL, or direct polling — the kubelet is configured with, but a ConfigMap value consumed as an environment variable is captured once at container start and never updates without restarting the Pod, and a ConfigMap mounted through subPath doesn't receive live updates at all.",
      },
      {
        title: "Secret's built-in types and the move away from long-lived tokens",
        body: "A Secret is structurally almost identical to a ConfigMap but is meant specifically for sensitive values such as passwords, tokens, and keys, and beyond the generic, default Opaque type, Kubernetes ships several built-in types to standardize common cases: kubernetes.io/tls for a certificate-and-key pair, kubernetes.io/dockerconfigjson for registry credentials, kubernetes.io/basic-auth and kubernetes.io/ssh-auth for those authentication schemes, and kubernetes.io/service-account-token for a ServiceAccount's own credential. That last type is explicitly flagged as a legacy mechanism now: since v1.22, the recommended way to obtain a ServiceAccount credential is a short-lived, automatically-rotating token from the TokenRequest API — through kubectl create token or a projected volume — rather than a persisted, non-expiring Secret, and the docs advise reaching for the Secret-based token only when TokenRequest genuinely isn't an option. Each built-in type carries its own validation, such as a dockerconfigjson Secret needing to parse as valid JSON or a tls Secret needing both tls.crt and tls.key present, but Kubernetes never inspects whether an Opaque Secret's contents are actually sensitive.",
      },
      {
        title: 'What Kubernetes does — and does not — do to protect Secret data',
        body: "Secrets get protections a ConfigMap doesn't: the kubelet stores mounted Secret data only in tmpfs on the node rather than on durable disk, it deletes its local copy once no Pod on that node still needs the Secret, and only nodes actually running a Pod that references a given Secret ever receive a copy of it in the first place. The documentation is nonetheless blunt about the limits of that protection: Secrets are stored unencrypted in etcd by default, so anyone with API server or etcd access can read them, and because Kubernetes has no data-level access control finer than the object itself, anyone authorized to create a Pod — or anything that creates Pods on their behalf, like a Deployment — in a namespace can get at every Secret that namespace holds simply by mounting each one into a Pod they control. Granting list or watch on Secrets is equally broad, exposing the namespace's whole Secret collection rather than just the ones a workload explicitly references, and a container that a node lets run in privileged mode can likewise read every Secret used anywhere on that node, regardless of which Pod it belongs to. The documented mitigations are encryption at rest, least-privilege RBAC, and restricting exactly which containers can reach a given Secret.",
      },
      {
        title: 'Immutable ConfigMaps and Secrets',
        body: "Setting immutable: true on a ConfigMap or Secret is a one-way switch: once flipped, its data or binaryData can never be edited again, only deleted and recreated, and the immutability itself cannot be reverted either. The payoff is concentrated in clusters that mount huge numbers of these objects — the documentation frames the benefit around clusters with, at minimum, tens of thousands of individual ConfigMap-to-Pod or Secret-to-Pod mount points — because the kubelet no longer has to keep an open watch on anything marked immutable, which measurably cuts load on the API server, on top of the everyday benefit of making it impossible to accidentally push a bad edit into a config object that many running Pods already depend on. Because existing Pods keep their mount point pointing at the deleted object even after a delete-and-recreate cycle, the guidance is to recreate those Pods too rather than assume they'll automatically pick up the replacement.",
      },
      {
        title: 'Operational edges: static Pods, optional references, and image pull Secrets',
        body: "Static Pods — the manifest-file-based Pods a kubelet runs without talking to the API server — categorically cannot reference a ConfigMap or a Secret, since resolving either one requires an API call the static-Pod mechanism never makes. For ordinary Pods, marking a referenced Secret or ConfigMap as optional: true tells Kubernetes to simply proceed without it if it doesn't exist, overriding the default behavior of blocking every container in the Pod from starting until all of its non-optional Secrets and ConfigMaps are available; if a Pod instead asks for one specific key inside a Secret that exists but is missing that key, the Pod still fails to start regardless of the optional setting. A separate mechanism, imagePullSecrets, is how a Pod hands the kubelet a registry-credential Secret so it can pull a private image, and rather than repeating that reference on every Pod, it can be attached once to a ServiceAccount so that every Pod created under that ServiceAccount inherits it automatically.",
      },
    ],
  },
  {
    domain: 'workloads',
    taskStatement: 'Self-healing workload primitives: ReplicaSet, StatefulSet, DaemonSet, and Job',
    topics: [
      {
        title: 'ReplicaSet: the reconciliation loop underneath a Deployment',
        body: "A ReplicaSet's entire job is to keep a specified number of Pods matching its selector running at all times, creating new ones the moment the count falls short and deleting the extras the moment it's over, and it identifies which Pods belong to it not by name but by a matching label selector plus an ownerReferences entry on the Pod pointing back at the ReplicaSet. That acquisition mechanism cuts both ways: a bare Pod created with no owner reference of its own, if its labels happen to match a ReplicaSet's selector, gets silently adopted by that ReplicaSet and immediately becomes eligible to be scaled down if keeping it would exceed the desired count — exactly why hand-created Pods are warned against ever sharing labels with a ReplicaSet's selector. Kubernetes explicitly recommends against creating or manipulating ReplicaSets directly except in the rare case of needing custom update orchestration, since a ReplicaSet has no native concept of a rolling update at all — that orchestration is precisely what a Deployment layers on top.",
      },
      {
        title: 'StatefulSet: giving each Pod a durable identity instead of treating them as interchangeable',
        body: "Where a Deployment's Pods are meant to be indistinguishable from one another, a StatefulSet's Pods each keep a persistent identity — an ordinal index running from 0 through N-1, a stable hostname of the form <statefulset-name>-<ordinal>, and, when volumeClaimTemplates are defined, a PersistentVolumeClaim dedicated to that specific ordinal — that follows the Pod across any rescheduling instead of being handed off to whichever replacement Pod happens to come up next. The stable-hostname piece depends entirely on a Headless Service, created with clusterIP: None, that the StatefulSet's own spec.serviceName field points at; a StatefulSet never creates that Service on its own, so supplying it is left to the cluster operator. Because the PersistentVolumeClaims generated from volumeClaimTemplates are deliberately left alone when their Pod or the whole StatefulSet is deleted — a data-safety decision rather than an oversight — reclaiming or fully tearing down that storage always requires a separate, manual cleanup step afterward.",
      },
      {
        title: "StatefulSet's ordering guarantees, pod management policies, and partitioned rollouts",
        body: "Under the default OrderedReady pod management policy, a StatefulSet with N replicas creates its Pods one at a time in order from 0 to N-1, waiting for each one to become Running and Ready before starting the next, and tears them down in the exact reverse order, waiting for each successor to fully terminate before touching its predecessor. The Parallel policy discards that sequencing for both scaling and, above a configured maxUnavailable, rolling updates as well, trading strict ordering for faster convergence. A RollingUpdate on a StatefulSet always proceeds from the highest ordinal down to the lowest, and it can be scoped with .spec.updateStrategy.rollingUpdate.partition: any Pod whose ordinal sits below that partition value is left on the old template even if it happens to be deleted and recreated, which is the documented technique for staging a canary or a phased rollout across a StatefulSet's replicas. If an update ever lands a broken Pod template that never becomes Ready, the rollout stalls and, due to a known limitation, won't even attempt to revert to the previous working template until the broken Pod has been deleted by hand first.",
      },
      {
        title: 'DaemonSet: guaranteeing one Pod per matching node',
        body: "A DaemonSet exists to run exactly one copy of a Pod on every node that matches its constraints — or on every node in the cluster if no nodeSelector or node affinity is set at all — automatically adding a Pod as new nodes join and garbage-collecting the corresponding Pod once a node leaves. Under the hood, the DaemonSet controller creates each Pod carrying a node affinity term pinned to that one specific node's name, then, in the ordinary case, lets the default scheduler take over and bind it just as it would any other Pod. To let critical, node-level DaemonSet workloads such as a CNI plugin start even on a node that isn't fully healthy yet, the controller automatically attaches a standard set of tolerations covering conditions like not-ready, unreachable, disk-pressure, and unschedulable — sidestepping the chicken-and-egg problem where a node can never become Ready without its networking Pod running, and that Pod can never schedule onto a node that isn't yet Ready.",
      },
      {
        title: 'Job: running Pods to completion, in one of three parallelism patterns',
        body: "A Job is built for workloads meant to finish rather than run forever, retrying Pod creation until it has accumulated the number of successful Pod completions it was told to reach, then stopping for good; its Pod template must set restartPolicy to Never or OnFailure, since Always — the default everywhere else in Kubernetes — isn't a valid choice for something designed to terminate. Three shapes are documented for how a Job's work can be structured: a non-parallel Job that runs a single Pod to completion, leaving completions and parallelism both unset in the spec so each falls back to 1; a fixed-completion-count Job whose spec pins completions at however many successful Pods are required, optionally in Indexed completion mode so each Pod is handed a stable 0-based index it can read from a Pod annotation, a Pod label, its own hostname, or the JOB_COMPLETION_INDEX environment variable; and a work-queue Job that leaves .spec.completions unset entirely and instead relies on its Pods coordinating over an external queue to work out, on their own, when the overall task is finished.",
      },
      {
        title: 'Job failure handling, deadlines, and cleanup',
        body: "Failed Pods are retried automatically with a doubling backoff delay between attempts, topping out at a six-minute ceiling, until .spec.backoffLimit, 6 by default, is exceeded, at which point the whole Job gets marked as having failed; a .spec.podFailurePolicy can override that default counting by matching specific container exit codes or Pod conditions to one of the actions FailJob, Ignore, Count, or, for indexed Jobs, FailIndex, which is how a Job can be set up to fail fast on a genuine bug while shrugging off Pod failures caused by preemption or eviction instead. Separately, .spec.activeDeadlineSeconds bounds a Job's total wall-clock runtime no matter how many Pods it has created along the way, and it takes precedence over backoffLimit — once the deadline is hit, every running Pod is terminated and the Job is marked Failed with reason DeadlineExceeded, with no automatic restart of the Job afterward. Because finished Jobs and their Pods otherwise stick around indefinitely by default — handy for reading logs, costly for API server load at scale — .spec.ttlSecondsAfterFinished is the documented way to have a TTL controller cascade-delete a finished Job and its Pods automatically once that many seconds have elapsed.",
      },
    ],
  },
  {
    domain: 'workloads',
    taskStatement: 'Workload autoscaling and Pod scheduling admission',
    topics: [
      {
        title: 'HorizontalPodAutoscaler: what it watches and what it can target',
        body: "A HorizontalPodAutoscaler doesn't scale Pods directly; it periodically adjusts the replica count of a target workload resource — a Deployment, ReplicaSet, or StatefulSet — through that resource's scale subresource, on an interval set by --horizontal-pod-autoscaler-sync-period (15 seconds by default) rather than continuously. On each sync, the controller resolves the HPA's scaleTargetRef to find its target, uses that target's own .spec.selector to find the Pods currently backing it, and pulls metrics for those Pods from one of three aggregated APIs: metrics.k8s.io for the built-in resource metrics — CPU and memory usage on a per-Pod basis — which is usually served by the Metrics Server add-on, or, for anything outside that built-in set, the separate custom-metrics and external-metrics aggregated API groups, both served via a vendor-supplied adapter. A DaemonSet is explicitly called out as a workload the HPA cannot scale, since a DaemonSet's replica count isn't something anyone sets directly — it's dictated entirely by how many nodes currently match its own constraints.",
      },
      {
        title: 'How the HPA turns metrics into a replica count',
        body: "At its core, the algorithm is a simple ratio: desiredReplicas equals currentReplicas multiplied by currentMetricValue divided by desiredMetricValue, rounded up — so Pods averaging 200m against a 100m target double the replica count, and Pods averaging 50m against the same target halve it. Before applying that ratio, the controller filters out Pods that would skew the result: Pods carrying a deletion timestamp and any failed Pods are dropped from per-Pod resource-metric calculations, and Pods that haven't been Ready for long enough — governed by --horizontal-pod-autoscaler-initial-readiness-delay, 30 seconds by default, and --horizontal-pod-autoscaler-cpu-initialization-period, 5 minutes by default — are set aside so a container still warming up doesn't get mistaken for a CPU spike demanding more replicas. A ratio close enough to parity — inside a tolerance band that defaults to 10% and can be adjusted — is treated as noise and produces no scaling action at all, which is the main defense against the HPA thrashing the replica count back and forth over marginal metric fluctuations.",
      },
      {
        title: 'Tuning HPA behavior, and why it conflicts with a fixed spec.replicas',
        body: "The autoscaling/v2 behavior field lets scaleUp and scaleDown be configured independently, each combining one or more rate-limiting policies — a maximum number of Pods, or a maximum percentage of current replicas, per time window — with a stabilizationWindowSeconds that smooths out flapping by looking back over recent scaling recommendations and picking whichever one would keep the most replicas running, rather than reacting to only the single latest data point; selectPolicy can be set to Max, Min, or Disabled to control which of several matching policies wins, or to switch off scaling in a given direction entirely. Whatever HorizontalPodAutoscaler gets attached to a Deployment or StatefulSet, the documentation is explicit that .spec.replicas should then be removed from that workload's own manifest — reapplying a manifest that still specifies replicas silently overwrites whatever count the autoscaler had converged on, reintroducing exactly the thrashing behavior the HPA's own smoothing exists to prevent.",
      },
      {
        title: 'Resource requests and limits: two different enforcement mechanisms',
        body: "A container's resource request is what the kube-scheduler consults at placement time — it will only bind a Pod to a node if that node's remaining allocatable capacity can cover every container's requests, even when actual usage on the node is currently low, precisely so there is still headroom left for real usage spikes later on. A limit, by contrast, is enforced locally on the node by the kubelet together with the kernel's cgroups, and CPU and memory limits are enforced in fundamentally different ways: a CPU limit works as a hard ceiling that throttling enforces, so a container simply cannot use more CPU time than its limit allows no matter how much capacity is free, while a memory limit is enforced reactively through the kernel's out-of-memory killer, meaning a container can transiently exceed its memory limit right up until the instant the kernel decides to kill it. Setting a limit but leaving the request unset, with nothing else defaulting one, makes Kubernetes copy the limit value and use it as the request too.",
      },
      {
        title: 'Steering placement with node affinity, and letting Pods attract or repel each other',
        body: "nodeAffinity generalizes the older, simpler nodeSelector field: a requiredDuringSchedulingIgnoredDuringExecution rule behaves like nodeSelector but with a richer expression language — operators In, NotIn, Exists, and DoesNotExist, plus Gt and Lt for node affinity specifically — while a preferredDuringSchedulingIgnoredDuringExecution rule is a soft preference the scheduler tries to honor, weighted from 1 to 100 and folded into that node's overall placement score, but never one that blocks scheduling outright. When both nodeSelector and nodeAffinity are set on the same Pod, both have to be satisfied at once; across multiple nodeSelectorTerms the terms are OR'd together, while multiple matchExpressions inside a single term are AND'd. A parallel mechanism for affinity and anti-affinity between Pods themselves reuses that same requiredDuringScheduling / preferredDuringScheduling vocabulary to constrain placement relative to Pods already running in a topology domain — expressing rules such as co-locating with Pods labeled security=S1, or never sharing a node with another Pod carrying app=web-store — though the documentation cautions that required anti-affinity rules depend on every node consistently carrying the relevant topologyKey label, and that the feature isn't recommended for clusters larger than a few hundred nodes because of the scheduling overhead it adds.",
      },
      {
        title: 'ResourceQuota: namespace-wide caps on aggregate consumption',
        body: "A ResourceQuota constrains total resource consumption across an entire namespace — compute resources such as requests.cpu and limits.memory, storage requests either overall or scoped to a particular StorageClass, and raw object counts such as count/pods or count/secrets — and any request that would push a namespace past one of its quota's hard limits is rejected outright with a 403 Forbidden response. The consequence that trips people up in practice: once a namespace carries a ResourceQuota covering cpu or memory, every new Pod created in that namespace must specify a request or limit for that resource or the API server refuses to admit it, and a LimitRange can be layered in to supply a default automatically so users aren't required to remember to set one themselves. A quota can be narrowed further with scopeSelector to match only a subset of Pods — BestEffort versus Guaranteed/Burstable QoS class, Terminating versus NotTerminating Pods, or Pods tied to a specific PriorityClass — letting an administrator, for instance, cap how much a namespace's low-priority Pods can consume without constraining its high-priority ones at all.",
      },
    ],
  },
  {
    domain: 'storage',
    taskStatement: 'Implement storage classes and dynamic volume provisioning',
    topics: [
      {
        title: 'StorageClass objects: naming a storage profile administrators offer',
        body: "A StorageClass is how a cluster administrator describes one flavor of storage it can hand out — say, a fast tier versus a cheap one, or one with a particular backup arrangement — without Kubernetes itself caring what the distinction actually means; unlike most API kinds, it has no separate spec block, so provisioner, parameters, and reclaimPolicy sit directly on the object. provisioner is mandatory and names which volume plugin actually creates storage for the class; parameters are free-form and interpreted however that provisioner sees fit, capped at 512 entries and 256 KiB total specifically so the field can't be abused as a data store of its own; reclaimPolicy sets what happens to any PV the class dynamically produces once its claim goes away, defaulting to Delete whenever the field is left out entirely. The object's own name is what users reference when requesting that class, so administrators are free to pick names that communicate intent — low-latency, archival, and so on — to the people consuming them.",
      },
      {
        title: 'Marking a default StorageClass and how PVCs inherit it',
        body: "Tagging one StorageClass with a storageclass.kubernetes.io/is-default-class value of true means any PersistentVolumeClaim created afterward with no storageClassName field at all automatically falls back to that class; if an administrator carelessly marks more than one class default at the same time, whichever was created most recently wins, though the guidance is to keep exactly one default and treat the multi-default allowance purely as a way to migrate from one default to another without a gap. A PVC can still be created with no storageClassName even when no default exists yet in the cluster — it simply sits with that field unset rather than failing outright. Once a default StorageClass later appears, a stable feature retroactively walks every existing PVC that has no storageClassName set at all and backfills it to point at the new default, but a PVC whose storageClassName was deliberately set to the empty string is left alone by that backfill so it keeps matching only classless PersistentVolumes.",
      },
      {
        title: 'Internal versus external provisioners, and the shift to CSI',
        body: "Some provisioners ship as part of Kubernetes itself, historically identified by a kubernetes.io/ prefix, while external provisioners are separate programs, written by anyone, that speak a documented provisioning protocol and can be run and versioned completely independently of the Kubernetes release cycle. NFS is a case where Kubernetes never included a built-in provisioner at all — an external one has always been required. For cloud block storage the trend runs the other direction: the in-tree AWS EBS driver was deprecated in the 1.19 release and stripped out of the codebase entirely by 1.27, and a StorageClass targeting AWS EBS today names the out-of-tree ebs.csi.aws.com CSI driver as its provisioner instead, taking parameters like volume type, IOPS, and encryption that are specific to that driver rather than to Kubernetes. A CSIMigration mechanism keeps this transition mostly invisible to existing configuration — requests aimed at a legacy in-tree plugin name get quietly redirected to the matching CSI driver so StorageClasses, PVs, and PVCs written against the old plugin keep working without being rewritten.",
      },
      {
        title: 'Controlling when binding happens with volumeBindingMode',
        body: "volumeBindingMode decides the moment a PersistentVolume actually gets tied to, or created for, a PVC, and Immediate — the default when the field is left unset — does that the instant the PVC is created, before anyone knows which node the consuming Pod will land on; for storage that's only reachable from certain nodes or zones, that early binding can strand a Pod that can never be scheduled next to the volume it was handed. WaitForFirstConsumer instead holds off until a Pod actually references the PVC, letting the scheduler's full view of that Pod's constraints — resource requests, node selectors, affinity and anti-affinity rules, taints and tolerations — steer where the volume ends up. A documented trap: if a Pod pins itself to a node with nodeName rather than a selector while relying on WaitForFirstConsumer, the scheduler is bypassed entirely and the PVC is left pending forever, so a kubernetes.io/hostname nodeSelector is the recommended substitute. allowedTopologies can additionally pin dynamic provisioning to specific zones when that finer control is still needed, though WaitForFirstConsumer alone is usually enough on its own.",
      },
      {
        title: 'Volume expansion and StorageClass-level mount options',
        body: "Setting allowVolumeExpansion to true on a StorageClass is what lets a user later request more space for a PVC drawn from that class simply by editing the claim's requested size — the feature only ever grows a volume, never shrinks one, and support depends on the underlying plugin (CSI, plus the deprecated FlexVolume and Portworx types). A class can also carry mountOptions that get applied to every volume it dynamically provisions, but those options are never validated against the class or the PV up front — an invalid one only surfaces later as a failed mount attempt. Local volumes are a special case worth remembering: they can't be dynamically provisioned at all, yet the documentation still recommends creating a StorageClass for them anyway — one using the kubernetes.io/no-provisioner marker — purely so its volumeBindingMode can be set to WaitForFirstConsumer and delay binding decisions until a Pod's full scheduling picture is known.",
      },
    ],
  },
  {
    domain: 'storage',
    taskStatement: 'Configure volume types, access modes and reclaim policies',
    topics: [
      {
        title: 'Ephemeral versus persistent volumes, and why bare containers fall short',
        body: "A container's own writable layer disappears the moment it crashes — the kubelet simply restarts it from a clean image state — which breaks any application that needs to survive that restart or share files with another container in the same Pod; Kubernetes' volume abstraction solves both problems by attaching a directory to the Pod itself, declared under .spec.volumes and mounted into one or more containers through their own volumeMounts entries, with data in any volume type surviving a container restart even though volumes can't be nested inside one another. Ephemeral volume types are scoped to the Pod that defines them and Kubernetes tears them down the moment that Pod goes away, whereas persistent volumes are built to outlive any one Pod that happens to be using them at a given moment, which is the dividing line that determines whether a workload can safely be rescheduled without losing its data.",
      },
      {
        title: 'emptyDir: throwaway working space that lives and dies with the Pod',
        body: "An emptyDir volume starts out with nothing in it the moment its Pod is placed on a node, and every container in that Pod can read and write into it, whether they all mount it at the same path or different ones; the volume, and everything in it, is wiped for good only once the Pod itself leaves the node — a container merely crashing doesn't touch it, since the kubelet just restarts the container in place. The medium field decides what backs the volume: leaving it unset stores files on whatever the node's own storage happens to be, while setting it to Memory instead mounts a RAM-backed tmpfs that's much faster to read and write but counts anything written there against the writing container's own memory limit. sizeLimit caps how much of the node's ephemeral storage the volume can consume; without one, a memory-backed emptyDir is instead capped by the node's allocatable memory. Typical uses include scratch space for something like an external merge sort, checkpointing a long-running computation, and handing files from a content-fetching sidecar off to the container actually serving them.",
      },
      {
        title: 'hostPath: a useful escape hatch with real security tradeoffs',
        body: "A hostPath volume reaches straight into a file or directory that already exists on the node's own filesystem, which is occasionally exactly what's needed — reading node-level logs, or feeding a static Pod a config file it can't get from a ConfigMap since static Pods can't talk to the API server — but the documentation is blunt that it should be avoided whenever a real alternative like a local PersistentVolume exists. The risk is concrete: a hostPath mount can expose sensitive host material such as kubelet credentials or the container runtime's own socket, opening a path to escaping the container entirely, and a Pod template that behaves fine on one node can behave differently on another purely because the two nodes' local filesystems don't match. The type field lets an administrator require something specific already be there — Directory, File, Socket, CharDevice, BlockDevice — or ask Kubernetes to create a DirectoryOrCreate or FileOrCreate path if nothing exists yet, rather than the default empty-string behavior that skips every check before mounting. hostPath usage also isn't tracked as ephemeral storage the way emptyDir is, so nothing warns an operator when it quietly fills up a node's disk.",
      },
      {
        title: 'CSI: the out-of-tree standard that most storage now plugs into',
        body: "The Container Storage Interface gives storage vendors a documented way to ship a driver that Kubernetes can call into without that vendor's code ever being merged into the Kubernetes source tree — the same out-of-tree idea FlexVolume also tried, though FlexVolume is now deprecated in CSI's favor. That matters because the old in-tree model tied support for any new storage backend to Kubernetes' own release schedule; CSI and its predecessor both let a driver be built, released, and upgraded on its own timeline instead. A Pod can consume a csi-backed volume three different ways: through an ordinary PersistentVolumeClaim, as a generic ephemeral volume tied to the Pod's own lifetime, or, if the specific driver supports it, as a CSI ephemeral volume configured directly in the Pod spec. CSI also has stable support for raw block volumes, exposing a device to the Pod with no filesystem layered on top at all for the lowest possible overhead, provided the application inside the container is written to talk to a block device directly rather than expecting an ordinary mounted directory.",
      },
      {
        title: 'Access modes describe what a volume can do, not what it will enforce',
        body: "ReadWriteOnce lets one node mount a volume read-write, though that single node can still run several Pods that all reach the volume at once — anyone needing to guarantee just one Pod in the whole cluster gets access should reach for ReadWriteOncePod instead, stable since the 1.29 release and, as of the current docs, limited to CSI volumes. ReadOnlyMany allows many nodes to mount read-only, and ReadWriteMany allows many nodes to mount read-write; the CLI shorthand for the four modes is RWO, ROX, RWX, and RWOP. Only one access mode can actually be in effect for a given mount even when the underlying volume plugin lists support for several, and — this is the part that catches people out — declaring a volume ReadOnlyMany doesn't by itself stop a write from being attempted; access modes describe compatibility for matching a PVC to a PV rather than acting as an enforced permission, with ReadWriteOncePod being the one mode that Kubernetes actually constrains at the mounting level. Which modes are even available depends entirely on the volume plugin backing a particular PV — HostPath only ever supports ReadWriteOnce, for instance, while NFS supports all three of the non-single-Pod modes.",
      },
      {
        title: 'Reclaim policies: Retain, Delete, and the deprecated Recycle',
        body: "A PersistentVolume's reclaim policy governs what happens to it once the claim that was using it gets deleted. Retain leaves both the PV object and its underlying storage asset in place, moving the PV to a released — not yet reusable — status, since whatever data the previous claim wrote is still sitting there; getting that capacity back for a new claim is a manual sequence of deleting the old PV, cleaning up the data on the storage asset by hand, and either deleting that asset too or standing up a fresh PV that reuses it. Delete instead removes the PV object and the real storage behind it together, and it's what any dynamically provisioned volume inherits from its StorageClass unless that class was configured otherwise, making it the default reclaim behavior for provisioner-created storage even though a manually created PV without a class defaults to nothing in particular until an administrator sets one explicitly. Recycle, now deprecated in favor of just using dynamic provisioning instead, used to run a bare filesystem scrub against the volume so it could be handed to a new claim, but it only ever worked for the nfs and hostPath plugin types. A PV created by hand keeps whatever reclaim policy it was given at creation regardless of which StorageClass name it carries, so changing that behavior later always means editing or patching the live PV object directly.",
      },
    ],
  },
  {
    domain: 'storage',
    taskStatement: 'Manage persistent volumes and persistent volume claims',
    topics: [
      {
        title: 'PersistentVolume and PersistentVolumeClaim: separating supply from demand',
        body: "A PersistentVolume is a cluster-scoped piece of real storage — the storage equivalent of what a Node represents for compute capacity — that an administrator either creates directly or that gets created on demand through dynamic provisioning, and it carries whatever implementation detail is needed to actually back the storage, whether that's an NFS export, an iSCSI target, or a cloud block device. A PersistentVolumeClaim plays the role for storage that a Pod's own resource requests play for CPU and memory: it's a user's request for some amount of space at some access mode, deliberately left ignorant of how that storage gets fulfilled underneath. Because users often need storage that differs in more than raw size and access pattern — performance characteristics being the obvious example — the StorageClass resource sits above both of them, letting an administrator publish several such offerings without the PVC author needing to understand what backs any one of them.",
      },
      {
        title: 'The provisioning, binding, using, and reclaiming lifecycle',
        body: "Provisioning happens one of two ways: statically, where an administrator has already created PV objects ahead of time describing existing storage, or dynamically, where the cluster creates a brand-new PV on the fly because no existing static PV satisfies an incoming PVC, driven entirely by whichever StorageClass the PVC names — a PVC that explicitly requests the empty-string class opts out of dynamic provisioning altogether, and turning dynamic provisioning on for defaulted PVCs additionally requires the DefaultStorageClass admission controller to be enabled on the API server. Binding is handled by a control loop that watches for unbound claims and looks for a matching volume — a dynamically provisioned PV is always bound straight to the PVC that caused its creation, while a statically provisioned match is guaranteed to meet or exceed what the PVC asked for, and either way the resulting PVC-to-PV pairing is an exclusive one-to-one relationship recorded through a bidirectional ClaimRef, so a PVC with no match anywhere simply stays unbound until a suitable PV shows up. Using a bound claim just means listing it under a Pod's persistentVolumeClaim volume entry, at which point the cluster resolves it to the underlying PV and mounts that volume for the Pod. Reclaiming kicks in once a user deletes the PVC, at which point whatever reclaim policy the bound PV carries determines the storage's fate.",
      },
      {
        title: 'What actually has to match before a claim gets bound to a volume',
        body: "Binding isn't purely about whether a PV is big enough — a PVC's storageClassName has to line up exactly with a candidate PV's storageClassName, with both left empty counting as a valid match for storage carrying no class at all, while a PVC that leaves storageClassName unset entirely follows a separate code path: its fate then depends on whether that cluster's admission chain currently has the DefaultStorageClass plugin turned on, and it can even be updated automatically later once a default class becomes available (a PVC carrying a label selector, however, is explicitly excluded from ever receiving a dynamically provisioned volume). When a selector is present, its matchLabels and matchExpressions entries are all combined with AND logic, so a PV has to satisfy every listed requirement at once to qualify. To reserve one specific PV for one specific PVC rather than leaving the match to the control loop, a claim can set its volumeName field to name that PV directly, and the PV itself can list that claim under its own claimRef to shut out every other PVC from taking it first — the standard way to reattach to a Retained volume that still holds data worth keeping.",
      },
      {
        title: 'Storage Object in Use Protection: finalizers that block a premature delete',
        body: "Deleting a PVC that some running Pod is still actively using doesn't remove it right away — the object instead moves into a Terminating state and stays there, held by a kubernetes.io/pvc-protection finalizer, until no Pod references it any longer, which exists specifically to stop an in-flight delete from yanking storage out from under a workload that's still writing to it. The same protection applies symmetrically to a PV that's currently bound to a PVC, held in place by a kubernetes.io/pv-protection finalizer until the binding itself is gone. A related but separate set of finalizers — external-provisioner.volume.kubernetes.io/finalizer for CSI-backed volumes, and kubernetes.io/pv-controller for dynamically provisioned in-tree ones — keeps a PV whose reclaim policy is Delete from disappearing from the API until its backing storage asset has actually been torn down in the external system, which guarantees that outcome no matter what order an administrator happens to delete the PV and PVC objects in.",
      },
      {
        title: 'Growing a PVC after it is already in use',
        body: "Expanding a PVC's storage after the fact is enabled by default, but only works against a StorageClass that has allowVolumeExpansion explicitly set to true, and it can only make a volume bigger — there's no supported way to shrink one back down. The mechanic is simply editing the PVC's own requested storage size; the control plane resizes the volume that's already backing the claim rather than swapping in a brand-new PV to replace it. If the volume holds a filesystem, that filesystem only actually grows once some Pod is mounting the PVC in read-write mode — either the moment that Pod starts up, or, for filesystems capable of resizing while mounted, at any point while it's already running. One documented pitfall worth remembering: manually editing a PV's own recorded capacity to match a PVC's newly requested size tricks the control plane into believing nothing needs to happen, since it only compares the two objects' declared sizes rather than confirming the real storage grew, so that shortcut silently skips the actual resize.",
      },
      {
        title: 'Filesystem versus Block volume mode, and how that affects binding',
        body: "volumeMode defaults to Filesystem, under which Kubernetes formats a filesystem onto an empty block device the first time it's mounted and hands a Pod what looks like an ordinary directory; setting it to Block instead presents the volume as a bare device with no filesystem layer at all, trading the convenience of a mounted directory for the lowest possible latency, at the cost of pushing device management onto the application itself. Only a handful of plugins support that raw mode — CSI, Fibre Channel, iSCSI, and local volumes among them. Binding rules get stricter once volumeMode enters the picture: an unspecified PV happily binds to a PVC that also leaves the field unspecified or asks for Filesystem, but a PV explicitly set to Block only ever binds to a PVC that also explicitly asks for Block, and any Filesystem-to-Block or Block-to-Filesystem mismatch between the two is rejected outright rather than silently coerced one way or the other.",
      },
    ],
  },
  {
    domain: 'troubleshooting',
    taskStatement: 'Troubleshoot clusters and nodes',
    topics: [
      {
        title: 'Reading node health: conditions, the NotReady timeline, and automatic taints',
        body: "kubectl get nodes is the starting point for any cluster-level investigation — it shows whether every node an administrator expects to see is present and sitting in the Ready state — and kubectl describe node or kubectl get node -o yaml pull the fuller picture for any node that looks wrong. When a node genuinely goes down — it loses network connectivity entirely, or its kubelet crashes and never comes back — the MemoryPressure, DiskPressure, PIDPressure, and Ready conditions all flip from their normal values to Unknown, each carrying a NodeStatusUnknown reason and a message noting that the kubelet stopped posting status. Kubernetes also stamps that node with an unreachable taint carrying both NoExecute and NoSchedule effects. Because ordinary Pods tolerate the matching NoExecute taint for a bounded window — 300 seconds by default — a downed node's Pods keep showing as running for about five minutes before they're evicted, which is why a freshly-unreachable node and a long-unreachable one can look different in kubectl get pods even though the node's own status hasn't changed.",
      },
      {
        title: 'Triaging Pod-level failure states: Pending, Waiting, and stuck Terminating',
        body: "kubectl describe pod is the first move for any misbehaving Pod, since its Events section usually names the actual blocker. A Pod stuck Pending can't be scheduled at all — most often because no node has enough free CPU or memory to satisfy its requests, which calls for trimming requests, freeing capacity, or adding nodes, or because it demands a hostPort that only a limited number of nodes can offer, in which case a Service is almost always the better fix than hostPort in the first place. A Pod stuck Waiting has been scheduled but can't start on its node, overwhelmingly because its image can't be pulled — worth checking the image name for typos, whether it was actually pushed to the registry, and whether pulling it manually from that node succeeds. A Pod stuck Terminating means a deletion was issued but a finalizer is still attached, and something — typically a validating or mutating admission webhook intercepting Pod UPDATE operations — is preventing the control plane from clearing that finalizer; checking for a ValidatingWebhookConfiguration or MutatingWebhookConfiguration targeting Pods is the way to confirm that theory. Because events are namespaced, kubectl get events only returns something useful when pointed at the right namespace (or run with --all-namespaces).",
      },
      {
        title: 'Getting a shell into the trouble: exec, ephemeral containers, and copy-to-debug',
        body: "kubectl exec <pod> -c <container> -- <command> is the first tool to reach for when a container's own image already ships a shell and basic utilities. It falls short the moment a container has crashed already, or its image is deliberately minimal (a distroless image, for instance) and has no shell to exec into at all — that's exactly the gap ephemeral debug containers close: kubectl debug -it <pod> --image=<debug-image> --target=<container> attaches a throwaway container to the already-running Pod and, through --target, shares the target container's process namespace so tools like ps actually show its processes (assuming the container runtime supports that sharing). When even attaching isn't enough — say, the Pod's own configuration is what's getting in the way of debugging it — kubectl debug --copy-to=<name> stands up a full copy of the Pod with a deliberate change applied instead of touching the original: adding a new container for extra tooling (--share-processes to let it see the other containers), replacing one container's command entirely with an interactive shell, or swapping every container's image at once via --set-image=*=<image>. A --profile flag (general, baseline, restricted, netadmin, sysadmin, or the legacy default) tunes the security context the debug container gets — sysadmin grants full capabilities for genuinely privileged work — and every one of these debugging Pods or containers should be deleted once the investigation wraps up.",
      },
      {
        title: 'kubectl debug node: an SSH-free shell on a Node',
        body: "kubectl debug node/<name> -it --image=<image> deploys a Pod onto the named node and drops into an interactive shell there, which matters most when direct SSH access to nodes isn't available — it just needs permission to create Pods, pin them to a specific node, and mount host filesystems. The node's own root filesystem shows up inside that debugging Pod at /host, and on a typical Linux node the files worth checking under there include /host/var/log/kubelet.log, kube-proxy.log, containerd.log, syslog, and kern.log. The debugging Pod runs inside the node's IPC, network, and PID namespaces, but it isn't privileged by default, so some operations — chroot /host among them — fail until a privileged profile (--profile=sysadmin) or a manually-built privileged Pod is used instead. The one hard limit worth remembering: this technique doesn't work at all against a node that's genuinely unreachable, since there's no kubelet left on it to run the debugging Pod — at that point reading its status and events from the API side, through kubectl describe node or a raw kubectl get node -o yaml, is the only option left.",
      },
      {
        title: 'Capturing packets on a Node or Pod during a privileged debug session',
        body: "For connectivity or DNS problems that need an actual packet trace, kubectl debug --profile=sysadmin node/<name> -it --image=ubuntu:latest opens a privileged shell on the node where a tool like tcpdump can be installed on the spot (apt-get install tcpdump) and pointed at the node's interfaces (tcpdump -i any -n). The same flag works against a specific workload instead of the whole node — running kubectl debug with --profile=sysadmin against pod/<name> in the right namespace, with an interactive Ubuntu-based debug image, captures traffic from that one Pod's own network namespace instead, which is the better choice when the suspect traffic is scoped to a single application rather than everything passing through a node. As with any kubectl debug invocation, the created debugging Pod (typically named something like node-debugger-<node>-<suffix>) has to be deleted by hand once the capture is done, or it lingers around in a Completed state.",
      },
      {
        title: 'What actually breaks when a cluster component fails, and how HA mitigates it',
        body: "Losing the API server — a crash or a VM shutdown — stops any new create, update, or scale operation cold, but Pods and Services that were already running keep working unless they specifically need to call the API themselves. Losing the storage behind the API server is worse: the apiserver can't come back healthy again until that state is manually recovered, though kubelets keep running whatever Pods they'd already been assigned and keep proxying traffic in the meantime. Because the scheduler and controller-manager are typically co-located with the apiserver and carry no persistent state of their own, their failure modes track the apiserver's closely. A single node shutting down just drops the Pods scheduled to it; a network partition splits the cluster's own view of itself, with each side believing the other is the one that's down; a crashing kubelet stops new Pods from starting on its node and gets that node marked unhealthy so a controller reschedules the affected workload elsewhere. The documented mitigations map straight onto these failure modes: automatic VM restart at the infrastructure layer, durable and replicated storage under the apiserver and etcd, a highly-available control-plane layout able to tolerate losing one or more nodes or components at once, periodic snapshots of that backing storage, and building workloads around replication — a Deployment or ReplicaSet, a Service in front of it, containers written to tolerate an unexpected restart — so that no single node or kubelet failure takes an application down.",
      },
    ],
  },
  {
    domain: 'troubleshooting',
    taskStatement: 'Troubleshoot cluster components: control plane and kubelet',
    topics: [
      {
        title: 'Where control-plane and node component logs actually live',
        body: "On a control-plane node provisioned by kubeadm, the kube-apiserver, kube-scheduler, and kube-controller-manager all run as static Pods, so journalctl won't show them directly — reading their output means going through the same Pod-log mechanism any other container uses, while an installation that runs those components as plain host processes instead keeps their logs as flat files at paths like /var/log/kube-apiserver.log. Worker nodes are more straightforward: /var/log/kubelet.log and /var/log/kube-proxy.log (or their journalctl -u kubelet / journalctl -u kube-proxy equivalents on a systemd host) are the first places to check when a node itself is misbehaving. The kubelet and the container runtime are the two components that never run inside a container at all, so they're the ones that genuinely do write straight to the systemd journal (or to flat files via a helper like kube-log-runner if no journal exists) — every other Kubernetes component's logs are, ultimately, container logs. etcd, almost always deployed as a static Pod too, follows that same Pod-log path, with etcd's own documentation the right place for interpreting what's actually in those logs.",
      },
      {
        title: 'Confirming kubectl itself can reach the cluster before blaming the cluster',
        body: "kubectl version prints both the client and server version in one shot; getting a connection-timeout error back in place of a server version means the problem is reachability, not cluster health, and is worth ruling out before assuming anything is actually broken on the cluster side. The kubeconfig file kubectl reads — by default ~/.kube/config, or a control-plane node's own /etc/kubernetes/admin.conf when nothing else has been set up yet — has to exist and be valid, and either the $KUBECONFIG environment variable or the --kubeconfig flag can silently point kubectl at the wrong file if a different one was expected. Because one kubeconfig can define several clusters and contexts at once, kubectl config get-contexts followed by kubectl config use-context <name> is how to confirm which one is actually active, rather than assuming a failing command is a cluster problem when it's really a stale context. A dropped VPN session produces symptoms that are indistinguishable from a genuinely unreachable API server, so reconnecting it is worth trying early, alongside a basic reachability check — pinging the API server's host, or the load balancer sitting in front of several control-plane nodes if one exists.",
      },
      {
        title: 'Diagnosing TLS and authentication failures against the API server',
        body: "An error along the lines of 'certificate signed by unknown authority' on an otherwise healthy-looking cluster almost always means a kubeconfig pointing at the wrong CA, or holding a client certificate that's simply expired. Both the client-certificate-data and certificate-authority-data fields inside a kubeconfig are base64-encoded, so decoding one with base64 -d and piping the result into openssl x509 -noout -dates is the quickest way to read off its notBefore/notAfter window and confirm whether expiry is actually the cause. A stale $KUBECONFIG environment variable left pointing at an old file is a common self-inflicted version of this problem; unsetting it, or explicitly exporting it to the control plane's /etc/kubernetes/admin.conf, forces kubectl back onto a known-good file, and as a last resort overwriting ~/.kube/config with a fresh copy of that admin.conf clears out a corrupted local configuration entirely. Authorization failures are a distinct category worth not conflating with these: a credential that authenticates cleanly but still gets a request rejected means RBAC doesn't grant that identity the verb or resource being requested, not that the certificate itself is bad.",
      },
      {
        title: 'kubeadm-specific control-plane failure patterns',
        body: "kubeadm init hanging indefinitely right after printing that it's waiting for the control plane to become ready traces to one of three usual causes: no network connectivity between the machines involved, a mismatch between the container runtime's configured cgroup driver and the kubelet's own, or the control-plane containers themselves crash-looping — checked by inspecting each one's logs through whatever tooling matches the runtime in use (crictl for a CRI-compliant runtime). kubeadm reset can get stuck removing Kubernetes-managed containers if the container runtime itself has stopped responding; restarting the runtime and re-running the reset is the documented fix, with crictl again useful for inspecting runtime state directly. Missing preflight utilities such as ebtables or ethtool surface only as warnings during kubeadm init rather than hard failures, but installing them anyway is worth doing since networking add-ons frequently depend on them. A version-skew gotcha worth knowing: a kubeadm v1.18-or-newer node joining a cluster originally bootstrapped by kubeadm v1.17 can fail because the newer kubeadm needs RBAC permission to read Node objects — added specifically to stop a node from joining under a name that's already taken — that the older cluster's bootstrap-token group was never granted; running kubeadm init phase bootstrap-token from a current control-plane node, or applying the missing ClusterRole and ClusterRoleBinding directly, resolves it.",
      },
      {
        title: 'Kubelet client certificate rotation failing on a node',
        body: "kubeadm configures every kubelet to rotate its own client certificate automatically through a symlink at /var/lib/kubelet/pki/kubelet-client-current.pem, referenced from /etc/kubernetes/kubelet.conf; when that rotation breaks, the telltale symptom shows up as x509 certificate-expiry errors in the kube-apiserver's own logs rather than anywhere obvious on the affected node itself. Recovery starts with backing up and removing the broken kubelet.conf and kubelet-client* files on that node, then generating a replacement kubeconfig from a control-plane node that still holds the cluster's CA key, using something like kubeadm kubeconfig user --org system:nodes --client-name system:node:<node-name> and adjusting the cluster name and API server address inside the result as needed. After copying that file back to /etc/kubernetes/kubelet.conf and restarting the kubelet, the client-certificate-current.pem symlink should reappear on its own; a final edit points kubelet.conf's client-certificate and client-key fields at that symlink rather than at embedded certificate data, followed by one more kubelet restart, after which the node should return to Ready.",
      },
      {
        title: 'CoreDNS as a signal of control-plane and add-on health',
        body: "CoreDNS sitting in Pending immediately after kubeadm init is expected rather than a symptom of anything wrong: kubeadm is deliberately network-provider-agnostic and installs no Pod network add-on on its own, so CoreDNS simply can't schedule until an administrator applies one — the same networking dependency that shows up elsewhere as 'no CNI plugin, no working cluster DNS.' CoreDNS actively crash-looping once a network add-on is already in place points at something different: on nodes running SELinux together with an older container runtime, CoreDNS's container can fail to start until the runtime is upgraded, SELinux is turned off (a real security tradeoff), or the coredns Deployment has allowPrivilegeEscalation flipped to true as a workaround. A second, unrelated cause of the same CrashLoopBackOff symptom is CoreDNS detecting a genuine DNS forwarding loop and exiting on purpose rather than looping forever, for which documented workarounds exist so Kubernetes stops endlessly restarting it every time the loop check fires. Because any Pod sitting in RunContainerError, CrashLoopBackOff, or Error state immediately after a bare kubeadm init — before any add-on has even been applied — is not expected at all, seeing that pattern that early is a stronger signal than CoreDNS's ordinary Pending phase and is worth treating as a potential kubeadm issue rather than routine startup noise.",
      },
    ],
  },
  {
    domain: 'troubleshooting',
    taskStatement: 'Monitor cluster and application resource usage',
    topics: [
      {
        title: 'The resource metrics pipeline: cAdvisor, the kubelet, and metrics-server',
        body: "Resource metrics flow through a short, fixed chain: cAdvisor, bundled inside the kubelet, collects and aggregates per-container CPU and memory figures straight from the container runtime; the kubelet then exposes a per-node summary of that data through its own /metrics/resource endpoint (or the older /stats/summary endpoint, for older metrics-server versions); metrics-server, a separate cluster add-on rather than something built into Kubernetes itself, polls every kubelet's endpoint in turn and aggregates the results cluster-wide. metrics-server is what actually implements and serves the Metrics API under metrics.k8s.io, and nothing downstream of it works — kubectl top included — until it, or an equivalent implementation, has actually been deployed into the cluster. The Metrics API's consumers sit at the top of that chain: the kube-apiserver serves the API itself to whoever queries it, and both the HorizontalPodAutoscaler and VerticalPodAutoscaler read from it to make their scaling decisions, alongside kubectl top for a quick human-readable view. A runtime that isolates workloads through something other than ordinary Linux cgroups has to separately implement the CRI Container Metrics interface, or the kubelet has no way to surface its metrics into this pipeline at all.",
      },
      {
        title: 'What CPU and memory actually mean in the Metrics API',
        body: "CPU usage is reported as an average number of cpu units — one cpu equating to one vCPU or core on a cloud provider, or one hyperthread on bare-metal Intel hardware — calculated from how fast a cumulative CPU counter the kernel maintains has been climbing across whatever window the API's window field reports, rather than an instantaneous snapshot of activity. Memory usage is reported as the container's working set in bytes at the exact instant it was sampled: the portion of in-use memory that can't be reclaimed under pressure, though the precise calculation is host-OS-dependent, leans on heuristics, and typically folds in some reclaimable file-backed cache alongside genuinely anonymous memory. Both figures live behind a real, directly-queryable API object (NodeMetrics or PodMetrics under metrics.k8s.io/v1beta1), reachable with something like kubectl get --raw against the appropriate path — handy for confirming the pipeline independently of kubectl top or for scripting against it. The Metrics API is deliberately scoped to just these two signals for autoscaling purposes; anything beyond CPU and memory needs a second, separate pipeline built on the custom-metrics or external-metrics API groups, served through a vendor-specific adapter rather than metrics-server.",
      },
      {
        title: 'kubectl top as the everyday resource-usage check',
        body: "kubectl top nodes and kubectl top pods read straight from the Metrics API that metrics-server populates, giving an immediate view of CPU and memory consumption without hand-querying the raw API; when metrics-server isn't deployed or isn't healthy, both commands fail outright instead of returning stale or zeroed-out numbers. Because the figures behind kubectl top are the exact ones an attached HorizontalPodAutoscaler evaluates on its own sync interval, a workload's kubectl top output is a reasonable stand-in for what that HPA currently sees, which makes it a natural first check whenever an HPA doesn't seem to be scaling the way it's expected to. kubectl describe node surfaces a related but genuinely different view — allocated resources, based on what Pods have requested and limited — which answers a different question (how much capacity is already committed) than kubectl top's answer (how much is actually being used right now); mixing the two up is a common source of confusion when a node looks saturated on one command but shows headroom on the other.",
      },
      {
        title: 'Diagnosing a broken or missing metrics pipeline',
        body: "The most common cause behind kubectl top or an HPA reporting that metrics are unavailable or unknown is simply that metrics-server was never deployed, has crashed, or can't reach the kubelets it's supposed to poll — confirming its Deployment is Running and its own logs are clean is the first troubleshooting step, well ahead of assuming anything is wrong with the workload being measured. Because metrics-server calls each kubelet directly over HTTP, a network policy, firewall rule, or certificate-trust problem that blocks that specific path breaks the pipeline in a way that looks, from kubectl top's point of view, identical to metrics-server being entirely absent. If per-Pod metrics are missing for just one workload while every other workload reports fine, checking whether that workload's nodes run a container runtime lacking CRI Container Metrics support is worth ruling out, since that's a narrower and easier-to-miss failure mode than metrics-server being down cluster-wide. Because the whole pipeline still carries a beta feature-state designation, an occasional transient gap in reported numbers is expected rather than alarming, though a sustained failure to report anything at all should still be chased down through the steps above rather than dismissed.",
      },
      {
        title: 'Beyond CPU and memory: monitoring node health signals directly',
        body: "Resource metrics answer 'how much CPU and memory is currently being used,' but a node can be unhealthy in ways that number never reveals — disk pressure, kernel-level faults, a corrupted filesystem, an unresponsive container runtime — which is exactly why cluster troubleshooting guidance also points at Node Problem Detector, a daemon built specifically to watch for these lower-level node-health symptoms and surface them as ordinary node conditions and events rather than as CPU or memory figures. kubectl describe node's Conditions block is where any such detected problem eventually becomes visible to an operator, the very same block that MemoryPressure, DiskPressure, and PIDPressure already appear in from the kubelet's own reporting, so a monitoring workflow doesn't need two separate places to look for kubelet-native conditions versus problem-detector-native ones. Taken together, resource-usage monitoring — metrics-server and kubectl top feeding HPA and VPA decisions — and node-health monitoring — conditions, events, and Node Problem Detector — cover two distinct failure classes: one says a workload needs more or fewer replicas, the other says a node itself is degrading in a way that no amount of rescheduling fixes until the underlying host problem is addressed.",
      },
    ],
  },
  {
    domain: 'troubleshooting',
    taskStatement: 'Manage and evaluate container output streams',
    topics: [
      {
        title: 'stdout/stderr as the baseline, and kubectl logs as the way to read it',
        body: "The simplest and most universal way a container reports what it's doing is by writing plain text to its own stdout and stderr — the two standard streams every process already has — and every container runtime Kubernetes supports captures both of them without any extra configuration required from the application itself. kubectl logs <pod> [-c <container>] retrieves whatever that container has written so far; the -c flag only matters once a Pod has more than one container, since a single-container Pod's logs are unambiguous without it. kubectl logs <pod> --previous instead retrieves output from the container's last run rather than its current one, which is the standard way to see why a container crashed right before it restarted, since the current instance's own logs won't contain anything written before it started. Because this whole mechanism depends on nothing more than a runtime capturing two ordinary streams, it works identically no matter which language or logging library an application uses — the tradeoff is that Kubernetes has no opinion at all about log format or structure, leaving that entirely to whatever reads the output downstream.",
      },
      {
        title: 'Log rotation, and why kubectl logs can’t always show everything',
        body: "The kubelet, not the container runtime or the application itself, owns rotating each container's logs and organizing the on-disk directory structure they live in, coordinating with the runtime over CRI so the actual log-writing follows a format every runtime implements the same way. Two kubelet settings bound that behavior directly: containerLogMaxSize (10Mi by default) caps how large a single log file gets before it's rotated, and containerLogMaxFiles (5 by default) caps how many rotated files are kept per container; two further settings, containerLogMaxWorkers and containerLogMonitorInterval, tune how many rotations can run concurrently and how often the kubelet checks whether one is needed, which matters on nodes generating a heavy volume of log output. The practical consequence: kubectl logs only ever reads the current, unrotated log file, so a container that's written well past the containerLogMaxSize threshold will only show its most recent slice of output through kubectl logs — older, already-rotated content isn't retrievable that way at all, even though it briefly existed on the node's disk. This is exactly why log data that needs to survive longer than a node's local rotation window, or needs querying across many Pods at once, has to be shipped somewhere else entirely — kubectl logs is a live tail, not an archive.",
      },
      {
        title: 'How nodes actually store logs: journald, flat files, and static Pods',
        body: "On a Linux node running systemd, the kubelet and the container runtime write their own operational logs to journald by default, read back with something like journalctl -u kubelet; on a node without systemd, both instead write plain .log files under /var/log, and a helper tool called kube-log-runner can redirect the kubelet's own output elsewhere if the default location isn't wanted. Container logs are a separate matter from those component logs entirely: by default the kubelet directs the runtime to write every container's logs under /var/log/pods, organized into per-namespace, per-Pod directories, a path that can be repointed through the kubelet's podLogsDir setting — though the guidance is explicit that changing it should be done cautiously, since a long history of tooling assumes that default location, and the new location still has to live on the very same disk /var itself is on, or the kubelet won't track how much space it's using at all. Control-plane components that run as static Pods — the API server, scheduler, controller-manager, and usually etcd — write their logs the same Pod-log way rather than to the systemd journal, worth remembering when a control-plane node's journalctl output looks unexpectedly quiet about one of them. Because Kubernetes performs no log rotation at all for anything written outside this container-log mechanism — a static Pod's logs shared onto a node-level directory through a volume, say — whatever handles rotation for that directory elsewhere on the host still has to be relied on or configured separately.",
      },
      {
        title: 'Cluster-level logging patterns: node agent, sidecar, and direct push',
        body: "Kubernetes deliberately ships no built-in destination for logs to live in permanently or be queried centrally — cluster-level logging always means layering one of a small number of documented patterns on top of the basic stdout/stderr mechanism. Running a logging agent as a DaemonSet, one per node, is the most common approach: it needs no changes to any application, reads whatever containers already write to stdout and stderr from the node's own log directory, and forwards it to a backend, at the cost of losing any distinction between differently-formatted log streams unless the agent is specifically told how to tell them apart. A streaming sidecar container instead has each sidecar tail one particular file, socket, or journald source inside the Pod and re-emit it to its own stdout, which keeps kubectl logs usable per stream — since the kubelet and any node agent still see ordinary container output — and suits an application that writes several differently-formatted files rather than one unified stream, though it roughly doubles the node storage the Pod's logging needs. Running a fully separate logging agent inside a sidecar, rather than a per-node DaemonSet, offers the most control over formatting and destination but costs the most resources and, notably, makes those logs invisible to kubectl logs entirely, since they never pass through the kubelet's own log path at all.",
      },
      {
        title: 'Splitting stdout from stderr on the wire (alpha)',
        body: "As an alpha capability gated behind the PodLogsQuerySplitStreams feature gate, off by default, the kubelet can expose a container's stdout and stderr as two independently queryable log streams instead of one interleaved one. With the gate enabled, a client holding read access to a Pod's log subresource can request just one stream by adding a stream query parameter set to Stdout or Stderr against the Pod's log API path, rather than getting kubectl logs' usual all-in-one output. This suits an application that deliberately separates informational output from error output and wants a consumer — a log-shipping pipeline, or a person chasing one specific failure — to pull just the error stream without filtering it back out of a merged log by hand. Being alpha, it's not something to rely on for an exam-relevant workflow yet, but it points at a direction: today, kubectl logs and the underlying log API treat a container's output as a single combined stream, and this feature is what would eventually let those streams be evaluated separately at the API level instead of only through client-side filtering.",
      },
      {
        title: 'The log-directory structure kubectl logs and rotation both rely on',
        body: "Every container's captured stdout and stderr ultimately lands in a predictable per-namespace, per-Pod, per-container path under /var/log/pods on the node, and that shared structure is exactly what both kubectl logs (through the kubelet) and the kubelet's own rotation logic are reading from and rotating in place — understanding it explains why rotation settings and kubectl logs behavior are two sides of one mechanism rather than unrelated features. Because the CRI logging format standardizes how a runtime hands log lines to the kubelet regardless of which runtime is running, this entire path — directory structure, rotation, and kubectl logs — behaves identically whether a node runs containerd, CRI-O, or any other CRI-compliant runtime. A Pod evicted from a node takes its containers' logs with it in the sense that actually matters operationally: once eviction happens, that log data is gone from the node's local disk along with everything else about the terminated containers, which is one more reason cluster-level log shipping exists — anything not already forwarded off-node before an eviction is simply unrecoverable afterward.",
      },
    ],
  },
  {
    domain: 'troubleshooting',
    taskStatement: 'Troubleshoot services and networking',
    topics: [
      {
        title: 'Step zero: does the Service even exist?',
        body: "It sounds trivial, but the single most common reason a Service 'doesn't work' is that it was never actually created — kubectl get svc <name> returning a not-found error is the very first thing worth checking before looking anywhere else. Trying to reach a nonexistent Service by name from inside a Pod produces a DNS failure that looks identical to a genuinely broken DNS setup, along the lines of 'Name or service not known,' which is exactly why confirming the Service object directly, rather than trusting a symptom that could have several unrelated causes, comes first in any real walkthrough. Once its existence is confirmed, checking early for any NetworkPolicy objects whose ingress rules target the Service's backing Pods is worth doing too, since a perfectly correct Service can still look broken if a policy is silently dropping the very connections being tested against it.",
      },
      {
        title: 'Working through DNS resolution for a Service, one suffix at a time',
        body: "Starting from any other Pod sitting in that same namespace, a lookup of just the Service's bare name should resolve; if that fails but the namespace-qualified form (name.namespace) works, the caller and the Service live in different namespaces and the app needs that qualified name, or needs moving into the same namespace as the Service. If only the fully-qualified form (name.namespace.svc.cluster.local) resolves, the problem sits somewhere upstream of the shorter forms rather than with the Service itself. A Pod's own resolver configuration is what makes the short forms work at all: its nameserver has to point at the cluster's DNS Service address (set on the kubelet through --cluster-dns), its search list has to include the namespace-local, all-namespaces, and cluster-wide suffixes in order (set through --cluster-domain), and its ndots option has to be high enough — 5 by default — that an unqualified name is actually tried against those search suffixes rather than treated as already complete. If even the cluster's own built-in kubernetes Service fails to resolve from inside a Pod, the problem isn't specific to whatever Service is under investigation at all — it's the cluster's DNS Service, and by extension kube-proxy, that needs debugging instead.",
      },
      {
        title: "Checking that a Service's own spec actually matches its Pods",
        body: "Reading a Service back with kubectl get service <name> -o yaml (or -o json) is the way to double-check details that are easy to get wrong writing one by hand: whether spec.ports[].port is actually the port callers are trying to reach, whether targetPort correctly names or numbers the port the Pods listen on, and whether a targetPort meant to be numeric got accidentally quoted as a string, or a named port doesn't match any port name the Pods actually expose. A mismatched protocol — TCP against a Pod actually listening on UDP, or vice versa — is another easy-to-miss cause of a Service that looks entirely correctly defined yet still refuses every connection. Getting these fields right matters more than it might seem, because none of the deeper checks further down the chain — EndpointSlices, kube-proxy's own dataplane — will ever reveal a simple port or protocol typo; a Service can be wired up to exactly the right Pods and still forward nothing usable if the port mapping itself is wrong.",
      },
      {
        title: 'Proving the Service actually selected the Pods it should have',
        body: "kubectl get endpointslices -l kubernetes.io/service-name=<service> shows the addresses the EndpointSlice controller has actually resolved for that Service's selector, and the count and addresses shown there should line up with the Pods expected to be backing it. An EndpointSlice with no addresses at all almost always traces back to a selector mismatch — the Service's spec.selector labels simply don't match the labels on any running Pod, a mistake that's easy to introduce by hand, the classic example being a Service selecting app=hostnames while the workload's own Pods carry run=hostnames instead, a labeling convention an older kubectl run used to apply automatically. Listing Pods with the exact selector the Service itself uses (kubectl get pods --selector=<key>=<value>,...) is the direct way to confirm what a human reading raw YAML might miss, since it shows precisely which Pods Kubernetes considers a match rather than which Pods a person assumes should match. Once endpoints are present but something still seems off, checking those same Pods' age and restart counts is worth a look too, since a crash-looping Pod behind an otherwise-correctly-wired Service still shows up as a valid endpoint yet produces inconsistent responses.",
      },
      {
        title: 'Verifying kube-proxy is actually programming the dataplane',
        body: "Confirming kube-proxy is running on a node (a plain process check for it) and that its logs show it setting endpoints for the Service in question, rather than errors reaching the API server, is the next layer down once DNS, the Service's own spec, and its EndpointSlices have all already checked out. kube-proxy's logs reveal which backend mode it's using — most commonly iptables or IPVS — and each mode has its own way to confirm the rules are actually in place: an iptables rule dump filtered for the Service's name should show one entry in a KUBE-SERVICES chain pointing at a per-Service KUBE-SVC chain, which in turn fans out to one KUBE-SEP chain per backing Pod, while an IPVS listing should instead show a virtual server for the Service's address with one weighted real server per Pod endpoint. If those rules exist but a direct connection to the Service's ClusterIP from a node still fails, restarting kube-proxy with a higher log verbosity and re-reading its output is the documented next step, since that usually surfaces whatever specific condition it's stuck on.",
      },
      {
        title: "The hairpin edge case: a Pod that can't reach itself through its own Service",
        body: "A Pod occasionally needs to reach itself through the same Service VIP that fronts its whole Deployment, and when that one specific case fails while everything else about the Service works fine, the cause is almost always hairpin traffic not being enabled on the node — a setting that lets a Service's own backend loop traffic back to itself through the proxy instead of only ever forwarding outward to other endpoints. This shows up specifically with kube-proxy running in iptables mode alongside bridge-based Pod networking, and the kubelet's own hairpin-mode flag has to be set to one of two specific values for it to work at all; both the flag's configured value and its actual effective value — visible in the kubelet's own logs, since compatibility fallbacks can make the two differ — are worth checking. Confirming it's genuinely active on the node depends on which mode is effective: one mode is confirmed by checking a hairpin flag on the node's virtual network interfaces, the other by checking that the relevant bridge interface reports promiscuous mode when inspected. This is a narrow, specific failure worth checking only after DNS, the Service definition, EndpointSlices, and kube-proxy's general operation have already been ruled out, since it explains exactly one symptom — a Pod failing to reach its own Service address — rather than Services being broken in general.",
      },
    ],
  },
];
