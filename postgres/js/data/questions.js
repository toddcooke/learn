// js/data/questions.js
// Quiz questions for the Architecture & Data Types domain (15% exam weight).
// Grounded in the official PostgreSQL 18 documentation cached under
// .cache/aws-docs/ during authoring (see scripts/fetch-doc.mjs). Every
// answer key was re-checked against the exact cached passage it is based
// on. Written in the author's own words, not copied verbatim from the docs.

export const QUESTIONS = [
  // --- Server architecture & process model (architecture-001..005) ---
  {
    id: 'architecture-001',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "In PostgreSQL's client/server process model, what happens when a new client connects to the server?",
    options: [
      'The client connects directly to an existing backend process that is already serving another client.',
      'The postmaster spawns a new thread within its own process to handle the connection.',
      'The postmaster forks a new backend process dedicated to that one connection.',
      'The postmaster handles every subsequent query for that client itself, without creating any new process.',
    ],
    correctIndexes: [2],
    explanation: 'The postmaster listens for connections and forks a dedicated backend process per client; from then on, that backend handles the session directly while the postmaster returns to listening for the next connection.',
  },
  {
    id: 'architecture-002',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "According to PostgreSQL's write-ahead logging design, why does committing a transaction typically require flushing only the WAL rather than every changed data page?",
    options: [
      'Because flushing the sequential WAL is cheaper than flushing data pages, and any gap can be redone from WAL.',
      'Because data pages are never written to disk until the database is shut down cleanly.',
      'Because WAL records are stored only in memory and are never themselves flushed to permanent storage.',
      'Because PostgreSQL disables all disk writes during a transaction and batches them once per checkpoint regardless of WAL.',
    ],
    correctIndexes: [0],
    explanation: 'WAL guarantees durability by requiring change records to be flushed before the corresponding data page reaches disk, so only the sequentially-written WAL needs syncing at commit time; a crash can be recovered by replaying WAL records forward.',
  },
  {
    id: 'architecture-003',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'For a dedicated PostgreSQL server with at least 1GB of RAM, what does the documentation suggest as a reasonable starting point for shared_buffers, and why does going far beyond it tend not to help?',
    options: [
      'Roughly 90% of system RAM, since shared_buffers is the only cache PostgreSQL uses and the OS cache plays no role.',
      'A fixed 8MB regardless of total system memory, since shared_buffers cannot be resized without recompiling the server.',
      'Roughly 25% of system RAM, because values above that cause PostgreSQL to disable checkpointing entirely.',
      'Roughly 25% of RAM, since the OS filesystem cache absorbs the rest and pushing past 40% rarely helps.',
    ],
    correctIndexes: [3],
    explanation: "shared_buffers is commonly started around a quarter of system RAM because the operating system's own page cache already absorbs a large share of caching duties, so pushing shared_buffers much higher usually yields diminishing returns.",
  },
  {
    id: 'architecture-004',
    domain: 'architecture',
    questionType: 'multiple-response',
    question: 'Which of the following statements about the work_mem configuration parameter are accurate?',
    options: [
      'work_mem sets a hard cap on the total memory a single client connection may use for the lifetime of that connection.',
      'work_mem caps the memory a single sort or hash operation may use before spilling to temporary disk files.',
      'work_mem is used only by ORDER BY sorts and has no effect on hash joins or hash-based aggregation.',
      'Because a query can run multiple sort or hash operations at once, and many sessions can run concurrently, total memory used across the server can be many times the configured work_mem value.',
      'Raising work_mem guarantees that PostgreSQL will never write temporary sort or hash data to disk, regardless of query complexity.',
    ],
    correctIndexes: [1, 3],
    explanation: 'work_mem bounds each individual sort or hash step rather than a session as a whole, and since a query can run several such operations at once while many sessions run concurrently, aggregate memory use can end up a large multiple of the setting.',
  },
  {
    id: 'architecture-005',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'Why might a PostgreSQL server reserve a small number of connection slots specifically for superusers via superuser_reserved_connections?',
    options: [
      'Because superuser connections consume no backend process resources, unlike ordinary connections.',
      'So an administrator can still log in to fix the server after ordinary slots run out.',
      'Because reserved connections are automatically used to run autovacuum workers.',
      'Because max_connections only limits non-superuser sessions, so superusers are never subject to any connection cap.',
    ],
    correctIndexes: [1],
    explanation: 'Reserved superuser connection slots exist so that, even when ordinary connection slots are exhausted, an administrator can still log in to diagnose or resolve whatever is consuming the remaining capacity.',
  },

  // --- Physical storage & TOAST internals (architecture-006..010) ---
  {
    id: 'architecture-006',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "Why can't you safely assume that a table's on-disk filename always matches its OID in pg_class?",
    options: [
      "Because PostgreSQL renames every table's file to a random name each time autovacuum runs.",
      'Because TRUNCATE, REINDEX, and CLUSTER can assign the table a new filenode while its OID stays fixed.',
      "Because filenodes are recalculated from the table's OID using a hash function that changes each server restart.",
      'Because only temporary tables use filenode-based file names; permanent tables are named directly after their OID.',
    ],
    correctIndexes: [1],
    explanation: "A table's filenode is a separate identifier from its OID, and commands like TRUNCATE, REINDEX, and CLUSTER can assign a fresh filenode to the underlying file while the table keeps its original OID in the catalog.",
  },
  {
    id: 'architecture-007',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "What happens once a table's main data file grows past the default 1 GB segment size?",
    options: [
      'PostgreSQL automatically moves the table into a new tablespace to make room for further growth.',
      'The table is automatically split into child tables, each capped at 1 GB.',
      'PostgreSQL compresses the entire file in place so it stays under the 1 GB limit.',
      'PostgreSQL rolls over to a new segment file, appending .1, .2, and so on after the filenode.',
    ],
    correctIndexes: [3],
    explanation: 'Table and index files are capped at roughly a gigabyte per segment mainly to sidestep old filesystem size limits; once that ceiling is crossed, PostgreSQL keeps writing into additional numbered segment files under the same filenode.',
  },
  {
    id: 'architecture-008',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "Within an 8kB PostgreSQL page, why can an index safely store a reference (CTID) to a row's item identifier rather than to the row's raw byte offset?",
    options: [
      'Because an item identifier keeps its slot until freed, even as the row data moves during compaction.',
      'Because CTIDs are automatically rewritten by a background process every time a row moves.',
      'Because row data is never moved once written; only entire pages are ever relocated.',
      'Because item identifiers are stored in a separate file from the page they describe, so they are unaffected by changes to that page.',
    ],
    correctIndexes: [0],
    explanation: "Because item identifiers occupy a fixed slot in the page's pointer array until freed, an index entry that references a slot number keeps working correctly even after the row's bytes get relocated during page compaction.",
  },
  {
    id: 'architecture-009',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "What triggers PostgreSQL's TOAST mechanism to start compressing or relocating a row's oversized column values?",
    options: [
      'Any single column value exceeding roughly 2kB by itself, regardless of the total row width.',
      'The table crossing 1GB in total size, which triggers TOAST for all of its variable-length columns at once.',
      "The row's total width crosses roughly 2kB, so TOAST-able columns get compressed and/or moved out-of-line.",
      'An explicit VACUUM FULL command, which is the only operation that invokes the TOAST code.',
    ],
    correctIndexes: [2],
    explanation: "TOAST only kicks in once a stored row's overall width crosses roughly a 2kB threshold, at which point PostgreSQL compresses and/or relocates TOAST-able column values, one at a time, until the row fits or nothing more can be shrunk.",
  },
  {
    id: 'architecture-010',
    domain: 'architecture',
    questionType: 'multiple-response',
    question: "Which of the following statements about PostgreSQL's TOAST storage strategies are correct?",
    options: [
      "PLAIN prevents both compression and out-of-line storage, and is the only strategy possible for columns whose type isn't TOAST-able.",
      'MAIN is the default strategy for most TOAST-able data types and always compresses column values before storing them in the main table.',
      'EXTERNAL allows out-of-line storage but disables compression, which can make substring operations on wide text columns faster since a slice can be fetched without decompressing the whole value.',
      'EXTENDED disables out-of-line storage entirely and only ever compresses values in place.',
      "A table owner cannot change a column's storage strategy once the table has been created.",
    ],
    correctIndexes: [0, 2],
    explanation: 'PLAIN blocks any TOAST handling and is mandatory for non-TOAST-able types, while EXTERNAL skips compression specifically to make partial reads of wide values cheaper; EXTENDED (not MAIN) is the default and permits both compression and out-of-line storage, and the strategy can be changed later with ALTER TABLE.',
  },

  // --- Core scalar & temporal data types (architecture-011..017) ---
  {
    id: 'architecture-011',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'Which numeric type does the documentation recommend as the default choice for an ordinary integer column, and why?',
    options: [
      'numeric, because whole numbers stored as numeric use less disk space than the same values stored as integer.',
      'smallint, because its smaller storage footprint always makes it faster than integer or bigint.',
      'bigint, because PostgreSQL silently promotes any integer arithmetic to 8 bytes internally regardless of the declared column type.',
      'integer, because it offers the best balance of range, storage size, and performance among the whole-number types.',
    ],
    correctIndexes: [3],
    explanation: 'integer is presented as the general-purpose default because it balances storage cost, numeric range, and arithmetic speed better than smallint or bigint for typical columns; smallint only pays off when disk space is genuinely tight.',
  },
  {
    id: 'architecture-012',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "How does PostgreSQL's numeric type handle the special value NaN when sorting or comparing it to other numeric values, and why?",
    options: [
      'It treats NaN as equal to every other value, including ordinary numbers, so NaN can never be distinguished from 0.',
      'It treats NaN as equal to itself and greater than every non-NaN value, keeping numeric columns sortable.',
      'It refuses to store NaN in any numeric column, raising an error on insert.',
      'It treats NaN as less than every non-NaN value, placing it first in ascending sort order.',
    ],
    correctIndexes: [1],
    explanation: 'Because ordinary floating-point NaN semantics would make NaN unusable in ordering, PostgreSQL instead defines NaN as equal to other NaNs and greater than every real number, so numeric columns remain fully sortable and indexable.',
  },
  {
    id: 'architecture-013',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "A developer claims that declaring a column as char(20) instead of varchar(20) will make comparisons on that column faster, since char is a 'more native' fixed-width type. What does the documentation say about this claim?",
    options: [
      "It's correct: char(n) avoids length-checking overhead entirely, making it measurably faster than varchar(n) for equality comparisons.",
      "It's correct, but only because char(n) values are stored with 4 bytes of overhead compared to 1 byte for varchar(n).",
      "It's incorrect: char(n) has no performance edge in PostgreSQL and is often the slowest of the three once padding is factored in.",
      "It's incorrect, but only because char(n) is deprecated and no longer usable in current PostgreSQL versions.",
    ],
    correctIndexes: [2],
    explanation: "Despite intuitions carried over from other database systems, PostgreSQL's char(n) gains nothing from its fixed width and instead tends to lag behind varchar/text once the cost of blank-padding is counted; it is not deprecated.",
  },
  {
    id: 'architecture-014',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'For which of these string types does PostgreSQL treat trailing spaces as semantically insignificant when comparing two values of that same type?',
    options: [
      'character(n) only',
      'All three of character(n), character varying(n), and text',
      'None of the three; trailing spaces are always significant in PostgreSQL string comparisons',
      'character varying(n) and text only',
    ],
    correctIndexes: [0],
    explanation: 'PostgreSQL strips and ignores trailing blanks specifically when comparing character(n) values, but the same trailing blanks remain meaningful for character varying and text comparisons, which can produce mismatches if the types are mixed.',
  },
  {
    id: 'architecture-015',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "A row is inserted with timestamptz '2024-03-01 09:00:00-05'. A session running in a different time zone later selects that column. What does PostgreSQL actually store and return?",
    options: [
      'It converts the value to UTC internally, drops the original offset, and displays it in the querying session\'s active zone.',
      "It stores the original '-05' offset alongside the timestamp and always redisplays the value using that same offset, regardless of the querying session's time zone.",
      'It stores the value as entered with no time zone conversion at all, identical to how a plain timestamp column would store it.',
      'It rejects the insert, because timestamptz values must always be entered already converted to UTC by the client.',
    ],
    correctIndexes: [0],
    explanation: "timestamptz normalizes every incoming value to UTC using either an explicit offset or the session's active time zone, keeps only that UTC instant, and re-renders it in whichever zone a later session happens to be using.",
  },
  {
    id: 'architecture-016',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'An application wants primary-key UUIDs that sort roughly in the order they were created, to help index locality, while still generating them without coordinating with any central counter. Which native PostgreSQL function fits this requirement, and why?',
    options: [
      'gen_random_uuid(), because it produces sequential integers encoded as UUIDs.',
      'uuidv4(), because version 4 UUIDs are generated in strictly increasing order by design.',
      'uuidv7(), since it embeds a timestamp, making its values time-ordered unlike random UUIDs.',
      'Neither native function can produce time-ordered UUIDs; that requires a third-party extension.',
    ],
    correctIndexes: [2],
    explanation: 'uuidv7() is documented as producing a time-ordered UUID because it computes its value from a Unix timestamp, so successive calls sort roughly by generation time, a property random UUIDv4 output (from gen_random_uuid() or uuidv4()) does not have.',
  },
  {
    id: 'architecture-017',
    domain: 'architecture',
    questionType: 'multiple-response',
    question: "Which of the following statements about PostgreSQL's real, double precision, and numeric types are accurate?",
    options: [
      'numeric arithmetic is faster than integer arithmetic because numeric values are stored as fixed-width binary floats.',
      'Declaring a column as NUMERIC with no precision or scale forces every value to be rounded to zero decimal places.',
      'real and double precision are inexact IEEE-754 binary floating-point types, so common decimal fractions may not be stored as exact values.',
      'double precision guarantees exact storage of any decimal value with up to 15 digits after the decimal point.',
      'numeric is the documented alternative to floating-point types for values like currency where exact arithmetic matters.',
    ],
    correctIndexes: [2, 4],
    explanation: "real and double precision approximate values using binary floating point, which is why numeric — with its exact, arbitrary-precision arithmetic (though far slower than integer math) — is recommended when rounding error can't be tolerated, such as for money.",
  },

  // --- Arrays, ranges, JSON, and NULL semantics (architecture-018..024) ---
  {
    id: 'architecture-018',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'A table is created with a column declared as integer[3][3]. What does PostgreSQL actually enforce for values inserted into that column?',
    options: [
      'Every inserted array must have exactly 3 rows and 3 columns, or the insert is rejected.',
      'Nothing beyond it being an integer array — the 3x3 size is purely documentation, not enforced.',
      'The array is automatically padded or truncated to fit a 3x3 shape on insert.',
      'Only one-dimensional arrays of exactly 3 elements are accepted; two-dimensional input raises an error.',
    ],
    correctIndexes: [1],
    explanation: 'PostgreSQL records any array size or dimension count written in a column definition purely as documentation for readers; it is never checked against the arrays actually stored, so mismatched sizes are accepted without complaint.',
  },
  {
    id: 'architecture-019',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'For the two-dimensional array column schedule, what does the subscript expression schedule[1:2][2] retrieve?',
    options: [
      "It retrieves only the single element at row 1 through 2, column 2, since [2] always means 'exactly index 2' regardless of surrounding syntax.",
      'It raises a syntax error, because mixing a slice dimension with a non-slice dimension in the same subscript is not allowed.',
      'It is interpreted the same as schedule[1:2][2:2], returning a one-column slice starting and ending at column 2.',
      'It equals schedule[1:2][1:2], since any slice-notation dimension makes every dimension in the subscript a slice.',
    ],
    correctIndexes: [3],
    explanation: 'Once any subscript in a multi-dimensional reference uses colon notation, every dimension is read as a slice, and a bare number in that context is shorthand for a slice running from 1 up to that number.',
  },
  {
    id: 'architecture-020',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "Why do the literals '[4,8]' and '(3,9)' represent the same underlying value when cast to the built-in integer range type int4range?",
    options: [
      'Because both literals are invalid and get silently coerced to the empty range.',
      'Because PostgreSQL ignores bound inclusivity entirely for all range types and always treats bounds as inclusive.',
      'Because int4range is discrete, PostgreSQL rewrites both into one canonical inclusive-exclusive form.',
      'Because int4range is a continuous range type, and continuous ranges never distinguish inclusive from exclusive bounds.',
    ],
    correctIndexes: [2],
    explanation: 'int4range has a well-defined next/previous integer, so PostgreSQL rewrites any equivalent bound style into one canonical inclusive-lower, exclusive-upper form; a subtype without that notion, like numeric, keeps its bounds exactly as written instead.',
  },
  {
    id: 'architecture-021',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'A client inserts the JSON text \'{"a":1,"a":2}\' into both a json column and a jsonb column. What does each column actually store?',
    options: [
      'Both columns silently drop the entire object and store a JSON null, since duplicate keys are invalid JSON.',
      'Both columns raise an insert error, because PostgreSQL rejects any JSON object containing duplicate keys.',
      'The json column keeps only the last pair; the jsonb column preserves both, since jsonb is documented as the more literal, text-preserving format.',
      'The json column keeps both key/value pairs exactly as entered; the jsonb column keeps only the last one, discarding the first "a":1.',
    ],
    correctIndexes: [3],
    explanation: "The json type retains a literal copy of whatever text was supplied, so repeated keys all survive; jsonb reparses that text into a decomposed form at write time and keeps only the last value for any key that appears more than once.",
  },
  {
    id: 'architecture-022',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'A table has a nullable column notes, and some rows have notes IS NULL. What does the query SELECT * FROM t WHERE notes = NULL; return?',
    options: [
      'Zero rows, since notes = NULL evaluates to NULL, not true, even when notes is null.',
      'Every row in the table, because a NULL comparison is treated as a wildcard match.',
      "Every row where notes is null, because = NULL is PostgreSQL's shorthand for IS NULL.",
      'An error, because PostgreSQL disallows comparing any column directly to the NULL literal.',
    ],
    correctIndexes: [0],
    explanation: 'Standard SQL comparison operators return unknown rather than true or false whenever either side is null, so a bare equality test against NULL can never match a row, even one where the compared column is itself null.',
  },
  {
    id: 'architecture-023',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'Two nullable columns, a and b, both currently hold NULL for a given row. What does a IS NOT DISTINCT FROM b evaluate to for that row, and how does this differ from a = b?',
    options: [
      'It evaluates to NULL, exactly like a = b would, since IS NOT DISTINCT FROM is just a more verbose spelling of the equality operator.',
      'It evaluates to true, treating the two nulls as matching each other; by contrast, a = b would evaluate to NULL (unknown), not true.',
      'It raises an error, since IS NOT DISTINCT FROM cannot be applied when both operands are null.',
      'It evaluates to false, because two null values are never considered equal under any PostgreSQL comparison operator.',
    ],
    correctIndexes: [1],
    explanation: "IS [NOT] DISTINCT FROM always resolves to a definite true or false, treating null as an ordinary comparable value; that's why two nulls count as matching under IS NOT DISTINCT FROM even though plain equality between them stays unknown.",
  },
  {
    id: 'architecture-024',
    domain: 'architecture',
    questionType: 'multiple-response',
    question: 'Which of the following statements are accurate?',
    options: [
      'json (not jsonb) is the preferred type for indexing because it retains exact input formatting.',
      'An EXCLUDE constraint backed by a GiST index can reject a new row whose range value overlaps a range already stored in the table, something a plain UNIQUE constraint cannot express.',
      'EXCLUDE constraints can only be defined using a B-tree index, never GiST.',
      'A plain UNIQUE constraint is the standard tool for preventing two reservations with overlapping time ranges from both being inserted.',
      'With the btree_gist extension installed, an exclusion constraint can combine an equality check on a scalar column with an overlap check on a range column, so overlapping ranges are only rejected when the scalar values also match.',
    ],
    correctIndexes: [1, 4],
    explanation: 'GiST-backed EXCLUDE constraints reject overlapping range values directly, something UNIQUE cannot express, and pairing them with the btree_gist extension lets an equality check on a plain column be combined with a range-overlap check in the same constraint.',
  },

  // --- Querying & SQL domain (20% exam weight, querying-001..024) ---
  // Grounded in the official PostgreSQL 18 documentation cached under
  // .cache/aws-docs/ during authoring. Every answer key was re-checked
  // against the exact cached passage it is based on. Written in the
  // author's own words, not copied verbatim from the docs.

  // --- Joins & subqueries (querying-001..006) ---
  {
    id: 'querying-001',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'In the query SELECT * FROM orders LEFT JOIN shipments ON orders.order_id = shipments.order_id;, what happens to a row of orders that has no matching row in shipments?',
    options: [
      'It causes the entire query to raise an error at execution time, since LEFT JOIN is documented as requiring every single left-side row to find at least one match on the right.',
      'It appears once for every row of shipments, with shipments\' columns repeated unchanged each time.',
      'It still appears in the output exactly once, with null values standing in for every column that would have come from shipments.',
      'It is silently dropped from the output, exactly as it would be under an INNER JOIN.',
    ],
    correctIndexes: [2],
    explanation: 'A LEFT OUTER JOIN starts from the inner-join result and then adds back one row for every left-side row that found no partner, filling the right-hand columns with null rather than dropping the row or duplicating it.',
  },
  {
    id: 'querying-002',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: "A query joins orders LEFT JOIN shipments ON orders.order_id = shipments.order_id and also needs to restrict output to rows where shipments.carrier = 'FastFreight'. Compared to adding that condition into the ON clause, what changes if it is instead written as a separate WHERE shipments.carrier = 'FastFreight'?",
    options: [
      'Nothing changes: ON and WHERE conditions are always fully interchangeable, for outer joins just as much as inner joins.',
      'PostgreSQL automatically rewrites the LEFT JOIN into a FULL JOIN so that no rows on either side get lost.',
      'Moving that same condition into the ON clause instead would be flatly rejected by the parser as invalid syntax, since ON is documented as only ever being allowed to reference the two specific columns actually named in the join.',
      'The WHERE version runs after the join completes, so it filters out the null-padded rows for unmatched orders rows, since a comparison against null is never true — effectively turning the LEFT JOIN into an inner join.',
    ],
    correctIndexes: [3],
    explanation: "A condition placed in ON is applied while the join itself is being computed, before unmatched rows get padded with nulls, so those rows survive; that same condition placed in WHERE instead runs against the already-completed join result, and because it can never be true against a null, it strips out the very padded rows the LEFT JOIN existed to preserve.",
  },
  {
    id: 'querying-003',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'The subquery SELECT manager_id FROM employees can return NULL for employees with no manager. If at least one row has a null manager_id, what does SELECT name FROM employees WHERE employee_id NOT IN (SELECT manager_id FROM employees) return?',
    options: [
      "The NULL in the subquery's result set is simply skipped over, so NOT IN behaves as if it had been written as a NOT EXISTS correlated subquery.",
      "Zero rows, because the presence of a null among the subquery's results makes the NOT IN comparison evaluate to null (unknown) for every outer row, and a WHERE clause treats unknown the same as false.",
      'PostgreSQL raises an error at query time, since NOT IN cannot legally be evaluated against a subquery whose result set contains a null value.',
      'Every employee who does not personally manage anyone, exactly matching what a reader would naturally expect this query to return.',
    ],
    correctIndexes: [1],
    explanation: "Because NOT IN is effectively an AND of not-equal comparisons across every subquery row, one null result poisons the whole expression to null/unknown for every outer row, and a WHERE clause discards unknown results just like false ones — so the query silently returns nothing.",
  },
  {
    id: 'querying-004',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: "Why is NOT EXISTS (SELECT 1 FROM employees WHERE manager_id = employees.employee_id) considered a safer way than the NOT IN version to find employees with no direct reports, given that manager_id can hold nulls?",
    options: [
      'A correlated EXISTS/NOT EXISTS test only cares whether the subquery returns at least one row at all, so it never has to directly compare against a value that might be null.',
      "NOT EXISTS automatically strips null rows out of its subquery's result set before evaluating anything, which NOT IN does not do.",
      'The query planner silently rewrites every NOT EXISTS predicate into an equivalent LEFT JOIN combined with an IS NULL check internally, which is supposedly the actual reason nulls stop mattering here.',
      'NOT EXISTS is guaranteed to scan its subquery all the way to completion on every call, unlike NOT IN, which can stop early.',
    ],
    correctIndexes: [0],
    explanation: "EXISTS-family predicates depend only on row presence, not on the contents of any row, so there is no comparison-against-a-value step for a null to poison — which is exactly the mechanism that makes NOT IN unsafe when the subquery's column can be null.",
  },
  {
    id: 'querying-005',
    domain: 'querying',
    questionType: 'multiple-response',
    question: 'Which of the following statements about the ANY, SOME, and ALL subquery quantifiers are accurate?',
    options: [
      'expression IN (subquery) is defined to behave identically to expression = ANY (subquery).',
      'expression NOT IN (subquery) is defined to behave identically to expression <> ALL (subquery).',
      'SOME is accepted as an exact, interchangeable synonym for ANY when quantifying a subquery comparison.',
      'expression operator ALL (subquery) always evaluates to false whenever the subquery returns zero rows.',
      'expression operator ANY (subquery) always evaluates to true whenever the subquery returns zero rows.',
    ],
    correctIndexes: [0, 1, 2],
    explanation: 'IN is documented as equivalent to = ANY and NOT IN as equivalent to <> ALL, and SOME is an accepted synonym for ANY; by contrast, ALL against zero rows is vacuously true (nothing to fail the test) rather than false, and ANY against zero rows is false rather than true, since no comparison ever succeeded.',
  },
  {
    id: 'querying-006',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'Two tables currently share only one column name, customer_id. A developer is deciding between JOIN ... USING (customer_id) and NATURAL JOIN. What risk is specific to NATURAL JOIN that USING avoids?',
    options: [
      'NATURAL JOIN is fundamentally incompatible with LEFT, RIGHT, or FULL outer joins and can only ever legally be paired with a plain INNER JOIN, whereas USING can be freely combined with any of the three outer join variants as well.',
      'NATURAL JOIN always degrades into a full Cartesian product instead of matching rows on the shared column name.',
      'USING requires every single column in both tables to share an identical name, not merely the columns actually being joined on.',
      'If either table later gains a new column whose name happens to match one in the other table, NATURAL JOIN silently pulls that column into the join condition, while USING only ever combines the columns actually named in the query.',
    ],
    correctIndexes: [3],
    explanation: 'NATURAL builds its join condition from every column name the two tables happen to share, so an unrelated later schema change that introduces a matching column name silently changes the query\'s results; USING and ON are unaffected because they only ever combine the columns actually named.',
  },

  // --- Common table expressions (querying-007..012) ---
  {
    id: 'querying-007',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'In WITH monthly_totals AS (...), big_spenders AS (SELECT customer_id FROM monthly_totals WHERE ...) SELECT ... FROM big_spenders;, what governs which parts of the query are allowed to reference which?',
    options: [
      'Only the primary statement may reference a CTE; auxiliary statements within the same WITH clause can never reference one another.',
      'A later auxiliary statement may reference one defined earlier in the same WITH clause, and the primary statement may reference any of them.',
      'CTEs may reference each other in either direction, earlier referencing later or later referencing earlier, with no restriction at all.',
      'A CTE may only be referenced from inside a nested subquery of the primary statement, never directly in its top-level FROM clause.',
    ],
    correctIndexes: [1],
    explanation: 'Each auxiliary statement in a WITH clause behaves like a temporary table available to statements that follow it, so big_spenders can draw on monthly_totals, and the primary query can in turn draw on either of the named auxiliary statements.',
  },
  {
    id: 'querying-008',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'The documentation notes that, despite its name, a WITH RECURSIVE query is not evaluated through actual recursive function calls. How is it actually carried out?',
    options: [
      'Step by step: the non-recursive term first populates a working table, and then the recursive term keeps re-running against whatever the working table currently holds, stopping the moment a round contributes zero additional rows.',
      'By unrolling every level of recursion in advance into one giant non-recursive UNION ALL before the query ever runs.',
      'By spawning one backend process per recursion level and merging their individual outputs together at the end.',
      'By running the non-recursive term and the recursive term simultaneously on separate parallel worker threads, periodically merging whatever partial rows each thread has produced so far until their outputs finally converge.',
    ],
    correctIndexes: [0],
    explanation: 'PostgreSQL seeds a working table from the non-recursive term, then repeatedly substitutes that working table into the recursive term and folds each round\'s fresh output back in, terminating naturally once a round produces nothing new — an iterative process rather than true recursion.',
  },
  {
    id: 'querying-009',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'A recursive CTE definition is followed by SEARCH DEPTH FIRST BY id SET ordercol before the primary SELECT. What does this clause actually accomplish?',
    options: [
      "It rewrites the recursive term's join condition so that the underlying traversal algorithm itself proceeds in depth-first order.",
      'It converts UNION ALL into UNION automatically, so duplicate rows are discarded before any sorting happens.',
      'It is expanded internally into essentially the same visited-path-array bookkeeping a developer would otherwise hand-write, and it adds the named column to the CTE\'s output so the final result can be sorted by it.',
      'It completely replaces the need for a primary-query ORDER BY clause, supposedly guaranteeing that the driver or client application always receives the CTE\'s rows already sorted in true depth-first traversal order.',
    ],
    correctIndexes: [2],
    explanation: "SEARCH DEPTH FIRST is sugar for the manual technique of carrying an ever-growing array of visited rows and sorting by it afterward; the clause does not change the order rows are actually produced during evaluation, only the value in the added column that a later ORDER BY can use.",
  },
  {
    id: 'querying-010',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'Appending CYCLE id SET is_cycle USING path to a recursive CTE adds two columns to its output. What do those two columns represent?',
    options: [
      'A count of how many times autovacuum has touched the underlying tables, and a boolean flagging whether that count exceeded a threshold.',
      'A path column of visited ids and a boolean is_cycle column flagging whenever the current row would repeat an id already on that path.',
      'A single boolean column reporting whether the query as a whole looped forever; the path keyword contributes no column of its own.',
      'A depth counter tracking how many recursive rounds have run, and a boolean marking whether a configured maximum depth was reached.',
    ],
    correctIndexes: [1],
    explanation: 'CYCLE is internally rewritten into the same pattern as manually carrying an array of already-visited keys and checking each new row against it; the path and is_cycle columns it adds are exactly that visited-array and its derived cycle flag.',
  },
  {
    id: 'querying-011',
    domain: 'querying',
    questionType: 'multiple-response',
    question: 'Which of the following statements about CTE materialization are accurate?',
    options: [
      'By default, a non-recursive, side-effect-free CTE that the parent query references exactly once is folded directly into the parent query\'s plan, letting an outer restriction be pushed down into it.',
      'Materialization behavior only ever applies to recursive CTEs; a non-recursive CTE is always inlined no matter how many times the parent references it.',
      'Using NOT MATERIALIZED always improves performance, because inlining a CTE\'s body can never cause that body to be evaluated more than once.',
      'By default, once the same CTE is referenced more than once by the parent query, PostgreSQL computes it a single time into a temporary result and reuses that result for every reference.',
      'The MATERIALIZED keyword can force a CTE to be computed separately even when it is referenced only once, which matters when the CTE\'s body calls an expensive or side-effecting function that must run exactly once.',
    ],
    correctIndexes: [0, 3, 4],
    explanation: 'A once-referenced, side-effect-free CTE is folded into the parent plan by default, while a multiply-referenced one is materialized by default; MATERIALIZED overrides that default to force separate computation. NOT MATERIALIZED can instead cause duplicate computation, so it is not a universal win, and the folding behavior is not limited to recursive CTEs.',
  },
  {
    id: 'querying-012',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'In WITH purged AS (DELETE FROM sessions WHERE expires_at < now() RETURNING *) INSERT INTO sessions_archive SELECT * FROM purged;, what actually makes the deleted rows available to the outer INSERT?',
    options: [
      'PostgreSQL automatically re-queries the sessions table for any row matching the DELETE\'s WHERE condition.',
      'The DELETE\'s RETURNING clause is what forms the temporary result the rest of the query can draw on; a data-modifying statement in WITH that lacks RETURNING produces no such result to reference.',
      'The sessions table itself becomes directly selectable through the name purged even without RETURNING, since PostgreSQL treats every WITH sub-statement as automatically exposing its full target table.',
      'The WITH clause first converts the DELETE into an equivalent SELECT and only performs the actual deletion afterward, as a separate side effect.',
    ],
    correctIndexes: [1],
    explanation: "It's the output of RETURNING — not the DELETE's target table — that becomes the temporary result purged refers to; a data-modifying WITH statement that omits RETURNING still runs to completion but leaves nothing behind for the rest of the query to select from.",
  },

  // --- Aggregation & window functions (querying-013..018) ---
  {
    id: 'querying-013',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'A GROUP BY group ends up with zero contributing rows for a particular aggregate call. What do sum() and array_agg() return for that group, and how does count() differ?',
    options: [
      'sum() and array_agg() both return null rather than 0 or an empty array; coalesce is needed to substitute a default explicitly, which is why the note singles out count() as behaving differently by returning 0 in the same situation.',
      'sum() returns 0 and array_agg() returns an empty array, matching the intuition most programmers bring from other languages.',
      'sum() raises a runtime error over an empty input, while array_agg() quietly returns an empty array without complaint.',
      'sum() returns null, but array_agg() still returns an empty array rather than null, since arrays and numbers are handled differently.',
    ],
    correctIndexes: [0],
    explanation: "Except for count(), these aggregate functions return null over zero rows — sum() of no rows is null, not zero, and array_agg() of no rows is null, not an empty array — so code depending on a numeric or array result generally needs coalesce to supply a sensible default.",
  },
  {
    id: 'querying-014',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'What is the key behavioral difference between percentile_cont(0.5) WITHIN GROUP (ORDER BY duration) and percentile_disc(0.5) WITHIN GROUP (ORDER BY duration)?',
    options: [
      'percentile_cont is documented as only accepting integer-typed or bigint-typed columns for its WITHIN GROUP ORDER BY argument, while percentile_disc can accept any sortable data type at all.',
      'percentile_disc requires an accompanying GROUP BY clause in the surrounding query, whereas percentile_cont does not.',
      'percentile_cont ignores its WITHIN GROUP ORDER BY entirely and instead sorts using only its numeric fraction argument.',
      'percentile_cont may interpolate a value that falls between two adjacent inputs, while percentile_disc always returns one of the values that was actually present in the input.',
    ],
    correctIndexes: [3],
    explanation: "percentile_cont computes a continuous percentile and will interpolate between neighboring sorted values when the requested fraction lands between them, while percentile_disc's discrete percentile always lands on one genuine input value rather than an interpolated one.",
  },
  {
    id: 'querying-015',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: "A query includes both WHERE oi.ship_date > CURRENT_DATE - INTERVAL '30 days' and HAVING sum(oi.quantity * oi.unit_price) > 10000. Why can HAVING reference an aggregate result here while WHERE could not?",
    options: [
      'HAVING is nothing more than an alternate spelling of WHERE; the two clauses are fully interchangeable in every query.',
      'Aggregate values are computed once for the whole table well before WHERE ever runs, so WHERE could technically reference them but the grammar simply forbids the syntax.',
      'WHERE is only permitted to reference aggregates in queries that also omit a GROUP BY clause entirely.',
      'WHERE filters individual rows before any grouping or aggregation has happened, while HAVING filters entire already-formed groups after aggregation has produced its results.',
    ],
    correctIndexes: [3],
    explanation: 'WHERE operates on the raw rows coming out of FROM, ahead of grouping and aggregation, so there is no aggregate value yet for it to reference; HAVING runs afterward against the collapsed group rows, which is exactly why its conditions are free to use aggregate expressions.',
  },
  {
    id: 'querying-016',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'Three rows tie for the second-highest salary in a department, ordered by salary descending. How do rank() and dense_rank() number those three tied rows and the row that follows them?',
    options: [
      'rank() assigns the tied rows three separate, consecutive numbers, while dense_rank() assigns all three of them the very same number.',
      'Both functions are documented as assigning a strictly increasing, unique number to every single row, ties included, which makes them behave in every respect exactly like row_number() does.',
      'rank() and dense_rank() are simply two different names PostgreSQL provides for calling the identical ranking function.',
      'Both give the three tied rows the same number, but rank() then jumps ahead by the size of that tie for the next row, while dense_rank() just continues with the next consecutive integer.',
    ],
    correctIndexes: [3],
    explanation: 'rank() numbers peer groups (tied rows sharing the same ORDER BY value) identically but leaves a gap afterward equal to the tie\'s size, whereas dense_rank() also numbers peer groups identically but never leaves a gap, so it effectively just counts distinct peer groups.',
  },
  {
    id: 'querying-017',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'sum(amount) OVER (ORDER BY sale_date) is used with no PARTITION BY and no explicit frame clause. What total does each row actually receive, and how could the query be changed to give every row the same whole-partition total instead?',
    options: [
      'Each row already receives the sum computed across the entire result set on every single call it makes, because sum() used together with an OVER clause is documented as always completely ignoring any ORDER BY clause entirely, unless a PARTITION BY clause has also been separately supplied alongside it within that same window definition.',
      'PostgreSQL raises an error, since sum() OVER demands an explicit frame clause the moment ORDER BY appears in the OVER clause.',
      'Each row gets a running, cumulative total through its own peer group, since the omitted frame clause defaults to a frame running from the partition\'s first row through the current row; dropping ORDER BY, or adding ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING, yields the whole-partition total instead.',
      'Each row gets a running total, and once ORDER BY appears in the OVER clause there is no way to recover a true whole-partition total.',
    ],
    correctIndexes: [2],
    explanation: "Supplying ORDER BY without a frame clause quietly sets the lower bound of the frame all the way back to row one of the partition and the upper bound to wherever the current row's peers end, producing a running total; omitting ORDER BY, or overriding the frame explicitly, switches the frame back to the whole partition.",
  },
  {
    id: 'querying-018',
    domain: 'querying',
    questionType: 'multiple-response',
    question: 'Which of the following statements about how window functions interact with the rest of a query are accurate?',
    options: [
      'Window functions are evaluated only after any grouping, aggregation, and HAVING filtering have already run, so whenever a query also uses GROUP BY, a window function sees the already-collapsed group rows rather than the original per-row data.',
      'A window function\'s arguments can never include a call to a plain aggregate function, since ordinary aggregates are evaluated after window functions, not before.',
      'Because window functions are evaluated last, a WHERE clause in that same SELECT can reference a window function\'s result directly, with no subquery needed.',
      'Filtering or grouping on a window function\'s output has to happen by wrapping the windowed query in a subquery (or CTE) and applying the condition against that outer query instead.',
      'A GROUP BY clause may directly group rows by a window function\'s output column, since window functions are documented as running before GROUP BY is applied.',
    ],
    correctIndexes: [0, 3],
    explanation: "Window functions run after grouping/aggregation/HAVING, and a call to one may only be written in a query's output column list or its outer ORDER BY — it is rejected anywhere in WHERE, GROUP BY, or HAVING — so filtering on their output requires an outer subquery; conversely, since ordinary aggregates run before window functions, a window function's arguments may legally include an aggregate call.",
  },

  // --- JSON, full-text search & upsert (querying-019..024) ---
  {
    id: 'querying-019',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: "A query chains data->'address'->'city' and needs the final result as a plain text value for display. Why does the last step in a chain like this typically switch from -> to ->>?",
    options: [
      '-> and ->> are fully interchangeable operators; swapping one for the other never changes the result\'s type.',
      "->> is documented as only ever being legally usable as the very first operator applied directly to a jsonb column, and it can never legitimately appear later in a chain after a preceding -> call.",
      "-> keeps returning its extracted piece wrapped as json or jsonb, so it can be chained into a further -> or ->> call, while ->> converts that final extracted piece into plain text instead.",
      '-> always fails with an error once it is applied to a nested object, which is why ->> has to be substituted at every step of the chain.',
    ],
    correctIndexes: [2],
    explanation: '-> hands back its result still wrapped as json/jsonb so a query can keep drilling further into nested structure, while ->> hands back that same extracted piece already converted to text, which is generally what a final display step wants.',
  },
  {
    id: 'querying-020',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'A jsonb column stores {"foo": {"bar": "baz"}}. Why does data @> \'{"bar": "baz"}\' evaluate to false, even though the pair bar/baz clearly exists somewhere inside the stored document?',
    options: [
      'Containment requires the right-hand structure to match the containing document at the same nesting level; bar sits one level down inside foo, not at the top level where the right-hand object lives.',
      'The @> operator only ever works when both sides are jsonb arrays; it is simply undefined for jsonb objects.',
      'Containment checks are documented as plain case-sensitive character comparisons applied across the whole document, and "bar" simply fails to match some differently-cased key that appears elsewhere within it.',
      'Any object used on the right-hand side of @> must first be wrapped in a one-element array, or the containment test automatically fails.',
    ],
    correctIndexes: [0],
    explanation: 'jsonb containment matches structure and content at corresponding nesting levels, possibly discarding extra keys or array elements along the way — but it does not search recursively through arbitrary depth, so a pair nested one level down does not satisfy a top-level containment check.',
  },
  {
    id: 'querying-021',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: "jsonb_set(target, '{a,b,c}', new_value) is called on a target that has a top-level key \"a\", but that value has no nested key \"b\" at all — an earlier step in the path is missing entirely. What does jsonb_set return?",
    options: [
      'new_value gets inserted at whatever point in the structure was reached before the path ran out, silently discarding the remaining path segments.',
      'An object with an empty "b": {} gets created automatically so the rest of the path has somewhere to attach new_value.',
      'PostgreSQL raises an error immediately, since jsonb_set can never tolerate any missing intermediate step in its path argument.',
      'target itself, completely unchanged, because every step of the path before its final segment must already exist in the document.',
    ],
    correctIndexes: [3],
    explanation: 'jsonb_set only ever creates or replaces the final item named by path; every step leading up to that final one must already be present in the document, or the call is a no-op that hands back the original target untouched.',
  },
  {
    id: 'querying-022',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'The identical JSON text \'{"bar": "baz", "balance": 7.77, "active":false}\' is cast once to json and once to jsonb, and both are printed back out with no other processing. Why do the two printed results end up with different key order and spacing?',
    options: [
      'Both types are documented to always reprint the exact original input text, so any observed difference would indicate a bug.',
      'json keeps a literal copy of whatever text was supplied, preserving key order and whitespace, while jsonb reparses the input into a decomposed internal form and does not preserve those semantically-insignificant details.',
      'jsonb is actually the type that reprints the original input text completely unchanged, while json is the one that normalizes key order, since json is documented as the less compact, less storage-efficient of the two formats.',
      'Neither type preserves any part of the original input; both regenerate their output using a fixed, always-alphabetical key ordering.',
    ],
    correctIndexes: [1],
    explanation: 'json stores and echoes back exactly the text it was given, keys, spacing, and all, whereas jsonb decomposes that text into its own internal representation at write time, which is why reordered keys and stripped whitespace are semantically equal even though the printed text differs.',
  },
  {
    id: 'querying-023',
    domain: 'querying',
    questionType: 'multiple-response',
    question: 'Which of the following statements about PostgreSQL full-text search are accurate?',
    options: [
      "A GIN index built as CREATE INDEX ... USING GIN (to_tsvector('english', body)) will also be used by a query filtering on WHERE to_tsvector(body) @@ ..., since both calls invoke to_tsvector on the same source column.",
      'The <-> (FOLLOWED BY) tsquery operator only matches when its two arguments\' matches are adjacent and in the given order, which is the mechanism phraseto_tsquery relies on to build multi-word phrase queries.',
      'A tsquery is built straight from raw, unnormalized search text, and @@ compares that raw text directly against a document\'s original, unprocessed wording.',
      'GIN is the only index type PostgreSQL ever supports for accelerating full-text search; GiST cannot be used for this purpose at all.',
      'Because to_tsquery normalizes its search terms using the same rules to_tsvector applies to documents, a query built from the word "error" correctly matches a document that only contains the word "errors".',
    ],
    correctIndexes: [1, 4],
    explanation: 'FOLLOWED BY genuinely requires adjacency and order, and shared normalization is why a stemmed search term like error matches errors in the document; by contrast, an index built on the two-argument to_tsvector form is only used by queries written with that identical two-argument form (a bare to_tsvector(body) call cannot use it), tsquery terms must go through normalization rather than being compared as raw text, and GiST is also documented as usable for text search indexes alongside GIN.',
  },
  {
    id: 'querying-024',
    domain: 'querying',
    questionType: 'multiple-response',
    question: 'Which of the following statements about INSERT ... ON CONFLICT (upsert) are accurate?',
    options: [
      'Exclusion constraints are fully usable as arbiters for both ON CONFLICT DO NOTHING and ON CONFLICT DO UPDATE, exactly like unique indexes and unique constraints.',
      'ON CONFLICT DO UPDATE must name a conflict_target explicitly, while ON CONFLICT DO NOTHING may omit one, in which case it handles conflicts against any usable constraint or unique index on the table.',
      'A DEFERRABLE unique constraint can still serve as an arbiter for ON CONFLICT, since deferrability only changes when the constraint is checked, not whether it is eligible to arbitrate.',
      'Inside an ON CONFLICT DO UPDATE clause, the special EXCLUDED pseudo-table refers to the row that was proposed for insertion, so a clause like SET quantity = inventory.quantity + EXCLUDED.quantity can merge the new value into the row already on disk.',
      'ON CONFLICT DO UPDATE is guaranteed atomic: under concurrent activity, the outcome for a given row is always either a clean insert or a clean update, never a lost update and never a spurious duplicate-key error.',
    ],
    correctIndexes: [1, 3, 4],
    explanation: 'A conflict_target is mandatory for DO UPDATE but optional for DO NOTHING, EXCLUDED exposes the row that was about to be inserted for merge-style updates, and the whole operation is documented as atomic; by contrast, a constraint or index can arbitrate only when it is not deferrable, which rules out both deferrable unique constraints and, specifically for the DO UPDATE form, exclusion constraints altogether.',
  },
];
