// js/data/questions.js
// Quiz questions covering all six exam domains: architecture, querying,
// indexing, transactions, administration, and replication.
// Grounded in the official PostgreSQL 18 documentation cached under
// .cache/docs/ during authoring (see scripts/fetch-doc.mjs). Every
// answer key was re-checked against the exact cached passage it is based
// on. Written in the author's own words, not copied verbatim from the docs.

export const QUESTIONS = [
  // --- Server architecture & process model (architecture-001..005) ---
  {
    id: 'architecture-001',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "In PostgreSQL's client/server process model, what happens when a new client connects to the server?",
    options: [
      'The client connects directly to an existing backend process that is already serving another client.',
      'The postmaster spawns a new thread within its own process to handle the connection.',
      'The postmaster forks a new backend process dedicated to that one connection.',
      'The postmaster handles every subsequent query for that client itself, without creating any new process.',
    ],
    correctIndexes: [2],
    explanation: 'The postmaster listens for connections and forks a dedicated backend process per client; from then on, that backend handles the session directly while the postmaster returns to listening for the next connection.',
  },
  {
    id: 'architecture-002',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "According to PostgreSQL's write-ahead logging design, why does committing a transaction typically require flushing only the WAL rather than every changed data page?",
    options: [
      'Because flushing the sequential WAL is cheaper than flushing data pages, and any gap can be redone from WAL.',
      'Because data pages are never written to disk until the database is shut down cleanly.',
      'Because WAL records are stored only in memory and are never themselves flushed to permanent storage.',
      'Because PostgreSQL disables all disk writes during a transaction and batches them once per checkpoint regardless of WAL.',
    ],
    correctIndexes: [0],
    explanation: 'WAL guarantees durability by requiring change records to be flushed before the corresponding data page reaches disk, so only the sequentially-written WAL needs syncing at commit time; a crash can be recovered by replaying WAL records forward.',
  },
  {
    id: 'architecture-003',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'For a dedicated PostgreSQL server with at least 1GB of RAM, what does the documentation suggest as a reasonable starting point for shared_buffers, and why does going far beyond it tend not to help?',
    options: [
      'Roughly 90% of system RAM, since shared_buffers is the only cache PostgreSQL uses and the OS cache plays no role.',
      'A fixed 8MB regardless of total system memory, since shared_buffers cannot be resized without recompiling the server.',
      'Roughly 25% of system RAM, because values above that cause PostgreSQL to disable checkpointing entirely.',
      'Roughly 25% of RAM, since the OS filesystem cache absorbs the rest and pushing past 40% rarely helps.',
    ],
    correctIndexes: [3],
    explanation: "shared_buffers is commonly started around a quarter of system RAM because the operating system's own page cache already absorbs a large share of caching duties, so pushing shared_buffers much higher usually yields diminishing returns.",
  },
  {
    id: 'architecture-004',
    domain: 'architecture',
    questionType: 'multiple-response',
    question: 'Which of the following statements about the work_mem configuration parameter are accurate?',
    options: [
      'work_mem sets a hard cap on the total memory a single client connection may use for the lifetime of that connection.',
      'work_mem caps the memory a single sort or hash operation may use before spilling to temporary disk files.',
      'work_mem is used only by ORDER BY sorts and has no effect on hash joins or hash-based aggregation.',
      'Because a query can run multiple sort or hash operations at once, and many sessions can run concurrently, total memory used across the server can be many times the configured work_mem value.',
      'Raising work_mem guarantees that PostgreSQL will never write temporary sort or hash data to disk, regardless of query complexity.',
    ],
    correctIndexes: [1, 3],
    explanation: 'work_mem bounds each individual sort or hash step rather than a session as a whole, and since a query can run several such operations at once while many sessions run concurrently, aggregate memory use can end up a large multiple of the setting.',
  },
  {
    id: 'architecture-005',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'A server is configured with max_connections = 100 and superuser_reserved_connections = 3. At what point does a new non-superuser connection attempt start being rejected?',
    options: [
      'Only once all 100 slots are occupied — the three reserved slots exist on top of max_connections, so up to 103 sessions can be active when superusers use them.',
      'As soon as 97 connections are active, since new connections are accepted only for superusers once the active count reaches max_connections minus superuser_reserved_connections.',
      'It is never rejected outright; the postmaster queues the attempt until one of the unreserved slots frees up.',
      'Only once all 100 slots are occupied, at which point superusers may still connect by exceeding max_connections by up to three extra sessions.',
    ],
    correctIndexes: [1],
    explanation: 'The reserved slots are carved out of max_connections rather than added on top of it: whenever the number of active connections is at least max_connections minus superuser_reserved_connections (97 here), new connections are accepted only for superusers, preserving an administrator\'s ability to log in and diagnose a server whose ordinary slots are exhausted. At most max_connections sessions can ever be active simultaneously — superusers cannot push past the cap — and a rejected attempt fails immediately rather than being queued.',
  },

  // --- Physical storage & TOAST internals (architecture-006..010) ---
  {
    id: 'architecture-006',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "Why can't you safely assume that a table's on-disk filename always matches its OID in pg_class?",
    options: [
      "Because PostgreSQL renames every table's file to a random name each time autovacuum runs.",
      'Because TRUNCATE, REINDEX, and CLUSTER can assign the table a new filenode while its OID stays fixed.',
      "Because filenodes are recalculated from the table's OID using a hash function that changes each server restart.",
      'Because only temporary tables use filenode-based file names; permanent tables are named directly after their OID.',
    ],
    correctIndexes: [1],
    explanation: "A table's filenode is a separate identifier from its OID, and commands like TRUNCATE, REINDEX, and CLUSTER can assign a fresh filenode to the underlying file while the table keeps its original OID in the catalog.",
  },
  {
    id: 'architecture-007',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "What happens once a table's main data file grows past the default 1 GB segment size?",
    options: [
      'PostgreSQL automatically moves the table into a new tablespace to make room for further growth.',
      'The table is automatically split into child tables, each capped at 1 GB.',
      'PostgreSQL compresses the entire file in place so it stays under the 1 GB limit.',
      'PostgreSQL rolls over to a new segment file, appending .1, .2, and so on after the filenode.',
    ],
    correctIndexes: [3],
    explanation: 'Table and index files are capped at roughly a gigabyte per segment mainly to sidestep old filesystem size limits; once that ceiling is crossed, PostgreSQL keeps writing into additional numbered segment files under the same filenode.',
  },
  {
    id: 'architecture-008',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "Within an 8kB PostgreSQL page, why can an index safely store a reference (CTID) to a row's item identifier rather than to the row's raw byte offset?",
    options: [
      'Because an item identifier keeps its slot until freed, even as the row data moves during compaction.',
      'Because CTIDs are automatically rewritten by a background process every time a row moves.',
      'Because row data is never moved once written; only entire pages are ever relocated.',
      'Because item identifiers are stored in a separate file from the page they describe, so they are unaffected by changes to that page.',
    ],
    correctIndexes: [0],
    explanation: "Because item identifiers occupy a fixed slot in the page's pointer array until freed, an index entry that references a slot number keeps working correctly even after the row's bytes get relocated during page compaction.",
  },
  {
    id: 'architecture-009',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "What triggers PostgreSQL's TOAST mechanism to start compressing or relocating a row's oversized column values?",
    options: [
      'Any single column value exceeding roughly 2kB by itself, regardless of the total row width.',
      'The table crossing 1GB in total size, which triggers TOAST for all of its variable-length columns at once.',
      "The row's total width crosses roughly 2kB, so TOAST-able columns get compressed and/or moved out-of-line.",
      'An explicit VACUUM FULL command, which is the only operation that invokes the TOAST code.',
    ],
    correctIndexes: [2],
    explanation: "TOAST only kicks in once a stored row's overall width crosses roughly a 2kB threshold, at which point PostgreSQL compresses and/or relocates TOAST-able column values, one at a time, until the row fits or nothing more can be shrunk.",
  },
  {
    id: 'architecture-010',
    domain: 'architecture',
    questionType: 'multiple-response',
    question: "Which of the following statements about PostgreSQL's TOAST storage strategies are correct?",
    options: [
      "PLAIN prevents both compression and out-of-line storage, and is the only strategy possible for columns whose type isn't TOAST-able.",
      'MAIN is the default strategy for most TOAST-able data types and always compresses column values before storing them in the main table.',
      'EXTERNAL allows out-of-line storage but disables compression, which can make substring operations on wide text columns faster since a slice can be fetched without decompressing the whole value.',
      'EXTENDED disables out-of-line storage entirely and only ever compresses values in place.',
      "A table owner cannot change a column's storage strategy once the table has been created.",
    ],
    correctIndexes: [0, 2],
    explanation: 'PLAIN blocks any TOAST handling and is mandatory for non-TOAST-able types, while EXTERNAL skips compression specifically to make partial reads of wide values cheaper; EXTENDED (not MAIN) is the default and permits both compression and out-of-line storage, and the strategy can be changed later with ALTER TABLE.',
  },

  // --- Core scalar & temporal data types (architecture-011..017) ---
  {
    id: 'architecture-011',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'Which numeric type does the documentation recommend as the default choice for an ordinary integer column, and why?',
    options: [
      'numeric, because whole numbers stored as numeric use less disk space than the same values stored as integer.',
      'smallint, because its smaller storage footprint always makes it faster than integer or bigint.',
      'bigint, because PostgreSQL silently promotes any integer arithmetic to 8 bytes internally regardless of the declared column type.',
      'integer, because it offers the best balance of range, storage size, and performance among the whole-number types.',
    ],
    correctIndexes: [3],
    explanation: 'integer is presented as the general-purpose default because it balances storage cost, numeric range, and arithmetic speed better than smallint or bigint for typical columns; smallint only pays off when disk space is genuinely tight.',
  },
  {
    id: 'architecture-012',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "How does PostgreSQL's numeric type handle the special value NaN when sorting or comparing it to other numeric values, and why?",
    options: [
      'It treats NaN as equal to every other value, including ordinary numbers, so NaN can never be distinguished from 0.',
      'It treats NaN as equal to itself and greater than every non-NaN value, keeping numeric columns sortable.',
      'It refuses to store NaN in any numeric column, raising an error on insert.',
      'It treats NaN as less than every non-NaN value, placing it first in ascending sort order.',
    ],
    correctIndexes: [1],
    explanation: 'Because ordinary floating-point NaN semantics would make NaN unusable in ordering, PostgreSQL instead defines NaN as equal to other NaNs and greater than every real number, so numeric columns remain fully sortable and indexable.',
  },
  {
    id: 'architecture-013',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "A developer claims that declaring a column as char(20) instead of varchar(20) will make comparisons on that column faster, since char is a 'more native' fixed-width type. What does the documentation say about this claim?",
    options: [
      "It's correct: char(n) avoids length-checking overhead entirely, making it measurably faster than varchar(n) for equality comparisons.",
      "It's correct, but only because char(n) values are stored with 4 bytes of overhead compared to 1 byte for varchar(n).",
      "It's incorrect: char(n) has no performance edge in PostgreSQL and is often the slowest of the three once padding is factored in.",
      "It's incorrect, but only because char(n) is deprecated and no longer usable in current PostgreSQL versions.",
    ],
    correctIndexes: [2],
    explanation: "Despite intuitions carried over from other database systems, PostgreSQL's char(n) gains nothing from its fixed width and instead tends to lag behind varchar/text once the cost of blank-padding is counted; it is not deprecated.",
  },
  {
    id: 'architecture-014',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'For which of these string types does PostgreSQL treat trailing spaces as semantically insignificant when comparing two values of that same type?',
    options: [
      'character(n) only',
      'All three of character(n), character varying(n), and text',
      'None of the three; trailing spaces are always significant in PostgreSQL string comparisons',
      'character varying(n) and text only',
    ],
    correctIndexes: [0],
    explanation: 'PostgreSQL strips and ignores trailing blanks specifically when comparing character(n) values, but the same trailing blanks remain meaningful for character varying and text comparisons, which can produce mismatches if the types are mixed.',
  },
  {
    id: 'architecture-015',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "A row is inserted with timestamptz '2024-03-01 09:00:00-05'. A session running in a different time zone later selects that column. What does PostgreSQL actually store and return?",
    options: [
      'It converts the value to UTC internally, drops the original offset, and displays it in the querying session\'s active zone.',
      "It stores the original '-05' offset alongside the timestamp and always redisplays the value using that same offset, regardless of the querying session's time zone.",
      'It stores the value as entered with no time zone conversion at all, identical to how a plain timestamp column would store it.',
      'It rejects the insert, because timestamptz values must always be entered already converted to UTC by the client.',
    ],
    correctIndexes: [0],
    explanation: "timestamptz normalizes every incoming value to UTC using either an explicit offset or the session's active time zone, keeps only that UTC instant, and re-renders it in whichever zone a later session happens to be using.",
  },
  {
    id: 'architecture-016',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'An application wants primary-key UUIDs that sort roughly in the order they were created, to help index locality, while still generating them without coordinating with any central counter. Which native PostgreSQL function fits this requirement, and why?',
    options: [
      'gen_random_uuid(), because it produces sequential integers encoded as UUIDs.',
      'uuidv4(), because version 4 UUIDs are generated in strictly increasing order by design.',
      'uuidv7(), since it embeds a timestamp, making its values time-ordered unlike random UUIDs.',
      'Neither native function can produce time-ordered UUIDs; that requires a third-party extension.',
    ],
    correctIndexes: [2],
    explanation: 'uuidv7() is documented as producing a time-ordered UUID because it computes its value from a Unix timestamp, so successive calls sort roughly by generation time, a property random UUIDv4 output (from gen_random_uuid() or uuidv4()) does not have.',
  },
  {
    id: 'architecture-017',
    domain: 'architecture',
    questionType: 'multiple-response',
    question: "Which of the following statements about PostgreSQL's real, double precision, and numeric types are accurate?",
    options: [
      'numeric arithmetic is faster than integer arithmetic because numeric values are stored as fixed-width binary floats.',
      'Declaring a column as NUMERIC with no precision or scale forces every value to be rounded to zero decimal places.',
      'real and double precision are inexact IEEE-754 binary floating-point types, so common decimal fractions may not be stored as exact values.',
      'double precision guarantees exact storage of any decimal value with up to 15 digits after the decimal point.',
      'numeric is the documented alternative to floating-point types for values like currency where exact arithmetic matters.',
    ],
    correctIndexes: [2, 4],
    explanation: "real and double precision approximate values using binary floating point, which is why numeric — with its exact, arbitrary-precision arithmetic (though far slower than integer math) — is recommended when rounding error can't be tolerated, such as for money.",
  },

  // --- Arrays, ranges, JSON, and NULL semantics (architecture-018..024) ---
  {
    id: 'architecture-018',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'A table is created with a column declared as integer[3][3]. What does PostgreSQL actually enforce for values inserted into that column?',
    options: [
      'Every inserted array must have exactly 3 rows and 3 columns, or the insert is rejected.',
      'Nothing beyond it being an integer array — the 3x3 size is purely documentation, not enforced.',
      'The array is automatically padded or truncated to fit a 3x3 shape on insert.',
      'Only one-dimensional arrays of exactly 3 elements are accepted; two-dimensional input raises an error.',
    ],
    correctIndexes: [1],
    explanation: 'PostgreSQL records any array size or dimension count written in a column definition purely as documentation for readers; it is never checked against the arrays actually stored, so mismatched sizes are accepted without complaint.',
  },
  {
    id: 'architecture-019',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'For the two-dimensional array column schedule, what does the subscript expression schedule[1:2][2] retrieve?',
    options: [
      "It retrieves only the single element at row 1 through 2, column 2, since [2] always means 'exactly index 2' regardless of surrounding syntax.",
      'It raises a syntax error, because mixing a slice dimension with a non-slice dimension in the same subscript is not allowed.',
      'It is interpreted the same as schedule[1:2][2:2], returning a one-column slice starting and ending at column 2.',
      'It equals schedule[1:2][1:2], since any slice-notation dimension makes every dimension in the subscript a slice.',
    ],
    correctIndexes: [3],
    explanation: 'Once any subscript in a multi-dimensional reference uses colon notation, every dimension is read as a slice, and a bare number in that context is shorthand for a slice running from 1 up to that number.',
  },
  {
    id: 'architecture-020',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: "Why do the literals '[4,8]' and '(3,9)' represent the same underlying value when cast to the built-in integer range type int4range?",
    options: [
      'Because both literals are invalid and get silently coerced to the empty range.',
      'Because PostgreSQL ignores bound inclusivity entirely for all range types and always treats bounds as inclusive.',
      'Because int4range is discrete, PostgreSQL rewrites both into one canonical inclusive-exclusive form.',
      'Because int4range is a continuous range type, and continuous ranges never distinguish inclusive from exclusive bounds.',
    ],
    correctIndexes: [2],
    explanation: 'int4range has a well-defined next/previous integer, so PostgreSQL rewrites any equivalent bound style into one canonical inclusive-lower, exclusive-upper form; a subtype without that notion, like numeric, keeps its bounds exactly as written instead.',
  },
  {
    id: 'architecture-021',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'A client inserts the JSON text \'{"a":1,"a":2}\' into both a json column and a jsonb column. What does each column actually store?',
    options: [
      'Both columns silently drop the entire object and store a JSON null, since duplicate keys are invalid JSON.',
      'Both columns raise an insert error, because PostgreSQL rejects any JSON object containing duplicate keys.',
      'The json column keeps only the last pair; the jsonb column preserves both, since jsonb is documented as the more literal, text-preserving format.',
      'The json column keeps both key/value pairs exactly as entered; the jsonb column keeps only the last one, discarding the first "a":1.',
    ],
    correctIndexes: [3],
    explanation: "The json type retains a literal copy of whatever text was supplied, so repeated keys all survive; jsonb reparses that text into a decomposed form at write time and keeps only the last value for any key that appears more than once.",
  },
  {
    id: 'architecture-022',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'A table has a nullable column notes, and some rows have notes IS NULL. What does the query SELECT * FROM t WHERE notes = NULL; return?',
    options: [
      'Zero rows, since notes = NULL evaluates to NULL, not true, even when notes is null.',
      'Every row in the table, because a NULL comparison is treated as a wildcard match.',
      "Every row where notes is null, because = NULL is PostgreSQL's shorthand for IS NULL.",
      'An error, because PostgreSQL disallows comparing any column directly to the NULL literal.',
    ],
    correctIndexes: [0],
    explanation: 'Standard SQL comparison operators return unknown rather than true or false whenever either side is null, so a bare equality test against NULL can never match a row, even one where the compared column is itself null.',
  },
  {
    id: 'architecture-023',
    domain: 'architecture',
    questionType: 'multiple-choice',
    question: 'Two nullable columns, a and b, both currently hold NULL for a given row. What does a IS NOT DISTINCT FROM b evaluate to for that row, and how does this differ from a = b?',
    options: [
      'It evaluates to NULL, exactly like a = b would, since IS NOT DISTINCT FROM is just a more verbose spelling of the equality operator.',
      'It evaluates to true, treating the two nulls as matching each other; by contrast, a = b would evaluate to NULL (unknown), not true.',
      'It raises an error, since IS NOT DISTINCT FROM cannot be applied when both operands are null.',
      'It evaluates to false, because two null values are never considered equal under any PostgreSQL comparison operator.',
    ],
    correctIndexes: [1],
    explanation: "IS [NOT] DISTINCT FROM always resolves to a definite true or false, treating null as an ordinary comparable value; that's why two nulls count as matching under IS NOT DISTINCT FROM even though plain equality between them stays unknown.",
  },
  {
    id: 'architecture-024',
    domain: 'architecture',
    questionType: 'multiple-response',
    question: 'Which of the following statements are accurate?',
    options: [
      'json (not jsonb) is the preferred type for indexing because it retains exact input formatting.',
      'An EXCLUDE constraint backed by a GiST index can reject a new row whose range value overlaps a range already stored in the table, something a plain UNIQUE constraint cannot express.',
      'EXCLUDE constraints can only be defined using a B-tree index, never GiST.',
      'A plain UNIQUE constraint is the standard tool for preventing two reservations with overlapping time ranges from both being inserted.',
      'With the btree_gist extension installed, an exclusion constraint can combine an equality check on a scalar column with an overlap check on a range column, so overlapping ranges are only rejected when the scalar values also match.',
    ],
    correctIndexes: [1, 4],
    explanation: 'GiST-backed EXCLUDE constraints reject overlapping range values directly, something UNIQUE cannot express, and pairing them with the btree_gist extension lets an equality check on a plain column be combined with a range-overlap check in the same constraint.',
  },

  // --- Querying & SQL domain (20% exam weight, querying-001..024) ---
  // Grounded in the official PostgreSQL 18 documentation cached under
  // .cache/docs/ during authoring. Every answer key was re-checked
  // against the exact cached passage it is based on. Written in the
  // author's own words, not copied verbatim from the docs.

  // --- Joins & subqueries (querying-001..006) ---
  {
    id: 'querying-001',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'In the query SELECT * FROM orders LEFT JOIN shipments ON orders.order_id = shipments.order_id;, what happens to a row of orders that has no matching row in shipments?',
    options: [
      'It causes the entire query to raise an error at execution time, since LEFT JOIN is documented as requiring every single left-side row to find at least one match on the right.',
      'It appears once for every row of shipments, with shipments\' columns repeated unchanged each time.',
      'It still appears in the output exactly once, with null values standing in for every column that would have come from shipments.',
      'It is silently dropped from the output, exactly as it would be under an INNER JOIN.',
    ],
    correctIndexes: [2],
    explanation: 'A LEFT OUTER JOIN starts from the inner-join result and then adds back one row for every left-side row that found no partner, filling the right-hand columns with null rather than dropping the row or duplicating it.',
  },
  {
    id: 'querying-002',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: "A query joins orders LEFT JOIN shipments ON orders.order_id = shipments.order_id and also needs to restrict output to rows where shipments.carrier = 'FastFreight'. Compared to adding that condition into the ON clause, what changes if it is instead written as a separate WHERE shipments.carrier = 'FastFreight'?",
    options: [
      'Nothing changes: ON and WHERE conditions are always fully interchangeable, for outer joins just as much as inner joins.',
      'PostgreSQL automatically rewrites the LEFT JOIN into a FULL JOIN so that no rows on either side get lost.',
      'Moving that same condition into the ON clause instead is rejected by the parser as invalid syntax, since an ON condition may reference only the columns being compared by the join.',
      'The WHERE version runs after the join completes, so it filters out the null-padded rows for unmatched orders rows, since a comparison against null is never true — effectively turning the LEFT JOIN into an inner join.',
    ],
    correctIndexes: [3],
    explanation: "A condition placed in ON is applied while the join itself is being computed, before unmatched rows get padded with nulls, so those rows survive; that same condition placed in WHERE instead runs against the already-completed join result, and because it can never be true against a null, it strips out the very padded rows the LEFT JOIN existed to preserve.",
  },
  {
    id: 'querying-003',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'The subquery SELECT manager_id FROM employees can return NULL for employees with no manager. If at least one row has a null manager_id, what does SELECT name FROM employees WHERE employee_id NOT IN (SELECT manager_id FROM employees) return?',
    options: [
      "The NULL in the subquery's result set is simply skipped over, so NOT IN behaves as if it had been written as a NOT EXISTS correlated subquery.",
      "Zero rows, because the presence of a null among the subquery's results makes the NOT IN comparison evaluate to null (unknown) for every outer row, and a WHERE clause treats unknown the same as false.",
      'PostgreSQL raises an error at query time, since NOT IN cannot legally be evaluated against a subquery whose result set contains a null value.',
      'Every employee who does not personally manage anyone, exactly matching what a reader would naturally expect this query to return.',
    ],
    correctIndexes: [1],
    explanation: "Because NOT IN is effectively an AND of not-equal comparisons across every subquery row, one null result poisons the whole expression to null/unknown for every outer row, and a WHERE clause discards unknown results just like false ones — so the query silently returns nothing.",
  },
  {
    id: 'querying-004',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: "Why is NOT EXISTS (SELECT 1 FROM employees WHERE manager_id = employees.employee_id) considered a safer way than the NOT IN version to find employees with no direct reports, given that manager_id can hold nulls?",
    options: [
      'A correlated EXISTS/NOT EXISTS test only cares whether the subquery returns at least one row at all, so it never has to directly compare against a value that might be null.',
      "NOT EXISTS automatically strips null rows out of its subquery's result set before evaluating anything, which NOT IN does not do.",
      'The query planner silently rewrites every NOT EXISTS predicate into an equivalent LEFT JOIN combined with an IS NULL check internally, and that rewrite is what makes the nulls stop mattering here.',
      'NOT EXISTS is guaranteed to scan its subquery all the way to completion on every call, unlike NOT IN, which can stop early.',
    ],
    correctIndexes: [0],
    explanation: "EXISTS-family predicates depend only on row presence, not on the contents of any row, so there is no comparison-against-a-value step for a null to poison — which is exactly the mechanism that makes NOT IN unsafe when the subquery's column can be null.",
  },
  {
    id: 'querying-005',
    domain: 'querying',
    questionType: 'multiple-response',
    question: 'Which of the following statements about the ANY, SOME, and ALL subquery quantifiers are accurate?',
    options: [
      'expression IN (subquery) is defined to behave identically to expression = ANY (subquery).',
      'expression NOT IN (subquery) is defined to behave identically to expression <> ALL (subquery).',
      'SOME is accepted as an exact, interchangeable synonym for ANY when quantifying a subquery comparison.',
      'expression operator ALL (subquery) always evaluates to false whenever the subquery returns zero rows.',
      'expression operator ANY (subquery) always evaluates to true whenever the subquery returns zero rows.',
    ],
    correctIndexes: [0, 1, 2],
    explanation: 'IN is documented as equivalent to = ANY and NOT IN as equivalent to <> ALL, and SOME is an accepted synonym for ANY; by contrast, ALL against zero rows is vacuously true (nothing to fail the test) rather than false, and ANY against zero rows is false rather than true, since no comparison ever succeeded.',
  },
  {
    id: 'querying-006',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'Two tables currently share only one column name, customer_id. A developer is deciding between JOIN ... USING (customer_id) and NATURAL JOIN. What risk is specific to NATURAL JOIN that USING avoids?',
    options: [
      'NATURAL JOIN is fundamentally incompatible with LEFT, RIGHT, or FULL outer joins and can only ever legally be paired with a plain INNER JOIN, whereas USING can be freely combined with any of the three outer join variants as well.',
      'NATURAL JOIN always degrades into a full Cartesian product instead of matching rows on the shared column name.',
      'USING requires every single column in both tables to share an identical name, not merely the columns actually being joined on.',
      'If either table later gains a new column whose name happens to match one in the other table, NATURAL JOIN silently pulls that column into the join condition, while USING only ever combines the columns actually named in the query.',
    ],
    correctIndexes: [3],
    explanation: 'NATURAL builds its join condition from every column name the two tables happen to share, so an unrelated later schema change that introduces a matching column name silently changes the query\'s results; USING and ON are unaffected because they only ever combine the columns actually named.',
  },

  // --- Common table expressions (querying-007..012) ---
  {
    id: 'querying-007',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'In WITH monthly_totals AS (...), big_spenders AS (SELECT customer_id FROM monthly_totals WHERE ...) SELECT ... FROM big_spenders;, what governs which parts of the query are allowed to reference which?',
    options: [
      'Only the primary statement may reference a CTE; auxiliary statements within the same WITH clause can never reference one another.',
      'A later auxiliary statement may reference one defined earlier in the same WITH clause, and the primary statement may reference any of them.',
      'CTEs may reference each other in either direction, earlier referencing later or later referencing earlier, with no restriction at all.',
      'A CTE may only be referenced from inside a nested subquery of the primary statement, never directly in its top-level FROM clause.',
    ],
    correctIndexes: [1],
    explanation: 'Each auxiliary statement in a WITH clause behaves like a temporary table available to statements that follow it, so big_spenders can draw on monthly_totals, and the primary query can in turn draw on either of the named auxiliary statements.',
  },
  {
    id: 'querying-008',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'The documentation notes that, despite its name, a WITH RECURSIVE query is not evaluated through actual recursive function calls. How is it actually carried out?',
    options: [
      'Step by step: the non-recursive term first populates a working table, and then the recursive term keeps re-running against whatever the working table currently holds, stopping the moment a round contributes zero additional rows.',
      'By unrolling every level of recursion in advance into one giant non-recursive UNION ALL before the query ever runs.',
      'By spawning one backend process per recursion level and merging their individual outputs together at the end.',
      'By running the non-recursive term and the recursive term simultaneously on separate parallel worker threads, periodically merging whatever partial rows each thread has produced so far until their outputs finally converge.',
    ],
    correctIndexes: [0],
    explanation: 'PostgreSQL seeds a working table from the non-recursive term, then repeatedly substitutes that working table into the recursive term and folds each round\'s fresh output back in, terminating naturally once a round produces nothing new — an iterative process rather than true recursion.',
  },
  {
    id: 'querying-009',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'A recursive CTE definition is followed by SEARCH DEPTH FIRST BY id SET ordercol before the primary SELECT. What does this clause actually accomplish?',
    options: [
      "It rewrites the recursive term's join condition so that the underlying traversal algorithm itself proceeds in depth-first order.",
      'It converts UNION ALL into UNION automatically, so duplicate rows are discarded before any sorting happens.',
      'It is expanded internally into essentially the same visited-path-array bookkeeping a developer would otherwise hand-write, and it adds the named column to the CTE\'s output so the final result can be sorted by it.',
      'It replaces the need for a primary-query ORDER BY clause, guaranteeing that the client application always receives the CTE\'s rows already sorted in true depth-first traversal order.',
    ],
    correctIndexes: [2],
    explanation: "SEARCH DEPTH FIRST is sugar for the manual technique of carrying an ever-growing array of visited rows and sorting by it afterward; the clause does not change the order rows are actually produced during evaluation, only the value in the added column that a later ORDER BY can use.",
  },
  {
    id: 'querying-010',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'Appending CYCLE id SET is_cycle USING path to a recursive CTE adds two columns to its output. What do those two columns represent?',
    options: [
      'A count of how many times autovacuum has touched the underlying tables, and a boolean flagging whether that count exceeded a threshold.',
      'A path column of visited ids and a boolean is_cycle column flagging whenever the current row would repeat an id already on that path.',
      'A single boolean column reporting whether the query as a whole looped forever; the path keyword contributes no column of its own.',
      'A depth counter tracking how many recursive rounds have run, and a boolean marking whether a configured maximum depth was reached.',
    ],
    correctIndexes: [1],
    explanation: 'CYCLE is internally rewritten into the same pattern as manually carrying an array of already-visited keys and checking each new row against it; the path and is_cycle columns it adds are exactly that visited-array and its derived cycle flag.',
  },
  {
    id: 'querying-011',
    domain: 'querying',
    questionType: 'multiple-response',
    question: 'Which of the following statements about CTE materialization are accurate?',
    options: [
      'By default, a non-recursive, side-effect-free CTE that the parent query references exactly once is folded directly into the parent query\'s plan, letting an outer restriction be pushed down into it.',
      'Materialization behavior only ever applies to recursive CTEs; a non-recursive CTE is always inlined no matter how many times the parent references it.',
      'Using NOT MATERIALIZED always improves performance, because inlining a CTE\'s body can never cause that body to be evaluated more than once.',
      'By default, once the same CTE is referenced more than once by the parent query, PostgreSQL computes it a single time into a temporary result and reuses that result for every reference.',
      'The MATERIALIZED keyword can force a CTE to be computed separately even when it is referenced only once, which matters when the CTE\'s body calls an expensive or side-effecting function that must run exactly once.',
    ],
    correctIndexes: [0, 3, 4],
    explanation: 'A once-referenced, side-effect-free CTE is folded into the parent plan by default, while a multiply-referenced one is materialized by default; MATERIALIZED overrides that default to force separate computation. NOT MATERIALIZED can instead cause duplicate computation, so it is not a universal win, and the folding behavior is not limited to recursive CTEs.',
  },
  {
    id: 'querying-012',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'In WITH purged AS (DELETE FROM sessions WHERE expires_at < now() RETURNING *) INSERT INTO sessions_archive SELECT * FROM purged;, what actually makes the deleted rows available to the outer INSERT?',
    options: [
      'PostgreSQL automatically re-queries the sessions table for any row matching the DELETE\'s WHERE condition.',
      'The DELETE\'s RETURNING clause is what forms the temporary result the rest of the query can draw on; a data-modifying statement in WITH that lacks RETURNING produces no such result to reference.',
      'The sessions table itself becomes directly selectable through the name purged even without RETURNING, since PostgreSQL treats every WITH sub-statement as automatically exposing its full target table.',
      'The WITH clause first converts the DELETE into an equivalent SELECT and only performs the actual deletion afterward, as a separate side effect.',
    ],
    correctIndexes: [1],
    explanation: "It's the output of RETURNING — not the DELETE's target table — that becomes the temporary result purged refers to; a data-modifying WITH statement that omits RETURNING still runs to completion but leaves nothing behind for the rest of the query to select from.",
  },

  // --- Aggregation & window functions (querying-013..018) ---
  {
    id: 'querying-013',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'A GROUP BY group ends up with zero contributing rows for a particular aggregate call. What do sum() and array_agg() return for that group, and how does count() differ?',
    options: [
      'sum() and array_agg() both return null rather than 0 or an empty array; coalesce is needed to substitute a default explicitly, which is why the note singles out count() as behaving differently by returning 0 in the same situation.',
      'sum() returns 0 and array_agg() returns an empty array, matching the intuition most programmers bring from other languages.',
      'sum() raises a runtime error over an empty input, while array_agg() quietly returns an empty array without complaint.',
      'sum() returns null, but array_agg() still returns an empty array rather than null, since arrays and numbers are handled differently.',
    ],
    correctIndexes: [0],
    explanation: "Except for count(), these aggregate functions return null over zero rows — sum() of no rows is null, not zero, and array_agg() of no rows is null, not an empty array — so code depending on a numeric or array result generally needs coalesce to supply a sensible default.",
  },
  {
    id: 'querying-014',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'What is the key behavioral difference between percentile_cont(0.5) WITHIN GROUP (ORDER BY duration) and percentile_disc(0.5) WITHIN GROUP (ORDER BY duration)?',
    options: [
      'percentile_cont is documented as only accepting integer-typed or bigint-typed columns for its WITHIN GROUP ORDER BY argument, while percentile_disc can accept any sortable data type at all.',
      'percentile_disc requires an accompanying GROUP BY clause in the surrounding query, whereas percentile_cont does not.',
      'percentile_cont ignores its WITHIN GROUP ORDER BY entirely and instead sorts using only its numeric fraction argument.',
      'percentile_cont may interpolate a value that falls between two adjacent inputs, while percentile_disc always returns one of the values that was actually present in the input.',
    ],
    correctIndexes: [3],
    explanation: "percentile_cont computes a continuous percentile and will interpolate between neighboring sorted values when the requested fraction lands between them, while percentile_disc's discrete percentile always lands on one genuine input value rather than an interpolated one.",
  },
  {
    id: 'querying-015',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: "A query includes both WHERE oi.ship_date > CURRENT_DATE - INTERVAL '30 days' and HAVING sum(oi.quantity * oi.unit_price) > 10000. Why can HAVING reference an aggregate result here while WHERE could not?",
    options: [
      'HAVING is nothing more than an alternate spelling of WHERE; the two clauses are fully interchangeable in every query.',
      'Aggregate values are computed once for the whole table well before WHERE ever runs, so WHERE could technically reference them but the grammar simply forbids the syntax.',
      'WHERE is only permitted to reference aggregates in queries that also omit a GROUP BY clause entirely.',
      'WHERE filters individual rows before any grouping or aggregation has happened, while HAVING filters entire already-formed groups after aggregation has produced its results.',
    ],
    correctIndexes: [3],
    explanation: 'WHERE operates on the raw rows coming out of FROM, ahead of grouping and aggregation, so there is no aggregate value yet for it to reference; HAVING runs afterward against the collapsed group rows, which is exactly why its conditions are free to use aggregate expressions.',
  },
  {
    id: 'querying-016',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'Three rows tie for the second-highest salary in a department, ordered by salary descending. How do rank() and dense_rank() number those three tied rows and the row that follows them?',
    options: [
      'rank() assigns the tied rows three separate, consecutive numbers, while dense_rank() assigns all three of them the very same number.',
      'Both functions are documented as assigning a strictly increasing, unique number to every single row, ties included, which makes them behave in every respect exactly like row_number() does.',
      'rank() and dense_rank() are simply two different names PostgreSQL provides for calling the identical ranking function.',
      'Both give the three tied rows the same number, but rank() then jumps ahead by the size of that tie for the next row, while dense_rank() just continues with the next consecutive integer.',
    ],
    correctIndexes: [3],
    explanation: 'rank() numbers peer groups (tied rows sharing the same ORDER BY value) identically but leaves a gap afterward equal to the tie\'s size, whereas dense_rank() also numbers peer groups identically but never leaves a gap, so it effectively just counts distinct peer groups.',
  },
  {
    id: 'querying-017',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'sum(amount) OVER (ORDER BY sale_date) is used with no PARTITION BY and no explicit frame clause. What total does each row actually receive, and how could the query be changed to give every row the same whole-partition total instead?',
    options: [
      'Each row already receives the sum computed across the entire result set on every single call it makes, because sum() used together with an OVER clause is documented as always completely ignoring any ORDER BY clause entirely, unless a PARTITION BY clause has also been separately supplied alongside it within that same window definition.',
      'PostgreSQL raises an error, since sum() OVER demands an explicit frame clause the moment ORDER BY appears in the OVER clause.',
      'Each row gets a running, cumulative total through its own peer group, since the omitted frame clause defaults to a frame running from the partition\'s first row through the current row; dropping ORDER BY, or adding ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING, yields the whole-partition total instead.',
      'Each row gets a running total, and once ORDER BY appears in the OVER clause there is no way to recover a true whole-partition total.',
    ],
    correctIndexes: [2],
    explanation: "Supplying ORDER BY without a frame clause quietly sets the lower bound of the frame all the way back to row one of the partition and the upper bound to wherever the current row's peers end, producing a running total; omitting ORDER BY, or overriding the frame explicitly, switches the frame back to the whole partition.",
  },
  {
    id: 'querying-018',
    domain: 'querying',
    questionType: 'multiple-response',
    question: 'Which of the following statements about how window functions interact with the rest of a query are accurate?',
    options: [
      'Window functions are evaluated only after any grouping, aggregation, and HAVING filtering have already run, so whenever a query also uses GROUP BY, a window function sees the already-collapsed group rows rather than the original per-row data.',
      'A window function\'s arguments can never include a call to a plain aggregate function, since ordinary aggregates are evaluated after window functions, not before.',
      'Because window functions are evaluated last, a WHERE clause in that same SELECT can reference a window function\'s result directly, with no subquery needed.',
      'Filtering or grouping on a window function\'s output has to happen by wrapping the windowed query in a subquery (or CTE) and applying the condition against that outer query instead.',
      'A GROUP BY clause may directly group rows by a window function\'s output column, since window functions are documented as running before GROUP BY is applied.',
    ],
    correctIndexes: [0, 3],
    explanation: "Window functions run after grouping/aggregation/HAVING, and a call to one may only be written in a query's output column list or its outer ORDER BY — it is rejected anywhere in WHERE, GROUP BY, or HAVING — so filtering on their output requires an outer subquery; conversely, since ordinary aggregates run before window functions, a window function's arguments may legally include an aggregate call.",
  },

  // --- JSON, full-text search & upsert (querying-019..024) ---
  {
    id: 'querying-019',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: "A query chains data->'address'->'city' and needs the final result as a plain text value for display. Why does the last step in a chain like this typically switch from -> to ->>?",
    options: [
      '-> and ->> are fully interchangeable operators; swapping one for the other never changes the result\'s type.',
      "->> is documented as only ever being legally usable as the very first operator applied directly to a jsonb column, and it can never legitimately appear later in a chain after a preceding -> call.",
      "-> keeps returning its extracted piece wrapped as json or jsonb, so it can be chained into a further -> or ->> call, while ->> converts that final extracted piece into plain text instead.",
      '-> always fails with an error once it is applied to a nested object, which is why ->> has to be substituted at every step of the chain.',
    ],
    correctIndexes: [2],
    explanation: '-> hands back its result still wrapped as json/jsonb so a query can keep drilling further into nested structure, while ->> hands back that same extracted piece already converted to text, which is generally what a final display step wants.',
  },
  {
    id: 'querying-020',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'A jsonb column stores {"foo": {"bar": "baz"}}. Why does data @> \'{"bar": "baz"}\' evaluate to false, even though the pair bar/baz clearly exists somewhere inside the stored document?',
    options: [
      'Containment requires the right-hand structure to match the containing document at the same nesting level; bar sits one level down inside foo, not at the top level where the right-hand object lives.',
      'The @> operator only ever works when both sides are jsonb arrays; it is simply undefined for jsonb objects.',
      'Containment checks are documented as plain case-sensitive character comparisons applied across the whole document, and "bar" simply fails to match some differently-cased key that appears elsewhere within it.',
      'Any object used on the right-hand side of @> must first be wrapped in a one-element array, or the containment test automatically fails.',
    ],
    correctIndexes: [0],
    explanation: 'jsonb containment matches structure and content at corresponding nesting levels, possibly discarding extra keys or array elements along the way — but it does not search recursively through arbitrary depth, so a pair nested one level down does not satisfy a top-level containment check.',
  },
  {
    id: 'querying-021',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: "jsonb_set(target, '{a,b,c}', new_value) is called on a target that has a top-level key \"a\", but that value has no nested key \"b\" at all — an earlier step in the path is missing entirely. What does jsonb_set return?",
    options: [
      'new_value gets inserted at whatever point in the structure was reached before the path ran out, silently discarding the remaining path segments.',
      'An object with an empty "b": {} gets created automatically so the rest of the path has somewhere to attach new_value.',
      'PostgreSQL raises an error immediately, since jsonb_set can never tolerate any missing intermediate step in its path argument.',
      'target itself, completely unchanged, because every step of the path before its final segment must already exist in the document.',
    ],
    correctIndexes: [3],
    explanation: 'jsonb_set only ever creates or replaces the final item named by path; every step leading up to that final one must already be present in the document, or the call is a no-op that hands back the original target untouched.',
  },
  {
    id: 'querying-022',
    domain: 'querying',
    questionType: 'multiple-choice',
    question: 'The identical JSON text \'{"bar": "baz", "balance": 7.77, "active":false}\' is cast once to json and once to jsonb, and both are printed back out with no other processing. Why do the two printed results end up with different key order and spacing?',
    options: [
      'Both types are documented to always reprint the exact original input text, so any observed difference would indicate a bug.',
      'json keeps a literal copy of whatever text was supplied, preserving key order and whitespace, while jsonb reparses the input into a decomposed internal form and does not preserve those semantically-insignificant details.',
      'jsonb is actually the type that reprints the original input text completely unchanged, while json is the one that normalizes key order, since json is documented as the less compact, less storage-efficient of the two formats.',
      'Neither type preserves any part of the original input; both regenerate their output using a fixed, always-alphabetical key ordering.',
    ],
    correctIndexes: [1],
    explanation: 'json stores and echoes back exactly the text it was given, keys, spacing, and all, whereas jsonb decomposes that text into its own internal representation at write time, which is why reordered keys and stripped whitespace are semantically equal even though the printed text differs.',
  },
  {
    id: 'querying-023',
    domain: 'querying',
    questionType: 'multiple-response',
    question: 'Which of the following statements about PostgreSQL full-text search are accurate?',
    options: [
      "A GIN index built as CREATE INDEX ... USING GIN (to_tsvector('english', body)) will also be used by a query filtering on WHERE to_tsvector(body) @@ ..., since both calls invoke to_tsvector on the same source column.",
      'The <-> (FOLLOWED BY) tsquery operator only matches when its two arguments\' matches are adjacent and in the given order, which is the mechanism phraseto_tsquery relies on to build multi-word phrase queries.',
      'A tsquery is built straight from raw, unnormalized search text, and @@ compares that raw text directly against a document\'s original, unprocessed wording.',
      'GIN is the only index type PostgreSQL ever supports for accelerating full-text search; GiST cannot be used for this purpose at all.',
      'Because to_tsquery normalizes its search terms using the same rules to_tsvector applies to documents, a query built from the word "error" correctly matches a document that only contains the word "errors".',
    ],
    correctIndexes: [1, 4],
    explanation: 'FOLLOWED BY genuinely requires adjacency and order, and shared normalization is why a stemmed search term like error matches errors in the document; by contrast, an index built on the two-argument to_tsvector form is only used by queries written with that identical two-argument form (a bare to_tsvector(body) call cannot use it), tsquery terms must go through normalization rather than being compared as raw text, and GiST is also documented as usable for text search indexes alongside GIN.',
  },
  {
    id: 'querying-024',
    domain: 'querying',
    questionType: 'multiple-response',
    question: 'Which of the following statements about INSERT ... ON CONFLICT (upsert) are accurate?',
    options: [
      'Exclusion constraints are fully usable as arbiters for both ON CONFLICT DO NOTHING and ON CONFLICT DO UPDATE, exactly like unique indexes and unique constraints.',
      'ON CONFLICT DO UPDATE must name a conflict_target explicitly, while ON CONFLICT DO NOTHING may omit one, in which case it handles conflicts against any usable constraint or unique index on the table.',
      'A DEFERRABLE unique constraint can still serve as an arbiter for ON CONFLICT, since deferrability only changes when the constraint is checked, not whether it is eligible to arbitrate.',
      'Inside an ON CONFLICT DO UPDATE clause, the special EXCLUDED pseudo-table refers to the row that was proposed for insertion, so a clause like SET quantity = inventory.quantity + EXCLUDED.quantity can merge the new value into the row already on disk.',
      'ON CONFLICT DO UPDATE is guaranteed atomic: under concurrent activity, the outcome for a given row is always either a clean insert or a clean update, never a lost update and never a spurious duplicate-key error.',
    ],
    correctIndexes: [1, 3, 4],
    explanation: 'A conflict_target is mandatory for DO UPDATE but optional for DO NOTHING, EXCLUDED exposes the row that was about to be inserted for merge-style updates, and the whole operation is documented as atomic; by contrast, a constraint or index can arbitrate only when it is not deferrable, which rules out both deferrable unique constraints and, specifically for the DO UPDATE form, exclusion constraints altogether.',
  },

  // --- Index types: B-tree/Hash/GiST/GIN/BRIN (indexing-001..006) ---
  {
    id: 'indexing-001',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: 'Which set of query conditions can the planner satisfy using a B-tree index, per the documentation?',
    options: [
      'Only exact equality tests using the = operator; every other comparison always forces a full sequential scan of the table instead.',
      'Any LIKE pattern at all, no matter where its wildcard character happens to sit within the string, since B-tree performs full pattern matching internally.',
      "Comparisons using <, <=, =, >=, or >, plus BETWEEN/IN, IS NULL/IS NOT NULL, and a left-anchored LIKE pattern such as col LIKE 'foo%'.",
      'Containment tests, such as the @> and <@ operators used to check array or range membership.',
    ],
    correctIndexes: [2],
    explanation: "B-tree handles the ordering operators (<, <=, =, >=, >) plus constructs built from them, IS [NOT] NULL, and patterns anchored at the string's start such as 'foo%' — though outside the C locale that pattern-matching support requires building the index with a special operator class such as text_pattern_ops. A pattern with a leading wildcard, like '%bar', cannot use the index at all; containment operators like @> belong to GiST/GIN operator classes instead, and equality-only lookups describe Hash, not B-tree.",
  },
  {
    id: 'indexing-002',
    domain: 'indexing',
    questionType: 'multiple-response',
    question: 'Which of the following are accurate properties or limitations of PostgreSQL Hash indexes, per the documentation?',
    options: [
      'A hash index supports only single-column indexing; it cannot be built across more than one column of a table.',
      'A hash index can be declared UNIQUE, exactly like a B-tree index, since only GIN and BRIN indexes are restricted from enforcing uniqueness.',
      'A hash index can accelerate a range condition such as WHERE col > 100, in addition to plain equality.',
      'Because a hash index tuple stores only the 4-byte hash of the indexed value rather than the value itself, a hash index can be considerably smaller than a B-tree when indexing long values such as UUIDs.',
      'A hash index can satisfy an ORDER BY clause on the indexed column without requiring PostgreSQL to add a separate explicit sort step.',
    ],
    correctIndexes: [0, 3],
    explanation: 'The documentation restricts the ability to be declared unique to the B-tree access method, notes a hash index is limited to one column and the equality operator alone (no range queries), and says sorted-output ORDER BY support is a B-tree-only capability — but since a hash tuple stores just a 4-byte hash rather than the original value, it can be much smaller than a B-tree for long indexed values like UUIDs.',
  },
  {
    id: 'indexing-003',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: 'What kind of index access method is GiST, per the documentation?',
    options: [
      'A balanced tree access method that serves as a base template for schemes like B-trees and R-trees, also supporting nearest-neighbor <-> ordering searches.',
      'A structure that stores only a fixed-width digest of each indexed value, making it usable for equality lookups but nothing else, much like a hash index.',
      'An index type restricted to two-dimensional geometric data types only, with no mechanism at all for anyone to define additional custom operator classes.',
      'Unlike BRIN, a GiST index scan is always exact, and the executor never needs to recheck any candidate row against the original query condition.',
    ],
    correctIndexes: [0],
    explanation: "GiST is described as a generalized, balanced tree-structured access method that serves as infrastructure for many indexing strategies (B-trees, R-trees, and more) and supports nearest-neighbor searches via ordering operators; it is not limited to geometric types, and it is not always exact — the documentation's own polygon-containment example shows a GiST scan returning overlapping candidates that the executor must recheck, so lossy behavior is not unique to BRIN.",
  },
  {
    id: 'indexing-004',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: "How does a GIN index represent an indexed composite value, such as an array, per the documentation's description of it as a Generalized Inverted Index?",
    options: [
      "It stores one B-tree leaf entry per row, containing a complete serialized copy of that row's entire array value, much like a B-tree index built on a plain scalar column.",
      'It stores a single summary bitmap per block range and leaves all correctness checking entirely up to the query executor at scan time.',
      'It stores a hash of the entire composite value and can only test whether two given arrays are completely identical to one another.',
      'For each distinct key found across the indexed items, it keeps one (key, posting-list) entry whose posting list records every row ID containing that key.',
    ],
    correctIndexes: [3],
    explanation: "GIN is an inverted index: it stores one entry per distinct key extracted from the indexed items, with each entry's posting list recording every row containing that key, so a key shared by many rows is stored compactly rather than duplicated per row; the option describing a single per-block-range summary bitmap describes BRIN's block-range summaries, and the option describing a hash of the whole value that can only test for exact identity describes an equality-only hash-style structure, neither of which matches GIN.",
  },
  {
    id: 'indexing-005',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: "The documentation notes that BRIN indexes work best when a column's values correlate with the physical row order of the table. For which of these columns would a BRIN index be a poor structural fit?",
    options: [
      'An order_date column in a sales table, where earlier orders tend, in practice, to occupy earlier physical positions in the table.',
      'A randomly-generated UUID primary key with no relationship to insertion order.',
      'A ZIP code column where rows belonging to the same city are physically grouped together.',
      'A large, monotonically increasing bigint identity column whose values grow in the same order that rows are inserted.',
    ],
    correctIndexes: [1],
    explanation: 'BRIN summarizes each physical block range with min/max-style data, so it is only useful when the indexed value tracks physical storage order; a randomly-ordered UUID column has no such correlation, making BRIN a poor fit — that is the trap of assuming BRIN behaves like a smaller B-tree. The other columns (order date, grouped ZIP codes, a monotonic identity column) are exactly the kind of naturally-correlated data the documentation cites as good BRIN candidates.',
  },
  {
    id: 'indexing-006',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: 'Per the documentation, what must happen after a BRIN index scan identifies which block ranges might contain matching rows?',
    options: [
      'The executor must recheck each returned tuple against the actual query condition and discard non-matches, since BRIN summaries are lossy.',
      'Nothing further is required: BRIN summaries record the exact indexed value for every row, so every returned tuple is guaranteed to satisfy the condition.',
      'The table must first be reorganized with a CLUSTER command before any BRIN scan result can be trusted at all.',
      'A recheck is required only for range conditions like < or >; equality conditions against a BRIN index never require rechecking a row.',
    ],
    correctIndexes: [0],
    explanation: "BRIN scans return every tuple in a block range whenever the range's summary is consistent with the query condition, and the executor is documented as being in charge of rechecking those tuples and discarding non-matches — in the documentation's own words, these indexes are lossy. This recheck requirement is not limited to range conditions, and it isn't solved by clustering the table or contradicted by any claim that BRIN stores exact per-row values.",
  },

  // --- Partial & expression indexes (indexing-007..012) ---
  {
    id: 'indexing-007',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: "In the documentation's access_log example, an index on client_ip is created with a WHERE clause that excludes the organization's own internal IP subnet. What benefit does this partial index provide?",
    options: [
      'It guarantees client_ip values are globally unique across the entire table, serving as a substitute for a UNIQUE constraint.',
      'PostgreSQL requires every index on an inet-typed column to carry a WHERE clause, or the CREATE INDEX statement is rejected outright.',
      'Excluding a common value keeps the index smaller, speeding up both the searches that use it and its update maintenance.',
      'It lets a query for an address inside the excluded subnet still use this index, something a full, non-partial index could never do.',
    ],
    correctIndexes: [2],
    explanation: 'Common values already tend not to use an index, so omitting them from a partial index only shrinks the index (helping both lookups and maintenance) without losing any real benefit; the example is about size and update overhead, not uniqueness, and a query for an address inside the excluded subnet is exactly the case this partial index cannot serve, since that IP was deliberately left out.',
  },
  {
    id: 'indexing-008',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: "According to the documentation, when can a query actually use a partial index's predicate to be considered applicable?",
    options: [
      'Whenever the query touches any column of the table the partial index was built on, regardless of whether the WHERE clauses relate to each other at all.',
      "Only when the planner can recognize, at planning time, that the WHERE condition mathematically implies the index's predicate, such as x < 1 implying x < 2.",
      'Only at query execution time, when each candidate row is individually tested against both the query condition and the index predicate.',
      'PostgreSQL includes a general theorem prover capable of recognizing any two WHERE conditions that are logically equivalent, however differently they are phrased.',
    ],
    correctIndexes: [1],
    explanation: "The documentation explicitly denies having a general theorem prover, states that predicate matching happens at planning time (not execution time, and not via a per-row runtime test), and gives the \"x < 1 implies x < 2\" case as the kind of simple inequality it can recognize — explaining why a prepared statement's parameterized \"x < $1\" clause can never be matched to a predicate like \"x < 2\".",
  },
  {
    id: 'indexing-009',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: "Applying the documentation's partial-unique-index pattern to a hiring table, CREATE UNIQUE INDEX hires_once_idx ON job_applications (candidate_id, position_id) WHERE hired; accomplishes what?",
    options: [
      'It enforces uniqueness of the (candidate_id, position_id) pair across every single row of the table, no matter what value is stored in hired.',
      'It prevents the table from ever containing more than one single row where the hired column happens to be false.',
      'It behaves as a NOT NULL constraint on the hired column, rejecting any inserted row where hired is left null.',
      'It enforces uniqueness of (candidate_id, position_id) only among rows where hired is true; other rows are left unconstrained and may repeat freely.',
    ],
    correctIndexes: [3],
    explanation: 'A unique partial index only enforces uniqueness among the rows that satisfy its WHERE predicate; here that means only "hired" (candidate, position) combinations must be unique, while any number of not-hired applications for the same pairing are unconstrained — this mirrors the documentation\'s own subject/target "successful test" example, and it says nothing about NOT NULL and does not limit how many false rows can exist.',
  },
  {
    id: 'indexing-010',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: "The documentation warns against building one partial index per category value across a whole set of non-overlapping categories (mytable_cat_1, mytable_cat_2, ... mytable_cat_N), calling it a bad idea. What does it recommend instead, and why?",
    options: [
      'A single BRIN index instead, because BRIN summaries can inherently distinguish which category each block range belongs to.',
      'This exact pattern of one partial index per category is actually endorsed as best practice whenever there are more than three category values.',
      'A single ordinary multicolumn index, such as (category, data), which avoids the per-query planning overhead of testing each partial index.',
      'Merging all of the per-category partial indexes together into a single GIN index built over just the category column.',
    ],
    correctIndexes: [2],
    explanation: 'The documentation calls the per-category partial-index scheme a bad idea because PostgreSQL does not understand the relationship between the partial indexes and must test each one individually, and it recommends a single non-partial multicolumn index (category first) instead, reserving actual table partitioning for cases where even one index proves too large — nothing in the passage suggests BRIN or GIN as the fix.',
  },
  {
    id: 'indexing-011',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: 'What is the main trade-off of indexing an expression, such as CREATE INDEX accounts_lower_email_idx ON accounts (lower(email));, rather than indexing the plain column, per the documentation?',
    options: [
      'They cost a bit more to maintain, since the expression is recomputed on every insert or non-HOT update, but reads stay fast.',
      'The expression is recomputed by the planner every time a matching SELECT runs, which slows down reads compared to a plain column index.',
      'Expression indexes cannot be declared UNIQUE, so lower(email) could never be used to enforce case-insensitive uniqueness on a column.',
      'Parentheses around the indexed expression must always be omitted, even for a multi-argument computation like a concatenated full-name expression.',
    ],
    correctIndexes: [0],
    explanation: 'The documentation states expression indexes add write-time cost (recomputing the expression for every insert/non-HOT update) but not read-time cost, since the value is already stored in the index at search time; it also explicitly shows declaring such an index UNIQUE (using its own lower(col1) example) to enforce case-insensitive uniqueness, and says parentheses can be omitted only for a bare function call, not for arbitrary expressions like a concatenation.',
  },
  {
    id: 'indexing-012',
    domain: 'indexing',
    questionType: 'multiple-response',
    question: 'Which of the following statements about partial or expression indexes are accurate, per the documentation?',
    options: [
      'Declaring lower(col1) as a UNIQUE index would prevent inserting rows whose col1 values differ only in letter case, effectively enforcing a case-insensitive uniqueness rule.',
      'Because expression indexes store their computed values, the executor still has to recompute the underlying expression for every candidate row it inspects, so expression indexes never save any CPU time during query execution.',
      "A partial index's predicate expression may reference only columns belonging to the table being indexed; it cannot reference other tables.",
      'A partial index predicate must be a simple equality test; PostgreSQL does not support arbitrary boolean expressions as predicates.',
      'A column can be limited to holding just one null value by making a unique index partial, restricted with an IS NULL condition in its predicate.',
    ],
    correctIndexes: [0, 2, 4],
    explanation: 'The documentation confirms a UNIQUE index on lower(col1) blocks case-only duplicates, that partial-index predicates may only involve columns of the indexed table, and that restricting a unique partial index to an IS NULL condition can cap a column at a single null value — but it explicitly says expression values are not recomputed during an indexed search, which is why the option claiming expression indexes must recompute the expression for every candidate row and therefore never save CPU time is false, and it says PostgreSQL "supports partial indexes with arbitrary predicates" rather than equality only, which is why the option claiming a partial-index predicate must be a simple equality test is also false.',
  },

  // --- EXPLAIN and the cost model (indexing-013..018) ---
  {
    id: 'indexing-013',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: 'What is the fundamental difference between a plain EXPLAIN and an EXPLAIN ANALYZE, per the documentation and grounding notes?',
    options: [
      'EXPLAIN ANALYZE only works on SELECT statements, whereas plain EXPLAIN also works on INSERT, UPDATE, DELETE, and MERGE statements.',
      "Plain EXPLAIN only prints the planner's estimated plan without running the statement, while EXPLAIN ANALYZE actually executes it and reports true row counts and run times too.",
      'Plain EXPLAIN silently executes the query and discards its output rows, while EXPLAIN ANALYZE never actually runs the query and only estimates.',
      "EXPLAIN ANALYZE never triggers a data-modifying statement's real side effects; it only simulates them for inspection purposes only.",
    ],
    correctIndexes: [1],
    explanation: 'EXPLAIN alone estimates a plan without running it; EXPLAIN ANALYZE actually executes the statement, which is why the documentation recommends wrapping a data-modifying statement in BEGIN/ROLLBACK if you want to inspect its plan without keeping its side effects — those side effects genuinely happen, and EXPLAIN ANALYZE works on INSERT/UPDATE/DELETE/MERGE just as well as SELECT.',
  },
  {
    id: 'indexing-014',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: 'A table has 500 disk pages and 20000 rows. Using the documented sequential-scan cost formula (pages × seq_page_cost) + (rows × cpu_tuple_cost) with unmodified default settings (seq_page_cost = 1.0, cpu_tuple_cost = 0.01), what total cost would EXPLAIN report for a plain sequential scan of the whole table?',
    options: [
      '500, counting only the page-fetch term and ignoring the per-row term entirely.',
      '200, counting only the per-row term and ignoring the page-fetch term entirely.',
      '600, from mistakenly applying cpu_index_tuple_cost (0.005) instead of cpu_tuple_cost to the row count.',
      '700, from (500 × 1.0) + (20000 × 0.01) = 500 + 200.',
    ],
    correctIndexes: [3],
    explanation: 'Following the documented formula exactly: (500 pages × 1.0 seq_page_cost) + (20000 rows × 0.01 cpu_tuple_cost) = 500 + 200 = 700, matching how the tenk1 example (345 pages, 10000 rows) is shown arriving at cost 445.',
  },
  {
    id: 'indexing-015',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: 'What is the documented default value of random_page_cost relative to seq_page_cost, and what does that default reflect?',
    options: [
      "random_page_cost defaults to 4.0 against seq_page_cost's 1.0, lower than random access truly costs, since most random reads are assumed cached.",
      'random_page_cost defaults to 1.0, identical to seq_page_cost, on the assumption that all storage is solid-state and access patterns no longer matter.',
      'random_page_cost defaults to 10.0, deliberately set high to discourage the planner from ever choosing an index scan at all.',
      'Setting random_page_cost below seq_page_cost is rejected outright by the server as an invalid configuration value.',
    ],
    correctIndexes: [0],
    explanation: 'The documentation gives seq_page_cost a default of 1.0 and random_page_cost a default of 4.0, explicitly noting that plain random access is normally much more expensive than 4x sequential access but a lower default is used because most random reads are assumed to already be cached; it also notes the server will let you set random_page_cost below seq_page_cost even though doing so is not physically sensible, so that is not rejected outright.',
  },
  {
    id: 'indexing-016',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: "In the documentation's worked example, WHERE unique1 = 42 produces a plain Index Scan plan rather than a Bitmap Heap/Bitmap Index Scan combination. Why does the planner prefer the plain Index Scan here?",
    options: [
      'A plain Index Scan never needs to visit the table heap at all, so it is unconditionally cheaper than any bitmap-based plan whatsoever, in every case.',
      'Bitmap Heap Scan cannot be used to satisfy an equality condition; it is only ever chosen for range conditions such as < or > instead.',
      'So few rows match that skipping the bitmap-build-and-sort step outweighs the extra cost of fetching those few rows directly in index order.',
      'The planner automatically disables all bitmap-scan plans whenever a query happens to have no LIMIT clause attached to it.',
    ],
    correctIndexes: [2],
    explanation: 'The documentation notes that fetching rows in index order is more expensive per row, but when so few rows qualify, that extra cost is smaller than the overhead of building and sorting a bitmap — this is why plain Index Scan plans are common for single-row lookups and for queries whose ORDER BY matches the index; a plain Index Scan can still require heap visits (it is not an index-only scan by default), and bitmap scans are used for equality predicates elsewhere in the same set of examples.',
  },
  {
    id: 'indexing-017',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: "Per the documentation's caveats on EXPLAIN ANALYZE, why might costs and timings measured on a small, 'toy-sized' table be misleading if extrapolated to a much larger table?",
    options: [
      'EXPLAIN ANALYZE always silently reuses cached statistics from whichever other table in the database happens to be the largest one.',
      'Cost estimates are nonlinear, so the chosen plan can differ by table size; a one-page table will nearly always get a sequential scan regardless of indexes.',
      'EXPLAIN ANALYZE rounds every reported cost to the nearest whole disk-page fetch, which hides any effect table size might have.',
      'Table size has no bearing whatsoever on which plan the planner selects; only the width of each row affects the chosen plan.',
    ],
    correctIndexes: [1],
    explanation: "The documentation explicitly warns that results should not be extrapolated to a much different situation because planner cost estimates are nonlinear, and gives the one-page table as the extreme case where a sequential scan nearly always wins regardless of indexes — there is no statement about reusing another table's statistics or rounding costs to whole pages.",
  },
  {
    id: 'indexing-018',
    domain: 'indexing',
    questionType: 'multiple-response',
    question: 'Which of the following statements about the EXPLAIN command and its ANALYZE option are accurate, per the documentation?',
    options: [
      "Running EXPLAIN ANALYZE on an UPDATE statement is always safe and never changes the table's data, even without wrapping it in a transaction.",
      'GENERIC_PLAN can be combined with ANALYZE to actually execute a generic plan built from parameter placeholders.',
      'The Planning Time reported by EXPLAIN ANALYZE includes the time spent parsing and rewriting the query, in addition to planning it.',
      "EXPLAIN ANALYZE reports both the planner's cost estimates and the actual measured row counts and run times observed for each plan node.",
      'The BUFFERS option is automatically turned on whenever ANALYZE is used, though it can be explicitly disabled if that output is not wanted.',
    ],
    correctIndexes: [3, 4],
    explanation: "EXPLAIN ANALYZE genuinely reports both estimated and actual figures, and BUFFERS is implicitly enabled by ANALYZE but can be turned off explicitly — however, the documentation warns that EXPLAIN ANALYZE really does execute a data-modifying statement (recommending BEGIN/ROLLBACK if you don't want the change kept), states GENERIC_PLAN cannot be combined with ANALYZE, and states Planning Time excludes parsing and rewriting.",
  },

  // --- Multicolumn indexes, index-only scans, combining indexes, and performance pitfalls (indexing-019..024) ---
  {
    id: 'indexing-019',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: 'For a multicolumn B-tree index defined as (major, minor), which statement about how effectively it can be used is accurate per the documentation?',
    options: [
      'The index is equally efficient no matter which of its columns the query constrains, exactly like a GIN or BRIN index would be instead.',
      'The index can be used only when the query supplies an equality constraint on every single one of its columns at the same time.',
      'Constraints on columns to the right of the leading columns are ignored entirely by the index and always force a separate filter against the table itself.',
      'It works on any subset of its columns, but is most efficient with constraints on the leading (leftmost) columns, which limit the range scanned.',
    ],
    correctIndexes: [3],
    explanation: "A B-tree index spanning several columns remains usable when a query constrains only some of them, but leading-column equality constraints (plus a trailing inequality) are what narrow the scanned range; trailing-column constraints are still checked inside the index (saving table visits) even though they don't necessarily shrink the scanned range — this leading-column sensitivity is explicitly contrasted with GIN and BRIN, whose effectiveness doesn't depend on which column is constrained.",
  },
  {
    id: 'indexing-020',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: 'A table has separate single-column B-tree indexes on x and on y, with no combined index. Per the documentation, how can PostgreSQL satisfy WHERE x = 5 AND y = 6 using both of these separate indexes?',
    options: [
      "It scans each index into its own in-memory bitmap, ANDs the bitmaps together, and then visits the resulting table rows, losing either index's ordering.",
      'PostgreSQL can only ever use one of the two indexes for a single given query, applying the other condition only as a plain filter on the table.',
      'Combining indexes this way always preserves the row order of whichever single index has the lower estimated cost of the two.',
      'A single index scan directly applies clauses joined by AND across two entirely different indexes without any intermediate bitmap step.',
    ],
    correctIndexes: [0],
    explanation: "The documentation describes exactly this bitmap AND/OR combination mechanism: each index is scanned into its own in-memory bitmap, the bitmaps are combined as the query requires, and because the resulting table rows are then visited in physical order (not either index's order), any ORDER BY still needs an explicit sort — a single index scan can only use clauses on its own indexed columns, not clauses spread across two separate indexes.",
  },
  {
    id: 'indexing-021',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: 'What determines whether an index-only scan can avoid visiting the table heap for a given candidate row, even when the query references only indexed columns?',
    options: [
      'Whether VACUUM FULL has ever been run against that particular table at any earlier point in its history.',
      'Whether the index in question happened to be originally built using the CONCURRENTLY option at creation time.',
      "The table's visibility map bit for that row's heap page, which records whether every row on the page is old enough to be visible to all transactions.",
      'Whether the query happens to be written using SELECT * rather than explicitly naming each of the individual columns it needs.',
    ],
    correctIndexes: [2],
    explanation: "Even when a query only needs indexed columns, PostgreSQL must still confirm each row is visible under MVCC; the visibility map tracks, per heap page, whether every row on that page is old enough to be visible to all transactions, and an index-only scan checks that bit before deciding whether it can skip the heap — VACUUM FULL history and the CONCURRENTLY build option aren't the documented mechanism, and naming only indexed columns is a separate prerequisite from the visibility-map check itself.",
  },
  {
    id: 'indexing-022',
    domain: 'indexing',
    questionType: 'multiple-choice',
    question: 'What is the effect of adding total as an INCLUDE column, as in CREATE INDEX orders_customer_idx ON orders(customer_id) INCLUDE (total);, compared to making total an ordinary trailing key column as in orders(customer_id, total)?',
    options: [
      "INCLUDE columns are indexed exactly like key columns and participate fully in both uniqueness enforcement and the index's overall sort ordering too.",
      "total becomes non-key 'payload', usable for index-only scans but not part of the search key, and a UNIQUE index would enforce uniqueness on customer_id alone.",
      'Adding total as an INCLUDE column always makes the index smaller than the equivalent plain multicolumn index orders(customer_id, total) would have been.',
      'Expressions are supported as INCLUDE columns just as freely as they are already supported as ordinary key columns.',
    ],
    correctIndexes: [1],
    explanation: "The documentation is explicit that an INCLUDE column is payload only — not part of the search key, not required to match an operator class, and excluded from a UNIQUE index's uniqueness condition — while also warning that non-key payload columns duplicate data and bloat index size rather than shrink it, and that INCLUDE does not currently accept an expression as one of its columns.",
  },
  {
    id: 'indexing-023',
    domain: 'indexing',
    questionType: 'multiple-response',
    question: 'Which of the following statements about index operator classes and index collations are accurate, per the documentation?',
    options: [
      'A single B-tree index column can support multiple collations simultaneously, so one index suffices regardless of which COLLATE clause a query specifies.',
      'An index column can specify an operator class, and a major reason multiple operator classes exist for one data type is that there can be more than one meaningful behavior for that type, such as ordering a complex-number type by magnitude in one class and by its real component in another.',
      "The text_pattern_ops family of operator classes compares values strictly character by character, which makes it usable for pattern-matching queries when the database isn't using the C locale, but ordinary <, <=, >, >= comparisons still need an index built with the default operator class.",
      'When the database uses the C locale, the xxx_pattern_ops operator classes become mandatory for any pattern-matching query to use an index.',
      'An operator family can group operator classes for different data types together, which is what enables cross-data-type operators to work with indexes even though those operators are not tied to any single class within the family.',
    ],
    correctIndexes: [1, 2, 4],
    explanation: 'The documentation confirms operator classes exist because a type can have more than one meaningful indexed behavior, that xxx_pattern_ops classes do strict character comparison useful outside the C locale (while ordinary inequality comparisons still need a default-opclass index), and that operator families let cross-data-type operators participate in indexing — but it also states an index supports only one collation per column (so multiple collations need multiple indexes) and that the C locale specifically makes xxx_pattern_ops unnecessary, not mandatory.',
  },
  {
    id: 'indexing-024',
    domain: 'indexing',
    questionType: 'multiple-response',
    question: "Which of the following are accurate recommendations from the documentation's guidance on examining and tuning index usage?",
    options: [
      "Testing with a very small data set is safe as long as the row count isn't zero, because the planner's cost estimates scale linearly with table size.",
      'Always run ANALYZE first: without it, the planner falls back to inaccurate default assumptions, making any conclusions drawn about index usage a lost cause.',
      'Temporarily disabling planner switches such as enable_seqscan can help confirm whether an index is being skipped for a fundamental reason, such as the query condition simply not matching the index at all.',
      'Inserting test data in sorted order, or making it completely random, produces the same planner statistics that a real production data distribution would.',
      'If forcing index usage through planner switches makes a query run faster, that alone proves the original cost estimates were accurate and no further investigation is warranted.',
    ],
    correctIndexes: [1, 2],
    explanation: "The documentation calls skipping ANALYZE a 'lost cause' for tuning index usage and recommends toggling switches like enable_seqscan to diagnose why an index isn't chosen — but it separately warns that tiny test data sets are especially misleading (a table small enough to fit one disk page beats any index plan regardless), that sorted or fully random test data skews statistics away from a real distribution, and that a faster forced-index run only means one of two things (the planner was right to avoid the index, or its cost estimates are off) rather than proving the estimates were correct.",
  },

  // --- MVCC Fundamentals & Snapshots (transactions-001..005) ---
  {
    id: 'transactions-001',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: "What is the main advantage the documentation gives for PostgreSQL's MVCC model over a purely lock-based approach to concurrency control?",
    options: [
      'It removes any need for table- or row-level locking facilities, since every conflict is resolved automatically by comparing row versions.',
      'Locks taken for reading and locks taken for writing never conflict with each other, so readers never block writers and writers never block readers.',
      'It guarantees every session always operates on the single newest committed version of a row, discarding older versions as soon as a newer one exists.',
      'It requires every query to take out an explicit table lock before running, but resolves those locks faster than a traditional locking database would.',
    ],
    correctIndexes: [1],
    explanation: "MVCC's documented advantage is exactly that read-acquiring locks and write-acquiring locks are non-conflicting, so reads and writes never block each other, and PostgreSQL preserves that even under Serializable Snapshot Isolation — but table- and row-level locking facilities are still explicitly offered for cases where MVCC alone isn't the right fit, a query sees a frozen snapshot rather than always the newest row version, and MVCC's whole point is to avoid the need for query-time table locks, not to speed up their resolution.",
  },
  {
    id: 'transactions-002',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: 'TRUNCATE, along with any ALTER TABLE variant that rewrites the table, is described in the documentation as not being safe under MVCC. Under what specific circumstance does the documentation say this will actually cause a concurrent transaction to see the affected table as unexpectedly empty?',
    options: [
      'Whenever the concurrent transaction happens to be running under the default Read Committed isolation level at the moment the DDL commits.',
      'Whenever the concurrent transaction is explicitly querying the system catalogs rather than the table itself, regardless of when it started.',
      'Only when the DDL command is issued by a superuser, since ordinary roles are prevented from rewriting a table that other sessions are using.',
      "Only for a transaction that hadn't accessed the table before the DDL started — one that had would already hold a blocking lock.",
    ],
    correctIndexes: [3],
    explanation: 'The caveat is narrowly scoped: a transaction that already accessed the target table before the DDL began would hold at least an ACCESS SHARE lock, which blocks TRUNCATE/table-rewriting ALTER TABLE from proceeding until that transaction finishes — so the empty-table surprise can only hit a transaction using an older snapshot that never touched the table beforehand. This has nothing to do with the isolation level of the concurrent transaction, is unrelated to whether that transaction is examining system catalogs, and has no documented dependency on who issued the DDL.',
  },
  {
    id: 'transactions-003',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: "A table is created by transaction A. Transaction B, running Repeatable Read, had already begun (and taken its snapshot) before A committed. Per the documentation, why can B immediately reference the new table by name, even though B cannot see any rows in it that were inserted after B's snapshot was taken?",
    options: [
      'Internal catalog lookups ignore the current isolation level, so the table resolves normally, though its row contents still obey normal snapshot rules.',
      'Repeatable Read automatically takes a fresh snapshot the instant any DDL command commits elsewhere in the database.',
      'CREATE TABLE takes an ACCESS EXCLUSIVE lock that forces every other open transaction to immediately refresh its snapshot before it can continue.',
      'Queries that explicitly select from system catalog tables such as pg_class also bypass the current isolation level, so they too would show the new table immediately.',
    ],
    correctIndexes: [0],
    explanation: "The documentation attributes this to internal system-catalog access being exempt from the current transaction's isolation level — the object itself resolves normally even though its row contents obey normal snapshot rules. Repeatable Read does not re-snapshot mid-transaction whenever some unrelated DDL command commits, no lock forces other sessions to refresh their snapshots, and an explicit query against a catalog table like pg_class is the contrasting case described in the documentation — those queries do respect the isolation level and won't show a concurrently created object at the higher isolation levels.",
  },
  {
    id: 'transactions-004',
    domain: 'transactions',
    questionType: 'multiple-response',
    question: "Which of the following statements about PostgreSQL's documented MVCC caveats are accurate?",
    options: [
      'TRUNCATE and any ALTER TABLE variant that rewrites the table fail to be MVCC-safe.',
      'An explicit query against the system catalogs, such as directly selecting from pg_class, will show rows for objects that were created by a transaction that committed after the querying transaction took its snapshot, even under Serializable.',
      'A transaction whose snapshot predates a concurrent TRUNCATE will nevertheless see the table as empty once that TRUNCATE commits, provided it had not already accessed the table beforehand.',
      "A newly created table's row contents are exempt from normal snapshot rules in the same way the table's existence is, so concurrent Repeatable Read transactions can see rows inserted into it immediately.",
      'A Repeatable Read transaction running on a hot standby can occasionally observe a transient state that would be inconsistent with any serial ordering of the transactions committed on the primary.',
    ],
    correctIndexes: [0, 2, 4],
    explanation: "Three of these are stated directly in the caveats material: TRUNCATE/table-rewriting ALTER TABLE break MVCC-safety, a snapshot taken before a concurrent TRUNCATE offers no protection once that TRUNCATE commits (the truncation becomes visible to the older snapshot — only a transaction that already accessed the table, and therefore holds a lock blocking the DDL, avoids this), and a standby's Repeatable Read transaction can see a transient state inconsistent with any serial execution on the primary. The claim about explicit catalog queries is backwards — the documentation says such queries do NOT see rows for concurrently created objects at the higher isolation levels; that exemption applies only to internal catalog lookups. Likewise the claim about a new table's contents is backwards — only the object's existence gets the exception; the rows it contains still follow ordinary snapshot visibility.",
  },
  {
    id: 'transactions-005',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: 'Given that proper use of MVCC generally outperforms locking, why does the documentation say PostgreSQL still offers table- and row-level locking facilities at all?',
    options: [
      'Because MVCC cannot by itself prevent dirty reads at the Read Committed isolation level without supplemental row locking.',
      'Because advisory locks depend internally on the table- and row-level locking subsystem in order to function at all.',
      "For applications that don't need full isolation and would rather handle a few conflict points explicitly themselves.",
      'Because without explicit locks, ordinary SELECT statements could block concurrent UPDATE statements on the same rows.',
    ],
    correctIndexes: [2],
    explanation: "The documentation states plainly that these facilities exist for applications that don't generally need full isolation and prefer explicit conflict management, while still noting MVCC alone usually performs better. Dirty reads are impossible at Read Committed regardless of any supplemental locking (PostgreSQL never allows them at any level), advisory locks are independent, application-defined locks rather than a layer built on the table/row lock subsystem, and plain SELECT never blocks a concurrent UPDATE under ordinary MVCC behavior.",
  },

  // --- Isolation Levels & the SQL Standard (transactions-006..011) ---
  {
    id: 'transactions-006',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: "A session runs SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED expecting to be able to observe another transaction's uncommitted writes. What actually happens?",
    options: [
      'The command is rejected with a syntax error, since READ UNCOMMITTED is not one of the isolation levels PostgreSQL recognizes.',
      'The request is accepted, but internally PostgreSQL treats it identically to Read Committed, so dirty reads remain impossible.',
      'The session silently gets Serializable behavior instead, since that is the only level PostgreSQL considers "safe enough" to substitute.',
      'The request is honored exactly as written, and the session can now see uncommitted data from any other concurrently running transaction.',
    ],
    correctIndexes: [1],
    explanation: 'PostgreSQL only implements three isolation levels internally, and a request for Read Uncommitted is quietly mapped onto Read Committed behavior — dirty reads stay impossible no matter which of the four standard levels is requested. The keyword itself is accepted without any syntax error, the fallback target is Read Committed rather than Serializable, and the request is not honored as literally written.',
  },
  {
    id: 'transactions-007',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: "The SQL standard only requires that Repeatable Read prevent dirty reads and nonrepeatable reads, leaving phantom reads possible. What does the documentation say about PostgreSQL's actual Repeatable Read implementation?",
    options: [
      "PostgreSQL's Repeatable Read is deliberately weakened to allow phantom reads, exactly matching the standard's minimum requirement and nothing more.",
      "PostgreSQL's Repeatable Read also prevents serialization anomalies, making it behave identically to Serializable in every respect.",
      "PostgreSQL flags this as a bug still awaiting a fix in a future release, since exceeding the standard's requirements is considered a deviation.",
      "PostgreSQL's Repeatable Read also prevents phantom reads — a stronger guarantee than the standard requires, and one it explicitly permits.",
    ],
    correctIndexes: [3],
    explanation: "The documentation is explicit that PostgreSQL's Repeatable Read blocks every phenomenon in the standard's table except serialization anomalies — including phantom reads, which the standard itself does not require at that level — and calls this acceptable because the standard defines only minimum, not maximum, protections. It is not deliberately weakened to match the bare minimum, serialization anomalies remain possible under Repeatable Read (only Serializable rules those out), and exceeding the standard is treated as an accepted design choice, not a bug.",
  },
  {
    id: 'transactions-008',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: "Under Read Committed, an UPDATE's WHERE clause targets a row that a different transaction already modified and committed while the UPDATE was waiting. Per the documentation, what happens once the wait ends?",
    options: [
      'The waiting UPDATE is unconditionally skipped for that row, on the theory that some other transaction already handled it.',
      'The waiting transaction is immediately rolled back with a serialization error, the same outcome Repeatable Read would produce in this situation.',
      "The WHERE clause is re-evaluated against the row's newly updated version, and if it still matches, the waiting UPDATE proceeds using that version.",
      'The waiting UPDATE keeps blocking until every other transaction in the entire session pool has committed or rolled back, not just the one that modified the row.',
    ],
    correctIndexes: [2],
    explanation: "Read Committed re-checks the search condition against the row's latest committed version once the blocking transaction finishes, and proceeds against that updated version if it still qualifies (unless the row was deleted, in which case the row is simply ignored). It does not unconditionally skip the row, it does not throw the concurrent-update serialization error that only Repeatable Read produces, and it only waits on the specific transaction holding the conflicting lock, not on every other session.",
  },
  {
    id: 'transactions-009',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: "A Repeatable Read transaction tries to UPDATE a row that another transaction already modified and committed after the Repeatable Read transaction's snapshot was taken. What does the documentation say happens?",
    options: [
      'The transaction is rolled back with an error, and the application is expected to retry the entire transaction from the beginning.',
      'The update is quietly applied against the newer, already-committed version of the row, the same as it would be under Read Committed.',
      'The transaction blocks forever on that row, since Repeatable Read never times out and never raises a concurrent-update error.',
      'The exact same outcome can happen to a read-only Repeatable Read transaction that never issues any UPDATE or DELETE itself.',
    ],
    correctIndexes: [0],
    explanation: "Repeatable Read waits for the first updater to finish and then, if that updater committed a real change to the row, aborts the waiting transaction with an error reporting that access couldn't be serialized because of the concurrent update, rather than silently applying the change — the application must then retry the whole transaction, which will pick up the already-committed change as part of its fresh initial view. Applying the update against the newer row silently is Read Committed behavior, not Repeatable Read; the wait does end (in an error) rather than hanging forever; and the documentation is explicit that only updating transactions can hit this error, read-only transactions never encounter serialization conflicts.",
  },
  {
    id: 'transactions-010',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: 'Per the performance guidance for Serializable transactions, why might lowering random_page_cost or raising cpu_tuple_cost reduce the rate of serialization failures?',
    options: [
      "It causes PostgreSQL's predicate-locking mechanism to switch from non-blocking monitoring to ordinary blocking locks, which conflict less often in practice.",
      'It turns off predicate locking altogether for any table the planner decides to scan sequentially, removing the monitoring entirely for that table.',
      'It automatically converts every Serializable transaction touching that table into a Repeatable Read transaction, which does not monitor for read/write dependencies.',
      'It nudges the planner toward index scans over sequential scans, and a sequential scan always needs a relation-level predicate lock, raising failure odds.',
    ],
    correctIndexes: [3],
    explanation: "The documentation ties this tuning advice directly to the fact that a sequential scan always necessitates a relation-level (rather than finer-grained) predicate lock, so encouraging index scans via these two cost parameters can reduce serialization-failure rates. Predicate locks never become ordinary blocking locks — they are non-blocking by design regardless of these settings — cost-parameter tuning doesn't disable predicate locking outright, and it has no effect on which isolation level a transaction is actually running under.",
  },
  {
    id: 'transactions-011',
    domain: 'transactions',
    questionType: 'multiple-response',
    question: 'Which of the following statements about Serializable transactions and replication are accurate per the documentation?',
    options: [
      "A Serializable transaction run directly on a hot standby server can never see a state inconsistent with a serial execution of the primary's transactions, because Serializable is fully supported there.",
      "The integrity protection that Serializable transactions provide hasn't yet been extended to cover either hot standby mode or logical replicas.",
      'Performing all permanent writes within Serializable transactions on the primary guarantees that a standby can never show any transient inconsistency, even before it has fully caught up.',
      'The strictest isolation level currently supported when connected directly to a hot standby server is Repeatable Read.',
      'Applications relying on a hot standby or a logical replica for consistency checks may be better off using Repeatable Read together with explicit locking on the primary instead.',
    ],
    correctIndexes: [1, 3, 4],
    explanation: 'The documentation states directly that Serializable\'s integrity guarantee doesn\'t extend to hot standby or logical replicas, that Repeatable Read is the strictest level currently supported when connected to a hot standby, and that Repeatable Read with explicit locking on the primary is the suggested alternative for those relying on standbys or logical replicas. The claim that Serializable is fully supported on a hot standby is false — it cannot even be requested there. The claim about writing everything through Serializable on the primary is also false: the documentation only says this makes standbys "eventually" reach a consistent state, while separately warning that a standby can see a transient state inconsistent with any serial ordering in the meantime.',
  },

  // --- Locking Mechanics: Table, Row & Advisory (transactions-012..017) ---
  {
    id: 'transactions-012',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: 'Table-level lock modes such as ROW SHARE and ROW EXCLUSIVE include the word "row" in their names. What does the documentation say about this naming?',
    options: [
      'These modes escalate automatically to true row-level locks once the number of affected rows crosses an internal threshold.',
      'Despite "row" in the name, these remain table-level locks; what actually distinguishes them is simply which modes conflict.',
      'They share the same underlying lock table used to track FOR UPDATE and FOR SHARE row-level locks.',
      'They are reserved names that a session can additionally acquire manually only through the LOCK TABLE command, never automatically.',
    ],
    correctIndexes: [1],
    explanation: 'The documentation is explicit that all of these are table-level locks regardless of "row" appearing in the mode name, and that the real distinguishing factor across modes is purely the set of other modes each one conflicts with. There is no documented escalation from a table lock to a row-granularity lock, they are tracked separately from the row-level lock table used for FOR UPDATE/FOR SHARE, and several of them (like ROW EXCLUSIVE, acquired automatically by UPDATE/DELETE/INSERT/MERGE) are routinely taken without any explicit LOCK TABLE command.',
  },
  {
    id: 'transactions-013',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: "According to the documentation's tip on table-level locks, which single lock mode is capable of blocking a plain SELECT statement that has no FOR UPDATE/FOR SHARE clause?",
    options: [
      'ONLY ACCESS EXCLUSIVE — the mode acquired by commands like DROP TABLE, TRUNCATE, and VACUUM FULL.',
      'EXCLUSIVE, because it conflicts with every mode besides ACCESS SHARE, so it necessarily blocks a plain read too.',
      'SHARE ROW EXCLUSIVE, because it is self-exclusive and only one session can hold it on a table at a time.',
      'ROW EXCLUSIVE, because it is the mode routinely acquired by UPDATE, DELETE, INSERT, and MERGE.',
    ],
    correctIndexes: [0],
    explanation: "The documentation's tip states plainly that only an ACCESS EXCLUSIVE lock blocks a plain SELECT. The EXCLUSIVE-mode distractor is actually the opposite of the truth: EXCLUSIVE is explicitly described as allowing concurrent ACCESS SHARE locks, meaning ordinary reads can still proceed alongside it. SHARE ROW EXCLUSIVE conflicts with ROW EXCLUSIVE and several other modes but not with the ACCESS SHARE lock a plain SELECT takes, and ROW EXCLUSIVE (the mode a writer itself takes) likewise leaves ACCESS SHARE unblocked.",
  },
  {
    id: 'transactions-014',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: 'Per the row-level lock conflict table, which other row-level lock mode does a plain FOR KEY SHARE lock actually conflict with?',
    options: [
      'It conflicts with FOR SHARE and FOR NO KEY UPDATE as well, since both are considered stronger than a plain read.',
      "It blocks any concurrent UPDATE or DELETE whatsoever, even one that touches none of the row's key columns.",
      "Only FOR UPDATE — a plain FOR KEY SHARE doesn't block FOR SHARE, FOR NO KEY UPDATE, or another FOR KEY SHARE.",
      'It is self-conflicting, so a second session requesting FOR KEY SHARE on the same already-locked row must wait.',
    ],
    correctIndexes: [2],
    explanation: 'The row-level conflict table shows FOR KEY SHARE conflicting with nothing except FOR UPDATE. It does not conflict with FOR SHARE or FOR NO KEY UPDATE, so the claim that it blocks either of those is false; the description text specifically says a key-shared lock blocks DELETE and any key-changing UPDATE (both of which acquire the FOR UPDATE mode) but not other UPDATE commands, so "any concurrent UPDATE or DELETE whatsoever" overstates it; and the table shows no conflict between two FOR KEY SHARE requests, so it is not self-conflicting.',
  },
  {
    id: 'transactions-015',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: 'What distinguishes a session-level advisory lock from a transaction-level advisory lock, per the documentation?',
    options: [
      'A session-level lock is automatically released the moment the transaction that acquired it commits, exactly like a transaction-level lock.',
      'A transaction-level lock also requires the session to explicitly call a matching unlock function before it will ever be released, exactly like a session-level lock does.',
      'Both a session-level lock and a transaction-level lock are immediately released as soon as the transaction that acquired them is rolled back.',
      'A session-level lock survives a rollback and lasts until unlocked or the session ends; a transaction-level lock releases automatically at transaction end.',
    ],
    correctIndexes: [3],
    explanation: 'The documentation states that a session-level advisory lock ignores transaction semantics entirely — it persists through a rollback and only goes away via an explicit unlock call or the session ending — whereas a transaction-level lock behaves like an ordinary lock and is released automatically at transaction end. So a session-level lock is not tied to transaction commit, a transaction-level lock has no explicit unlock operation to call, and a rollback does not clear a session-level lock the way it would a transaction-level one.',
  },
  {
    id: 'transactions-016',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: 'The documentation warns against calling an advisory-lock function directly inside a query whose row set is restricted with ORDER BY and LIMIT. Why is that combination dangerous?',
    options: [
      'PostgreSQL raises a syntax error whenever an advisory-lock function call appears in the same SELECT list as a LIMIT clause.',
      "There's no guarantee the LIMIT gets enforced before the lock-acquiring function runs, so extra rows can end up locked and left dangling.",
      'The locks taken this way are automatically released the moment the query finishes executing, so no dangling-lock cleanup is ever needed.',
      'Wrapping the same advisory-lock call inside an inner subquery that applies the LIMIT before the outer query runs makes no real difference to this hazard.',
    ],
    correctIndexes: [1],
    explanation: "The hazard the documentation describes is exactly an evaluation-order problem: there's no guarantee the LIMIT gets enforced ahead of when the locking function actually runs, so more rows than intended can get locked, and those extra locks stay dangling (though still visible in pg_locks) until the session ends. No syntax error is raised for this pattern, the resulting locks are explicitly described as persisting rather than auto-releasing at query end, and pushing the LIMIT into an inner subquery before the lock call is the documented fix, not an ineffective one.",
  },
  {
    id: 'transactions-017',
    domain: 'transactions',
    questionType: 'multiple-response',
    question: 'Which of the following statements about the pg_locks system view are accurate per the documentation?',
    options: [
      'Row-level locks normally do not show up as rows in pg_locks, because information about them is tracked on disk rather than kept in memory.',
      'pg_blocking_pids() is recommended over self-joining pg_locks against itself to work out which processes are blocking a given waiting process.',
      'A relation that nothing is currently holding or waiting on will still show up as a row in pg_locks, just with its granted column set to false.',
      'Locktype values such as advisory identify rows in the view that represent application-defined advisory locks rather than locks on relations.',
      "When the view is queried, fast-path lock data and the rest of the regular lock manager's data are always captured together as one single atomic snapshot.",
    ],
    correctIndexes: [0, 1, 3],
    explanation: "The documentation confirms three of these: row-level lock information lives on disk rather than in the in-memory lock table (so it normally doesn't appear as its own pg_locks row), pg_blocking_pids() is recommended precisely because a self-join is hard to get right, and the locktype column includes an advisory value for exactly this kind of lock. The claim about an unlocked object appearing anyway is false — the documentation says such an object won't appear in the view at all. The claim about one single atomic snapshot is also false — fast-path lock data is gathered backend-by-backend without freezing the whole lock manager first, so it is explicitly not captured together with the rest as one atomic action.",
  },

  // --- Deadlocks & Serialization Failure Handling (transactions-018..022) ---
  {
    id: 'transactions-018',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: 'What is the documented effect of increasing the deadlock_timeout setting?',
    options: [
      'It causes the server to check for a deadlock more frequently while a process is waiting on a lock.',
      'It eliminates the possibility of deadlocks occurring once the value is set high enough.',
      'It cuts down on needless deadlock checks (since the check is relatively expensive) but slows how quickly a genuine deadlock gets reported.',
      'It only changes how long the server waits before logging a lock-wait message under log_lock_waits, with no bearing on the deadlock check itself.',
    ],
    correctIndexes: [2],
    explanation: 'The documentation frames deadlock_timeout as a trade-off: because the deadlock check itself is relatively expensive, waiting longer before running it avoids needless checks in the common case where no deadlock exists, at the cost of a slower real-deadlock report. Raising the value makes the server check less often, not more; it changes only the timing of detection, not whether deadlocks can occur at all; and it directly gates the deadlock check itself, with the log_lock_waits message timing being a secondary effect that piggybacks on the same setting rather than the only thing it controls.',
  },
  {
    id: 'transactions-019',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: 'What does the documentation recommend as the best general defense against deadlocks?',
    options: [
      'Have every application lock shared objects in the same consistent order, taking the strongest mode actually needed as the first lock on each.',
      'Rely entirely on the automatic deadlock detector and give no thought to the order in which locks are acquired, since PostgreSQL always resolves the situation cleanly.',
      'Always request an ACCESS EXCLUSIVE lock first in every transaction, regardless of what that transaction will actually do to the object.',
      'Switch every transaction to Serializable isolation, since the predicate locks Serializable relies on can never be part of a deadlock cycle.',
    ],
    correctIndexes: [0],
    explanation: 'The documentation frames avoidance, not just reactive detection, as the best defense: get all applications to lock shared objects in a consistent order, and take the most restrictive mode actually needed as the first lock on an object. Leaning solely on the detector isn\'t the recommended posture — it resolves deadlocks by aborting a transaction, which is disruptive, not something to lean on instead of avoidance. Requesting ACCESS EXCLUSIVE unconditionally overshoots the "most restrictive mode actually needed" guidance. And while it\'s true that Serializable\'s SIRead predicate locks can never contribute to a deadlock, ordinary row- and table-level locks used within a Serializable transaction still can, so switching isolation levels alone isn\'t the documented fix.',
  },
  {
    id: 'transactions-020',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: 'When PostgreSQL detects a deadlock between two transactions, what does the documentation say it does?',
    options: [
      'It always aborts whichever of the two transactions most recently requested the conflicting lock.',
      'It queues the newer request indefinitely and waits for a database administrator to intervene manually.',
      "It downgrades one of the two transactions' lock requests to a weaker, non-conflicting mode, letting both transactions continue instead of aborting either one.",
      "It automatically aborts one of the transactions, letting the other proceed — but which one gets picked is hard to foresee and shouldn't be relied on.",
    ],
    correctIndexes: [3],
    explanation: "The documentation says PostgreSQL automatically resolves a detected deadlock by aborting one of the transactions and letting the rest continue, explicitly cautioning that which transaction gets aborted is hard to predict and shouldn't be depended on. There's no rule tying the choice to which lock request came most recently, no manual-intervention requirement, and no lock-downgrading mechanism described as an alternative to aborting a transaction.",
  },
  {
    id: 'transactions-021',
    domain: 'transactions',
    questionType: 'multiple-choice',
    question: 'How does the documentation say an application should treat a unique-key violation (SQLSTATE 23505) compared to a serialization_failure (SQLSTATE 40001)?',
    options: [
      'Both codes should always be retried unconditionally with identical logic, since PostgreSQL provides a built-in automatic-retry facility for each.',
      'A 23505 error should never be retried under any circumstance, while a 40001 error should be retried an unlimited number of times with a guarantee of eventual success.',
      'A 23505 failure needs more caution before retrying than a 40001 failure does, since it can reflect a persistent conflict, not just a transient one.',
      'A 23505 error actually indicates that a deadlock was detected and resolved, while a 40001 error indicates the transaction committed successfully.',
    ],
    correctIndexes: [2],
    explanation: "The documentation recommends retrying serialization_failure (40001) unconditionally but says more care is warranted for a unique-key failure (23505), since it can represent a persistent condition rather than a transient one, even though in some corner cases it is genuinely a disguised serialization problem the server had no way to detect as such. There is no automatic-retry facility for either code — the documentation explicitly says PostgreSQL doesn't offer one, and it never promises that a retried transaction is guaranteed to eventually succeed. The claim tying 23505 to deadlocks and 40001 to success is simply a mismatch of what each SQLSTATE code actually means.",
  },
  {
    id: 'transactions-022',
    domain: 'transactions',
    questionType: 'multiple-response',
    question: 'Which of the following statements about application-level data consistency checks are accurate per the documentation?',
    options: [
      'SELECT FOR UPDATE guarantees a locked row can never be updated or deleted by any other transaction, even after the transaction that issued the SELECT FOR UPDATE has committed.',
      'Enforcing data-integrity business rules is very hard to do with Read Committed transactions, since what a query sees can shift from one statement to the next within the same transaction.',
      'Checking a running total by issuing two SELECT sum(...) queries one after another and comparing their results is unreliable in Read Committed mode, since the later query is likely to include commits the earlier one missed.',
      'A SHARE mode (or stronger) lock on a table guarantees there are no uncommitted changes in that table from any transaction other than the one holding the lock.',
      "A Repeatable Read transaction's snapshot is frozen the instant its BEGIN statement runs, before any explicit LOCK TABLE statement issued afterward could still precede that freeze.",
    ],
    correctIndexes: [1, 2, 3],
    explanation: "Three of these are stated directly: Read Committed makes business-rule enforcement hard because the visible data shifts statement to statement, comparing successive sum() queries under Read Committed is unreliable for the same reason, and a SHARE-or-stronger table lock does guarantee no uncommitted changes from other transactions remain. The SELECT FOR UPDATE claim is false — once the locking transaction ends, a previously blocked transaction proceeds with its conflicting UPDATE/DELETE unless the row was actually modified while the lock was held, so the lock does not protect the row forever. The snapshot-timing claim is also false — a Repeatable Read transaction's snapshot is frozen at the start of its first non-transaction-control statement (its first real query or data-modifying command), not at BEGIN, which is precisely why an explicit LOCK TABLE issued right after BEGIN can still be obtained before the snapshot is frozen.",
  },

  // --- Roles & Permissions (administration-001..006) ---
  {
    id: 'administration-001',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'In PostgreSQL, how does the object created by CREATE USER differ, structurally, from one created by plain CREATE ROLE?',
    options: [
      'There is no structural difference at all — CREATE USER is just CREATE ROLE with the LOGIN attribute defaulting to on instead of off.',
      'CREATE USER always requires superuser privileges to run, while CREATE ROLE can be run by any authenticated session with no special privilege.',
      'CREATE USER automatically grants membership in every existing role, while CREATE ROLE grants membership in none of them by default.',
      'CREATE USER produces a distinct kind of database object that supports login, while CREATE ROLE can never produce an object that logs in.',
    ],
    correctIndexes: [0],
    explanation: 'PostgreSQL treats users and roles as the same underlying kind of object; the only distinction is that CREATE USER flips the default from NOLOGIN to LOGIN, whereas plain CREATE ROLE defaults to NOLOGIN. The claim that CREATE USER yields some separate, login-capable object type is false — a plain CREATE ROLE can log in too, it just needs LOGIN spelled out explicitly. Neither spelling has a different privilege requirement for who may run it, and neither one grants any role membership automatically just by virtue of which keyword was used.',
  },
  {
    id: 'administration-002',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'Role app_writer is granted membership in role db_admin (which carries the CREATEDB attribute), and that membership grant has the INHERIT option turned on. What happens when app_writer tries to run CREATE DATABASE?',
    options: [
      'It succeeds immediately, because an INHERIT membership automatically propagates every attribute of db_admin, including CREATEDB, to app_writer.',
      'It succeeds, but only because CREATEDB happens to be carved out as an exception to the general non-inheritance rule for role attributes.',
      'It fails, because role attributes such as CREATEDB are non-inheritable regardless of the INHERIT option.',
      'It fails permanently and unconditionally, since a role attribute can never be exercised by anyone other than the exact role it was assigned to.',
    ],
    correctIndexes: [2],
    explanation: "The attributes handed out by CREATE ROLE (such as CREATEDB, SUPERUSER, CREATEROLE) do not flow through role membership even when that membership has the INHERIT option enabled — inheriting a role's privileges on its owned objects is a separate thing from inheriting its account-level attributes. To actually exercise db_admin's CREATEDB, app_writer would need the membership's SET option so it can switch into db_admin first. Nothing carves CREATEDB out as a special exception, and the block isn't truly permanent — switching into the role remains a documented workaround.",
  },
  {
    id: 'administration-003',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: "Which statement about a role's CONNECTION LIMIT setting is accurate?",
    options: [
      "It is enforced with total precision, so it's impossible for two sessions racing for the last available slot to both slip through.",
      'It counts prepared transactions and background worker connections toward the total, in addition to ordinary client connections.',
      "The setting is disregarded for superuser roles altogether — no configured limit will ever stop a superuser from connecting.",
      'Its default value of zero connections means a freshly created role cannot open any session until an administrator explicitly raises the limit.',
    ],
    correctIndexes: [2],
    explanation: "The documentation is explicit that connection-limit enforcement is skipped for superuser roles no matter how the limit is set. It also says the opposite of precise enforcement — the check is only approximate, so two sessions racing for the same last slot can, in fact, both fail. Prepared transactions and background-worker connections are specifically excluded from the count, not included. And the default value is -1 (meaning unlimited), not zero, so an unconfigured role isn't locked out.",
  },
  {
    id: 'administration-004',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: "A role is created with CREATE ROLE app WITH LOGIN PASSWORD 'x123', without the ENCRYPTED keyword. What happens to the stored password?",
    options: [
      'It is stored as plain, unencrypted text in the system catalogs, since the ENCRYPTED keyword was never supplied.',
      'It ends up encrypted regardless — the password is always stored encrypted, and the ENCRYPTED keyword itself has no real effect on that outcome.',
      'The command is rejected outright, because PostgreSQL requires ENCRYPTED to be present any time a password value is supplied.',
      'It is protected using whatever scheme the connecting client library happens to negotiate at connection time, independent of any server-side setting.',
    ],
    correctIndexes: [1],
    explanation: "PostgreSQL never keeps a role's password in cleartext form on disk; it's scrambled before being written to the catalog no matter whether ENCRYPTED was typed, and that keyword survives in the grammar purely for backwards compatibility rather than doing anything functional. Which scrambling method gets used is governed by the server-side password_encryption parameter, not by anything the client negotiates. Nothing about omitting ENCRYPTED causes the command to fail, and the value is never simply written out in the clear.",
  },
  {
    id: 'administration-005',
    domain: 'administration',
    questionType: 'multiple-response',
    question: 'For the role-membership form of GRANT (GRANT role_name TO role_specification), which statements about the ADMIN, INHERIT, and SET options are accurate?',
    options: [
      'The ADMIN option defaults to FALSE, and without it an ordinary member cannot grant or revoke membership in the role to or from others.',
      "If the INHERIT option is left unspecified for a new membership, it defaults to the new member role's own inheritance attribute, rather than to a fixed TRUE or FALSE.",
      'The SET option defaults to FALSE, so a freshly created membership grant never lets the member switch into the granted role unless SET TRUE is explicitly requested.',
      'A superuser account sits above the ADMIN-option bookkeeping entirely, so it can add or remove any role as a member of any other role no matter how existing grants are configured.',
      'A role is considered to hold admin rights on itself automatically, so it can always grant its own membership to other roles without any extra step.',
    ],
    correctIndexes: [0, 1, 3],
    explanation: "Three claims hold up: ADMIN really does default to FALSE and gates who can extend or revoke membership further; an unspecified INHERIT option really does fall back to the new member's own inheritance attribute rather than a hardcoded value; and superusers really can override any existing ADMIN setting when granting or revoking membership. The SET-option claim is wrong the other way around — SET actually defaults to TRUE, so a fresh membership grant normally does let the member switch roles unless SET was explicitly turned off. And a role does not automatically hold admin rights over itself; that has to be granted like anything else.",
  },
  {
    id: 'administration-006',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'What is true about the pg_read_all_data predefined role?',
    options: [
      'It grants exactly the same effect as the BYPASSRLS role attribute, so row-level security policies never apply to a role holding it.',
      'It is itself a member of pg_monitor, giving it access to monitoring views on top of its data-reading capability.',
      'It lets the holder read every table, view, and sequence, but it does not bypass row-level security policies.',
      'It bundles both read and write access, letting the holder modify any table in the database in addition to reading from it.',
    ],
    correctIndexes: [2],
    explanation: "pg_read_all_data is documented as granting read access across every table, view, and sequence, but explicitly not bypassing row-level security — administrators who need that are pointed toward the separate BYPASSRLS attribute instead, showing the two are not equivalent. Write access is handled by a completely separate role (pg_write_all_data), not bundled into this one. And pg_read_all_data isn't described as a member of pg_monitor; that composition runs the other direction, with pg_monitor built out of other roles like pg_read_all_settings and pg_read_all_stats.",
  },

  // --- VACUUM & Autovacuum (administration-007..012) ---
  {
    id: 'administration-007',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'A heavily updated table is vacuumed regularly with plain VACUUM (never VACUUM FULL). What happens to the size of its on-disk file over time?',
    options: [
      "It stays essentially the same size, aside from possibly a few pages trimmed off the very end.",
      'It grows without any bound, since plain VACUUM does not reclaim any space whatsoever until a VACUUM FULL eventually runs.',
      'It shrinks the very first time VACUUM is run and then stays fixed at that size forever, no matter how much update activity follows.',
      'It shrinks every single time VACUUM runs, since VACUUM always writes out a fresh copy of the table with the dead space stripped out.',
    ],
    correctIndexes: [0],
    explanation: "Plain VACUUM reclaims dead-row space for reuse within the same table but, apart from an easy case involving fully empty pages at the very end of the file, it does not return that space to the operating system — so the file's on-disk size stays roughly steady rather than shrinking. Rewriting the whole file to remove dead space is what VACUUM FULL does, not the plain form. And plain VACUUM does reclaim space for reuse continually, so neither \"grows without bound\" nor \"only shrinks once and freezes\" matches how it actually behaves.",
  },
  {
    id: 'administration-008',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'Will relying purely on the autovacuum daemon ever result in it automatically running VACUUM FULL on a bloated table?',
    options: [
      "No — the daemon runs standard VACUUM often enough that VACUUM FULL shouldn't be needed, and it will never issue VACUUM FULL on its own.",
      'Yes, once a table has accumulated more dead tuples than the configured autovacuum_vacuum_max_threshold, the daemon escalates to VACUUM FULL automatically.',
      'Yes, but only during whatever low-traffic maintenance window is configured through autovacuum_naptime.',
      'Yes, but only for tables that are approaching transaction ID wraparound, where the daemon switches to VACUUM FULL to free space faster.',
    ],
    correctIndexes: [0],
    explanation: 'The documented goal of autovacuum is to run plain VACUUM frequently enough that a table never needs the disruptive VACUUM FULL treatment, and the daemon is stated outright to never issue VACUUM FULL under any circumstance, including as tables approach the wraparound-prevention age threshold. There is no dead-tuple count, threshold, or naptime-driven maintenance window that causes autovacuum to escalate to VACUUM FULL — that command always has to be run by hand.',
  },
  {
    id: 'administration-009',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'A table has 1,000,000 live tuples, and every autovacuum vacuum-threshold parameter is left at its default. Roughly how many dead tuples need to accumulate before autovacuum will trigger a VACUUM on that table?',
    options: [
      'About 50 dead tuples, since a fixed base count alone determines when autovacuum fires, independent of the table\'s size.',
      'About 200,050 dead tuples, combining the default fixed base amount with 20% of the table\'s 1,000,000 live tuples.',
      "Exactly 1,000,000 dead tuples, since autovacuum by default waits for the table's contents to turn over completely once.",
      "About 100,000,000 dead tuples, since that figure is autovacuum's hard ceiling on the trigger threshold no matter how large the table is.",
    ],
    correctIndexes: [1],
    explanation: "The vacuum-triggering threshold combines a small fixed base count (50 by default) with a scale factor (20% by default) multiplied by the table's tuple count, so for a million-row table that works out to roughly 50 + 200,000 = 200,050 dead tuples. Using only the fixed base ignores the size-proportional term entirely, waiting for a full turnover of the table overstates what's required, and the 100,000,000 figure is actually the documented ceiling that caps the formula for very large tables — it isn't what triggers vacuuming at this table's size.",
  },
  {
    id: 'administration-010',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'For a table whose entire contents get wiped on a recurring schedule, the documentation favors TRUNCATE over DELETE followed by VACUUM. What is the trade-off being made?',
    options: [
      "TRUNCATE runs more slowly than DELETE plus VACUUM, but in exchange it sidesteps the exclusive lock that DELETE would otherwise require.",
      "TRUNCATE clears the table right away without needing a follow-up VACUUM, but it violates strict MVCC semantics.",
      'TRUNCATE and a DELETE-then-VACUUM sequence behave identically in every respect, and the documentation expresses no real preference between the two.',
      "TRUNCATE still needs a subsequent VACUUM FULL before the freed disk space becomes usable again, unlike DELETE which frees it on the spot.",
    ],
    correctIndexes: [1],
    explanation: "TRUNCATE is recommended precisely because it removes all of a table's rows immediately and doesn't leave behind dead row versions that a later VACUUM would need to clean up — the cost of that convenience is that it breaks strict MVCC semantics (concurrent transactions can observe the table as already empty rather than seeing a normal snapshot). The documentation treats the two approaches as clearly different, not equivalent, and it's DELETE that leaves work for VACUUM afterward, not TRUNCATE needing a VACUUM FULL.",
  },
  {
    id: 'administration-011',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'A database has crossed the point where PostgreSQL refuses to assign any new transaction IDs, to avoid wraparound data loss. What is and is not still possible at that point?',
    options: [
      'The entire server stops accepting connections outright, including read-only ones, until it is restarted in single-user mode.',
      'VACUUM itself is blocked along with every other command, so the only remaining fix is dropping and recreating the affected tables.',
      'Read-only transactions can still run, writes and relation truncation fail, but an ordinary VACUUM command still works to fix the problem.',
      'Resolving it safely requires running VACUUM FULL as a superuser, since that command is specifically built to consume the last remaining transaction IDs.',
    ],
    correctIndexes: [2],
    explanation: "Once the safety cutoff is reached, transactions already running can finish and new read-only transactions can still start, but anything that would modify records or truncate a relation is refused; a plain VACUUM command keeps working and is the documented way out. The server as a whole doesn't stop accepting all connections, and single-user mode is explicitly not required for this situation in modern PostgreSQL. VACUUM FULL is actually the wrong move here — outside of superuser mode it needs a transaction ID to run and will simply fail, and even as a superuser it consumes a transaction ID and pushes the system closer to wraparound rather than away from it.",
  },
  {
    id: 'administration-012',
    domain: 'administration',
    questionType: 'multiple-response',
    question: "Which statements about the autovacuum daemon's coverage and blind spots are accurate?",
    options: [
      "Autovacuum schedules ANALYZE purely based on how much a table's statistical distribution has actually shifted, skipping tables whose distribution hasn't meaningfully changed even after heavy row modification.",
      'Autovacuum cannot reach temporary tables at all — any vacuuming or analyzing of them has to be done through session-issued SQL commands instead.',
      "Autovacuum does not issue ANALYZE for partitioned tables directly, and changes made to a table's child partitions do not trigger an autoanalyze of the parent — its statistics have to be refreshed by hand.",
      'Autovacuum treats a partitioned table exactly like any ordinary table for vacuum purposes, since a partitioned table stores its own tuples directly just like a regular one.',
      'Autovacuum skips issuing ANALYZE for foreign tables, since it has no way of judging how often analyzing them would actually be worthwhile.',
    ],
    correctIndexes: [1, 2, 4],
    explanation: "Three of these hold up: session-local temporary tables genuinely sit outside autovacuum's reach and need manual handling; a partitioned table's own row count never actually changes (only its leaf partitions hold data), so the daemon can't and doesn't auto-analyze the parent just because child partitions changed; and foreign tables are skipped for the same reason — the daemon has no visibility into how often their statistics need refreshing. The distribution-based scheduling claim is backwards — autovacuum actually schedules ANALYZE purely by counting how many rows were touched, with no awareness of whether that changed the statistics meaningfully. And a partitioned table can't be treated like an ordinary one for this purpose, since a partitioned table holds no rows of its own at all — only its partitions do.",
  },

  // --- Backup & Restore (administration-013..018) ---
  {
    id: 'administration-013',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'An administrator runs plain pg_dump against a single database inside a cluster that also has several custom roles and tablespaces defined. What ends up inside the resulting dump?',
    options: [
      'Only the objects belonging to that one database — pg_dump does not export other databases, roles, or tablespaces.',
      'That single database along with the definitions of every role and tablespace in the cluster, since those are treated as part of any single-database dump.',
      'Nothing usable — pg_dump refuses to produce output unless it is first pointed at a pg_dumpall-generated file.',
      'That single database along with every other database that exists in the same cluster, since pg_dump operates across the whole cluster by default.',
    ],
    correctIndexes: [0],
    explanation: 'pg_dump is scoped to exactly one database at a time; it neither pulls in the cluster\'s other databases nor captures objects that live above the database level, such as roles and tablespaces — those require pg_dumpall instead. It does not need a pg_dumpall file as a prerequisite to run, and it never silently expands its scope to the whole cluster on its own.',
  },
  {
    id: 'administration-014',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: "Beyond ordinary database contents, what does pg_dumpall capture for a cluster that a plain pg_dump of each individual database in that cluster would miss?",
    options: [
      "Table indexes and constraints, which the documentation says pg_dump omits from its output by default.",
      'The contents of the WAL archive directory, since pg_dumpall doubles as a continuous-archiving tool rather than a purely logical dump utility.',
      'Query-plan statistics gathered by EXPLAIN ANALYZE runs, which pg_dump is documented as deliberately stripping out.',
      'Database roles, tablespaces, and privilege grants that apply to configuration parameters — cluster-wide objects that pg_dump does not save.',
    ],
    correctIndexes: [3],
    explanation: 'pg_dumpall is explicitly documented as additionally dumping the cluster-wide objects that a per-database pg_dump run leaves out: roles, tablespaces, and the privilege grants attached to configuration parameters. pg_dump actually does include a table\'s indexes and constraints as part of a normal dump, so that first claim is backwards. pg_dumpall has nothing to do with WAL archiving — logical dumps of either kind are explicitly documented as unusable for that purpose — and neither tool concerns itself with EXPLAIN ANALYZE output at all.',
  },
  {
    id: 'administration-015',
    domain: 'administration',
    questionType: 'multiple-response',
    question: "Which statements about pg_dump's output formats are accurate?",
    options: [
      'The directory format is the only output format that supports spreading a single dump across multiple worker processes running in parallel.',
      'The plain-text script format can only be reloaded using pg_restore — feeding it to psql will not work.',
      "The tar format does not support compression, and the relative order in which table data items appear cannot be changed during a restore from a tar archive.",
      'Both the custom format and the directory format are compressed by default, unlike the plain-text script format.',
      'Directory-format archives cannot be touched with ordinary Unix tools like gzip, since they use a proprietary, opaque container structure.',
    ],
    correctIndexes: [0, 2, 3],
    explanation: 'Three of these match the documentation: the directory format really is the only one that lets multiple worker processes write dump data at the same time; the tar format really is uncompressed and really does lock in the relative ordering of table data at restore time; and the custom and directory formats really are both compressed by default while the plain-text script format is not. The claim about plain-text scripts is backwards, though — those are meant to be fed to psql, not pg_restore. And directory-format archives are explicitly described as ordinary files on disk that standard Unix tools like gzip, lz4, or zstd can operate on directly, not some opaque proprietary container.',
  },
  {
    id: 'administration-016',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'A team wants to use pg_basebackup to back up just one database out of a multi-database cluster, connecting as a role with ordinary login rights only (no REPLICATION attribute, not a superuser). What does the documentation say about this plan?',
    options: [
      'It will work exactly as described — pg_basebackup has always supported picking out a single database for this kind of selective backup.',
      "It fails on two counts: pg_basebackup always backs up the entire cluster, and the connecting role needs REPLICATION permission or superuser status.",
      'It will succeed for taking the backup, but only a superuser is later permitted to restore output produced by pg_basebackup, regardless of who created it.',
      'It fails only because of the missing permission — pg_basebackup does otherwise support backing up a single chosen database.',
    ],
    correctIndexes: [1],
    explanation: "Two separate documented facts sink this plan at once: pg_basebackup always operates on the whole cluster and has no mechanism for singling out one database or object (a tool like pg_dump is what's needed for that), and taking any backup at all requires the connecting role to either hold the REPLICATION attribute or be a superuser. There's no special restore-time superuser requirement layered on top of that, and the claim that only the permissions issue is blocking the plan wrongly implies selective single-database backups are otherwise supported.",
  },
  {
    id: 'administration-017',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'During a point-in-time recovery built on continuous WAL archiving, must the replay process work its way through every archived WAL record, all the way to the most recent one?',
    options: [
      'Yes — WAL replay is all-or-nothing, so recovery always lands the database at the exact instant of the newest archived record.',
      'No, but stopping the replay early always leaves the database in a corrupted state that has to be repaired with VACUUM FULL before use.',
      'Yes, unless the DBA manually edits the archived WAL files ahead of time to delete everything after the desired stopping point.',
      'No — replay can be halted at any chosen point, producing a consistent snapshot of the database as of that moment.',
    ],
    correctIndexes: [3],
    explanation: "Point-in-time recovery works precisely because replay does not have to run to the end of the archived WAL sequence — stopping it partway still yields a database state that is internally consistent as of that stopping point. There's no requirement to hand-edit archived WAL files to achieve this, and stopping early does not corrupt anything or require a VACUUM FULL cleanup afterward; the replayed state is a valid, ordinary database snapshot.",
  },
  {
    id: 'administration-018',
    domain: 'administration',
    questionType: 'multiple-response',
    question: 'Which statements about setting up WAL archiving with archive_command are accurate?',
    options: [
      'WAL archiving will also restore any manual edits later made to postgresql.conf, pg_hba.conf, or pg_ident.conf, since those files travel along with the WAL stream.',
      'In the archive_command string, %p is substituted with the path to the file being archived, and %f is substituted with just the bare file name.',
      'A pg_dump or pg_dumpall export can be fed directly into a WAL archiving pipeline, since both produce output detailed enough for WAL replay to consume.',
      'A zero exit status from the archive command is treated as success and the segment gets removed or recycled; any nonzero status is treated as a failure that PostgreSQL retries periodically.',
      'Setting archive_timeout forces a WAL segment switch once that much time has elapsed, bounding how much unarchived data could be lost — but setting it too short can bloat archive storage.',
    ],
    correctIndexes: [1, 3, 4],
    explanation: "Three of these are accurate: the %p/%f substitution rule for archive_command is exactly as described; the archive command's exit status genuinely drives success-or-retry behavior the way described; and archive_timeout genuinely trades a smaller potential data-loss window against extra archive storage when set aggressively short. The other two are false: logical exports from pg_dump or pg_dumpall are explicitly documented as unusable for WAL replay, since they don't capture the low-level information continuous archiving depends on, and WAL archiving is explicitly documented as not covering hand-edited configuration files at all, since those changes never go through the WAL stream in the first place.",
  },

  // --- Configuration, Extensions & Partitioning (administration-019..024) ---
  {
    id: 'administration-019',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'A parameter is set to one value in postgresql.conf, and a different value for that same parameter is set through ALTER SYSTEM. Which value takes effect for sessions that connect afterward?',
    options: [
      "The value from postgresql.conf always wins, since an ALTER SYSTEM change only takes effect after the next full server restart, not from a plain configuration reload.",
      "The ALTER SYSTEM value wins, because it lands in postgresql.auto.conf, which is read after and overrides postgresql.conf.",
      'Neither value applies to anything until a client issues its own explicit SET command naming that parameter within its session.',
      "It's left undefined which one wins — the server logs a conflict warning and refuses to start until the duplicate setting is manually removed.",
    ],
    correctIndexes: [1],
    explanation: 'ALTER SYSTEM changes are written into postgresql.auto.conf, and that file is documented as being read after postgresql.conf with its settings overriding anything postgresql.conf specifies — so the ALTER SYSTEM value is the one new sessions pick up. It doesn\'t require a full restart to take hold, only the same reload mechanism postgresql.conf itself uses; a session-level SET is not required for the change to apply at connection time; and there\'s no described conflict-detection mechanism that would block server startup over this.',
  },
  {
    id: 'administration-020',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'After hand-editing postgresql.conf to change a parameter that is documented as reloadable (not fixed at server start), what does it take for the new value to actually apply?',
    options: [
      'Nothing further — PostgreSQL continuously polls postgresql.conf in the background and applies any detected change within a few milliseconds automatically.',
      'Every already-open client connection has to be manually dropped and reopened, since a reload signal is documented as affecting only brand-new connections.',
      'The change can only be applied through the ALTER SYSTEM command instead — direct hand-edits to postgresql.conf are ignored entirely once the server is running.',
      'The server process needs to receive a SIGHUP signal (via pg_ctl reload or pg_reload_conf()), which it also propagates to already-connected sessions.',
    ],
    correctIndexes: [3],
    explanation: "The configuration file is only reread when the server process gets a SIGHUP signal, most conveniently sent through pg_ctl reload or the pg_reload_conf() SQL function, and that signal is passed along to already-connected sessions as well so they pick up the new value too. There's no continuous background polling that applies changes automatically, existing sessions do adopt the new value rather than being left stuck on the old one, and hand-editing postgresql.conf is very much still a valid way to change reloadable settings — ALTER SYSTEM is an alternative to that, not a replacement requirement.",
  },
  {
    id: 'administration-021',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: "An extension's control file sets trusted = true, while leaving the default superuser = true also in effect. What happens when a non-superuser role — one that only holds CREATE privilege on the current database — runs CREATE EXTENSION for it?",
    options: [
      'The command is rejected outright, since a trusted flag only relaxes requirements for ALTER EXTENSION later on, never for the initial CREATE EXTENSION.',
      "Installation is allowed, but the calling user's whole session gets permanently promoted to superuser status for the remainder of that session.",
      'Installation is allowed only if the calling user also happens to hold a predefined role whose name matches the extension being installed.',
      'Installation is allowed, and the install script runs as the bootstrap superuser rather than as the calling user.',
    ],
    correctIndexes: [3],
    explanation: "Marking an extension trusted is specifically what lets a non-superuser with database-level CREATE privilege install it despite the extension's script needing superuser-level operations — the script itself is executed as the bootstrap superuser rather than as the calling user, while the calling user still becomes the owner of the extension object for future privilege checks. There's no session-wide promotion to superuser involved, and nothing ties eligibility to holding some matching predefined role. The trusted flag governs the initial CREATE EXTENSION itself, not just later updates.",
  },
  {
    id: 'administration-022',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'A database has the hstore extension installed, which created a batch of functions and operators as part of loading it. When pg_dump exports that database, what happens to those extension-created objects?',
    options: [
      "pg_dump writes out a separate CREATE statement for every individual function and operator the extension created, exactly as if they weren't grouped into an extension at all.",
      'pg_dump skips the extension altogether, so restoring the dump leaves the resulting database without hstore or anything it created.',
      "pg_dump converts each extension-created object into an equivalent built-in construct, so the restored database no longer needs the extension installed at all.",
      "pg_dump does not dump the extension's individual member objects; it just emits a CREATE EXTENSION command instead.",
    ],
    correctIndexes: [3],
    explanation: 'pg_dump is documented as understanding that an extension\'s member objects belong together, so instead of dumping each function or operator individually it just emits a CREATE EXTENSION command and relies on that extension\'s own installation script to rebuild everything when the dump is restored. The extension is not skipped or silently dropped from the dump, and its objects are not converted into some extension-independent built-in equivalent — restoring the dump still depends on the extension\'s files being available.',
  },
  {
    id: 'administration-023',
    domain: 'administration',
    questionType: 'multiple-choice',
    question: 'A DBA wants to add a new partition to a large, busy partitioned table with as little disruption as possible. Comparing CREATE TABLE ... PARTITION OF against building the table separately first and then running ALTER TABLE ... ATTACH PARTITION, what is the documented locking difference?',
    options: [
      'ATTACH PARTITION only needs a weaker SHARE UPDATE EXCLUSIVE hold on the parent, while CREATE TABLE ... PARTITION OF takes the far more disruptive ACCESS EXCLUSIVE lock.',
      'Both approaches take the identical ACCESS EXCLUSIVE lock on the partitioned table, for the same duration, with no meaningful difference between them.',
      'CREATE TABLE ... PARTITION OF needs only a SHARE UPDATE EXCLUSIVE lock, while ATTACH PARTITION needs the much stronger ACCESS EXCLUSIVE lock, making CREATE TABLE ... PARTITION OF the friendlier option.',
      'Neither approach takes a lock any stronger than ACCESS SHARE, because partition-structure changes were made fully lock-free in current PostgreSQL versions.',
    ],
    correctIndexes: [0],
    explanation: "Building the new table separately and attaching it afterward is the more concurrency-friendly path precisely because ATTACH PARTITION only needs a SHARE UPDATE EXCLUSIVE lock on the parent, in contrast to the ACCESS EXCLUSIVE lock that CREATE TABLE ... PARTITION OF requires. The option that reverses which command needs the stronger lock has the relationship exactly backwards. Neither approach avoids locking altogether — ACCESS EXCLUSIVE and SHARE UPDATE EXCLUSIVE are both well above ACCESS SHARE — and the two approaches are not equivalent in their locking cost.",
  },
  {
    id: 'administration-024',
    domain: 'administration',
    questionType: 'multiple-response',
    question: 'Which statements about partition pruning are accurate?',
    options: [
      "What makes pruning possible is purely the bounds baked into each partition's own definition — whether an index happens to exist on the partition key columns plays no role in the decision.",
      'Partition pruning requires a CHECK constraint to be manually written on every partition describing its bounds, since PostgreSQL cannot infer that from the partition definition on its own.',
      'Unlike constraint exclusion, which only ever runs at query-planning time, partition pruning can also remove partitions during actual query execution, using values that are only known once execution is underway.',
      "The default setting for constraint_exclusion is on, so the planner examines CHECK constraints for every query, whether or not the query touches a partitioned or inherited table.",
      'Partition pruning can be turned off entirely through the enable_partition_pruning configuration setting.',
    ],
    correctIndexes: [0, 2, 4],
    explanation: "Three of these hold up: partition pruning really does rely purely on the bounds baked into each partition's definition rather than on indexes, which is why an index on the partition key isn't required for pruning to work; pruning really can happen during query execution as well as during planning, which is exactly the capability that sets it apart from constraint exclusion; and enable_partition_pruning really is documented as the switch that can disable the whole mechanism. The other two are wrong: partition boundary constraints are generated automatically rather than needing to be hand-written, and the documented default for constraint_exclusion is the intermediate 'partition' setting, not 'on' — 'on' would make the planner examine CHECK constraints even for queries that have nothing to do with partitioning.",
  },

  // --- Streaming replication (replication-001..004) ---
  {
    id: 'replication-001',
    domain: 'replication',
    questionType: 'multiple-choice',
    question: "PostgreSQL's built-in streaming replication is left at its default settings. If the primary crashes an instant after a client receives a commit acknowledgment, what can happen to that transaction on the standby?",
    options: [
      'Nothing is ever at risk, because the primary is documented as always waiting for the standby before acknowledging any commit by default.',
      'The transaction is guaranteed to survive, since streaming replication only ever ships WAL files that are already a full segment (16MB) in size.',
      'It can be lost, because streaming replication is asynchronous by default and the WAL for that commit may not have reached the standby yet.',
      'It can be lost, but only if the cluster was configured to use file-based log shipping instead of streaming replication in the first place.',
    ],
    correctIndexes: [2],
    explanation: 'Streaming replication ships WAL continuously but does not, by default, make the primary wait for standby confirmation before reporting a commit as successful, so a small window of unreplicated data always exists unless synchronous replication is explicitly configured. The option claiming the primary always waits by default has the default backwards, the 16MB-segment claim describes file-based log shipping rather than streaming replication (which sends records as they are generated, not by whole files), and restricting the risk to file-based shipping ignores that streaming replication carries the same asynchronous exposure, just with a much shorter window.',
  },
  {
    id: 'replication-002',
    domain: 'replication',
    questionType: 'multiple-choice',
    question: 'Compared to relying only on a large wal_keep_size setting, what advantage does configuring a replication slot for a standby give the primary?',
    options: [
      'The slot keeps only the WAL segments the standby still needs, avoiding the excess a fixed wal_keep_size tends to retain.',
      'The slot removes any possibility that pg_wal could ever grow large, since slots are documented as being exempt from disk space limits.',
      'The slot is required before a standby can use streaming replication at all; wal_keep_size only supports file-based log shipping.',
      'The slot lets the standby skip taking a fresh base backup entirely, no matter how far behind it has fallen or how long it stayed disconnected.',
    ],
    correctIndexes: [0],
    explanation: "A replication slot tracks precisely which WAL a connected consumer still needs, so the primary retains only that much — a fixed wal_keep_size, by contrast, commonly ends up retaining more segments than are actually required. Slots are not exempt from disk pressure (an inactive slot can still cause pg_wal to fill up, which is why max_slot_wal_keep_size exists), slots are not a prerequisite for streaming replication (wal_keep_size alone can support it too), and a standby that has fallen too far behind for its needed WAL to still exist can still require a fresh base backup regardless of slot usage.",
  },
  {
    id: 'replication-003',
    domain: 'replication',
    questionType: 'multiple-choice',
    question: "A primary's synchronous_commit is changed from its default of on to remote_write for a synchronous standby. What durability guarantee actually changes?",
    options: [
      'None — on and remote_write are documented as producing identical durability, differing only in how the standby state is labeled in pg_stat_replication.',
      "A commit now waits only until the standby's OS has the WAL handed to it, not until that data reaches the standby's own durable storage.",
      'remote_write becomes strictly stronger than on, because it additionally waits for the standby to replay the transaction before the primary is released.',
      'The primary itself stops flushing its own WAL to disk at commit time, since remote_write shifts that responsibility entirely onto the standby.',
    ],
    correctIndexes: [1],
    explanation: "remote_write weakens the guarantee relative to on: a commit only waits until the standby has taken delivery of the WAL and handed it off to its host OS for writing, not until that write actually lands on durable storage there, so an OS-level crash on the standby could still lose the data even though a PostgreSQL-level crash there would not. The claim that remote_write is stronger than on has confused it with remote_apply, which waits for replay and visibility rather than just an OS-level write; the claim that the two settings are functionally identical, differing only in monitoring labels, is also wrong — they genuinely differ in what a commit waits for; and the primary's own WAL flush at commit is unaffected by what durability level is requested from the standby.",
  },
  {
    id: 'replication-004',
    domain: 'replication',
    questionType: 'multiple-response',
    question: 'Which of the following statements about streaming and cascading replication are accurate?',
    options: [
      "A cascading standby forwards only the WAL it streamed directly from its upstream server; anything it restored from an archive is never relayed further downstream.",
      'A replication slot name may be made up of lower-case letters, digits, and the underscore character.',
      'After switching a log-shipping standby over to streaming replication, archive_timeout still needs to be set to just a few seconds to keep the data-loss window small.',
      "Comparing the primary's current WAL write position against the last WAL position the standby has received is one documented way to gauge replication lag.",
      "A cascading standby's link to its downstream servers can be configured as synchronous on its own terms, independent of the primary's synchronous_standby_names setting.",
    ],
    correctIndexes: [1, 3],
    explanation: "Two of these hold up: replication slot names really are restricted to lower-case letters, numbers, and underscores, and gauging lag by comparing the primary's current WAL write position with what the standby reports as received is exactly the documented monitoring approach. The other three are wrong: a cascading standby actually relays WAL it obtained from an archive too, not just WAL it streamed directly, so downstream servers are not cut off from archive-sourced records; once streaming replication is active there is no need to keep archive_timeout low, since streaming already closes the data-loss window without it; and cascading replication is currently asynchronous only, with the primary's synchronous-standby configuration having no effect on it at all, so a cascading link cannot be made synchronous independently.",
  },

  // --- Logical replication (replication-005..008) ---
  {
    id: 'replication-005',
    domain: 'replication',
    questionType: 'multiple-choice',
    question: "How does PostgreSQL's built-in logical replication construct the stream of changes it sends to a subscriber, and how does that differ from physical streaming replication?",
    options: [
      'It polls each subscribed table on a fixed interval to diff its current contents against the last snapshot, entirely independent of WAL.',
      "It reuses the exact same block-addressed WAL stream that physical replication sends, only relabeling it as logical once it reaches the subscriber.",
      'It requires the publisher and subscriber to run identical major PostgreSQL versions, a restriction that plain physical streaming replication does not share.',
      "It decodes the WAL into row-level changes keyed to each table's replica identity, instead of shipping raw block-addressed WAL.",
    ],
    correctIndexes: [3],
    explanation: "Logical replication decodes the same underlying WAL that physical replication uses, but turns it into a stream of row-level insert/update/delete changes rather than shipping the raw block-addressed records physical replication sends for byte-for-byte reconstruction. It does not work by periodically diffing table contents — it is still WAL-driven — and it does not simply relabel the physical WAL stream. The cross-version claim is backwards: replicating between different major PostgreSQL releases is one of logical replication's documented use cases, while physical log shipping across different major release levels is the one that is not supported.",
  },
  {
    id: 'replication-006',
    domain: 'replication',
    questionType: 'multiple-choice',
    question: 'A published table has neither a primary key nor any unique index that could serve as its replica identity, but the publication is meant to replicate UPDATE and DELETE operations against it. What must be done?',
    options: [
      'The publisher must set the replica identity to FULL, so the entire row is used to locate the matching row on the subscriber.',
      'Nothing — logical replication automatically falls back to matching rows on the subscriber by their physical on-disk location.',
      'UPDATE and DELETE statements against that table are silently omitted from the replication stream, with no error raised on either side.',
      'The table must be dropped and recreated with a generated surrogate key, since replica identity FULL is no longer offered in current releases.',
    ],
    correctIndexes: [0],
    explanation: "When no primary key or suitable unique index exists, setting replica identity FULL is the documented fallback: the entire row is treated as the key for locating the matching row on the subscriber (though that search can be inefficient without a supporting index there). Matching by on-disk physical location isn't how it works — a subscriber is a separate database with no shared physical layout — and attempting UPDATE or DELETE against a table whose replica identity can't support them produces an error on the publisher rather than silently skipping those changes. Replica identity FULL is a real, current option, not something that forces a schema redesign.",
  },
  {
    id: 'replication-007',
    domain: 'replication',
    questionType: 'multiple-choice',
    question: "A column is added to a table on a logical replication publisher after its subscription is already running, and the subscriber's copy of that table is not updated to match. What happens?",
    options: [
      'The subscriber inspects the incoming WAL stream, infers the new column definition, and applies an equivalent ALTER TABLE before replaying the change.',
      "Nothing on the subscriber's side matters: whatever the publisher sends will apply cleanly regardless of the subscriber table's own column definitions.",
      "The DDL itself isn't replicated, and once incoming rows stop fitting the subscriber's table shape, replication for that table starts erroring.",
      'The entire cluster of subscribers is automatically paused until every one of them is manually confirmed to have an identical schema to the publisher.',
    ],
    correctIndexes: [2],
    explanation: "Schema and DDL changes are outside what logical replication carries — administrators are expected to apply matching changes to the subscriber by hand, and once replicated rows stop fitting the subscriber's existing table shape, replication for that table begins failing with errors until the schema catches up. There's no automatic inference of an ALTER TABLE from the WAL stream, and the subscriber's own table structure very much does matter for whether incoming data will apply cleanly. Nor is there any cluster-wide pause-and-confirm mechanism — each subscription simply keeps applying changes (or erroring) independently of the others.",
  },
  {
    id: 'replication-008',
    domain: 'replication',
    questionType: 'multiple-response',
    question: "Which of the following are accurate statements about what PostgreSQL's built-in logical replication does and does not carry over to a subscriber?",
    options: [
      "The numeric value a sequence has reached on the publisher isn't kept in sync by logical replication, even though the data already generated from it in a serial or identity column is replicated.",
      "A TRUNCATE issued on the publisher is never propagated to a subscriber, no matter how the publication or subscription is configured.",
      'A view cannot itself be the direct target of a publication; only ordinary tables, including partitioned ones, can be replicated this way.',
      "A subscriber-side table must declare its columns in the same left-to-right order as the publisher's table, or replication for that table fails.",
      'Large objects can be logically replicated as long as the table referencing them has a replica identity configured.',
      "Renaming the subscriber's copy of a replicated table so its name no longer matches the publisher's breaks replication for that table, since tables are paired by fully qualified name.",
    ],
    correctIndexes: [0, 2, 5],
    explanation: "Three of these are documented accurately: a sequence's own counter value is not replicated even though data already produced from it and stored in a column is; only regular tables and partitioned tables can be the target of a publication, with views excluded; and because publisher and subscriber tables are paired by fully qualified name, renaming the subscriber's copy breaks that pairing. The other three are wrong: TRUNCATE actually is supported for replication (subject to some foreign-key caveats across subscription boundaries), the subscriber's column order is explicitly allowed to differ from the publisher's, and large objects have no logical-replication workaround at all — a replica identity on the referencing table doesn't change that.",
  },

  // --- Failover concepts (replication-009..010) ---
  {
    id: 'replication-009',
    domain: 'replication',
    questionType: 'multiple-choice',
    question: 'A PostgreSQL primary server stops responding. What does core PostgreSQL itself do to detect that failure and promote a standby in its place?',
    options: [
      'It opens a dedicated witness connection from the postmaster to the nearest standby and instructs that standby to promote before the primary process exits.',
      'Nothing — core PostgreSQL includes no software for detecting a primary failure or triggering a promotion; that coordination is left to separate tooling.',
      'Any standby named first in synchronous_standby_names automatically calls pg_promote() on itself once wal_sender_timeout has elapsed with no heartbeat.',
      'max_wal_senders dropping to zero active connections is treated by every connected standby as an automatic signal to run pg_ctl promote.',
    ],
    correctIndexes: [1],
    explanation: 'PostgreSQL is explicit that it ships no built-in mechanism to detect that a primary has failed or to notify a standby that it should take over — administrators are expected to bring in separate tooling (such as a heartbeat-based cluster manager) to handle that detection and to trigger promotion via pg_ctl promote or pg_promote() themselves. There is no witness-connection feature on the postmaster, no self-promotion tied to a standby\'s position in synchronous_standby_names, and no automatic promotion triggered merely by max_wal_senders reaching zero — all three describe automated failover behavior the documentation says core PostgreSQL does not provide.',
  },
  {
    id: 'replication-010',
    domain: 'replication',
    questionType: 'multiple-choice',
    question: 'After a standby is promoted to take over for a failed primary, and before a fresh standby has been created to replace it, what is this single-server situation called, and what risk does it create if the old primary later comes back online unmodified?',
    options: [
      "It's called catchup mode, and the risk is that the newly promoted server will refuse read-only queries until the old primary is formally decommissioned.",
      "It's called split-brain mode, and the risk is limited to the two servers contending over the same shared disk array, a concern specific to shared-disk failover setups.",
      "It's called quorum-loss mode, and the risk only arises when synchronous_standby_names uses the quorum-based ANY method rather than a priority-based list.",
      "It's called a degenerate state; the risk is both servers ending up believing they are primary at once, which STONITH-style safeguards guard against.",
    ],
    correctIndexes: [3],
    explanation: "This single-server state is called a degenerate state, and the real danger is the old primary rejoining without being told its role has changed, leaving two servers that each believe they are the primary — the kind of split-brain confusion that STONITH-style safeguards (forcibly taking the old primary offline) exist to prevent. Catchup mode is a real term, but it describes a standby still synchronizing after it first attaches, not this post-failover state, and it doesn't involve refusing read queries. Framing the risk as purely a shared-disk contention issue mislabels a concern that applies well beyond shared-disk configurations, and there is no documented 'quorum-loss mode' tied only to the ANY method.",
  },

  // --- Connection pooling & read replicas (replication-011..014) ---
  {
    id: 'replication-011',
    domain: 'replication',
    questionType: 'multiple-choice',
    question: "A standby server is continuously replaying WAL from the primary, but hot_standby is off, so no client can run a query against it. What is this configuration correctly called, and what change would let it also serve read-only queries?",
    options: [
      'This is a warm standby; turning hot_standby on so the standby accepts read-only connections while still replaying WAL is what makes it a hot standby.',
      "This is already a hot standby; the hot_standby parameter only affects how quickly it can be promoted, not whether client queries are permitted.",
      'This is a cold standby, and read access requires restoring it fresh from an archived base backup rather than changing any running parameter.',
      'This configuration can never be queried under any setting, since read access to a replaying standby was removed in favor of logical replication subscribers.',
    ],
    correctIndexes: [0],
    explanation: 'A standby that is applying WAL but cannot yet be queried is what the documentation calls a warm standby; it becomes a hot standby, informally usable as a read replica, once hot_standby is on and it starts accepting read-only connections while replay continues. Calling it a hot standby already gets the parameter\'s role backwards — hot_standby is precisely what gates read-only query access, not promotion speed. A cold standby describes a much more static setup than a server continuously replaying WAL, and read-only querying of a replaying standby is exactly the documented hot standby capability, not something removed from PostgreSQL.',
  },
  {
    id: 'replication-012',
    domain: 'replication',
    questionType: 'multiple-choice',
    question: 'Which of the following is NOT something built into core PostgreSQL, and is instead typically added through a separate third-party layer sitting in front of it?',
    options: [
      'A replication slot that retains WAL for a standby that is still connected and catching up.',
      'pg_ctl promote, the documented command that ends standby mode and switches a standby server over into ordinary read/write operation.',
      'A connection pooler that multiplexes many client connections onto a smaller set of server-side database connections.',
      "Cascading replication, in which a standby relays WAL onward to further downstream standbys.",
    ],
    correctIndexes: [2],
    explanation: "Connection pooling, like automatic failover detection, is explicitly left to external software (PgBouncer being the best-known example) rather than shipped inside core PostgreSQL. The other three are all documented native capabilities: replication slots for retaining WAL a standby still needs, pg_ctl promote for ending standby mode, and cascading replication for relaying WAL through an intermediate standby are each part of the built-in replication feature set, not add-ons.",
  },
  {
    id: 'replication-013',
    domain: 'replication',
    questionType: 'multiple-choice',
    question: "PgBouncer's pool_mode is changed from the session default to transaction. What changes about how a server-side PostgreSQL connection gets shared among clients?",
    options: [
      "The server connection is now released back to the pool after every single query, whether or not the query sits inside a transaction.",
      "A server connection returns to the pool the moment each transaction finishes, letting another client reuse it, rather than it being held for one client's whole session.",
      'Nothing changes about connection sharing; pool_mode only controls how PgBouncer authenticates clients on its own listening port.',
      'Multi-statement transactions must now be disabled entirely, since only a single statement is permitted per pooled server connection under this mode.',
    ],
    correctIndexes: [1],
    explanation: "Transaction pool_mode releases the server-side connection back to the pool the moment a transaction completes, rather than holding it for one client for the full lifetime of its session the way session mode does — so a different client's next transaction can pick up that same server connection right away. Releasing on every individual query, regardless of transaction boundaries, describes statement mode instead. pool_mode governs exactly this server-connection-sharing behavior, not client authentication. And the restriction against multi-statement transactions belongs to statement mode, per the documentation — transaction mode still allows a transaction made up of several statements.",
  },
  {
    id: 'replication-014',
    domain: 'replication',
    questionType: 'multiple-response',
    question: 'Which of the following statements about running read-only queries against a hot standby are accurate?',
    options: [
      "Turning on hot_standby_feedback removes every possible reason a query could be canceled on the standby, not just cleanup-related conflicts.",
      "Once a standby query has been blocking WAL replay for longer than max_standby_streaming_delay allows, the standby always terminates that client's entire session rather than just canceling the one statement.",
      "Because hot standby connections are read-only, they are free to use LISTEN and NOTIFY the same way an ordinary read-only transaction on the primary can.",
      "A hot standby session cannot create a temporary table for its own private use, even though doing so would seem to affect only that one session.",
      "Autovacuum keeps running against a hot standby exactly as it does on the primary, since read-only queries still leave dead row versions behind.",
      "A hot standby session is rejected if it tries to run SELECT ... FOR UPDATE, because taking that row lock would require modifying the underlying data file.",
    ],
    correctIndexes: [3, 5],
    explanation: "Two of these check out: a hot standby session is barred from creating even a private temporary table, because doing so would still require assigning a transaction ID and updating catalog structures that recovery does not permit; and SELECT ... FOR UPDATE is rejected outright, since taking a row lock would mean writing to the underlying data file. The rest are wrong: hot_standby_feedback only heads off cleanup-related cancellations caused by vacuum removing rows a standby query still needs — it doesn't touch other cancellation causes such as a conflicting Access Exclusive lock; exceeding the standby delay settings normally just cancels the offending statement, with full session termination reserved for specific cases like a DROP DATABASE conflict; hot standby sessions are actually more restricted than ordinary read-only transactions and cannot use LISTEN or NOTIFY at all; and autovacuum is inactive throughout recovery, only resuming once the standby reaches the end of recovery.",
  },
];
